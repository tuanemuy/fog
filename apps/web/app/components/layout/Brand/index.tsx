/** The wordmark: the product name and the brand dot. */
export function Brand() {
  return (
    <span className="fog-brand">
      <span className="fog-brand-name">fog</span>
      <span className="fog-brand-dot" aria-hidden="true" />
    </span>
  );
}
