# 通常系-8: 認証画面どうしの導線がすべて機能する

**結果**: PASS
**対応する受け入れ基準**: AC-11 / AC-13（PAGE-login-004 / 005・PAGE-signup-004）
**実行時間**: 約35秒

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | ログアウト状態で `/login` を開く | ログイン画面が表示され、「アカウント登録」「パスワードを忘れた」リンクがある | `http://localhost:3000/login` / 見出し「ログイン」・link "アカウント登録"・link "パスワードを忘れた" を確認 | PASS |
| 2 | 「アカウント登録」リンクを押す | `/signup` へ遷移 | URL `http://localhost:3000/signup`、見出し「アカウント登録」、link "ログイン" あり | PASS |
| 3 | `/signup` で「ログイン」リンクを押す | `/login` へ遷移 | URL `http://localhost:3000/login`、見出し「ログイン」 | PASS |
| 4 | `/login` で「パスワードを忘れた」を押す | `/password-reset` へ遷移し「準備中」表示と `/login` への戻り導線がある（404 でない） | URL `http://localhost:3000/password-reset`、見出し「パスワードリセット」、本文「この機能は準備中です」、link "ログインに戻る" | PASS |
| 5 | 「ログインに戻る」を押す | `/login` へ遷移 | URL `http://localhost:3000/login`、h1 = 「ログイン」 | PASS |

## 確認ポイントの検証

- `/password-reset` は 404 にならず、専用の「準備中」画面が描画された。
- リセットフォーム自体は描画されておらず（本スライス対象外）、「ログインに戻る」の戻り導線のみ。仕様どおり。
- 押しても何も起きないコントロールは無し（4本のリンクすべてが実在ルートへ遷移）。

## 失敗詳細

なし。
