# TC-12: ゴミ箱のアイコン行操作と確認ダイアログ、設定画面

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-tc-12
**証跡**: TC-12.png / TC-12.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `/trash` の冒頭 | 一文「ここにある項目は保持期限を過ぎると完全に削除されます。」＋「空にする（N）」（ADR-038） | 先頭 `p` が同文言。ボタン `空にする（4）` / `disabled=false` / 赤字 | PASS |
| 2 | 行の構造 | 種別ピル＋タイトル＋残り日数、行末は 2 つのアイコンボタン、`a` は無い | ピル `メモ` / `トピック` / `ドキュメント`（9999px）、`残り30日`、`{タイトル} を復元` / `{タイトル} を完全に削除` のアイコンボタン、`a` 0 個 | PASS |
| 3 | 復元 | 行に `復元中…` → 行が消えてトースト、トーストに「タイムラインで見る」は無い | トースト「メモを復元しました」（操作 0 個）。`復元中…` は 40ms ポーリングでは捉えられず（E-1 で観測） | PASS（一部未観測） |
| 4 | セット削除の配下 | トピックの行の下に一段インデントして小さめ | 子 `li` は親 `li` にネスト、先頭テキスト x が 38 → 62（+24px）、`font-size` 15.2px → 12.48px | PASS |
| 5 | 完全削除の確認 | 「完全に削除しますか？」＋「履歴ごと消え、元に戻せません。」＋赤い「完全に削除」＋「キャンセル」、中央モーダルで全幅縦積み | `dialog` は `:modal` / centerX=195 / centerY=422 / `radius 26px`。文言一致。両ボタンとも 302px 幅で縦に 2 段、確認は赤字 | PASS |
| 6 | 「空にする」の確認 | 「ゴミ箱を空にしますか？」＋「全件（N件）が…」＋「空にする」＋「キャンセル」 | `ゴミ箱を空にしますか？\|全件（3件）が完全に削除され、元に戻せません。\|空にする\|キャンセル`、同じ寸法・縦積み | PASS |
| 7 | `/settings` | AI → ログイン手段 → ゴミ箱 → データ → アカウントの順と各中身 | `h2` の DOM 順が期待どおり。AI 0 件の文言・メール行＋メタ「メール・パスワード」・「連携を追加」・エクスポートの説明・パスワード変更＋文字だけの「ログアウト」を確認 | PASS |
| 8 | 保持期限の保存 | トースト「保持期限を保存しました」 | `45` で保存 → 同トースト、`/trash` が `残り45日` に。`30` に戻して保存済み | PASS |

## 未観測

- 行の失敗表示（`RowError`）と語「リトライ」— 失敗を再現できないため（testing.md の「再現できない場合は確認しない」に従った）。`trash/__tests__/trashBoard.dom.test.tsx` が担保。
- `復元中…` の表示と、ゴミ箱が空のときの「ゴミ箱は空です」／押せない「空にする」— この手順では捉えられず、いずれも E-1 で観測済み（`復元中…`・`ゴミ箱は空です`・`空にする` の `disabled=true`）。
