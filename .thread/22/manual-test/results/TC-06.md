# TC-06: 成功と一度きりの通知は操作の無いトーストで出て数秒で消える

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-tc-06
**証跡**: TC-06.png / TC-06.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | メモを投稿 | 黒い角丸ピルに 1 文、操作を一切持たない | `role="status"` / `aria-live="polite"` の中の `P`（`radius 9999px` / 黒地 / 白字 / `"メモを追加しました"`）。`button` / `a` / `[role=button]` は 0 | PASS |
| 2 | 位置 | シート下端中央、コンポーザーの直上に同じ中央軸 | トースト centerX=195 / bottom=754、コンポーザー centerX=195 / top=762、`main` centerX=195 | PASS |
| 3 | 消え方 | 約 4 秒で消え、消えた後に画面がずれない | `life_ms = 4000`。消失前後で `main` 先頭子要素 y=73、メモ件数 8 で不変 | PASS |
| 4 | 削除 | 「メモを削除しました」、位置は同じ | 文言一致、centerX=195 / bottom=754、操作 0 個 | PASS |
| 5 | `?memo=missing-memo-id` | 通常のタイムライン＋「メモが見つかりませんでした」 | URL 保持、トースト一致、日付見出しとメモ 8 件・コンポーザーも表示 | PASS |
| 6 | ゴミ箱から復元 | 「メモを復元しました」、コンポーザーの無い画面は同じ下端 | 文言一致、centerX=195 / bottom=814（シート bottom=844）、操作 0 個 | PASS |
| 7 | 保持期限を保存 | 「保持期限を保存しました」 | 文言一致、bottom=814、操作 0 個 | PASS |
| 8 | 同じ操作を 3 回 | 同じ文言は積み上がらず 1 つに置き換わる | 可視トーストの遷移は `0 → 1:メモを追加しました → 0` のみで、2 つ以上になった瞬間なし | PASS |
| 9 | 背後の操作 | トーストがポインタを奪わない | トーストとその親は `pointer-events: none`、外側の 390×844 は `auto` | PASS |

## 未観測

- 成功と部分失敗の混在（ゴミ箱「空にする」の一部失敗、AI 接続の一括失効）— testing.md の指示により対象外。`trash/__tests__/trashBoard.dom.test.tsx`・`settings/__tests__/aiConnectionsPanel.dom.test.tsx` が担保。
