# TC-03: モバイルのナビはハンドル付きの白いボトムシート

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 15:07 JST）
**セッション**: verify-tc-03
**証跡**: TC-03.png / TC-03.webm

## 実行ログ（手順書の改訂後の再観測）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 「メニュー」を押す | シートが開く | `dialog "メニュー"` の中に `navigation "モバイルナビゲーション"` と 5 リンク | PASS |
| 2 | シートの見た目 | 下端から迫り上がる白カード、上辺だけ丸い、中央上部にハンドル、左右はシート本体と同じ横フレーム | `dialog` は `position: fixed` / `inset: 494px 14px 0`、直下 `div` が x=14 / 362×350 / `border-radius: 26px 26px 0 0` / `oklch(1 0 0)` / 上向きの影。ハンドルは 36×4 / `radius 9999px` / `aria-hidden="true"` / 中心 x=195。`main` も x=14 / 幅 362 | PASS |
| 3 | 背後の覆い | 暗い覆いで操作できない | `::backdrop` = `oklch(0.21 0.006 270 / 0.28)`、`:modal` に一致（背後は不活性） | PASS |
| 4 | 項目と現在地 | 5 項目＋ヘアライン。現在地だけ太字＋行頭に `--color-primary`（紫）の丸 | 5 リンク、2 番目以降 `border-top: 1px oklch(0.945 0.004 286)`。`トピック` が `aria-current="page"` / 600 / 丸 `oklch(0.63 0.13 292)`、他 4 つの丸は透明 | PASS |
| 5 | ハンドルを押す | 装飾で何も起きない | `mouse down`/`up` 後も `dialog.open === true`、URL 不変 | PASS |
| 6 | 覆いを押す | 閉じる | `dialog.open === false`、フォーカスは「メニュー」に戻る | PASS |
| 7 | Escape | 閉じる | `dialog.open === false`、フォーカスは「メニュー」 | PASS |
| 8 | 項目「検索」 | `/search` へ遷移しシートは閉じる | URL `/search`、`dialog.open === false` | PASS |
| 9 | 開いている間の背面スクロール | スクロールしない | `/settings`（`main` は scrollHeight 1402 / clientHeight 771）で背面にマウスを置き `scroll down 300` → `main.scrollTop` は 0 のまま、`window.scrollY` も 0、`dialog.open === true` | PASS |
| 10 | 1280×900 | メニューボタンもボトムシートも出ない | ボタンは矩形 0×0（親が `display:none`）、`dialog` は `open === false`。`aside` が 5 項目を常設 | PASS |

前回「未観測」だった背面スクロールの抑止は、スクロールする `/settings` を使ったホイール操作で確認できた。
