# TC-01: 戻る付きヘッダーの英語タイトルがブランド書体で出る

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-tc-01
**証跡**: TC-01.png / TC-01.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 390×844 でトピック詳細を開く | ヘッダー左に `topic` | ヘッダーのテキスト末端は 1 つだけ、`SPAN` / `lang="en"` / `"topic"` | PASS |
| 2 | `topic` と本文の書体を比べる | `lang="en"` ＋ ブランド書体（Avenir Next 系）／ウェイト medium ／本文は変わらない | `topic`: `"Avenir Next", Avenir, "Helvetica Neue", Arial` / 500 / 16.8px。`h1`・`p` は `"Helvetica Neue", Arial, Hiragino…` / 700・400 | PASS |
| 3 | ドキュメント閲覧のヘッダー | `document`、ブランド書体、medium | `SPAN` / `lang="en"` / `"document"` / Avenir Next 系 / 500 | PASS |
| 4 | メモ履歴のヘッダー | `memo`、ブランド書体、medium | `H1` / `lang="en"` / `"memo"` / Avenir Next 系 / 500 | PASS |

## 未観測

- 字形（`g` / `a` の形）の目視比較はログでは行っていない。書体の適用は `font-family` / `font-weight` の計測値で確認済みで、`--font-brand` の値と未定義参照は `lint/designTokens.test.ts` が担保する。

## 補足

- 英語タイトルの要素は画面で異なる（`topic` は `SPAN`、`memo` は `H1`）。これはシート側が見出しを持たない画面でヘッダーが `h1` を持つ設計（`PageHeading`）によるもので、期待結果は要素名を規定していない。
