# TC-11: 行・空状態・フォームの基準形 — タイムライン / トピック / ドキュメント / 検索

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 15:45 JST）
**セッション**: verify-tc-11
**証跡**: TC-11.png / TC-11.webm

## 実行ログ（手順書の改訂後の再観測）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | タイムラインの行とメニュー | 日付見出し＋時刻＋本文＋「メモの操作」、メニューは編集/履歴/削除で削除だけ赤 | `h2 "2026年9月12日(土)"`、時刻は `time`（`datetime` 付き）、行末に `button[aria-label="メモの操作"]`。`menuitem` 3 つで「削除」だけ `oklch(0.55 0.19 27)` | PASS |
| 2 | 絞り込み欄 | 虫眼鏡＋プレースホルダー `タイムラインを絞り込む…` | `rounded-full` のフォームに `svg data-icon="search"` と `input[placeholder="タイムラインを絞り込む…"]` / `aria-label="キーワードで絞り込む"` | PASS |
| 3 | 0 件の絞り込み | 一文＋文字だけのボタン「絞り込みを解除」、欄の右端にアイコン | `main` は一文＋「絞り込みを解除」だけ。アイコン版（`✕` パス / `6px`）と文字版（`9999px` / 透明 / `oklch(0.52 0.01 275)`）の 2 つ | PASS |
| 4 | 解除して戻る | 戻る | URL が `/` に戻りタイムライン表示 | PASS |
| 5 | 日付移動のカード | 可視ラベル＋日付入力＋塗りの小ボタン「移動」 | 可視 `label "日付を指定して移動"`、`input[type=date]`、`button "移動"`（`9999px` / `oklch(0.54 0.15 292)` / 白字） | PASS |
| 6 | メモの無い日付へ移動 | 移動先の日付見出しに「…のメモはありません」が添う | `/?date=2026-07-22` で先頭 `h2` が `2026年9月12日(土)` ＋ `2026年7月22日(水)のメモはありません`（モック `.day-head > .sub` と同形） | PASS |
| 7 | メモ 0 件の空状態 | 「最初のメモを書いてみましょう」の一文だけ | 15 件以上のメモがあり再現できず | 未観測 |
| 8-9 | トピック一覧 | 名前＋説明＋ドキュメント数の行、完了済みは折りたたみ、末尾に `+` の追加行 | 行は `a[href="/topics/{id}"]`（幅 266）＋独立した「トピックの操作」、末尾は `+`（`M10 4V16M4 10H16`）付きの `BUTTON`、「完了済み（1）」を開くと `読書メモ` が現れる | PASS |
| 10 | トピック 0 件の空状態 | 「最初のトピックを作ってみましょう」 | トピックが 4 件あり再現できず | 未観測 |
| 11-12 | トピック詳細 | `h1` はシートの中、枠線「完了にする」／完了済みは「完了を解除」と「完了済み」、「ドキュメント」＋追加行 | `main` の `h1` がトピック名（`header` に `h1` なし）、`h2` は「ドキュメント」、完了済みは `完了済み \| 完了を解除` | PASS |
| 13 | ドキュメント閲覧 | トピック名 → `h1` → 更新時刻 → 本文。出典 0 件なら「出典」領域ごと出ない。ヘッダー 3 アイコンは 3 つとも中立色 | DOM 順は `A` → `H1` → `P/TIME "2026年9月12日 13:32 更新"` → 本文。`出典` / `元になったメモ` の文字列なし。3 アイコンとも `oklch(0.58 0.01 275)` | PASS |
| 14 | ドキュメント編集 | 枠なしの大きなタイトル、`本文を書く…`、可視ラベルの「変更理由」、保存はヘッダーの塗り小ボタン、「やめる」無し | `title` は枠 0 / 19.55px / ラベル `sr-only`、`body` は `本文を書く…`、`changeReason` のラベルは可視で枠線あり、ヘッダーに塗り「保存」。`やめる` / `編集をやめる` は 0 件 | PASS |
| 15 | 新規作成の「出典を追加」 | 枠線ボタン（文字は黒寄り） | `9999px` / 透明 / `oklch(0.21 0.006 270)` / `1px oklch(0.875 0.006 278)` | PASS |
| 16-18 | 検索 | 初期文言・プレースホルダー・送信ボタン無し・先頭チップ「すべて」・完了バッジ・種別バッジと件数・0 件の文言 | すべて一致（`button[type=submit]` 0 個、`2件` と種別バッジ、`「存在しない語彙xyz」に一致する…`） | PASS |
| 19-20 | 履歴 2 画面 | 「履歴」ラベル＋版の行（比較元/比較先）＋差分＋`比較元 · 日時`＋塗り「この内容に戻す」、2 画面が同形 | メモ履歴・ドキュメント履歴とも同じ構造・同じ計算値（選択行 `oklch(0.93 0.04 292)` / `radius 10px`、塗りボタン `oklch(0.54 0.15 292)` / `9999px`）。`現在の内容` は無い（followups.md の既知差） | PASS |
| 21 | 日時の表記 | `2026年7月20日 12:42` 形式（ADR-034） | 全画面で `2026年9月12日 13:32` / `2026年9月12日(土)` 形式。年を落とす形は出ない | PASS |

## 未観測

- タイムラインとトピック一覧の空状態（Step 7 / 10）— 既存データを全消ししないと再現できないため。`timeline/__tests__/timelineBoard.dom.test.tsx` が「最初のメモを書いてみましょう」の有無を両側で押さえ、トピック側は `topics/__tests__/topicList.dom.test.tsx` が「最初のトピックを作ってみましょう」を持つ。
