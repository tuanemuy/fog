# E-6: ダイアログとポップオーバーのキーボード操作

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-e-6
**証跡**: E-6.png / E-6.webm（後半は録画停止後にビューポートを戻して撮り直し）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1-4 | 「メモの操作」を開き矢印キーで移動 | 項目を移動できる | 開いた直後の `activeElement` は `menuitem "編集"`、↓で「履歴」→「削除」、↑で「履歴」 | PASS |
| 5 | Escape | 閉じてフォーカスがトリガーに戻る | `menuitem` 0 個、トリガーの `aria-expanded="false"`、`activeElement` がトリガー（`aria-label="メモの操作"`） | PASS |
| 6 | 削除確認ダイアログで Escape | 閉じる | `dialog.open` が true → false、行はそのまま残る | PASS |
| 7 | ボトムシートを開いて Tab を繰り返す | 背後の要素へフォーカスが抜けない | 5 項目を巡回し、1 ステップだけ `document.body` に載るが背後の操作要素へは移らず、`dialog.open` は常に true | PASS |
