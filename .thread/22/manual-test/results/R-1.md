# R-1: ルートを移した 5 画面の URL が変わっていない

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-r-1
**証跡**: R-1.png / R-1.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 未ログインで 5 URL を開く | そのままの URL で開ける（認証が要る 2 つはログインへ） | `/login`・`/signup`・`/password-reset` は URL 保持。`/ai-clients/authorize` → `/login?redirect=%2Fai-clients%2Fauthorize`、`/password-reset/done` → `/login?redirect=%2Fpassword-reset%2Fdone`（TC-10 で観測） | PASS |
| 2 | ログイン済みで開く | 認証後の画面は URL 保持 | `/password-reset`・`/password-reset/done`・`/ai-clients/authorize` は URL 保持し中身も期待どおり。`/login`・`/signup` は `/` へ（従来どおり） | PASS |
| 3 | `/login?redirect=…` | リダイレクト先が効く | 未ログインで `/settings` → `/login?redirect=%2Fsettings`、ログイン後 `/settings` へ | PASS |
