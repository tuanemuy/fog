# R-4: 基本フローが壊れていない

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 16:38〜16:49、`clearCache` の修正後）
**セッション**: r4b（本観測）/ r4rec（録画）
**証跡**: R-4.png / R-4.webm

## 実行ログ（基本フロー）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | メモを投稿 | タイムラインに即時反映 | 42ms で行が出現（トーストは `保存中…` → `メモを追加しました`）、127ms の 1 フレームだけ巻き戻り、151ms で確定。4068ms でトーストが消える | PASS |
| 2 | トピックを作成 → 行を開く | 一覧に即時反映 → 詳細へ遷移 | 39ms で一覧に出現、91ms の 1 フレームだけ消え、106ms で確定。行クリックで詳細（`h1` はトピック名） | PASS |
| 3 | ドキュメントを作成 | 閲覧へ遷移 | `/documents/01a09495-…` へ遷移、本文と更新時刻、ヘッダーの `編集` / `履歴を表示` / `削除` を確認 | PASS |
| 4 | 編集 → 保存 | 「保存しました」のトースト | 61ms でトースト、88ms で閲覧 URL へ、144ms で追記が反映、4061ms でトーストが消える | PASS |
| 5 | 履歴から「この内容に戻す」 | トースト「{日時} の内容に戻しました」→ 対象の画面へ遷移 | 確認ダイアログ →「戻す」で 78ms に閲覧へ遷移、間に 1 フレームだけスケルトン（`読み込み中` ＋ スタンドインの文字列）、437ms でトースト「2026年9月12日 16:47 の内容に戻しました」 | PASS |
| 6 | ログアウト | `/login` へ全画面遷移 | `navigation` エントリが `navigate http://localhost:3000/login`（ドキュメント読み込みが `/login` で発生） | PASS |

## 実行ログ（削除の 2 経路・各 6 回）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 7 | ドキュメント閲覧の「削除」→「削除」（6 回） | トースト「ドキュメントを削除しました」が出てトピック詳細へ遷移 | 6 回とも `削除中…` → トースト「ドキュメントを削除しました」→ URL が `/documents/{id}` から `/topics/{id}` へ | PASS |
| 8 | 同上 | 途中で「ドキュメントが見つかりません」が一瞬でも出ない | 6 回・全フレームを通じて `見つかりません` を含むフレームは **0**。URL が変わった後も確認ダイアログと削除前の本文が描かれたまま遷移し、その次がスケルトン | PASS |
| 9 | トピック詳細の「トピックの操作」→「削除」→「削除」（6 回） | `/topics` へ遷移し一覧からその行が消える（トーストは無い） | 6 回とも `/topics` へ遷移、確定した一覧に削除したトピックは無い。トーストは全フレームで 0 | PASS |
| 10 | 同上 | 途中で「トピックが見つかりません」が出ない | 6 回・全フレームを通じて `見つかりません` を含むフレームは **0** | PASS |
| 11 | 遷移先の内容 | 削除後の内容になっている。遷移直後にスケルトン（「読み込み中」）が 1 回出てから中身が入るのは期待どおりで、削除前の内容が一瞬出るのは FAIL | 12 回すべてで、URL 変更後に `読み込み中` で始まるスケルトンのフレームが 1 回だけ入り（290〜310ms）、次に削除後の中身が確定。スケルトンに並ぶ `ブランド刷新` / `サイト構成の方針` / `引っ越し` / `読書メモ` は TC-08 で確認したスタンドインの文字列で、実データではない。遷移先が削除前の内容（削除した行を含む一覧・削除したドキュメントを含む詳細）になったフレームは 0 | PASS |
| 12 | 遷移先でブラウザの「戻る」 | 消した画面は「ドキュメントが見つかりません」「トピックが見つかりません」になる | doc5 の後に戻ると `ドキュメントが見つかりません\|トピック一覧へ`、top5 の後に戻ると `トピックが見つかりません\|トピック一覧へ`（キャッシュから戻ってこない） | PASS |

前回 FAIL だった「遷移直後の 1 フレームだけ『見つかりません』が出る」事象は、`invalidate({ filter })` をやめ `router.clearCache()` を遷移の前後 2 回呼ぶ形に変えた後の 12 回（ドキュメント 6・トピック 6）で 1 フレームも再現しなかった。

## 補足（観測事実・環境への影響）

- 「この内容に戻す」の確認ダイアログ（`この内容に戻しますか？`）は testing.md の手順に無いが、`spec/pages/index.md`（確認あり）とモック `document-history.html` どおり。
- セッション A 側の録画は `record start` 後の開き直しで途切れたため、録画はセッション B（`r4rec`）で撮り直している（`R-4.webm` はその区間、`R-4.png` は top6 の削除直後の `/topics`）。
- 観測でメモ 1 件・トピック 8 件・ドキュメント 7 件を作成し、ドキュメント 6 件とトピック 6 件を削除した（いずれもゴミ箱行きで、ゴミ箱の中身が増えている）。
