# TC-07: エラーは各持ち場に出て、画面に持続する説明はインラインに残る

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 15:23 JST）
**セッション**: verify-tc-07
**証跡**: TC-07.png / TC-07.webm

## 実行ログ（手順書の改訂後の再観測）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 誤ったパスワードでログイン | メール欄の**上**に赤い枠（`FormError`） | `role="alert"`「メールアドレスまたはパスワードが正しくありません」x=38 / y=204 / 314×67、`oklch(0.45 0.19 27)` / `oklch(0.95 0.025 20)` / 上辺 1px。メール欄は y=322 | PASS |
| 2 | 登録済みメールで登録 | 同じ枠の中に「ログイン」リンク | 同じ計算値の枠に `a "ログイン" → /login` 1 つ、メール欄は y=323 | PASS |
| 3 | パスワード `short` | パスワード欄の直下に「8文字以上で入力してください」 | `role="alert"` y=443（欄の下端 436 の直下）、`aria-invalid="true"` / `aria-describedby` に error と helper | PASS |
| 4 | 空のトピック名 | 名前欄の直下に「トピック名を入力してください」 | `role="alert"` y=423（欄の下端 416 の直下）、`aria-invalid="true"` | PASS |
| 5 | 空タイトルで保存 | 「タイトルを入力してください」 | `role="alert"` y=173（欄の下端 164 の直下）、`aria-invalid="true"` | PASS |
| 6 | 保持期限の `0` | ブラウザから確認しない（改訂後の手順で対象外） | 実行していない | 対象外 |
| 7 | 失敗がトーストにならない | トーストにならず数秒で消えない | 1〜5 のいずれでもトースト領域（`[role="status"][aria-live="polite"]`）は空 / 矩形 0×0。`role="alert"` は待っても消えない | PASS |
| 8 | リセットメール送信 | タイトル下の一文は前後で残る、成功はトースト、フォームも残る、`?sent=1` は無い | 送信前後で本文は同一文字列、URL はクエリなしの `/password-reset`、トースト「リセットメールの送信を受け付けました」が出て約 6 秒後に空になる | PASS |
| 9 | 再試行の語の分かれ | 行＝リトライ／面＝再試行 | この手順では失敗を起こしておらず、どちらの語も出なかった | 未観測 |

## 未観測

- 再試行の語の持ち場による分かれ（Step 9）— 手順が再試行を伴う失敗を起こさないため。行の `RowError`（「リトライ」）は `trash/__tests__/trashBoard.dom.test.tsx`、面の `InlineAlert` は E-1 の観測（「一覧を読み直す」＝一覧の面）と `ui/feedback` の DOM テストが持つ。
- 保持期限 `0` の `FieldError`（改訂後は手順から除外）— `settings/__tests__/retentionForm.dom.test.tsx` が担保。

## 補足（環境への影響）

- 観測中に `サイト構成の方針` のヘッダー「保存」を 1 回押しており、このドキュメントの版が 1 つ増えている可能性がある。
