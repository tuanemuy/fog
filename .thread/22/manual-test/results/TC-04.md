# TC-04: 基準形のプリミティブが全画面で同じ形になる

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 15:15 JST）
**セッション**: verify-tc-04
**証跡**: TC-04.png / TC-04.webm

## 実行ログ（手順書の改訂後の再観測）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 7 画面のボタンを計算値で突き合わせる | ボタンは 6 段だけで画面をまたいで同じ形 | 値の組は 6 通り: 塗りの丸（`9999px`/`oklch(0.54 0.15 292)`/白＝追加・保存・エクスポート・パスワードを変更）、枠線の丸（`9999px`/透明/`1px oklch(0.875 0.006 278)`＝完了にする・SSO・出典を追加）、文字だけ（`9999px`/透明/枠 0＝キャンセル・ログアウト・絞り込みを解除）、赤い文字の破壊的（`oklch(0.55 0.19 27)`＝ダイアログの「削除」・`/trash` の「空にする」）、アイコンのみ（ヘッダー `10px`/`padding 8px`、行内 `6px`/`padding 4px`）、追加行（`10px`/透明/`oklch(0.54 0.15 292)`）。同じ役割は画面をまたいで同値 | PASS |
| 2 | 文字の記号 | 「×」「＋」「←」が出ない | 6 画面の `body.innerText` で `×:0` `✕:0` `＋:0` `+:0` `←:0` `→:0` `<:0` | PASS |
| 3 | アイコンのみのボタンのネーム | 一覧のネームを持つ | 「戻る」「メニュー」「メモを追加」「メモの操作」「トピックの操作」「編集」「履歴を表示」「削除」「日付を指定して移動」「キーワードで絞り込む」「絞り込みを解除」「{タイトル} を復元」「{タイトル} を完全に削除」を確認、中の `svg` はすべて `aria-hidden="true"` | PASS |
| 4 | 追加フォームの「キャンセル」「追加」 | 主操作は塗りの丸、キャンセルは文字だけ | 追加＝`9999px`/`oklch(0.54 0.15 292)`/白、キャンセル＝`9999px`/透明/`oklch(0.52 0.01 275)` | PASS |
| 5 | ドキュメント閲覧のヘッダー 3 アイコン | 3 つとも中立色で削除も赤くない | 3 つとも `oklch(0.58 0.01 275)` / 透明 / `10px` / `padding 8px` | PASS |
| 6 | `/trash` の行末 2 アイコン | アイコンのみのボタン | 復元＝`oklch(0.63 0.13 292)`、完全に削除＝`oklch(0.58 0.01 275)`、どちらも `6px`/`padding 4px`/27×27 | PASS |
| 7 | 行 (a) | 行全体が 1 リンク、行末に `--color-primary` の斜め矢印、押せる面が行いっぱい | トピック詳細のドキュメント行は `A` 幅 342（`-mx-md`）、矢印 `d="M5 15L15 5M15 5H7.5M15 5V12.5"` の色は `oklch(0.63 0.13 292)`、`hover:bg-neutral-50` がホバーで実際に効く。`/search` の結果行も同形 | PASS |
| 8 | 行 (b) | 行はホバーに反応せず、ボタンだけ反応 | `/trash` の行は `DIV`（`closest('a')` は null）、ホバーで背景は透明のまま。ボタンだけ `:hover` で色が変わる | PASS |
| 9 | トピック一覧の行 | 矢印もホバー面も無いのが正しい（FAIL にしない） | `A`（`svgCount: 0` / `padding: 0` / 幅 266）＋ 行内に独立した「トピックの操作」 | PASS |
| 10 | 行間 | ヘアライン 1 本、カードは並ばない | 区切りは親側の `*:not-first:border-t`（`1px oklch(0.945 0.004 286)`）、行自身に背景色なし | PASS |

## 補足

- ヘッダーの lockup `svg`（`viewBox="0 0 64.7 26"`）の中の `circle` はちょうど 1 個（`fill: oklch(0.62 0.19 40)`）で、`fog` の語尾に 2 つ目のドットは無い（logo.md「二重ドットを避ける」）。
- 赤い文字の破壊的ボタンは、ダイアログ（`9999px`）と `/trash` の「空にする」（`6px`）で角丸が違うが、モックの `.dialog-btn`（`--radius-full`）と `.empty-btn`（`--radius-sm`）どおり。
