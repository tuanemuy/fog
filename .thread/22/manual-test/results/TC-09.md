# TC-09: 読み込み失敗と 404 が枠の中に共通の形で出る

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-tc-09
**証跡**: TC-09.png / TC-09.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `documentId` を 250 文字にして開く | シートの中が「読み込めませんでした」＋塗りボタン「再試行」だけ。ヘッダーとナビは残る。文言・スタック・コードは出ない。`role="alert"` | `main.innerText` は `読み込めませんでした\|再試行` の 2 行。`header`（`document`）と「メニュー」は残る。`role="alert"` の中のボタンは「再試行」1 つ（塗り / 9999px） | PASS |
| 2 | 「再試行」を押す | ローダーが走り直し、一瞬押せなくなってから戻る。表示は同じ | `disabled=true → false`、`main` の内容は不変 | PASS |
| 3 | `/no-such-page` | 認証シートの枠（中央の白カード、頭に lockup）に「ページが見つかりません」＋「タイムラインへ」 | `header` / `aside` / メニュー / `nav` いずれも無し。lockup は centerX=195.5（ビューポート中心 195）。`main` は 362×281 / `radius 26px` / 白 / 影。押すと `/` へ | PASS |
| 4 | `/topics/no-such-topic-id` | アプリシェルのシート内に「トピックが見つかりません」＋「トピック一覧へ」 | ヘッダー（`topic`）とメニューは残り、`main` は該当文言＋`href="/topics"` の塗りボタン | PASS |
| 5 | `/memos/no-such-memo-id/history` | 「メモが見つかりません」＋「タイムラインへ」 | ヘッダー（`memo`）＋該当文言＋`href="/"` の塗りボタン | PASS |
| 6 | ルートエラーと 404 の形の比較 | 中央寄せの一文＋操作 1 つで同形 | 4 種とも「一文＋塗りボタン 1 つ（9999px）」。ログが測った `text-align: start` は `role="alert"` の外側ラッパーの値で、一文は `EmptyState`（`py-2xl text-center`）が中央寄せしている | PASS |

## 未観測

- `_app` / `_sheet` のレイアウトルート自身の失敗と、アプリシェル内の 404 — testing.md の指示によりブラウザから起こしていない。`components/layout/__tests__/routeBoundaries.dom.test.tsx` が両方を持つ。
