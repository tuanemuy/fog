# R-2: 認証済みの 2 画面に `Cache-Control: no-store` が付く

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: —（`curl` によるヘッダー確認）
**証跡**: なし（画面の証跡は撮っていない。ヘッダー値は `R-2.md` の観測ログが持つ）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | セッション Cookie 付きで `/password-reset/done` と `/ai-clients/authorize` を取得 | `Cache-Control: no-store` | 両方 200 で `no-store, private` / `vary: cookie` | PASS |
| 2 | `_app` 配下も同様か | `/`・`/settings` なども `no-store` | `/`・`/settings`・`/topics`・`/trash`・`/search` すべて `no-store, private` / `vary: cookie` | PASS |

## 補足

- 未認証の画面（`/password-reset`）とログイン済みでの `/login`・`/signup`（307 で `/` へ）には `cache-control` が付かない。testing.md はこの 3 つを規定していない。
