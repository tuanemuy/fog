# E-7: 長い本文・長いタイトルの折り返し

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-e-7
**証跡**: E-7.png / E-7.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | タイトル 120 文字・本文に `a` 80 文字で保存 | 保存できる | `/documents/01a09408-…` へ遷移 | PASS |
| 2 | ヘッダー | 長い固有名は出ず `document` のまま | `header.innerText = "document"`、`scrollWidth = clientWidth = 362` | PASS |
| 3 | シート内のタイトル | 折り返される | `h1` は 120 文字で 314×235（約 8 行）、`overflow-wrap: anywhere` | PASS |
| 4 | 長い英単語の本文 | 折り返される | `p` は 314×91、`overflow-wrap: anywhere`、行幅はコンテンツ幅と同じ | PASS |
| 5 | 横スクロール | 出ない | `main` / ルート / `documentElement` いずれも `scrollWidth = clientWidth`。例外はスキップリンク（`overflow-x: hidden` の視覚的非表示要素） | PASS |
