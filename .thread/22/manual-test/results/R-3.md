# R-3: シートがスクロールコンテナになったことによる影響

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-r-3
**証跡**: R-3.png / R-3.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 下までスクロール → 履歴へ遷移 → 戻る | 戻った位置がスクロール前の位置に復元される | 遷移前 `main.scrollTop = 334`（最大値）、戻ると `scrollTop = 334` / `scrollHeight = 1105` | PASS |
| 2 | 下端で過去のメモが自動読み込みされる | 番兵で自動読み込み（ボタンは無い） | 「過去のメモを読み込む」「新しいメモを読み込む」は 0 件（撤去は正しい）。自動読み込みはメモ 10 件が 1 ページに収まるため発火せず | 未観測 |
| 3 | `/?memo={既存ID}` | そのメモの位置までスクロールし対象行が強調される | `main.scrollTop = 334`（= `scrollHeight - clientHeight`）、対象 `article` は y=593 でビューポート内、背景 `oklch(0.93 0.04 292)`（`bg-primary-lighter`） | PASS |

## 未観測

- 下端到達での過去メモの自動読み込み（Step 2）— 検証アカウントのメモが 10 件で 1 ページに収まり、番兵が発火する条件を作れなかった。ボタンが存在しないこと（ADR-037 の撤去）は確認済み。
