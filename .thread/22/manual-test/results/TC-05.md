# TC-05: コンポーザーは 1 行のピルのまま複数行へ伸びる

**結果**: PASS
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-tc-05
**証跡**: TC-05.png / TC-05.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | コンポーザーを見る | 左端に送信アイコン＋右に入力欄の 1 本のピル（角丸＝高さの半分）、浮いて影を持つ | 310×52 / `border-radius: 26px`（= 52 の半分）/ 影 `0 12px 36px`。ボタン x=56（左）、textarea x=97 | PASS |
| 2 | 空のとき | 送信ボタンが `disabled` | `disabled = true` / 色 `oklch(0.875 0.006 278)` | PASS |
| 3 | 1 文字入れる | 押せるようになる | `disabled = false`、箱は 310×52 のまま | PASS |
| 4 | 3 行にする | 下端固定で上へ伸び、角丸は 26px のまま、送信アイコンは最終行の高さ | 310×108、bottom=814 のまま、`radius 26px`。ボタン centerY=788 = 最終行の中心 | PASS |
| 5-6 | 6 行 → 8 行 | 6 行超で伸びず、中がスクロールして末尾が見える | 6 行 192px → 8 行も 192px。`clientHeight 169` / `scrollHeight 225` / `scrollTop 51` / キャレットは末尾 | PASS |
| 7 | 入力を消す | 1 行のピルに戻り `disabled` | キーボード消去で `value=""` / `disabled=true` | PASS |
| 8 | 「音声で入力」 | 無い | BOX 内の `button` は「メモを追加」1 つだけ | PASS |
| 9 | 送信中 | アイコンが回るスピナー | 送信中は `disabled=true` / svg に `animate-spin`（`animation-name: spin`）、完了後 `none` | PASS |
| 10 | 横はみ出し | 出ない | 全状態で `main.scrollWidth = clientWidth = 362`、ルートも 390 = `innerWidth` | PASS |

## 補足

- `agent-browser` の `fill ""` では DOM 値だけが変わり React の state が更新されず `disabled` が戻らない。判定はキーボード消去の観測を採った（ツール由来で実装の挙動ではない）。
