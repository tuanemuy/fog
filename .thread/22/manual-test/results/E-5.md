# E-5: `lg` 以上でモバイル専用の部品が出ない

**結果**: PASS
**実行時間**: —（観測ログに記録なし。再実行 2026-09-12 15:50 JST）
**セッション**: verify-e-5
**証跡**: E-5.png / E-5.webm

## 実行ログ（手順書の改訂後の再観測）

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 1280×900 の `/` | lockup・メニューボタン・英語タイトル横の橙の点が出ない | `[aria-label="fog"]` は矩形 0×0（親 `display:none`）、`[aria-label="メニュー"]` も 0×0、色付きの丸は 0 個。`header.innerText` は `sr-only` の `タイムライン` | PASS |
| 2 | 1280×900 の `/topics` | 同上 | 同じ状態（lockup 0×0 / メニュー 0×0 / 丸 0 個） | PASS |
| 3 | 1280×900 のドキュメント閲覧 | 同上 | `header` 内に lockup は存在せず、メニューは 0×0、橙の丸（`oklch(0.62 0.19 40)`）は `display:none` / 0×0。英語タイトル `document` は表示されたまま | PASS |
| 4 | ボトムシートを開こうとする | 開けない | 通常のクリックはサイドバー（`aside.hidden.lg:flex`）に覆われて成立しない。JS から `click()` を強制すると `open` にはなるが `dialog` も中の `nav` も矩形 0×0 で画面に出ない | PASS |
| 5 | サイドバーと現在地 | 常設で、現在地が太字＋`--color-primary`（紫）の丸 | `aside` 200×900、lockup は `a[href="/"]`、`nav[aria-label="メインナビゲーション"]` に 5 リンク。現在地は `aria-current="page"` / 600 / 丸 `oklch(0.63 0.13 292)`、非選択の丸は透明 | PASS |
| 6 | 選択マークの色の出所 | primary。accent はロゴの点と戻る付きヘッダーのドット専用 | 解決値 `--color-primary: oklch(0.63 0.13 292)` / `--color-accent: oklch(0.62 0.19 40)`。accent が出たのは lockup 内の `circle` と（lg では非表示の）ヘッダーの橙ドットだけ | PASS |
