# E-5: `lg` 以上でモバイル専用の部品が出ない

**結果**: FAIL
**実行時間**: —（観測ログに記録なし）
**セッション**: verify-e-5
**証跡**: E-5.png / E-5.webm

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | 1280×900 の `/`・`/topics`・ドキュメント閲覧 | ヘッダーの lockup・メニューボタン・英語タイトル横の橙の点が出ない | lockup は親が `display:none` で矩形 0×0、メニューボタンも 0×0、橙の点（`oklch(0.62 0.19 40)`）は `display:none` | PASS |
| 2 | メニューボタンを `click()` | ボトムシートは開けない | `dialog.open = false` / `display:none` のまま | PASS |
| 3 | サイドバー | 常設で、現在地が太字＋**橙**の丸 | `aside` 幅 200、現在地は `aria-current="page"` / `font-weight:600` / 丸あり。丸の色は `oklch(0.63 0.13 292)` = `--color-primary`（橙ではない） | FAIL |

## 失敗詳細

- **失敗ステップ**: Step 3
- **期待**: 現在地の丸は橙
- **実際**: 丸は `--color-primary`
- **画面状態**: サイドバー 5 項目、現在地だけ太字＋丸、他は丸なし
- **三分**: **手順書起因**
- **根拠**: `tokens.md` は `--color-accent` を「ロゴアイコンの点・戻る付きページヘッダーのドットのみ」と限定し、選択マークは `--color-primary`。モックの `.side-link[aria-current="page"] .mark` も primary（TC-02 と同根）。
