# E-5: `lg` 以上でモバイル専用の部品が出ない

**再実行（手順書の修正後）**: 2026-09-12 15:50 JST
**テストソース**: `.thread/22/testing.md` エッジケース E-5（改訂後）
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 1280 × 900
**証跡**: E-5.png / E-5.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 1 | 1280 × 900 で `/` を開く | ヘッダーの lockup・メニューボタン・英語タイトル横の橙の点が出ない | `header` 矩形 x=300 / 幅 880 / 高さ 63、`header.innerText` は `タイムライン`（視覚的には `h1` が `sr-only`）。`header` 内の `[aria-label="fog"]` は DOM にあるが矩形 0×0 で親の `display: none`（自身は `display: block`）。`[aria-label="メニュー"]` も矩形 0×0 で親の `display: none`（自身は `display: inline-flex`）。`border-radius: 9999px` の色付き丸（橙の点）はヘッダー内に 0 個 |
| 2 | 1280 × 900 で `/topics` を開く | 同上 | `header` 矩形 x=300 / 幅 880 / 高さ 44、`innerText` は `トピック`。`[aria-label="fog"]` 矩形 0×0 / 親 `display: none`。`[aria-label="メニュー"]` 矩形 0×0 / 親 `display: none`。色付きの丸 0 個 |
| 3 | 1280 × 900 でドキュメント閲覧を開く | 同上 | `header` 矩形 x=300 / 幅 880 / 高さ 69、`innerText` は `document`（英語タイトルは表示されたまま）。`header` 内に `[aria-label="fog"]` は**存在しない**。`[aria-label="メニュー"]` 矩形 0×0 / 親 `display: none`。橙の丸（`background-color: oklch(0.62 0.19 40)`）は DOM に 1 個あるが `display: none` / 矩形 0×0 |
| 4 | ボトムシートを開こうとする | 開けない | ヘッダーの「メニュー」ボタンを通常のクリックで押すと agent-browser が `Element 'header button[aria-label=メニュー]' is covered by <aside.hidden.lg:flex> at its click point` を返してクリックが成立しない。JS から `button.click()` を直接呼ぶと `dialog.open === true` になるが、`dialog` の矩形は 0×0、中の `nav[aria-label="モバイルナビゲーション"]` の矩形も 0×0（画面には出ない） |
| 5 | サイドバーと現在地の示し方を見る | サイドバーが常設で、現在地が太字＋`--color-primary`（紫）の丸 | `aside` 矩形 x=0 / y=0 / 200×900 / `display: flex`。`aside` 内の `svg[aria-label="fog"]` は x=38 / y=40 / 65×26、`role="img"`、含む `a` は `href="/"`。`nav[aria-label="メインナビゲーション"]` は x=24 / y=106 / 152×250、リンクは `タイムライン`/`トピック`/`検索`/`ゴミ箱`/`設定` の 5 つ。`/` では `タイムライン` が `aria-current="page"` / `font-weight: 600` / 行頭マーク `background-color: oklch(0.63 0.13 292)`（6×6 / `border-radius: 9999px`）。`/topics` とドキュメント閲覧では `トピック` が同じ状態。非選択の 4 項目は `aria-current` なし / `font-weight: 500` / 同位置マークの `background-color: rgba(0, 0, 0, 0)` |
| 6 | 選択マークの色がトークンのどれか | 選択マークは primary。橙の `--color-accent` はロゴの点と戻る付きヘッダーのドット専用 | ルート要素の解決値は `--color-primary: oklch(0.63 0.13 292)` / `--color-accent: oklch(0.62 0.19 40)`。サイドバーの選択マークの `background-color` は `oklch(0.63 0.13 292)`（= primary）。`oklch(0.62 0.19 40)`（= accent）が出たのは、ドキュメント閲覧のヘッダーの橙ドット（1280 では `display: none`）と、lockup `svg` 内の `circle`（`fill: oklch(0.62 0.19 40)`）だけ |
