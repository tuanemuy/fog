# E-5: `lg` 以上でモバイル専用の部品が出ない

**テストソース**: `.thread/22/testing.md` エッジケース E-5
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 1280 × 900
**証跡**: E-5.png / E-5.webm

**期待結果**: ヘッダーの lockup・メニューボタン・英語タイトル横の橙の点が出ない。ボトムシートは開けない。サイドバーが常設で、現在地が太字＋橙の丸で示される。

| 画面 | ヘッダーの lockup | メニューボタン | ヘッダーの丸 | `dialog` | `aside` |
|---|---|---|---|---|---|
| `/` | DOM にあるが矩形 0×0、親要素が `display: none` | 矩形 0×0（`display: inline-flex`） | 該当要素なし | `open = false` / `display: none` | 幅 200 / 高さ 900 |
| `/topics` | 同上 | 同上 | 該当要素なし | 同上 | 同上 |
| `/documents/01a093e3-…` | ヘッダー内に `[aria-label="fog"]` は無い | 矩形 0×0 | 1 個（幅 0 / `display: none` / `background-color: oklch(0.62 0.19 40)`） | 同上 | 同上 |

| # | 操作 | 観測した内容 |
|---|---|---|
| 1 | `/` で `header` のメニューボタンを `click()` する | `dialog.open = false`、矩形 0×0、`display: none`（ボトムシートは開かなかった） |
| 2 | サイドバーの現在地表示を見る | `nav[aria-label="メインナビゲーション"]` の 5 項目。`/` では「タイムライン」が `aria-current="page"` / `font-weight: 600` / 丸の `background-color: oklch(0.63 0.13 292)`、他の 4 項目は `aria-current` なし / `font-weight: 500` / 丸なし。`/topics` とドキュメント閲覧では「トピック」が同じ形で現在地。<br>この丸の色 `oklch(0.63 0.13 292)` はトークンの `--color-primary`。ヘッダーの英語タイトル横の丸に使われる `--color-accent` は `oklch(0.62 0.19 40)` |
