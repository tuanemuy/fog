# TC-12: ゴミ箱のアイコン行操作と確認ダイアログ、設定画面

**テストソース**: `.thread/22/testing.md` 確認項目 12
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: TC-12.png / TC-12.webm

開始時のゴミ箱の中身: `トーストの確認3`（メモ）/ `引っ越し`（トピック、配下に `内見メモ`）/ `ロゴの使い分け`（ドキュメント）

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | `/trash` の冒頭の説明文と「空にする」ボタンを見る | 一文「ここにある項目は保持期限を過ぎると完全に削除されます。」と、右に「空にする（N）」 | `main` の先頭 `p` は `"ここにある項目は保持期限を過ぎると完全に削除されます。"`。ボタンのテキストは `"空にする（4）"`、`disabled = false`、`color: oklch(0.55 0.19 27)` |
| 2 | 行の構造を見る | 種別のピル（`メモ` / `ドキュメント` / `トピック`）＋タイトル＋残り日数。行末の操作は 2 つのアイコンボタン | `li[0]`: `トーストの確認3|メモ|残り30日`、ピル `"メモ"`（`border-radius: 9999px` / `background-color: oklch(1 0 0)`）、ボタンは `"トーストの確認3 を復元"` / `"トーストの確認3 を完全に削除"`。`li[1]`: `引っ越し|トピック|残り30日|内見メモ`、ピル `"トピック"`、ボタンは `"引っ越し を復元"` / `"引っ越し を完全に削除"` / `"内見メモ を復元"` / `"内見メモ を完全に削除"`。`li[3]`: `ロゴの使い分け|ドキュメント|残り30日`、ピル `"ドキュメント"`、ボタンは `"ロゴの使い分け を復元"` / `"ロゴの使い分け を完全に削除"`。すべてアイコンボタン（TC-04 参照：svg 1 個・テキストなし）で、`a` は 1 つも無い |
| 3 | メモの行の「〜 を復元」を押し、進行中の表示とトーストを見る | 復元中は行に `復元中…` が出て、成功するとその行が消え、下端にトーストが出る。トーストに「タイムラインで見る」は付かない | `トーストの確認3 を復元` を押した。40ms 間隔のポーリングで捉えた `main` の `innerText` は最初のフレームからすでに `ここにある項目は…|空にする（3）|引っ越し|トピック|残り30日|内見メモ|ロゴの使い分け|ドキュメント|残り30日`（行が消えた後）で、`復元中…` は**観測できなかった**。トーストは `"メモを復元しました"`、操作要素 0 個 |
| 4 | トピックの行の配下のドキュメントの置かれ方を見る | トピックの行の下に一段インデントして小さめに並ぶ | 配下の `内見メモ` の `li` は親の `li`（`引っ越し`）の中にネストされている（`li[1].contains(li[2]) = true`）。`li` の矩形はどちらも x=38 / 幅 314 で同じだが、行内の先頭テキストの x は親 38 / 子 62（+24px）、`font-size` は親 15.2px / 子 12.48px |
| 5 | ドキュメントの行の「〜 を完全に削除」を押し、確認ダイアログを見て「キャンセル」で閉じる | 「完全に削除しますか？」＋「履歴ごと消え、元に戻せません。」＋赤い「完全に削除」＋「キャンセル」。中央のモーダルで、確認ボタンと取り消しボタンが全幅で縦に積まれる | `dialog` は `open = true` / `:modal` に一致 / 矩形 14,291 362×262 / 内側要素の `border-radius: 26px` / `centerX = 195`（ビューポート中心 195）/ `centerY = 422`（ビューポート中心 422）。`innerText` は `完全に削除しますか？|履歴ごと消え、元に戻せません。|完全に削除|キャンセル`。ボタン「完全に削除」は 44,423 302×41 / `background-color: oklch(1 0 0)` / `color: oklch(0.55 0.19 27)` / `border-radius: 9999px`、「キャンセル」は 44,472 302×41 / 背景透明 / `color: oklch(0.52 0.01 275)` / `border-radius: 9999px`（同じ幅 302 で縦に並ぶ）。「キャンセル」を押すと `dialog.open = false`、ゴミ箱の中身は変わらず |
| 6 | 「空にする（3）」を押し、ダイアログを見て「キャンセル」で閉じる | 「ゴミ箱を空にしますか？」＋「全件（N件）が完全に削除され、元に戻せません。」＋「空にする」＋「キャンセル」 | 内側要素は 14,291 362×262 / `border-radius: 26px` / `centerX = 195`。`innerText` は `ゴミ箱を空にしますか？|全件（3件）が完全に削除され、元に戻せません。|空にする|キャンセル`。ボタン「空にする」は 44,423 302×41 / `background-color: oklch(1 0 0)` / `color: oklch(0.55 0.19 27)` / 9999px、「キャンセル」は 44,472 302×41 / 背景透明 / `color: oklch(0.52 0.01 275)` / 9999px。「キャンセル」で `dialog.open = false` |
| 7 | `/settings` の 5 つのセクションの並びと中身を見る | 上から AI → ログイン手段 → ゴミ箱 → データ → アカウント | `h2` の DOM 順は `"AI" > "ログイン手段" > "ゴミ箱" > "データ" > "アカウント"`。`main` の `innerText`: `AI|接続しているAIはありません。AIアプリの設定で fog（http://localhost:3000/mcp）を追加すると接続できます。|ログイン手段|fog-verify-2026-09-12@example.com|メール・パスワード|連携を追加|Google で続行|Apple で続行|ゴミ箱|保持期限|既存のゴミ箱の項目にも適用されます|日|保存|データ|エクスポート|Markdown形式。ゴミ箱・履歴は含まれません|エクスポート|アカウント|現在のパスワード|新しいパスワード|8文字以上|パスワードを変更|パスワードを忘れた|ログアウト` |
| 8 | 「保持期限」を `45` にして「保存」、その後元の値に戻して「保存」 | 成功でトースト「保持期限を保存しました」 | `45` で保存 → トースト `"保持期限を保存しました"`、入力値は `45`。その後 `/trash` の残り日数表示が `残り45日` に変わった。`30` に戻して保存 → 入力値は `30` |

## 観測しなかった項目

- 行の失敗表示（`RowError`）と再試行の語「リトライ」は、失敗を再現できないため確認していない（testing.md の「再現できない場合は確認しない」に従った）。
- `/trash` が空のときの「ゴミ箱は空です」は、ゴミ箱を空にしていないため観測していない。
- `/trash` が空のときの「空にする」が押せないことも同様に観測していない。
