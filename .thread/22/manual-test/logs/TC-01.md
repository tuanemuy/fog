# TC-01: 戻る付きヘッダーの英語タイトルがブランド書体で出る

**テストソース**: `.thread/22/testing.md` 確認項目 1
**アカウント**: `fog-verify-2026-09-12@example.com`
**URL**: http://localhost:3000
**ビューポート**: 390 × 844
**証跡**: TC-01.png / TC-01.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | 390×844 で `/topics/01a093e2-719f-7368-88d6-d66c118ee430`（`ブランド刷新`）を開く | ヘッダー左に `topic` | ヘッダー内のテキストを持つ末端要素は 1 つだけで `SPAN` / `lang="en"` / text=`"topic"` |
| 2 | ヘッダーの `topic` とシート内の本文の書体を比べる | `topic` は `lang="en"` を持ち、本文の日本語スタックとは別の書体（Avenir Next 系。無ければ Helvetica Neue / Arial にフォールバック）／ウェイトは medium ／本文はブランド書体に変わらない | `topic`: `font-family = "Avenir Next", Avenir, "Helvetica Neue", Arial, sans-serif` / `font-weight = 500` / `font-size = 16.8px`。<br>シート内 `h1`（`ブランド刷新`）: `font-family = "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", "Noto Sans JP", sans-serif` / `font-weight = 700`。<br>シート内 `p`（`サイトとロゴの見直し`）: `font-family = "Helvetica Neue", Arial, "Hiragino Kaku Gothic ProN", "Hiragino Sans", "Noto Sans JP", sans-serif` |
| 3 | `/documents/01a093e3-163e-74fa-af9f-78cdbe92a6c9`（`サイト構成の方針`）のヘッダーを見る | ヘッダー左に `document`、ブランド書体、medium | ヘッダー内のテキストを持つ末端要素は 1 つだけで `SPAN` / `lang="en"` / text=`"document"` / `font-family = "Avenir Next", Avenir, "Helvetica Neue", Arial, sans-serif` / `font-weight = 500`。ヘッダー内 `svg` は 5 個で、いずれも `role` 属性なし・`aria-label` なし・`<title>` なし |
| 4 | `/memos/01a093e1-cb25-7028-81c3-6f8d35a388aa/history` のヘッダーを見る | ヘッダー左に `memo`、ブランド書体、medium | ヘッダー内のテキストを持つ末端要素は 1 つだけで `H1` / `lang="en"` / text=`"memo"` / `font-family = "Avenir Next", Avenir, "Helvetica Neue", Arial, sans-serif` / `font-weight = 500`。ヘッダー内 `svg` は 2 個で、いずれも `role` 属性なし・`aria-label` なし・`<title>` なし |

## 補足（観測事実）

- 実行環境は macOS（Darwin 25.6.0）の headless Chrome for Testing。`Avenir Next` が実際にどの字形で描画されたかはスクリーンショット `TC-01.png` に残す。字形（`g` と `a` の形）の目視比較はこのログでは行っていない。
- `topic` は `SPAN`、`memo` は `H1`（要素名が画面によって異なる）。
