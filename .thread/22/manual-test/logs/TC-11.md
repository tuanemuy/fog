# TC-11: 行・空状態・フォームの基準形 — タイムライン / トピック / ドキュメント / 検索

**テストソース**: `.thread/22/testing.md` 確認項目 11
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: TC-11.png / TC-11.webm

## タイムライン

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | `/` で日付の見出し・時刻・メモ本文・「メモの操作」メニューを見る | 日付の見出しは `2026年7月22日(水)` 形式、各メモは時刻（`10:30`）＋本文＋行末の「メモの操作」（点 3 つ）。メニュー項目は「編集」「履歴」「削除」の 3 つで、「削除」だけ赤い | `main` の `h2` は `"2026年9月12日(土)"` の 1 つ。先頭の `article[id^=memo-]` の `innerText` は `13:46|連投1`（`DIV:"13:46"` と `DIV:"連投1"` の 2 つの子）。「メモの操作」の svg は `circle` 3 つ（`c 3,8; c 8,8; c 13,8`）。メニュー項目は `"編集"`（`color: oklch(0.21 0.006 270)`）/ `"履歴"`（同）/ `"削除"`（`color: oklch(0.55 0.19 27)`）の 3 つ |
| 2 | ヘッダーの「キーワードで絞り込む」を押し、絞り込み欄を見る | 虫眼鏡＋入力（プレースホルダー `タイムラインを絞り込む…`） | `input[name="q"]` / `type="text"` / `placeholder="タイムラインを絞り込む…"` / `aria-label="キーワードで絞り込む"`。欄の左に虫眼鏡の svg（`circle` + `path d="M13.5 13.5L17 17"`） |
| 3 | `見積もり` と入れて Enter | 語を入れると右端に「絞り込みを解除」のアイコンが出る。0 件のときは「「見積もり」に一致するメモは見つかりませんでした」の一文と、文字だけのボタン「絞り込みを解除」 | URL は `/?q=%E8%A6%8B%E7%A9%8D%E3%82%82%E3%82%8A`。`main` の `innerText` は `「見積もり」に一致するメモは見つかりませんでした|絞り込みを解除`。「絞り込みを解除」は 2 つあり、(a) アイコンボタン（svg `d="M5 5L15 15M15 5L5 15"` = 線画の ✕、`border-radius: 10px`、背景透明）、(b) 文字だけのボタン（svg 0 個、`border-radius: 9999px`、背景透明） |
| 4 | 「絞り込みを解除」を押して戻る | — | URL が `http://localhost:3000/` に戻った |
| 5 | ヘッダーの「日付を指定して移動」を押し、開くカードを見る | `日付を指定して移動` のラベル＋日付入力＋塗りの小ボタン「移動」 | `form` の `innerText` は `日付を指定して移動|移動`。`label[for]` の文字列は `"日付を指定して移動"`（可視、`position: static`）。`input[type="date"]` は `name="date"` / 154×49 / `border-radius: 10px` / `background-color: oklch(1 0 0)`。ボタン「移動」は 65×39 / `background-color: oklch(0.54 0.15 292)` / `border-radius: 9999px` |
| 6 | メモの無い日付（2026-07-22）へ移動する | 移動先の日付見出しに `2026年7月22日(水)のメモはありません` が添う | URL は `/?date=2026-07-22`。`main` の先頭の `h2` の `textContent` は `2026年9月12日(土)2026年7月22日(水)のメモはありません`（同じ `h2` の中に、当日の日付見出しと「2026年7月22日(水)のメモはありません」が並んでいる）。その後にメモの行が続く |
| 7 | メモが 1 件も無いときの空状態 | 「最初のメモを書いてみましょう」の一文だけ | このアカウントにはメモが残っているため**観測できなかった**（全メモを消さずに再現する手段が無い） |

## トピック

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 8 | `/topics` の行・完了済みの折りたたみ・末尾の追加行を見る | 行はトピック名＋説明＋ドキュメント数で行全体がリンク。完了済みは「完了済み（1）」の折りたたみの中。末尾に `+` アイコン付きの追加行「新しいトピック」 | `main` の `innerText` は `ブランド刷新|ドキュメント数:|1|サイトとロゴの見直し|新しいトピック|完了済み（1）`。行リンク内のテキスト要素は `SPAN:"ドキュメント数:"` と `SPAN:"サイトとロゴの見直し"`。追加行の svg は `d="M10 4V16M4 10H16"`（線画の +）。折りたたみボタンは `"完了済み（1）"` / `aria-expanded="false"`、押すと `innerText` に `読書メモ|ドキュメント数:|0` が加わる |
| 9 | トピックが空のときの空状態 | 「最初のトピックを作ってみましょう」 | 本検証のシード投入前（トピック 0 件）の `/topics` に `paragraph "最初のトピックを作ってみましょう"` が出ていた |
| 10 | `ブランド刷新` の詳細を見る | `h1` はトピック名（ヘッダーではなくシートの中）。枠線ボタン「完了にする」。「ドキュメント」のセクションラベルの下に行、末尾に追加行「新しいドキュメント」 | `main` の `h1` は `"ブランド刷新"`、`header` に `h1` は無い。`h2` は `"ドキュメント"`。`main` の `innerText` は `ブランド刷新|サイトとロゴの見直し|完了にする|ドキュメント|サイト構成の方針|2026年9月12日(土) 更新|新しいドキュメント` |
| 11 | 完了済みトピックの詳細を見る | 「完了を解除」と「完了済み」のラベル | `読書メモ` の `main` の `innerText` は `読書メモ|今年読んだ本の抜き書き|完了済み|完了を解除|ドキュメント|新しいドキュメント` |

## ドキュメント

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 12 | `サイト構成の方針` の閲覧画面を見る | トピック名 → `h1` のタイトル → `2026年7月20日 12:42 更新` → 本文（Markdown 組版）→ 「出典」 | `main` の `innerText` は `ブランド刷新|サイト構成の方針|2026年9月12日 13:32 更新|方針|見出しは3階層まで。|追記：導線は1本にする`。DOM 順は `A:"ブランド刷新"` → `H1:"サイト構成の方針"` → `TIME:"2026年9月12日 13:32"` → `H2:"方針"` → `P:"見出しは3階層まで。"`。**「出典」のセクションはこの画面に出ていない**（出典を 1 件も登録していない状態） |
| 13 | ヘッダーの「編集」を押し、フォームとヘッダーの「保存」を見る | タイトルは枠の無い大きな入力（プレースホルダー `タイトル`）、本文は `本文を書く…`、その下に可視ラベル付きの「変更理由」（プレースホルダー `手動編集`）。保存はヘッダー右の塗りの小ボタン「保存」で、画面内に「やめる」「編集をやめる」は無い | `input[name="title"]`: `placeholder="タイトル"` / `border-width: 0px` / `font-size: 19.55px`、ラベル `"タイトル"` は 1×1px / `position: absolute`（視覚的に非表示）。`textarea[name="body"]`: `placeholder="本文を書く…"` / `border-width: 0px` / `font-size: 15.2px`、ラベル `"本文"` も 1×1px / `absolute`。`input[name="changeReason"]`: `placeholder="手動編集"` / `border-width: 1px`、ラベル `"変更理由"` は 50×19px / `position: static`（可視）。ヘッダーのコントロールは `"戻る"`（背景透明 / radius 10px）/ `"保存"`（`oklch(0.54 0.15 292)` / radius 9999px）/ `"メニュー"`。アプリのルート要素の `innerText` に `やめる` / `編集をやめる` は 0 件 |
| 14 | 新規作成画面の「出典を追加」を見る | 枠線ボタン（ADR-040 により文字は黒寄り） | `main` の `innerText` は `ブランド刷新|タイトル|本文|出典|出典を追加`。「出典を追加」は `background-color: rgba(0, 0, 0, 0)` / `color: oklch(0.21 0.006 270)` / `border: 1px oklch(0.875 0.006 278)` / `border-radius: 9999px` |

## 検索

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 15 | `/search` の初期表示 | 「キーワードでメモとドキュメントを探せます」。入力欄はプレースホルダー `メモとドキュメントを検索…` で送信ボタンは無い。チップの先頭は「すべて」、完了済みトピックのチップには「完了」のバッジ | `main` の `innerText` は `すべて|ブランド刷新|読書メモ|完了|キーワードでメモとドキュメントを探せます`。`input[name="q"]` / `placeholder="メモとドキュメントを検索…"`。`main` 内の `button[type=submit]` は 0 個。チップは `"すべて" -> /search` / `"ブランド刷新" -> /search?topic=01a093e2-719f-7368-88d6-d66c118ee430` / `"読書メモ完了" -> /search?topic=01a093e2-db11-7092-a038-bbdb0dae75d7` |
| 16 | `構成` で検索 | 結果行には「メモ」「ドキュメント」の種別バッジと件数 | `main` の `innerText` は `すべて|ブランド刷新|読書メモ|完了|2件|ドキュメント|サイト構成の方針 ## 方針 見出しは3階層まで。 - 追記：導線は1本にする|2026年9月12日 13:32|ブランド刷新|メモ|ワイヤーフレームの構成、先週の議論を整理して固めた。|2026年9月12日 13:30` |
| 17 | `存在しない語彙xyz` で検索 | 0 件は「「存在しない語彙xyz」に一致するメモ・ドキュメントは見つかりませんでした」 | URL は `/search?q=%E5%AD%98%E5%9C%A8%E3%81%97%E3%81%AA%E3%81%84%E8%AA%9E%E5%BD%99xyz`。`main` の `innerText` は `すべて|ブランド刷新|読書メモ|完了|「存在しない語彙xyz」に一致するメモ・ドキュメントは見つかりませんでした` |

## 履歴 2 画面

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 18 | `/documents/01a093e3-…/history` を開き、版の行を 2 つ押す | 「履歴」のセクションラベルの下に版の行（押すと比較元／比較先のバッジ）、その下に `…→… の差分`、`+` と `−` の行、末尾に `比較元 · 2026年7月20日 12:42` と塗りボタン「この内容に戻す」 | 初期表示の `main.innerText`: `サイト構成の方針|履歴|2026年9月12日 13:32|あなた · 作成|2026年9月12日 13:32|あなた · 構成の追記`（`h2` は `"履歴"`、差分も「この内容に戻す」も無い）。1 行目を押すと `…|比較元|…|2026年9月12日 13:32 → 2026年9月12日 13:32（最新） の差分|## 方針|見出しは3階層まで。|+|追加:|+|追加:|- 追記：導線は1本にする|比較元 · 2026年9月12日 13:32|この内容に戻す`。2 行目も押すと 2 行目に `比較先` バッジが付き、差分の見出しが `2026年9月12日 13:32 → 2026年9月12日 13:32 の差分` になる。選択された版の行は `background-color: oklch(0.93 0.04 292)` / `border-radius: 10px`、「この内容に戻す」は `oklch(0.54 0.15 292)` / `border-radius: 9999px` |
| 19 | `/memos/01a093e1-…/history` で同じことをする | 2 画面が同じ形になっている | 初期表示の `main.innerText`: `履歴|2026年9月12日 13:30|あなた|2026年9月12日 13:31|あなた`。2 行押した後: `履歴|2026年9月12日 13:30|あなた|比較元|2026年9月12日 13:31|あなた|比較先|2026年9月12日 13:30 → 2026年9月12日 13:31 の差分|−|削除:|ワイヤーフレームの構成を固めた。|+|追加:|ワイヤーフレームの構成、先週の議論を整理して固めた。|比較元 · 2026年9月12日 13:30|この内容に戻す`。行と「この内容に戻す」の色・角丸はドキュメント履歴と同じ値。`現在の内容` の文字列は 0 件 |

## 日時の表記（観測値）

| 出た場所 | 文字列 |
|---|---|
| タイムラインの日付見出し | `2026年9月12日(土)` |
| タイムラインの各メモ | `13:46` など時刻のみ |
| トピック詳細のドキュメント行 | `2026年9月12日(土) 更新` |
| ドキュメント閲覧の更新時刻 | `2026年9月12日 13:32 更新` |
| 履歴の版の行・差分の見出し・比較元 | `2026年9月12日 13:32` |
| 検索結果の行 | `2026年9月12日 13:32` / `2026年9月12日 13:30` |
