# TC-11: 行・空状態・フォームの基準形 — タイムライン / トピック / ドキュメント / 検索

**再実行（手順書の修正後）**: 2026-09-12 15:45 JST
**テストソース**: `.thread/22/testing.md` 確認項目 11（改訂後）
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: TC-11.png / TC-11.webm

## タイムライン

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 1 | `/` の日付見出し・時刻・本文・「メモの操作」メニュー | 日付見出しは `2026年7月22日(水)` 形式、各メモは時刻＋本文＋行末の「メモの操作」。メニューは「編集」「履歴」「削除」の 3 つで、「削除」だけ赤い | `h2` は `2026年9月12日(土)`（`font-size: 12.48px` / `font-weight: 600` / `color: oklch(0.21 0.006 270)` / x=38 / y=113 / 314×32.7）。時刻は `time` 要素（`14:45`、`datetime="2026-09-12T05:45:49.598Z"`、`font-size: 11.52px` / `color: oklch(0.69 0.01 275)` / x=38 / 31×17.3）。1 画面に `time` が 15 個。行末は `button aria-label="メモの操作"`（x=328 / 24×24）。メニューは `[role="menu"]`（`aria-label` なし）で `[role="menuitem"]` が 3 つ: `編集`（`color: oklch(0.21 0.006 270)`）/ `履歴`（同）/ `削除`（`color: oklch(0.55 0.19 27)`）。3 つとも `font-weight: 500` / `font-size: 12.48px` / 152×42.7 |
| 2 | ヘッダーの「キーワードで絞り込む」を押して絞り込み欄を見る | 虫眼鏡＋入力（プレースホルダー `タイムラインを絞り込む…`） | `form class="flex items-center gap-md rounded-full bg-neutral-50 p-(--pad-input) …"` の中に `svg data-icon="search"`（`circle cx=9 cy=9 r=5.5` …、`aria-hidden="true"`）と `input type="text"`（`placeholder="タイムラインを絞り込む…"` / `aria-label="キーワードで絞り込む"` / x=84 / y=125 / 252×22.8） |
| 3 | `見積もり` と入れて Enter | 右端に「絞り込みを解除」のアイコンが出る。0 件のときは「「見積もり」に一致するメモは見つかりませんでした」の一文と、文字だけのボタン「絞り込みを解除」 | URL `/?q=%E8%A6%8B%E7%A9%8D%E3%82%82%E3%82%8A`。`main` のテキストは `「見積もり」に一致するメモは見つかりませんでした | 絞り込みを解除` の 2 つだけ。ボタンは 2 つ: アイコンのみの `絞り込みを解除`（x=312 / y=125 / 24×24、`✕` パス `M5 5L15 15M15 5L5 15`）と、文字の `絞り込みを解除`（x=131 / y=305 / 127×38.7、`border-radius: 9999px` / 背景透明 / `color: oklch(0.52 0.01 275)`） |
| 4 | 「絞り込みを解除」を押す | 戻る | URL が `/` に戻り、`main` は `2026年9月12日(土) | 14:45 | レース1 | …` のタイムラインに戻った |
| 5 | ヘッダーの「日付を指定して移動」を押す | `日付を指定して移動` のラベル＋日付入力＋塗りの小ボタン「移動」 | `form` の中に可視ラベル `<label class="font-base text-sm font-medium leading-tight text-neutral-900">日付を指定して移動</label>`（`sr-only` ではない）、`input type="date"`（x=96 / y=104 / 154×48.8、snapshot 上は `spinbutton "年 年"` / `"月 月"` / `"日 日"` と `button "日付選択ツールを表示"`）、`button "移動"`（`border-radius: 9999px` / `background-color: oklch(0.54 0.15 292)` / `color: oklch(1 0 0)` / `padding: 10px 20px` / `font-size: 12.48px` / x=258 / 65×38.7）。`form` 自身は背景透明 / `border-radius: 0px` / 枠 `0px` / `box-shadow: none` |
| 6 | 指定日にメモが無いとき | 移動先の日付見出しに `2026年7月22日(水)のメモはありません` が添う | 日付を `2026-07-22` にして「移動」を押す → URL `/?date=2026-07-22`。`main` の最初の `h2` の `innerText` は `2026年9月12日(土)\n2026年7月22日(水)のメモはありません`（= 一番上の日付見出し `2026年9月12日(土)` の中にこの一文が入っている）。続く行は `14:45 | レース1 | …` のまま |
| 7 | メモが 1 件も無いときの空状態 | 「最初のメモを書いてみましょう」の一文だけ | **観測できなかった**。この環境のアカウントには 15 件以上のメモがあり、全件を消さないと再現できないため実行していない |

## トピック

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 8 | `/topics` の進行中の行・完了済みの折りたたみ・末尾の追加行 | 行はトピック名＋説明＋ドキュメント数で行全体がリンク。完了済みは「完了済み（1）」の折りたたみの中。末尾に `+` アイコン付きの追加行「新しいトピック」 | `main` のテキストは `R-4 確認トピック | ドキュメント数: | 1 | ブランド刷新 | ドキュメント数: | 3 | サイトとロゴの見直し | 引っ越し | ドキュメント数: | 1 | 候補エリアの比較 | 新しいトピック | 完了済み（1）`。各行は `a[href="/topics/{id}"]`（矩形 幅 266、`padding: 0px`）で、同じ行に独立した `button "トピックの操作"`。末尾の `新しいトピック` は `BUTTON`（`padding: 16px 14px` / `color: oklch(0.54 0.15 292)`）で `svg d="M10 4V16M4 10H16"` を持つ |
| 9 | 「完了済み（1）」を開く | 完了済みが中に入っている | 展開後のテキストに `完了済み（1） | 読書メモ | ドキュメント数: | 0` が加わった |
| 10 | 空のときの「最初のトピックを作ってみましょう」 | — | **観測できなかった**（トピックが 4 件あり、空にしていない） |
| 11 | トピック詳細（`ブランド刷新`） | `h1` はトピック名（ヘッダーではなくシートの中）。枠線ボタン「完了にする」。「ドキュメント」のセクションラベルの下に行が並び、末尾に追加行「新しいドキュメント」 | `main` 内の `h1` が `ブランド刷新`、`header` 内に `h1` は無い。`h2` は `ドキュメント` の 1 つ。テキストは `ブランド刷新 | サイトとロゴの見直し | 完了にする | ドキュメント | 長いタイトルの…（省略） | 2026年9月12日(土) 更新 | ロゴの使い分け | 2026年9月12日(土) 更新 | サイト構成の方針 | 2026年9月12日(土) 更新 | 新しいドキュメント`。「完了にする」は `border-radius: 9999px` / 背景透明 / `border: 1px solid oklch(0.875 0.006 278)`（TC-04 の計測） |
| 12 | 完了済みトピック（`読書メモ`） | 「完了を解除」と「完了済み」のラベル | テキストは `読書メモ | 今年読んだ本の抜き書き | 完了済み | 完了を解除 | ドキュメント | 新しいドキュメント` |

## ドキュメント

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 13 | ドキュメント閲覧（`サイト構成の方針`） | トピック名 → `h1` のタイトル → `2026年7月20日 12:42 更新` → 本文。出典 0 件なら「出典」の領域ごと出ない。ヘッダー右にアイコン 3 つ、3 つとも中立色 | 上から順に `P/A "ブランド刷新"`（y=114 / y=115）→ `H1 "サイト構成の方針"`（y=141）→ `P "2026年9月12日 13:32 更新"`（y=178、中に `TIME "2026年9月12日 13:32"`）→ `H2 "方針"`（y=231）→ `P "見出しは3階層まで。"`（y=276）。`main` に `出典` / `元になったメモ` の文字列は **無い**（正規表現一致なし）。ヘッダーのコントロールは `戻る` / `編集` / `履歴を表示` / `削除` / `メニュー` で、3 アイコンはいずれも `color: oklch(0.58 0.01 275)`（TC-04 の計測） |
| 14 | ドキュメント編集 | タイトルは枠の無い大きな入力（プレースホルダー `タイトル`）、本文は `本文を書く…`、その下に可視ラベル付きの「変更理由」（プレースホルダー `手動編集`）。保存はヘッダー右の塗りの小ボタン「保存」で、画面内に「やめる」「編集をやめる」は無い | `input[name="title"]`: `placeholder="タイトル"` / ラベルは `sr-only` / `border-top: 0px solid` / `font-size: 19.55px` / `font-weight: 700`。`textarea[name="body"]`: `placeholder="本文を書く…"` / ラベル `sr-only` / 枠 `0px`。`input[name="changeReason"]`: `placeholder="手動編集"` / ラベル `変更理由` は **`sr-only` ではない**（可視）/ `border-top: 1px solid oklch(0.875 0.006 278)`。ヘッダーは `戻る` / `保存`（`border-radius: 9999px` / `background-color: oklch(0.54 0.15 292)` / `color: oklch(1 0 0)`）/ `メニュー`。`document.body.innerText` に `やめる` / `編集をやめる` は無い |
| 15 | 作成時（`/topics/{id}/documents/new`） | 「出典を追加」の枠線ボタンが出る（文字は黒寄り） | `main` のテキストは `ブランド刷新 | タイトル | 本文 | 出典 | 出典を追加`。「出典を追加」は `border-radius: 9999px` / 背景 `rgba(0, 0, 0, 0)` / `color: oklch(0.21 0.006 270)` / `border-top: 1px oklch(0.875 0.006 278)` |

## 検索

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 16 | `/search` の初期表示 | 「キーワードでメモとドキュメントを探せます」。入力欄はプレースホルダー `メモとドキュメントを検索…` で送信ボタンは無い。チップの先頭は「すべて」、完了済みトピックのチップには「完了」のバッジ | テキストは `すべて | R-4 確認トピック | ブランド刷新 | 引っ越し | 読書メモ | 完了 |  | キーワードでメモとドキュメントを探せます`。`input` は `placeholder="メモとドキュメントを検索…"` / `aria-label="メモとドキュメントを検索"`。`main` 内の `button[type="submit"]` は 0 個。チップは `すべて` / `R-4 確認トピック` / `ブランド刷新` / `引っ越し` / `読書メモ` ＋ バッジ `完了` |
| 17 | `構成` で検索 | 結果行には「メモ」「ドキュメント」の種別バッジと件数 | URL `/search?q=構成`。テキストに `2件` が出て、行は `ドキュメント / サイト構成の方針 ## 方針 見出しは3階層まで。 - 追記：導線は1本にする / 2026年9月12日 13:32 / ブランド刷新` と `メモ / ワイヤーフレームの構成、先週の議論を整理して固めた。 / 2026年9月12日 13:30` |
| 18 | `存在しない語彙xyz` で検索 | 「「存在しない語彙xyz」に一致するメモ・ドキュメントは見つかりませんでした」 | テキストは `すべて | R-4 確認トピック | ブランド刷新 | 引っ越し | 読書メモ | 完了 |  | 「存在しない語彙xyz」に一致するメモ・ドキュメントは見つかりませんでした` |

## 履歴 2 画面

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 19 | `/memos/01a093e1-…/history` | 「履歴」のセクションラベルの下に版の行（押すと比較元／比較先のバッジ）、その下に `…→… の差分`、`+` と `−` の行、末尾に `比較元 · 2026年7月20日 12:42` と塗りボタン「この内容に戻す」 | 初期: `h2` は `履歴` の 1 つ、行は `2026年9月12日 13:30 / あなた` と `2026年9月12日 13:31 / あなた`（どちらも `BUTTON` / `border-radius: 10px` / 背景透明）。1 行目を押すとその行に `比較元` が付き、下に `比較元 · 2026年9月12日 13:30` と `この内容に戻す` が出る。2 行目も押すと `比較先` が付き、`2026年9月12日 13:30 → 2026年9月12日 13:31 の差分` と `− 削除: ワイヤーフレームの構成を固めた。` / `+ 追加: ワイヤーフレームの構成、先週の議論を整理して固めた。`。選択中の 2 行は `background-color: oklch(0.93 0.04 292)`。「この内容に戻す」は `border-radius: 9999px` / `background-color: oklch(0.54 0.15 292)` / `color: oklch(1 0 0)`。`h1` はこの画面には無い（ヘッダーの `memo` が `h1`）。`現在の内容` の欄は無い |
| 20 | `/documents/01a093e3-…/history` | 同じ形 | `h1` は `サイト構成の方針`、`h2` は `履歴`。行は `2026年9月12日 13:32 / あなた · 作成` と `2026年9月12日 13:32 / あなた · 構成の追記`。2 行選ぶと `比較元` / `比較先` のバッジ、`2026年9月12日 13:32 → 2026年9月12日 13:32 の差分`、`## 方針 | 見出しは3階層まで。 | + 追加: | + 追加: | - 追記：導線は1本にする`、末尾に `比較元 · 2026年9月12日 13:32` と `この内容に戻す`（同じ計算値）。選択中の行の背景も同じ `oklch(0.93 0.04 292)` |

## 観測できなかったこと

- タイムラインの空状態（「最初のメモを書いてみましょう」）とトピック一覧の空状態（「最初のトピックを作ってみましょう」）は、既存データを全消ししないと再現できないため実行していない。
- 日時は全画面で `2026年9月12日 13:32` / `2026年9月12日(土)` 形式で、`7月20日 12:30` 形式（年を落とす形）は出なかった。
