# E-1: トーストが操作を持たず、続けて出しても積み上がらない

**テストソース**: `.thread/22/testing.md` エッジケース E-1
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: E-1.png / E-1.webm

開始時のゴミ箱: `引っ越し`（トピック、配下に `内見メモ`）/ `ロゴの使い分け`（ドキュメント） = 「空にする（3）」。「復元」ボタンは `引っ越し を復元` / `内見メモ を復元` / `ロゴの使い分け を復元` の 3 つ。

**期待結果**: `/trash` で 3 件を続けて「復元」する。同じ文言のトーストは 1 つに置き換わり、違う文言なら最大 3 つまで縦に積まれる。どのトーストにも閉じるボタン・取り消しリンクが無く、押せる要素を含まない。約 4 秒で消える。

**観測した内容**（`requestAnimationFrame` で `main` の `innerText` と可視トーストを記録。時刻は `Date.now() % 100000` のミリ秒）

### 行の状態

| 時刻 | `main` の `innerText` |
|---|---|
| 52727 | `ここにある項目は保持期限を過ぎると完全に削除されます。|空にする（3）|引っ越し|トピック|残り30日|内見メモ|ロゴの使い分け|ドキュメント|残り30日` |
| 52742 | `…|空にする（3）|引っ越し|トピック|復元中…|内見メモ|復元中…|ロゴの使い分け|ドキュメント|復元中…` |
| 52782 | `…|空にする（1）|ロゴの使い分け|ドキュメント|復元中…` |
| 52798 | `…|空にする（1）|「内見メモ」はゴミ箱に見つかりません。完全に削除されたか、別の画面で復元されています|一覧を読み直す|ロゴの使い分け|ドキュメント|復元中…` |
| 52809 以降 | `…|空にする|「内見メモ」はゴミ箱に見つかりません。完全に削除されたか、別の画面で復元されています|一覧を読み直す|ゴミ箱は空です` |

### トースト

- 可視の `[role=status]` の要素は終始 **1 個**。その中に文言が改行区切りで入った。
- 52782: `トピックを復元しました`（操作要素 0 個）
- 52809: 同じ要素の中身が `トピックを復元しました` + `ドキュメントを復元しました` の 2 行になる（操作要素 0 個）
- 56774: `ドキュメントを復元しました` の 1 行だけになる
- 56808: 可視トースト 0 個
- 表示時間: `トピックを復元しました` は 52782 → 56774（約 3992ms）、`ドキュメントを復元しました` は 52809 → 56808（約 3999ms）
- 3 回の復元のうち 2 回はドキュメント（`内見メモ` と `ロゴの使い分け`）なので、文言は `トピックを復元しました` と `ドキュメントを復元しました` の 2 種類。同じ文言が 2 つ並んだ瞬間は観測されなかった。
- どのトーストにも `button` / `a` / `[role=button]` は 0 個。「タイムラインで見る」の文字列も無い。

### 行の下に出た失敗表示

- `role="alert"`: テキスト `「内見メモ」はゴミ箱に見つかりません。完全に削除されたか、別の画面で復元されています|一覧を読み直す`、矩形 x=38 / y=192 / 314×108、`background-color: oklch(0.95 0.025 20)`、`color: oklch(0.45 0.19 27)`、中のボタンは `"一覧を読み直す"` 1 つ。
- この表示はトーストではなく画面に残り続けた（4 秒では消えなかった）。
- 再試行の語は `リトライ` でも `再試行` でもなく `一覧を読み直す`。

### 終了時の状態

- `main` の `innerText`: `ここにある項目は保持期限を過ぎると完全に削除されます。|空にする|「内見メモ」はゴミ箱に見つかりません。…|一覧を読み直す|ゴミ箱は空です`
- 「空にする」ボタン（件数なし）は `disabled = true`
- 空のときの表示は `ゴミ箱は空です`

## 補足（観測事実）

- `引っ越し を復元` を押すと配下の `内見メモ` も一緒に戻ったため、同時に押した `内見メモ を復元` が「ゴミ箱に見つかりません」になった。この 3 ボタン同時押しは、testing.md の「3 件を続けて復元する」を、ゴミ箱に残っていた 3 つの復元ボタンすべてに対して行ったもの。
