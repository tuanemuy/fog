# R-3: シートがスクロールコンテナになったことによる影響

**テストソース**: `.thread/22/testing.md` 既存機能への影響確認 R-3
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: R-3.png / R-3.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | `/` を下までスクロールし、メモの行を開いて（履歴へ遷移）から戻る | 戻った位置がスクロール前の位置に復元される | `/` の `main` は `scrollHeight = 1105` / `clientHeight = 771`。`main.scrollTop = scrollHeight` を代入すると 334（最大値）。最下部のメモ（`memo-01a093e1-c450-73fc-b69f-7866f73cb9c8`）の「メモの操作」→「履歴」で URL が `http://localhost:3000/memos/01a093e1-c450-73fc-b69f-7866f73cb9c8/history`。ブラウザの戻るで `/` に戻ると `main.scrollTop = 334`（遷移前と同じ値）、`scrollHeight = 1105` |
| 2 | `/` を下端までスクロールし続けて、過去のメモが自動で読み込まれる（番兵による。「過去のメモを読み込む」のボタンは無い） | 自動で読み込まれる | 「過去のメモを読み込む」「新しいメモを読み込む」の文字列はアプリのルート要素の `innerText` に 0 件。<br>自動読み込みは**観測できなかった**: このアカウントのメモは 10 件で 1 ページに収まっており、下端まで（`scrollTop = 334` = 最大値）スクロールして 2 秒待っても `scrollHeight` は 1105 のまま、メモ件数は 10 件のまま、`[aria-busy=true]` は 0 個だった。`main` 内に高さ 8px 以下でテキストを持たない末端要素を探したが、見つかったのは「メモの操作」アイコンの `circle`（3×2.8px）だけで、番兵らしき要素は特定できなかった |
| 3 | `/?memo={既存のメモID}` を開く | そのメモの位置までシートがスクロールし、対象の行が強調される | `/?memo=01a093e1-c450-73fc-b69f-7866f73cb9c8` を開くと `main.scrollTop = 334`（`scrollHeight - clientHeight` = 334 と一致）、対象の `article` の矩形 y=593（ビューポート高 844 の中）。対象の `article` の `background-color` は `oklch(0.93 0.04 292)`、`class` は `-mx-md rounded-md bg-primary-lighter px-md py-sm`。`box-shadow` は `none` |
