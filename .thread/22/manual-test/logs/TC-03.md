# TC-03: モバイルのナビはハンドル付きの白いボトムシート

**再実行（手順書の修正後）**: 2026-09-12 15:07 JST
**テストソース**: `.thread/22/testing.md` 確認項目 3（改訂後）
**アカウント**: `fog-verify-2026-09-12@example.com`
**証跡**: TC-03.png / TC-03.webm

## モバイル幅（390 × 844）

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 1 | `/topics` でヘッダー右の「メニュー」を押す | シートが開く | `button[aria-label="メニュー"]` をクリック → `snapshot -i -c` は `- dialog "メニュー"` の中に `navigation "モバイルナビゲーション"` と 5 つの link |
| 2 | シートの見た目を見る | 画面下端から迫り上がる白いカード。上辺だけ丸い。中央上部に短い横棒（ハンドル）。左右はシート本体と同じ横フレーム | `DIALOG`（`aria-label="メニュー"`、`role` 属性なし、`aria-modal` 属性なし、`d.matches(':modal') === true`、`open === true`）。`position: fixed` / `inset: 494.016px 14px 0px` / 矩形 x=14 / y=494 / 362×350。dialog 自身は `border-radius: 0px` / `background-color: rgba(0, 0, 0, 0)` で、直下の `DIV`（`class="flex flex-col gap-md rounded-t-lg bg-bg-card px-lg pt-md pb-safe-b-2xl shadow-lg"`）が矩形 x=14 / y=494 / 362×350、`border-radius: 26px 26px 0px 0px`、`background-color: oklch(1 0 0)`、`box-shadow` の最後が `oklch(0.21 0.006 270 / 0.2) 0px -8px 40px 0px`。ハンドルは `DIV` 矩形 x=177 / y=508 / 36×4、`background-color: oklch(0.875 0.006 278)`、`border-radius: 9999px`、`aria-hidden="true"`、テキスト空。シートの左右 x=14 / 幅 362 は `main` の矩形（x=14 / 幅 362）と一致 |
| 3 | 背後の覆いを見る | 暗い覆いになり操作できない | 別要素のオーバーレイは検出されず（全画面を覆う不透明背景の要素は 0 個）。`dialog::backdrop` の計算値が `background-color: oklch(0.21 0.006 270 / 0.28)` / `backdrop-filter: none` / `opacity: 1`。ネイティブ modal（`:modal` に一致）なので背後は不活性 |
| 4 | 項目と現在地の示し方を見る | 5 項目が縦に並び、間にヘアライン 1 本。現在地だけ太字＋行頭に `--color-primary`（紫）の丸。他の項目の丸は見えない | `nav[aria-label="モバイルナビゲーション"]` の下に `タイムライン`→`/`、`トピック`→`/topics`、`検索`→`/search`、`ゴミ箱`→`/trash`、`設定`→`/settings`。2 番目以降の `A` は `border-top: 1px oklch(0.945 0.004 286)`、先頭は `border-top: 0px`。`/topics` では `トピック` が `aria-current="page"` / `font-weight: 600`、行頭マークの `background-color: oklch(0.63 0.13 292)`（6×6 / `border-radius: 9999px`）。他 4 項目は `aria-current` なし / `font-weight: 500` / 同位置マークの `background-color: rgba(0, 0, 0, 0)` |
| 5 | ハンドルを押す | 装飾（`aria-hidden`）で何も起きない | ハンドル中央 (195, 510) を `mouse down`/`mouse up` → `dialog.open === true` のまま、URL は `/topics` のまま |
| 6 | シートの外（覆いの部分）を押す | 閉じる | (195, 200) を `mouse down`/`mouse up` → `dialog.open === false`、URL `/topics`、`document.activeElement` は `BUTTON aria-label="メニュー"` |
| 7 | もう一度「メニュー」→ Escape | 閉じる | `dialog.open === false`、URL `/topics`、`document.activeElement` は `BUTTON aria-label="メニュー"` |
| 8 | もう一度「メニュー」→ 項目「検索」 | `/search` へ遷移し、シートは閉じている | `dialog a[href="/search"]` をクリック → URL `/search`、`dialog.open === false`。ヘッダーの「メニュー」ボタンは存在 |
| 9 | 開いている間、背後のシートがスクロールしないこと | スクロールしない | `/settings`（`main` は scrollHeight 1402 / clientHeight 771 / `overflow-y: auto`）でシートを開いた状態で、背後 (195, 300) にマウスを置いて `scroll down 300` → `main.scrollTop` は 0 のまま、`window.scrollY` も 0、`dialog.open === true`。`document.body` の `overflow` は `visible`。なお JS から `main.scrollTop = 400` と代入した場合は 400 になる（ユーザー操作ではない） |

## `lg` 以上（1280 × 900）

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 10 | `/topics` でヘッダーを見る | メニューボタンもボトムシートも出ない。サイドバーが同じ 5 項目を常設で持つ | `button[aria-label="メニュー"]` は DOM に存在するが矩形 0×0、自身は `display: inline-flex`、親が `display: none`。`dialog` は DOM に存在し `open === false`。`aside` は存在（TC-02 の観測どおり 200×900 で 5 項目） |
