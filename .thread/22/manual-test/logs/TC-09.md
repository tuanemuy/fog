# TC-09: 読み込み失敗と 404 が枠の中に共通の形で出る

**テストソース**: `.thread/22/testing.md` 確認項目 9
**アカウント**: `fog-verify-2026-09-12@example.com`（ログイン済み）
**ビューポート**: 390 × 844
**証跡**: TC-09.png / TC-09.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | `/documents/` に `a` を 250 文字並べた URL を開く | シートの中が「読み込めませんでした」の 1 文と、その下の塗りボタン「再試行」だけになる。ヘッダーとナビはそのまま残る。サーバーが返した文言・スタック・エラーコードは出ない。`role="alert"` として読み上げられる | `main` の `innerText` は `読み込めませんでした|再試行` の 2 行だけ。`header` は幅 362 / 高さ 74 で残り、その `innerText` は `document`。`[aria-label="メニュー"]` は幅 35 で表示されたまま。`aside` は DOM にあるが幅 0（モバイル幅なので）。`role="alert"` の要素: テキスト `読み込めませんでした|再試行`、矩形 x=38 / 幅 314、`text-align: start`、中のボタンは「再試行」1 つで `background-color: oklch(0.54 0.15 292)` / `border-radius: 9999px` |
| 2 | 「再試行」を押す | ローダーが走り直し、ボタンが一瞬押せなくなってから戻る。原因が残っているので同じ表示のままで、ボタンは再び押せる | ボタンの状態遷移は `text="再試行" disabled=true` → `text="再試行" disabled=false`。最終的な `main` の `innerText` は `読み込めませんでした|再試行` で変化なし |
| 3 | `http://localhost:3000/no-such-page` を開く | アプリシェルではなく認証シートの枠（画面中央の白いカード、頭に lockup）の中に「ページが見つかりません」と塗りボタン「タイムラインへ」。押すと `/` へ行く | URL は `/no-such-page`。`header` なし、`aside` なし、`[aria-label="メニュー"]` なし、`nav` は 0 個。`[aria-label="fog"]` の svg が 65×26 で x=163 / y=322（中心 X=195.5、ビューポート中心 195）。`main` は x=14 / y=282 / 362×281、`border-radius: 26px`、`background-color: oklch(1 0 0)`、`box-shadow` あり。`main` の `innerText` は `ページが見つかりません|タイムラインへ`。ボタン「タイムラインへ」は `href="/"` / `background-color: oklch(0.54 0.15 292)` / `border-radius: 9999px`。押すと URL が `http://localhost:3000/` になった。`role="alert"` の要素は 0 個 |
| 4 | ログイン済みのまま `/topics/no-such-topic-id` を開く | アプリシェルのシートの中に「トピックが見つかりません」＋「トピック一覧へ」 | URL は `/topics/no-such-topic-id`。`header` は幅 362 / 高さ 74 で `innerText` は `topic`。`[aria-label="メニュー"]` は幅 35。`main` の `innerText` は `トピックが見つかりません|トピック一覧へ`。ボタンは `href="/topics"` / `background-color: oklch(0.54 0.15 292)` / `border-radius: 9999px`。`role="alert"` は 0 個 |
| 5 | `/memos/no-such-memo-id/history` を開く | 「メモが見つかりません」＋「タイムラインへ」 | URL は `/memos/no-such-memo-id/history`。`header` は幅 362 / 高さ 74 で `innerText` は `memo`。`main` の `innerText` は `メモが見つかりません|タイムラインへ`。ボタンは `href="/"` / `background-color: oklch(0.54 0.15 292)` / `border-radius: 9999px`。`role="alert"` は 0 個 |

## 形の比較（期待結果「ルートエラーと 404 が同じ形（中央寄せの一文＋操作 1 つ）」）

| 画面 | 一文 | 操作 | `role="alert"` | `text-align` |
|---|---|---|---|---|
| ルートエラー（長い documentId） | 読み込めませんでした | 再試行（塗り・9999px） | あり | `start` |
| `/no-such-page` | ページが見つかりません | タイムラインへ（塗り・9999px） | なし | 未計測（`role=alert` が無いため同じ計測を取っていない） |
| `/topics/no-such-topic-id` | トピックが見つかりません | トピック一覧へ（塗り・9999px） | なし | 同上 |
| `/memos/no-such-memo-id/history` | メモが見つかりません | タイムラインへ（塗り・9999px） | なし | 同上 |

## 観測しなかった項目

- 「`_app` / `_sheet` のレイアウトルート自身の失敗」と「アプリシェル内の 404」は testing.md の指示どおりブラウザから起こしていない。
