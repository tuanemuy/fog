# R-4: 基本フローが壊れていない

**テストソース**: `.thread/22/testing.md` 既存機能への影響確認 R-4
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: R-4.png / R-4.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | `/` でメモ `R-4 の基本フロー確認` を投稿する | タイムラインに即時反映 | `requestAnimationFrame` ごとの `件数/本文一致`: `10/false`（クリック直前）→ 15ms 後に `11/true` → … 途中 2 フレームだけ `10/false` に戻り → `11/true` に確定。最終 11 件 |
| 2 | `/topics` でトピック `R-4 確認トピック` を作る | 一覧に即時反映 → 詳細へ遷移 | 「追加」押下の 5ms 後に `main` の `innerText` に `R-4 確認トピック` が現れ、途中 1 フレームだけ消えて再び現れた。行をクリックすると URL が `http://localhost:3000/topics/01a0940b-e0af-730c-869c-a7cc964abcd9`、`main` の `innerText` は `R-4 確認トピック|完了にする|ドキュメント|新しいドキュメント` |
| 3 | そのトピックにドキュメント `R-4 確認ドキュメント` を作る | 閲覧へ遷移 | 保存後の URL は `http://localhost:3000/documents/01a0940c-27cb-7279-a3be-e4ebd1400cfb`、`main` の `innerText` は `R-4 確認トピック|R-4 確認ドキュメント|2026年9月12日 14:16 更新|最初の版。` |
| 4 | 編集 → 本文に追記し「変更理由」に `R-4 の追記` を入れて保存 | 「保存しました」のトースト | トースト `"保存しました"`、URL は `/documents/01a0940c-27cb-7279-a3be-e4ebd1400cfb` |
| 5 | 履歴から「この内容に戻す」を押す | トースト「{日時} の内容に戻しました」→ 対象の画面へ遷移 | 「この内容に戻す」を押すと、まず `main` の中に `dialog` が開く。その `innerText` は `この内容に戻しますか？|2026年9月12日 14:16 の内容で新しいリビジョンを作ります。これまでの履歴は残ります。|戻す|キャンセル`（ボタンは `戻す` と `キャンセル`）。<br>この確認ダイアログを閉じずに放置している間はトーストも遷移も起きない。<br>「戻す」を押すと、トースト `"2026年9月12日 14:16 の内容に戻しました"`、URL が `/documents/01a0940c-27cb-7279-a3be-e4ebd1400cfb/history` → `/documents/01a0940c-27cb-7279-a3be-e4ebd1400cfb` に遷移。`main` の `innerText` は `R-4 確認トピック|R-4 確認ドキュメント|2026年9月12日 14:18 更新|最初の版。` |
| 6 | `/settings` の「ログアウト」を押す | `/login` へ全画面遷移 | URL が `http://localhost:3000/login`。`performance.getEntriesByType('navigation')` は押す前が 1 件、押した後も 1 件で `type = "navigate"` / `name = "http://localhost:3000/login"`（＝ドキュメントの読み込みが `/login` で起きている＝全画面遷移） |

## 補足（観測事実・環境への影響）

- testing.md の手順には「この内容に戻す」の後の確認ダイアログが書かれていないが、実装では確認ダイアログが 1 段挟まる。
- メモ `R-4 の基本フロー確認`、トピック `R-4 確認トピック`、ドキュメント `R-4 確認ドキュメント`（3 版）を作成した。
