# R-5: 撤去された導線

**テストソース**: `.thread/22/testing.md` 既存機能への影響確認 R-5
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: R-5.png / R-5.webm

**期待結果**: ゴミ箱の復元成功のトーストに「タイムラインで見る」が無い。ドキュメント編集の画面内に「編集をやめる」「やめる」が無く、戻りはヘッダーの「戻る」とブラウザの戻るだけ。タイムラインに「過去のメモを読み込む」「新しいメモを読み込む」のボタンが無い。**いずれも撤去が正しい状態**。

| # | 操作 | 観測した内容 |
|---|---|---|
| 1 | `/trash`（`空にする（1）` / `連投1|メモ|残り30日`）で `連投1 を復元` を押す | トースト `"メモを復元しました"`。トーストの中の `a` / `button` / `[role=button]` は 0 個。`innerHTML` の長さは 173 文字で、`タイムラインで見る` の文字列は含まれない |
| 2 | `/documents/01a0940c-…/edit` を開く | アプリのルート要素の `innerText` に `編集をやめる` は 0 件、`やめる` も 0 件。画面上の可視ボタン・リンクは `"本文へ移動"` / `"戻る"` / `"保存"` / `"メニュー"` / `"R-4 確認トピック"` の 5 つだけ |
| 3 | `/` を下端までスクロールして、読み込みのボタンを探す | アプリのルート要素の `innerText` に `過去のメモを読み込む` は 0 件、`新しいメモを読み込む` も 0 件。`main` 内の `button` は `メモの操作` が 12 個だけ |
