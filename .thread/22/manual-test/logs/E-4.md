# E-4: コンポーザーの二重送信と空送信

**テストソース**: `.thread/22/testing.md` エッジケース E-4
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: E-4.png / E-4.webm

**期待結果**: 空のまま送信ボタンを押そうとする（押せない）。次に文字を入れて素早く 2 回続けて押す。メモは 1 件しか増えず、送信中はアイコンがスピナーになる。

| # | 操作 | 観測した内容 |
|---|---|---|
| 1 | `/` で入力欄が空のまま送信ボタンの状態を見る | `textarea.value = ""`、`button[aria-label="メモを追加"]` は `disabled = true`。タイムラインのメモは 9 件 |
| 2 | その状態で送信ボタンをクリックする | `agent-browser` の `click` は `✓ Done` を返したが、メモの件数は 9 件のまま変化なし |
| 3 | `二重送信の確認` と入力し、`click()` を 2 回連続で呼ぶ | `requestAnimationFrame` ごとの状態遷移（件数 / `disabled` / svg の `animation-name`）:<br>`count=9 disabled=false anim=none` → `count=10 disabled=true anim=spin` → `count=9 disabled=true anim=none` → `count=10 disabled=true anim=none` |
| 4 | 結果を確認する | 最終的なメモ件数は 10 件（+1）。本文に `二重送信の確認` を含む `article` は **1 件**。送信中にボタンは `disabled = true` になり、svg の `animation-name` が `spin` になった |

## 補足（環境への影響）

- メモ `二重送信の確認` を 1 件投稿した。
