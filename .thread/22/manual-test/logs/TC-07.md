# TC-07: エラーは各持ち場に出て、画面に持続する説明はインラインに残る

**テストソース**: `.thread/22/testing.md` 確認項目 7
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: TC-07.png / TC-07.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|---|---|---|
| 1 | ログアウトして `/login` を開き、`fog-verify-2026-09-12@example.com` と誤ったパスワード `wrong-password-x` で「ログイン」を押す | ログイン失敗のメッセージが、メールアドレス欄の**上**に赤い枠で出る。トーストにはならない | `/settings` の「ログアウト」を押すと URL が `/login` に遷移。ログイン失敗後も URL は `/login`。`role="alert"` の要素: テキスト `"メールアドレスまたはパスワードが正しくありません"`、矩形 x=38 / y=204 / 314×67、`border-top: 1px oklch(0.55 0.19 27)`、`color: oklch(0.45 0.19 27)`、`background-color: oklch(0.95 0.025 20)`、リンク 0 個。`input[name=email]` は y=322、`input[name=password]` は y=422（アラートはその上） |
| 2 | `/signup` を開き、既に登録済みの `fog-verify-2026-09-12@example.com` と `fog-test-password-2026` で「登録する」を押す | 「このメールアドレスは既に登録されています。」と、同じ枠の中に「ログイン」へのリンク | URL は `/signup` のまま。`role="alert"`: テキスト `"このメールアドレスは既に登録されています。 ログイン"`、矩形 x=38 / y=205 / 314×67、`border-top: 1px oklch(0.55 0.19 27)`、`background-color: oklch(0.95 0.025 20)`。枠内のリンクは `ログイン -> /login` の 1 つ。`input[name=email]` は y=323（アラートはその上） |
| 3 | `/signup` で「パスワード」に `short` と入れて「登録する」を押す | 「パスワード」欄の直下に「8文字以上で入力してください」 | `role="alert"`: テキスト `"8文字以上で入力してください"`、矩形 x=38 / y=443 / 314×19、`border-width: 0px`、`color: oklch(0.55 0.19 27)`、背景透明。`input[name=password]` は y=387 / bottom=435、`aria-invalid="true"`、`aria-describedby="_R_3apaq_-error _R_3apaq_-helper"`。補助文 `"8文字以上"`（`P`、y=360、`color: oklch(0.52 0.01 275)`、`role` なし）はそのまま残る |
| 4 | 再ログインして `/topics` を開き、「新しいトピック」→ 名前を空のまま「追加」を押す | 名前欄の直下に「トピック名を入力してください」 | 再ログイン後の URL は `/`。`role="alert"`: テキスト `"トピック名を入力してください"`、矩形 x=38 / y=274 / 314×19、`border-width: 0px`、`color: oklch(0.55 0.19 27)`、背景透明。`input[name=name]` は y=217 / bottom=266、`aria-invalid="true"`、`aria-describedby="_r_3_-error"` |
| 5 | `/documents/01a093e3-163e-74fa-af9f-78cdbe92a6c9/edit` でタイトルを空にして「保存」を押す | 「タイトルを入力してください」 | `role="alert"`: テキスト `"タイトルを入力してください"`、矩形 x=38 / y=173 / 314×19、`color: oklch(0.55 0.19 27)`。`input[name=title]` は y=135 / bottom=165、`aria-invalid="true"` |
| 6 | `/settings` の「保持期限」に `0` を入れて「保存」を押す | 数値欄の直下に「1以上の日数を入力してください」 | `input[name=retentionDays]` は `type="number"` / `min="1"` / `required`。`0` を入れて「保存」を押した直後は `role="alert"` が 0 件、トーストも無し、ページ本文に `1以上` の文字列も無し。このとき `input.validity.rangeUnderflow = true` で `input.validationMessage` は `"値は 1 以上にする必要があります。"`（ブラウザ組み込みの検証メッセージ）、`form.noValidate = false`。<br>`form.noValidate = true` にして同じボタンを押すと、`role="alert"`: テキスト `"1以上の日数を入力してください"`、y=746、`color: oklch(0.55 0.19 27)`。`input[name=retentionDays]` は y=684 / bottom=732、`aria-invalid="true"`（アラートはその直下）。トーストは出ない |
| 7 | `/password-reset` を開き、`fog-verify-2026-09-12@example.com` で「リセットメールを送る」を押す | タイトル下の一文は送信の前後を通じて常に表示され続ける。送信の成功はトーストで出てフォームはそのまま残る | 送信前のページ本文: `パスワードリセット | アカウントが存在する場合、リセットリンクをお送りします | メールアドレス | リセットメールを送る`。送信後: トースト `"リセットメールの送信を受け付けました"`（操作要素 0 個）、URL は `/password-reset` のまま、ページ本文は送信前と同一文字列 |
| 8 | 再試行の語を見る | 行の下は「リトライ」、面は「再試行」 | 上記 7 ステップのいずれの画面でも、アプリのルート要素の `innerText` に `再試行` / `リトライ` は 1 件も現れなかった（このシナリオでは再試行を伴う失敗を起こしていない） |

## 補足（観測事実・環境への影響）

- Step 3 で `fog-verify-weak-2026-09-12@example.com` をメール欄に入れたが、パスワード検証で止まっているためアカウントは作られていない。
- Step 6 のあと、保持期限を `30` に戻して保存した。
- どのエラーもトーストにはならず、数秒で消える挙動は観測されなかった（観測時点では表示され続けていた）。
