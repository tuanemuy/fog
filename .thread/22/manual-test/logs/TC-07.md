# TC-07: エラーは各持ち場に出て、画面に持続する説明はインラインに残る

**再実行（手順書の修正後）**: 2026-09-12 15:23 JST
**テストソース**: `.thread/22/testing.md` 確認項目 7（改訂後）
**アカウント**: `fog-verify-2026-09-12@example.com`
**ビューポート**: 390 × 844
**証跡**: TC-07.png / TC-07.webm

| # | 操作 | 期待結果 | 観測した内容 |
|---|------|---------|-------------|
| 1 | ログアウト状態の `/login` に `fog-verify-2026-09-12@example.com` ＋ 誤ったパスワード `wrong-password-x` で「ログイン」 | フォームの先頭（`FormError`）にメッセージが、メールアドレス欄の**上**に赤い枠で出る | URL は `/login` のまま。`[role="alert"]` が 1 つ: text `メールアドレスまたはパスワードが正しくありません`、矩形 x=38 / y=204 / 314×67、`color: oklch(0.45 0.19 27)`、`background-color: oklch(0.95 0.025 20)`、`border-top: 1px solid oklch(0.55 0.19 27)`、`border-radius: 10px`、`font-size: 12.48px`。メールアドレス欄は y=322（= 枠の下）。トースト領域（`div[aria-live="polite"][role="status"]`）は矩形 0×0 で空 |
| 2 | `/signup` に登録済みの `fog-verify-2026-09-12@example.com` ＋ `fog-test-password-2026` で「登録する」 | 「このメールアドレスは既に登録されています。」と、同じ枠の中に「ログイン」へのリンク | URL は `/signup` のまま。`[role="alert"]` text `このメールアドレスは既に登録されています。 ログイン`、矩形 x=38 / y=205 / 314×67、同じ計算値（`oklch(0.45 0.19 27)` / `oklch(0.95 0.025 20)` / `1px solid oklch(0.55 0.19 27)` / `10px`）。枠の中の `a` は text `ログイン` / `href="/login"`。メールアドレス欄は y=323 |
| 3 | `/signup` で「パスワード」に `short` を入れて「登録する」 | 「パスワード」欄の直下に「8文字以上で入力してください」 | `[role="alert"]` text `8文字以上で入力してください`、矩形 x=38 / y=443 / 314×19、`color: oklch(0.55 0.19 27)`、`background-color: rgba(0, 0, 0, 0)`、`border-top: 0px`、`border-radius: 0px`、`font-size: 12.48px`。パスワード欄は y=387 / 高さ 49（下端 436）で、エラーはその直下。パスワード欄は `aria-invalid="true"` / `aria-describedby="_R_3apaq_-error _R_3apaq_-helper"` |
| 4 | 再ログインして `/topics` →「新しいトピック」→ 名前を空のまま「追加」 | 名前欄の直下に「トピック名を入力してください」 | `[role="alert"]` text `トピック名を入力してください`、矩形 x=38 / y=423 / 314×19、`color: oklch(0.55 0.19 27)` / 背景透明 / 枠 `0px`。`input[name="name"]`（ラベル `トピック名`）は y=367 / 高さ 49（下端 416）、`aria-invalid="true"` / `aria-describedby="_r_7_-error"` |
| 5 | `/documents/01a093e3-163e-74fa-af9f-78cdbe92a6c9/edit` でタイトルを空にして「保存」 | 「タイトルを入力してください」 | URL は `/documents/.../edit` のまま。`[role="alert"]` text `タイトルを入力してください`、矩形 x=38 / y=173 / 314×19、`color: oklch(0.55 0.19 27)` / 背景透明 / 枠 `0px`。`input[name="title"]` は y=135 / 高さ 29（下端 164）、`aria-invalid="true"` / `aria-describedby="_r_3_"` |
| 6 | 保持期限の `0` | ブラウザから確認しない | 手順どおり実行していない |
| 7 | 失敗がトーストにならないこと | いずれの失敗もトーストにならず、数秒で消えない | 1〜5 のいずれでも `div[aria-live="polite"][role="status"]`（トースト領域）は text が空で矩形 0×0。エラーは `[role="alert"]` として画面にとどまり、`wait` を挟んでも消えなかった |
| 8 | `/password-reset` に `fog-verify-2026-09-12@example.com` で「リセットメールを送る」 | タイトル下の一文が送信の前後を通じて残る。成功はトーストで出て数秒で消え、フォームはそのまま残る。`?sent=1` の画面は無い | 送信前の本文: `パスワードリセット | アカウントが存在する場合、リセットリンクをお送りします | メールアドレス | リセットメールを送る`。送信直後: URL は `/password-reset`（クエリなし）、本文は**同一のまま**、`[role="status"][aria-live="polite"]` に text `リセットメールの送信を受け付けました`、矩形 x=59 / y=767 / 272×47。約 6 秒後: 同じ `[role="status"]` の text が空になり、本文は変わらず（一文もフォームも残る） |
| 9 | 再試行の語の分かれ（行の下は「リトライ」／面は「再試行」） | 持ち場で分かれる | この手順では失敗を意図的に起こしていないため、「リトライ」「再試行」のどちらも画面に出さずに終わった（未観測） |

## 補足（観測事実）

- 観測中の操作で、ドキュメント `サイト構成の方針` のヘッダー「保存」を 1 回、タイトルを変えずに押してしまった（URL が `/documents/01a093e3-163e-74fa-af9f-78cdbe92a6c9` に遷移）。この結果、このドキュメントの版が 1 つ増えている可能性がある。
- `/signup` の手順 3 では、メールアドレスに `fog-new-tc07@example.com` を入れた（弱いパスワードで止まるため、アカウントは作られていない）。
