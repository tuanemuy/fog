# 指摘台帳 — Issue #35 / PR #46

判定は `fix`（このPRで直す）/ `wont-fix`（対応しない）/ `defer`（別Issueへ）の3値。
Key が一致する既出指摘は判定を継承して再審議しない。

## ラウンド1

### Blockers

| Key | タイトル | 判定 | 理由・反映内容 |
|---|---|---|---|
| B-001 | `usableForLogin` 相当が `CredentialRef` に無い | `fix` | 2レイヤーが独立検出。DTO どおり読むと SSO 専用アカウントにパスワード変更フォームが出る。ドメイン側に足して画面仕様と揃える |
| B-002 | 新設した画面約束に対応するユースケースが無い | `fix` | 画面だけが約束して実装契約が無い状態。設計 第5.4節 (ii) が必須導線と定めているのでユースケース側を足す |
| B-003 | イベント名だけの期待値が knowledge のテストケース4行に残存 | `fix` | 2レイヤーが独立検出。AC-3 違反。`V-3` では構造的に検出できない穴 |
| B-004 | `userId スコープ` が20ファイル34行残存（8ファイルは `coverage.md` の誤判定） | `fix` | 2レイヤーが独立検出。改訂後の `spec/domains/index.md`（「例外は無い」）と正面から矛盾。`coverage.md` の判定も是正する |
| B-005 | 期限切れ項目の列挙ポートが消えたまま代替が無い | `fix` | テストケースが列挙の DB 例外を要求しており、契約が宙に浮いている |
| B-006 | `purgeAfter` 一括再計算の書き込み経路がどのポートにも無い | `fix` | `CLAUDE.md` が UoW の書き込み経路を閉じた集合と宣言しているため、経路不在は宣言と矛盾 |
| B-007 | `executePasswordReset` に `sessionEpoch` 前進が無い | `fix` | design.md:1331 が正本。リセット後も旧セッションが生き残る認可上の破れ |
| B-008 | 不透明カーソルの期限切れ判定が純関数に置かれ実装不能 | `fix` | 2レイヤーが独立検出。`now` を受け取らない `SearchQuery.create` では判定できない |
| B-009 | `credential_locators` / `credential_mappings` に主キー宣言が無い | `fix` | 共通方針の「例外は `search_entries` だけ」という排他宣言が不成立 |
| B-010 | handoff 前方互換点2 が `spec/` に無い | `fix` | `.thread/34/handoff.md` が「#37 が落とすと #45 がどう設計しても後から入れられなくなる」と名指しした4点のうちの1つ |

### Warnings

| Key | タイトル | 判定 | 理由・反映内容 |
|---|---|---|---|
| W-001 | 読み取り専用ユースケースのテストに書き込み期待 | `fix` | |
| W-002 | `43シナリオ` が実測 39 と不一致 | `fix` | AC-18 の取り残し |
| W-003 | P-11 の「もっと読む」「カーソル期限切れ」がシナリオ・手順書に降りていない | `fix` | |
| W-004 | 不透明カーソルの期限切れ検証責任がドメインとポートで矛盾 | `fix` | B-008 と同根。まとめて直す |
| W-005 | 画面文言「試行が制限されている」が P-13 に無い | `fix` | 設計が #35 へ渡した項目 |
| W-006 | `TOPIC_NOT_FOUND` の画面状態が P-11 に無い | `fix` | |
| W-007 | Directory 側書き込みのポートが不在 | `fix` | ADR-025 の意図的判断だが実装契約が皆無なのは行き過ぎ |
| W-008 | `credential_locators` 等が domain 側に契約を持たない | `fix` | W-015 と同根 |
| W-009 | `trashed ⇔ purgeAfter` の新不変条件にテストケースが無い | `fix` | |
| W-010 | リセット系に旧語彙・旧契約が残存 | `fix` | |
| W-011 | trash のユビキタス言語が旧定義のまま、`isExpired` が呼び出し元を喪失 | `fix` | |
| W-012 | AI `delete` が `Memo.softDelete` の新シグネチャに未追随 | `fix` | |
| W-013 | external-content FTS5 の `'delete'` が特殊コマンド構文である旨が無い | `fix` | |
| W-014 | OCC の共通方針が `account` / `user_settings` に当てはまらない | `fix` | |
| W-015 | 非集約ストア6つの書き込み口が spec に0件 | `fix` | `CLAUDE.md` にしか無いのは正本の置き場として逆 |
| W-016 | `jobs` の `done` / `poison` 行への再投入規則が無い | `fix` | |
| W-017 | `search_entries` の PK 形が domains と database で二重管理 | `fix` | |
| W-018 | `purge-trash` の再計算フェーズの自己消尽述語が無い | `fix` | |
| W-019 | identity の中間状態語彙8語が上流に名前のアンカーを持たない | `fix` | 同じ概念が2通りの綴りで現れる問題。上流に定義を置く |
| W-020 | `userId スコープ` の書き換えた行と残した行が併存 | `fix` | B-004 と同根。まとめて直す |
| W-021 | マニュアルテストの TC 番号が並び順と不一致 | `fix` | |
| W-022 | 追加テストデータ D-A2 / D-A3 の配置 | `fix` | |
| W-023 | `CLAUDE.md`「Key concepts」導入文が新規項に対して嘘 | `fix` | |
| W-024 | 「ポートの同期契約」の例外理由が判定基準にならない | `fix` | |
| W-025 | 前方互換点3 が肯定形だけ | `fix` | B-010 と同種。否定形を明記する |
| W-026 | ADR が自分に課した「PR 本文に明記する」が未履行 | `fix` | PR 本文に設計からの意図的なずれを明記する |
| W-027 | `README.md` が改訂後 `CLAUDE.md` と矛盾 | `defer` | **Issue #35 が明示的にスコープ外とした「docs の更新 → #38」に該当する。** README のランタイム記述全体（D1 + Queues、outbox publish / queue consumption のワーカー構成）は #37 が実装を移すまで現状として正しく、`:53` の1行だけを直すと節内で不整合になる。#38（[docs] DO 構成のドキュメント・セキュリティ・運用手順）へ申し送る |
| W-028 | step14-checklist の件数が実際と不一致 | `fix` | |
| W-029 | AC-14 / AC-15 未達（ステップ18 未実行） | `fix` | **判定は fix だが実行順序は最後。** レビューで台帳の ID が動くため、APPROVED 後にステップ18 を実行する |
