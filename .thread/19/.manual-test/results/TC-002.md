# TC-002: 既存のアカウント列挙耐性

**結果**: PASS
**実行時間**: 68秒
**セッション**: verify-tc-002

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
| --- | --- | --- | --- | --- |
| 1 | `http://localhost:8787/login` を開き、snapshot でフォームを確認 | ログインフォームの操作対象を ref で特定できる | メール欄 `@e4`、パスワード欄 `@e5`、ログインボタン `@e6` を特定した | PASS |
| 2 | ケースA: 未登録の `issue19-unknown-20260728t-browser1@example.com` と指定の有効パスワードを、`fill → Tab → fill → Tab → click` で送信 | 登録有無を示さない汎用エラーが表示される | alert に `メールアドレスまたはパスワードが正しくありません` が表示された。メール欄だけが保持され、パスワード欄は空に戻った | PASS |
| 3 | ケースAの serverFn response を Network の read-only 機能で確認 | status 422、`validation`、`INVALID_CREDENTIALS` になる | request body を成果物へ残さず response metadata のみ抽出し、status 422、kind `validation`、code `INVALID_CREDENTIALS`、message `Invalid email or password` を確認した | PASS |
| 4 | ケースB前に再度 snapshot を取得し、登録済みの `issue19-password-20260728t-browser1@example.com` と指定の誤パスワードを同じ ref・操作順で送信 | ケースAと同じ汎用エラーが表示される | 処理完了後の snapshot で、ケースAと同じ alert `メールアドレスまたはパスワードが正しくありません` を確認した。メール欄だけが保持され、パスワード欄は空に戻った | PASS |
| 5 | ケースBの serverFn response を Network の read-only 機能で確認し、ケースAと比較 | status、kind、code、message がケースAと一致する | status 422、kind `validation`、code `INVALID_CREDENTIALS`、message `Invalid email or password` で全項目がケースAと一致した | PASS |
| 6 | browser console と page error を確認 | 予期しない console / page error がない | console message 0件、page error 0件だった | PASS |
| 7 | SSO-only と不正形式メールの手動実行可否を seed-data と照合 | UIから無理に fixture を作らず、自動契約テストへ委ねる | SSO-only は統合テスト専用 fixture、不正形式は `type=email` の送信前検証対象のため手動実行しなかった。既存の integration / unit contract を補完証跡とする | SKIP |
| 8 | `verify-tc-002` を終了する | テストセッションが残らない | セッションを close し、`agent-browser session list` が `No active sessions` であることを確認した | PASS |

## 比較結果

| ケース | UI message | HTTP status | kind | code |
| --- | --- | --- | --- | --- |
| 未登録メール | メールアドレスまたはパスワードが正しくありません | 422 | `validation` | `INVALID_CREDENTIALS` |
| 登録済みメール + 誤パスワード | メールアドレスまたはパスワードが正しくありません | 422 | `validation` | `INVALID_CREDENTIALS` |

手動実行した2ケースの公開応答に差はなかった。目視の応答時間は
timing equalisation の判定に使用していない。

## 自動契約テストによる補完

- `apps/web/app/testing/__tests__/requestStateBoundary.integration.test.ts`
  は未登録、SSO-only、誤パスワード、不正形式の4ケースで status 422 と
  同一の公開 error envelope になることを検証する。
- `packages/core/src/application/identity/__tests__/loginWithPassword.test.ts`
  は4ケースの credential lookup、password verify、authority read の
  work profile と PII 非記録を検証する。
- 今回は既存の自動契約を証跡として参照し、テストコマンドの再実行、
  SSO-only fixture の通常 Worker への露出、不正形式送信のための UI 回避は
  行っていない。

## エラー・Issue

失敗、console error、page error はなく、Issue 起票は不要である。
