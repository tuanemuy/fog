# 通常系-6: グローバルナビの全項目から遷移でき、現在地が示される

**結果**: PASS
**対応する受け入れ基準**: AC-14（PAGE-common-001）
**実行時間**: 約70秒

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `test@example.com` / `password123` でログインし `/` を開く（1280px） | タイムラインが表示され、サイドバーにナビ5項目が並ぶ | URL `/`、h1「タイムライン」、`complementary` 内の `navigation "グローバルナビゲーション"` に「タイムライン/トピック/検索/ゴミ箱/設定」の5リンク。href は `/`・`/topics`・`/search`・`/trash`・`/settings` | PASS |
| 2 | `/` の時点でナビの現在地表示を確認 | 「タイムライン」に `aria-current="page"` と視覚的強調 | `aria-current="page"` あり。強調あり（color `oklch(0.21 0.006 270)` / font-weight 600 に対し非アクティブは `oklch(0.52 0.01 275)` / 500） | PASS |
| 3 | ナビ「トピック」を押す | `/topics` へ遷移。404・エラー画面にならない（プレースホルダー可） | URL `/topics`、title「トピック」、h1「トピック」、本文「準備中です」。「トピック」に `aria-current="page"` + 強調、他4項目は null | PASS |
| 4 | ナビ「検索」を押す | `/search` へ遷移 | URL `/search`、title「検索」、h1「検索」、本文「準備中です」。「検索」のみ `aria-current="page"` + 強調 | PASS |
| 5 | ナビ「ゴミ箱」を押す | `/trash` へ遷移 | URL `/trash`、title「ゴミ箱」、h1「ゴミ箱」、本文「準備中です」。「ゴミ箱」のみ `aria-current="page"` + 強調 | PASS |
| 6 | ナビ「設定」を押す | `/settings` へ遷移 | URL `/settings`、title「設定」、h1「設定」、「アカウント」セクション表示。「設定」のみ `aria-current="page"` + 強調 | PASS |
| 7 | ナビ「タイムライン」を押す | `/` へ戻る | URL `/`、title「タイムライン」、h1「タイムライン」。「タイムライン」のみ `aria-current="page"` | PASS |

## 確認ポイントの検証

- **ナビ位置の同一性**: 全5画面でナビの `getBoundingClientRect()` が `x=24, y=106, width=152` で完全一致。シェルがリーフに含まれておらず、画面ごとに崩れない。
- **押しても何も起きない項目**: 無し。5項目すべてで URL が変わり、対応する h1 / `document.title` も切り替わった。
- **404・エラー画面**: 無し。未実装の3ルート（`/topics` `/search` `/trash`）は「準備中です」のプレースホルダーで正しく描画された。

## 失敗詳細

なし。
