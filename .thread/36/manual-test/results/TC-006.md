# TC-006: `pnpm db:migrate` の既定が D1 を指している

**結果**: PASS
**実行時間**: 約5秒

対応する確認項目: testing.md「確認項目6」（AC-3 / AC-4）

## 実行ログ

| # | 操作 | 期待結果 | 実際の結果 | 判定 |
|---|------|---------|-----------|------|
| 1 | `pnpm db:migrate` | `wrangler d1 migrations apply tanstack-start-template-d1 --local` として走り、適用済みなら「no migrations to apply」で正常終了 | exit 0。委譲チェーンが出力に表示：`pnpm --filter @repo/web db:migrate` → `pnpm db:migrate:cf` → `wrangler d1 migrations apply tanstack-start-template-d1 --local`。`Resource location: local` / `✅ No migrations to apply!` | PASS |
| 2 | `pnpm db:generate` | `drizzle.config.ts`（D1 用）で走り、スキーマ無変更なら新しい SQL を生成しない | exit 0。`pnpm --filter @repo/web db:generate` → `pnpm db:generate:cf` → `drizzle-kit generate --config=./drizzle.config.ts`。`4 tables`（`_occ_guard` / `outbox_events` / `processed_events` / `users`）を読み取り `No schema changes, nothing to migrate 😴`。`packages/core/src/adapters/d1/migrations/` は前後とも `0000_initial.sql` + `meta` のみで新規 SQL なし、`git status --porcelain` にトラッキング済み差分なし | PASS |
| 3 | `apps/web/package.json` の `db:migrate` / `db:generate` 定義を目視確認 | それぞれ `db:migrate:cf` / `db:generate:cf` のエイリアス | `"db:migrate": "pnpm db:migrate:cf"` / `"db:migrate:cf": "wrangler d1 migrations apply tanstack-start-template-d1 --local"` / `"db:generate": "pnpm db:generate:cf"` / `"db:generate:cf": "drizzle-kit generate --config=./drizzle.config.ts"` | PASS |
| 4 | 確認ポイント: `tsx: command not found` / `Cannot find module 'scripts/migrate.node.ts'` が出ないこと | エイリアスの張り替え漏れがない | どちらも発生せず。`package.json` 群に `migrate.node` / `migrate.aws` / `migrate.gcp` / `drizzle.libsql` の文字列は0件 | PASS |
| 5 | 確認ポイント: `db:generate` の出力先と `drizzle.libsql.config.ts` の削除 | `packages/core/src/adapters/d1/migrations/` 以外に書き出さない／`drizzle.libsql.config.ts` は削除済み | `apps/web/drizzle*` は `drizzle.config.ts` のみ（`drizzle.libsql.config.ts` は存在しない）。`drizzle.config.ts` の `out` は `../../packages/core/src/adapters/d1/migrations`、`dialect: "sqlite"` | PASS |

## 備考

- dev サーバー起動中でも `wrangler d1 migrations apply --local` はロック競合なく完走した。
