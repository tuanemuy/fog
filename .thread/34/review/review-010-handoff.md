# レビュー 010 — 引き継ぎ性・成果物制約・ドキュメント品質（最終ラウンド）

**対象:** PR #43 / ブランチ `issue/34/do-boundary-design` / Issue #34
**観点:** 後続 Issue（#35 / #37 / #38 / #44）への引き継ぎ性・成果物制約・ドキュメント品質
**方式:** ゼロベース。Issue #34 の受け入れ条件9項目、`.thread/34/testing.md` の機械検証20件、design.md 第1.4節の検査1〜9（検査7a / 7b / 8 / 9 を含む）をすべて実行した。第11.1節のカバレッジ主張と本文中の数値・パス・行番号引用は実測で突き合わせた。

## 実行結果の要約（すべて記載どおり）

- **design.md 第1.4節の検査1〜9** — 全項目パス。検査1 `12` / `I-3 OK` / `rotate-remap` 0件、検査2 投入点なし0件・(A)3 (B)2 (C)7・`I-2(A) OK`・`I-2(B) OK`・分類がちょうど1つでない行0件・`I-2 重複なし OK`、検査3 7ストア・`MISSING in 8.2:` なし・アダプター専用1件、検査4 `12` / `I-7 OK`、検査5 両方 `12`、検査6 `16` / `MISSING in 4.1.1:` なし、検査7a ヒット7行が「秘密4つ / `jobs` 12列 / `kind` 各クラス6種」と一致、検査7b **`NG` ゼロ・grep ヒット数8 = `ok` 呼び出し8**、検査8 `MISSING column` なし（列一覧 **62件**）、検査9 候補ゼロ。
  - 検査8 は design.md の該当コードブロックが2スペース字下げされているため、**字下げを剥がして貼る（＝レンダリング後の見た目どおりに貼る）**必要がある。字下げを保ったまま貼ると `IFS=: read` が `t` に空白を含め全行 `MISSING` になる。これは第1.4節が `tbl()` で `sub(/^[ \t]+/,"",l)` を入れているのと同じ前提であり、検査自体の欠陥ではない（Blocker / Warning 相当ではない）。
- **`.thread/34/testing.md` の確認項目20件** — 全件パス。`ls -1 .adr/ | wc -l` = 4、`.adr/` 差分は `A` 3行のみ（`001` の行なし）、H1 4件 OK / 5節 3件 OK、`.adr/00[234]` の禁止トークン0件・42/38/42行・コードフェンス0件、`spec/adr/` 差分は `M` 1行のみ・`本文不変 OK`、`.thread/1/adr.md` は `2 0`（削除ゼロ / 追加は空行1 + ポインタ1）、確認項目6 の手順1〜4 すべて空、ラベル未付与の節0件・36+25+5=66=総数、未決語0件、`awk` 抽出1,037行（0でない）、`従属` は第5.1節の設計上の用法1件のみ、ADP 台帳85件・`comm` 残り32件・行き先空欄0件、Account Home の断定あり / 未決事項に出現なし、#35 / #37 の必須パス `MISSING` 0件・実在チェック0件、外部参照の `.thread/36/` ヒットは自己完結を明示している第11.2節の1件のみ、リンク切れ判定 (b) 空・無修飾 `ADR-NNN` は逐語引用1件のみ、`.adr/` 3件すべて design.md を参照 / 逆参照26行、`.thread/34/adr.md` は142件（ADR-020 以降123件）、確認項目19 は手順1が4行・手順2が `6` と `8`・手順3が両方0・手順4が `7`・手順5が `6` と `1`、確認項目20 は12個の `1` と末尾 `0`。
- **成果物制約** — `.adr/` 新規は 002 / 003 / 004 の3件だけ。`.adr/001` は無改変（差分に現れない）。`spec/adr/` は `005` のステータス行1行の書き換えのみで新規ファイルなし。コード・コンフィグの差分ゼロ（`git diff --name-only main...HEAD` に `packages/` / `apps/web/app/` / `infra/` / `.ts` / `.toml` / `.json` / `.sql` が1件も無い）。`pnpm lint` exit 0（150 files / 2 infos）、`pnpm format:check` exit 0（167 files）— testing.md の実測値と一致。
- **第11.1節のカバレッジ主張** — 実測と完全一致。`spec/` 非レビュー md **101**、レビュー md **39**（`database` 3 / `design` 8 / `domains` 9 / `manual-tests` 7 / `pages` 3 / `spec/review` 3 / `scenario` 3 / `usecases` 3 = 内訳も一致）、語彙走査ヒット **62**、未ヒット **39**（うち3件を手段2・3 が拾い残り36件が手段4）。判定の集合を機械抽出すると **改訂対象72（spec のみ。+ `CLAUDE.md`）/ 影響なし29** で、**重複ゼロ・union が実ファイル集合 101 と完全一致**（`comm -3` 空）。`spec/usecases/review/002.md` の走査語ヒット7件も一致。
- **参照の実在性** — design.md / `.adr/00[234]` が使う `第N.M節` 参照は全件が実在する見出しに解決（未解決0件）。`path:行番号` 形式の引用21件はすべて範囲内で、抽出した10箇所（`spec/requirements.md:87,108` / `di/types.ts:37,53,70` / `spec/database/index.md:350,355-357` / `spec/domains/search.md:264` / `spec/usecases/knowledge.md:16` / `spec/domains/trash.md:239` / `spec/usecases/trash.md:315` / `server.cloudflare.ts:4,33,44`）は本文の記述内容と一致。`spec/adr/005` の参照6本は design.md が名指しした6箇所と行番号まで一致。
- **本文中の数値主張** — 実測一致。`.thread/1/adr.md` 1,664行 / `unitOfWork.ts` 19行 / `pendingBatch.ts` 98行 / `env.ts` 57行 / `eventRelayWorker.ts` 301行 / `outboxPrune.ts` 25行 / `buildDecoder.ts` 37行 / `event.ts` 81行 / `events.ts` 62行 / `entity.ts` 227行 / `handlers.ts` 138行 / `wrangler.toml` 162行 / `spec/domains/search.md` 271行 / `spec/database/index.md` 403行 / `spec/manual-tests/account.md` 562行 / `adapters/d1/` 20ファイル 2,514行（プロダクション8ファイル 914行）/ `spec/usecases/memo.md` の `collectEvents` 7箇所 / `db*` 10本・`deploy*` 24本。

## 引き継ぎ性・成果物制約・ドキュメント品質

### Blockers

なし。

- Issue #34 の受け入れ条件9項目はすべて満たされている。`.adr/` は3件だけで薄さも保たれ（禁止トークン0・コードフェンス0・50行以内）、設計の詳細は `.thread/34/` に留まり、旧 ADR 2箇所に supersede の履歴が残り、`.thread/1/adr.md` の本文は無改変、`spec/adr/` に新規ファイルは無く、User Data DO の保持データ範囲とルーティング・Identity Directory DO の5論点・PII 方針・FTS5 同期更新 / Alarm / UoW 契約 / lazy migration がいずれも断定形で決着している。
- **#35 のロールプレイ:** 受け入れ条件7項目が第11.1節の対応表で1対1に割り付けられ、条件2 / 条件4 は「削除だけでなく追記が要る」ことと追記内容の要旨まで書かれている。条件3 は Issue 本文の文言（`query` / `upsert` / `remove`）が設計結論（`query` 1本）と食い違う点を**訂正指示として明示**しており、条件7 は照合対象に #13 を追加している。改訂対象72件は全件が「削除する記述 / 追加・置換する記述」の2列を持つので、着手時に手が止まる箇所は見当たらない。
- **#37 のロールプレイ:** 第2.1節（依拠事実 F-1〜F-32、裏付け種別つき）→ 第3〜9章 → 第11.2節の順で読めば、削除・作り直す・改修の区分つき一覧、新設対象、テーブル・列の全数（第4.1.1節）、`kind` 12種の所有者・投入点・再武装分類、UoW の新旧対比、lazy migration の起動点と forward-only 方針がそろう。Issue 本文と設計が食い違う3点（UoW 契約は「維持」ではなく差し替え / `new_sqlite_classes` ではなく `exports` / #35 受け入れ条件3）はいずれも**訂正指示として明示**されている。spike 待ちの9件はすべて「本設計への影響: 無い」または既定値つきで、着手を止めない。ローテーションについては #44 の結論を待たずに着手できる根拠（初期デプロイは1世代のみ）と、**#37 が落としてはいけない4点**（`credential_locators` の一意性を `(credentialId, generation)` に / `record-credential-locator` を upsert に / `credential_mappings.credentialId` の bucket 内 UNIQUE / 述語のキーを `credentialId` に）が明記されている。**「ここで手が止まる」箇所は見つからなかった。**
- **#44 のロールプレイ:** 第6.8節が方針8点・委譲の宣言・論点6件・#37 との境界表（8行）・0件証明の枠組みを持ち、#44 の Issue 本文の「決着させる論点」6件と1対1で対応する。前ラウンドで訂正された2点（`generation` は locator 名と keyring の両方 / `bucketCount` は keyring だけ、一時注入は決着に含めない）も design.md 第6.8節 方針3・方針4・第5.2.3節・第6.2節と一致している。ただし下の W-001 が1点ある。

### Warnings

- **[W-001]** 「bucket に routing keyring を配る」という #44 の選択肢が、design.md の中で**在り**と**無し**の両方に書かれている（自己矛盾。#44 の Issue 本文とも食い違う）
  - 場所: `.thread/34/design.md:435`（第3.2節）と `.thread/34/design.md:849`（第5.2.3節）/ `.thread/34/design.md:1411`（第6.8節 方針4）。関連して `.thread/34/design.md:1642`（第7.4節）
  - 理由: 第3.2節:435 は「**「bucket が routing secret をバインディングとして保持しない」という本節の境界は、#44 がどの形を選んでも変わらない**」と断定している。一方 第5.2.3節:849 と 第6.8節:1411 は、一時注入を決着扱いにしない理由として「**#44 が取りうる解（bucket に routing keyring を配る / 専用の maintenance Worker を立てて境界を引き直す）を先に禁じることになる**」と書いており、`849` はさらに「**正本は #44 側であり**」と付けている。前者が不変と宣言している境界を、後者は #44 が選べる解として提示している。`bucket に routing keyring を配る` は「一時注入」とは別物として列挙されているので、意味するのは常設のバインディング（＝ state Worker 側に `DIRECTORY_ROUTING_SECRET` を置く）以外にありえず、第3.2節の非重複配布そのものである。**#44 の Issue 本文（前提欄の4点目の訂正文）もこの2つの解を名指ししている**ので、#44 の担当者は「第3.2節の境界を引き直してよいのか」を自分の Issue のスコープ（「#34 で決着済みの方針の再検討」はスコープ外）だけでは決められない。あわせて 第7.4節:1642 の「`rotate-remap` を Alarm ジョブにしない結論は**どの形を選んでも変わらない**（制約 (ii) が入力）」も、bucket が keyring を常設で持つ形を選ばれた場合には前提（Alarm 起動時に鍵が手元に無い）が崩れる。
  - 提案: どちらか一方に倒す。(a) 第3.2節:435 の最後の文を「**この境界を引き直すかどうかも #44 の論点2 に含まれる。引き直す場合は本節の非重複配布と方針5（`rotate-remap` を Alarm ジョブにしない）を同時に見直す判断が要り、その判断は #44 が下す**」に緩める、または (b) `849` / `1411` の選択肢列挙から「bucket に routing keyring を配る」を落とし、**制約 (ii)（routing 鍵を bucket の SQLite・インスタンスフィールドへ永続化しない）が既にその形を排除している**ことを1行で書く（この場合は #44 の Issue 本文の前提欄も同じ形に揃える）。
  - Blocker にしない理由: #44 は残る2つの形（一時注入 + 検証手段の新設 / 専用 maintenance Worker）で着手でき、第11.3節が「maintenance 経路の到達性を先に再発行する順序」を #44 が採りうる方向として既に示している。#37 は移送を実装しないので影響を受けない。修正量も1文である。

- **[W-002]** PR 本文の Test plan の件数・規模が現物と食い違う（事実と食い違う主張。成果物本体ではなく PR 説明の陳腐化）
  - 場所: PR #43 本文（`.thread/34/testing.md` に**機械検証17件**・人間判断6件 / `.thread/34/design.md`（**11章73節**））
  - 理由: 現物の `.thread/34/testing.md` の確認項目は **20件**（`### 1.` 〜 `### 20.`）+ 人間判断6件で、17件ではない。design.md の見出しは `##` 11 + `###` 55（+ `####` 48）で「73節」に当たる数が無い。レビュー往復で項目が増えたのに PR 本文が追随していない。
  - 提案: PR 本文を「機械検証20件・人間判断6件」「11章55節（+ 小見出し48）」へ更新する。design.md / testing.md 側は正しいので触らない。

### Notes

- **[N-001]** 第1.4節の不変条件（I-1〜I-8 + I-5b）と検査1〜9 が、今回のゼロベース実行でも**すべて機械的に判定できる形**で成立していた。とくに検査7b が「注記に期待値を書かず左辺・右辺を両方コマンドで出す」形になっているため、R8 で問題になった「注記の陳腐化を機械検出できない」という穴が構造的に閉じている。検査8 の列一覧も注記に件数を持たず、再カウントコマンドが `62` を出して整合した。**Blocker / Warning 相当ではない（良い点）。**
- **[N-002]** 第11.1節の判定が `spec/` 非レビュー md 101ファイルの**厳密な分割**（改訂対象72 + 影響なし29、重複ゼロ、取りこぼしゼロ）になっていることを機械的に確認した。「走査の方法（#35 が再実行できる形で記録する）」の4手段と再現コマンドも実行して同じ数（62 / 39 / 36）が出た。#35 が受け入れ条件1（旧前提の残存ゼロ）を証明する手段が引き継がれている。**Blocker / Warning 相当ではない（良い点）。**
- **[N-003]** `.adr/002〜004` と design.md の間に食い違いは見つからなかった。`.adr/002` の影響節（`.thread/1/adr.md` ADR-002 のトレードオフ / セッション方式の別 ADR を起こさない）は design.md 第3.1節:418 と、`.adr/003` の「再確認が覆れば本決定そのものが成立しない」は第2.1節 F-11 / F-12・第11.4節と、`.adr/004` の「ドメインポートの `Promise` 契約が変わる」は第8.2.1節と一致する。`.adr/` から design.md への参照先の節はすべて実在する。**Blocker / Warning 相当ではない（良い点）。**
- **[N-004]** 「未確認」項目は第11.4節の10行にまとめられ、全行に「決める主体 / いつ / 本設計への影響」がある。9行が #37 の着手時 spike（うち2行は #37 → #38 の2段）、1行が #44 で、影響欄はいずれも「無い」または既定値つきである。委譲（#44）と未決（spike）が別種として書き分けられており、確認項目19 の (i)(ii)(iii) がすべて満たされている。**Blocker / Warning 相当ではない（良い点）。**
- **[N-005]** `.thread/36/`（未 commit・clone から読めない）と未コミットの `apps/web/wrangler.{request,state}.{staging,production}.toml` 4本について、本文が「読めないこと」「動かないこと」を明示し、内容を第11.2節へ再掲したうえで「本 Issue では commit も削除もしない」と結論している。testing.md のホワイトリスト・エッジケース2 と整合していた。**Blocker / Warning 相当ではない（良い点）。**
