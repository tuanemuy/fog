# レビュー 009 — 引き継ぎ性・成果物制約・ドキュメント品質

- **対象:** PR #43 / ブランチ `issue/34/do-boundary-design` / Issue #34
- **主成果物:** `.thread/34/design.md`（2,471行）/ `.adr/002`〜`.adr/004` / `.thread/34/testing.md`
- **前提:** 鍵ローテーション**手順**は Issue #44 へ切り出し済み。手順の未決は Blocker として扱わない
- **実施日:** 2026-07-30（9ラウンド目 / ゼロベース）

## 実施した検証

すべて実際に実行した。

| 対象 | 結果 |
|---|---|
| `.thread/34/testing.md` 確認項目 1〜20（機械検証） | **20/20 期待どおり** |
| `design.md` 第1.4節 検査1〜9（I-1〜I-8 / I-5b） | **9/9 パス**（`diff` 系の `OK` 出力も確認） |
| 第11.1節の走査カバレッジ主張 | `spec/` 非レビュー md **101**・語彙走査ヒット **62**・review 配下 **39**・`ADP-*` distinct **85** — **すべて実測一致**。101件全件に判定あり（改訂72 / 影響なし29、内訳 27+2 も一致） |
| 節番号・章番号参照（design.md + `.adr/002〜004`、66件 + 範囲表記8件） | **未定義参照ゼロ** |
| `F-1`〜`F-32`（+ `4b` / `27b` / `32b`）の定義・参照 | **35件、未定義参照ゼロ・未参照ゼロ** |
| リポジトリ内パス参照（177件） | 実在しないのは既知3分類（ADR短縮表記 / 先行ブランチ限定 / 本文が不在を明示）のみ |
| コード行番号引用（`errors.ts:206-210` / `error.ts:35-37` / `valueObject.ts:47,125,142` / `d1/unitOfWork.ts:39` / `currentUser.ts:17-26,28-33` / `authState.ts:18-23` / `0000_initial.sql:46,47` ほか） | **全件一致** |
| 行数・件数の実測主張（d1 20ファイル/2,514行・うち本体8ファイル/914行、`eventRelayWorker.ts` 301行、`outboxPrune.ts` 25行、`handlers.ts` 138行 `handleQueue:82`/`handleDlq:120`、`event.ts` 81行、`identity/events.ts` 62行、`entity.ts` 227行、`wrangler.toml` 162行 DOバインディング0、`spec/database/index.md` 403行、`spec/domains/search.md` 271行、`.thread/1/adr.md` 1,664行、`spec/manual-tests/account.md` 562行、`db*` 10本 / `deploy*` 24本、`spec/adr/005` 参照6箇所とその行番号、`collectEvents` in `spec/usecases/memo.md` 7箇所、`TC-maintainSearchIndex` 28件 / `TC-pruneExpiredTrashItems` 17件） | **全件一致** |
| `spec/` 側の行番号引用スポットチェック（40箇所超） | **全件一致**（`timeline.md:29-33` の範囲始点が1行ずれる程度） |
| `.thread/34/adr.md` の ADR 採番（136件） | **重複ゼロ・欠番ゼロ**。testing.md が名指しする ADR-126 / 131〜136 すべて実在 |
| Issue 相互参照（#10 / #12 / #13 / #26 / #40 / #44） | #12 のチェックリスト54行に OAuth/PKCE が0件・#13 に P-14 / `approveAiClientAuthorization` / `DOM-identity-017` / `TC-revokeAiClientConnection-002` が実在 — design.md の断定どおり |
| 成果物制約（`.adr/` 3件のみ・001 無改変・`spec/adr/` 新規なし・`.thread/1/adr.md` 追加2/削除0・コード/コンフィグ変更ゼロ・未コミット残ゼロ） | **すべて充足** |

### Issue #34 受け入れ条件の照合

| # | 受け入れ条件 | 判定 |
|---|---|---|
| 1 | `.adr/002` 以降に3件、それ以外が増えていない | ✅ `.adr/` は4件（001 + 002/003/004）、差分は `A` 3行のみ |
| 2 | 対応項目3・4 の設計が `.thread/34/` にあり `.adr/` に詳細が流れ込んでいない | ✅ `.adr/` 3件は 42/38/42行、禁止トークン0・コードフェンス0・`bucket` 0件 |
| 3 | 旧 ADR に superseded の履歴、`.thread/1/adr.md` 本文不変 | ✅ `spec/adr/005` は `## コンテキスト` 以降が1文字も変わらず（機械判定）、`.thread/1/adr.md` は追加2/削除0 |
| 4 | `spec/adr/` に新規ファイルなし | ✅ commit 済み・未コミットとも `A` なし |
| 5 | User Data DO の保持データ範囲とルーティング | ✅ 第4.1節（Issue 列挙7項目の対応表）/ 第4.1.1節（テーブル16行の全数）/ 第5.1節 |
| 6 | Identity Directory DO の5論点 | ✅ 第6.1〜6.6節（解決責務 (a)〜(d) / 分割方式3案4軸 / 部分失敗 / 冪等性 / SSO link・unlink） |
| 7 | DO ID / routing key に PII を使わない方針 | ✅ 第5.2節 (a)(b)(c) + 第5.2.1〜5.2.5節 |
| 8 | 同期 FTS5 / Alarm / UoW 契約 / lazy migration | ✅ 第7.1節・第7.4〜7.5節・第8.2節・第9.1〜9.5節。すべて断定形 |
| 9 | #35 / #37 が成果物だけで着手できる | ⚠️ 下記 W-002 を除いて成立（後述） |

---

## 引き継ぎ性・成果物制約・ドキュメント品質

### Blockers

- **[B-001]** 第6.8節「本 Issue が決着させた方針」項3 が **`bucketCount` を locator 名に含めると述べており、同一文の後半・第5.2.3節・第6.2節と矛盾している。しかもこの誤りが訂正なしで #44 の Issue 本文の「#34 で決着済みのこと（本 Issue の前提。再検討しない）」へ伝播している。**
  - 場所: `.thread/34/design.md:1369`（一次）/ `.thread/34/adr.md:860`（ADR-026 = 誤りの出所）/ `.thread/34/adr.md:3133`（ADR-126 の方針8点の要約）/ **#44 の Issue 本文 前提 第3項**
  - 実測:

    ```
    design.md:1369  「**`bucketCount` を locator 名（`dir:g{generation}:b{index}`）と keyring エントリの両方に含める**
                     — 名前に入るのは世代と index で、bucket 数は keyring が持つ（第6.2節）。」
    design.md:1078  「locator 名は `dir:g{generation}:b{index}` の形にする —
                     **名前に入るのは世代と index であり、bucket 数そのものは名前に入らない。**」
    design.md:836   「`bucketCount` を世代のメタデータとして持つのは、previous 世代を引くときの剰余計算に
                     その世代の bucket 数が必要だからである。DO 名 … に入っているのは index であって
                     bucket 数ではないので、名前からは復元できない。」
    adr.md:860      「**bucket 数を locator 名に世代とともに埋め込み（`dir:g{generation}:b{index}`）**」
    adr.md:3133     「鍵は `{ generation, key, bucketCount }` の keyring として配布し
                     bucket 数を locator 名と keyring の両方に含める」
    #44 本文        「鍵は世代（generation）を持つ keyring として配布し、
                     bucket 数を locator 名と keyring エントリの両方に含める」
    ```
  - 理由: **1文の中で自己矛盾している**（前半は「locator 名に含める」、後半は「名前に入らない」）うえ、第5.2.3節・第6.2節はこれを**設計上の必然**として説明している — `bucketCount` が keyring のメタデータなのは「DO 名から復元できないから」であり、名前に入れられるならその論拠ごと消える。第1.4節の I-1〜I-8 はいずれも表どうしの整合を見る形式なので、**この種（散文の断定が同じ断定と食い違う）は検査9件のどれにも掛からない**（確認項目20 が同型の破れを別項目として持っているのと同じ理由）。
    さらに悪いのは伝播先である。**#44 の Issue 本文はこの誤りを訂正クローズなしで「前提。再検討しない」欄に載せている。** #44 の担当者が方針3 をそのまま前提にすると、移送中に「移送元世代の `bucketCount` を DO 名から読む」設計へ倒れうる — それは第5.2.3節が「名前からは復元できない」と断定した経路そのもので、`bucketCount` を keyring に持たせた理由（previous 世代の剰余計算）を無効化する。#44 の論点1・論点2 が扱う領域の中心にある値なので、影響は #44 の設計全体に及ぶ。
  - 提案: 主語が `generation` の誤記である（keyring エントリと DO 名の**両方に入っているのは `generation`**）。3箇所を次のように直し、#44 の Issue 本文も同時に訂正する。
    - `design.md:1369` → 「**`generation` を locator 名（`dir:g{generation}:b{index}`）と keyring エントリの両方に含める。`bucketCount` は keyring エントリだけが持つ**（名前からは復元できない。第5.2.3節・第6.2節）」
    - `adr.md:860`（ADR-026 Decision） → 「**世代を locator 名に埋め込み（`dir:g{generation}:b{index}`）、bucket 数は keyring エントリのメタデータとして持ち**、bucket 数の変更を鍵ローテーションとまったく同じ再写像機構で処理する」
    - `adr.md:3133`（ADR-126）→ 同趣旨に修正
    - **#44 の Issue 本文 前提 第3項** → 「鍵は世代（generation）を持つ keyring として配布し、`generation` を locator 名と keyring エントリの両方に含める。**bucket 数は keyring エントリだけが持ち、locator 名には入らない**」
    - 併せて第1.4節に「同一段落内で相反する断定を置かない」型の検査は無いことを踏まえ、確認項目20 のパターンに `bucket 数そのものは名前に入らない` の1件（＝出現1回、かつ肯定形の「locator 名に含める」が0件）を足すと再発を機械検出できる。

### Warnings

- **[W-001]** 移送（`rotate-remap`）の**実行主体**が、「本 Issue で決着した方針」と「#44 が決着させる論点」の両方に置かれている。しかも #44 の Issue 本文の前提6点には該当方針が**含まれていない**ので、2つの文書が「再検討してよいか」で食い違う。
  - 場所: `.thread/34/design.md:1370`（第6.8節 方針4）/ 同 `:839-841`（第5.2.3節）/ 同 `:1209`（第6.4節 cross-DO 表の鍵ローテーション行）/ 同 `:769`（第5.1節 (3-c)）/ 同 `:1393`（論点2）/ `.thread/34/adr.md:3133`（ADR-126）
  - 実測:
    - 方針側（決着扱い）: 第6.8節 方針4「**再 HMAC は bucket の中で行い…鍵は RPC 引数として一時注入し、SQLite にもインスタンスフィールドにも書かない**」/ 第5.2.3節「**この2点（再 HMAC の場所と鍵を永続化しないこと）は本 Issue の決定である。**」/ 第6.4節の表「**operator の maintenance 経路が駆動する `rotate-remap` チャンク**」
    - 論点側（#44）: 第5.2.3節「**したがって移送の実行主体そのものを決め直す判断が必要であり、その判断は #44 に属する**」/ 第5.1節 (3-c)「**移送の実行主体そのものを決め直す判断が要る**」/ 第7.4節「**実行主体そのものを何にするかは #44 が決める**」/ 論点2「移送（`rotate-remap`）の**実行主体**・ガード・RPC エントリとしての位置づけ」
    - #44 の Issue 本文の前提6点に「再 HMAC は bucket 内 / 鍵は一時注入」は**無い**（`rotate-remap` が Alarm ジョブでないこと・移送で `credentialVersion` を増やさないこと・リセットトークン行を移送しないことも同様に無い）
  - 理由: 「一時注入」は実行主体の選択と不可分である — 注入する主体が居なくなれば注入という形も消える。第5.2.3節自身が「**この空白は「ガードを1つ足せば閉じる」形ではない**」として実行主体の再決定を要求しているので、方針4 を「再検討しない前提」として読むと **#44 が取りうる解（bucket に routing keyring を配る / 専用の maintenance Worker を立てて境界を引き直す）を、design.md 側が先に禁じている**状態になる。#44 は自分の Issue 本文（前提に方針4 が無い）と design.md 第6.8節（方針8点として決着）のどちらを正本と読むかで結論が変わる。design.md は論点表について「**本表はその一覧の正本ではない**（正本は #44 側）」と書いているが、**方針表についてはどちらが正本かを書いていない。**
  - 提案: 方針4 を「制約」と「実行主体」に割る。**残す（#44 が守る制約）**: 「平文 canonical を Worker 境界の外へ bulk で出さない」「routing 鍵を bucket の SQLite / インスタンスフィールドへ永続化しない」の2点（第5.2.3節が bulk 越境との天秤で正当化している部分）。**#44 へ渡す**: 「誰が起動し、誰が鍵を保持し、鍵をどう渡すか」＝一時注入という**形**そのもの。あわせて第6.4節 cross-DO 表の「operator の maintenance 経路が駆動する」を「**実行主体は #44 が決める**（本表はこの操作が cross-DO 操作の1つであることの記録）」に揃え、#44 の Issue 本文の前提側にも残す2制約を明記する。

- **[W-002]** `packages/core/src/application/di/env.ts` が**削除対象の `application/workers/` を value-import している**のに、design.md のどこにも現れない。#37 の Issue 対応項目5 が名指ししている「調整用 env」の実体がこのファイルであり、design.md が第7.3節で「**実測した全数**」を宣言している廃止範囲からも漏れている。
  - 場所: `.thread/34/design.md:1548-1554`（第7.3節「実測した全数」）/ 同 `:2338`（第11.2節 `application/workers/` 削除行）/ 同 `:2430`（#40 の扱い）— いずれにも `env.ts` / `TuningEnv` / `OUTBOX_*` の記述なし
  - 実測:

    ```
    $ grep -n 'import\|DEFAULT_' packages/core/src/application/di/env.ts
    2: import {
    3:   DEFAULT_BATCH_SIZE,
    4:   DEFAULT_LEASE_MS,
    5:   DEFAULT_MAX_ATTEMPTS,
    7: import { DEFAULT_OUTBOX_RETENTION_MS } from "../workers/outboxPrune";
    29:  batchSize: z.coerce.number().int().positive().default(DEFAULT_BATCH_SIZE),
    ...
    $ grep -n 'di/env\.ts\|TuningEnv\|OUTBOX_BATCH_SIZE\|調整用 env\|チューニング定数' .thread/34/design.md
    （0件）
    ```
  - 理由: 3点が重なっている。
    1. **`application/workers/` を削除した時点で `env.ts` がコンパイル不能になる。** design.md の変更対象一覧は全数を宣言していないので制約違反ではないが、`env.ts` は `application/workers/` に依存する**唯一のプロダクションモジュール**であり、それが一覧に無いのは「削除の区分を付けた表」の趣旨から外れる（他の依存先 — `registerWithPassword.ts` / `entity.ts` / `di/types.ts` — はすべて行を持っている）。
    2. **CLAUDE.md が `pnpm start` の起動不能の依存経路として名指ししているのがこのファイルである**（`server.cloudflare.ts → di/serverCloudflare.ts → di/env.ts → workers/eventRelayWorker.ts`）。design.md `:2430` は「真因は `eventRelayWorker.ts` の module-scope `crypto.randomUUID()` であり、同ファイルは第7.3節で削除されるので **#40 は対象消滅する**」と断定しているが、**#40 は 2026-07-29 に `NOT_PLANNED` でクローズ済みで、クローズコメントが3つの論点を明示的に #37 / #24 へ引き継いでいる。** そのうち論点1（「worker identity は入口が採番して注入する。**DO 構成でも同じ制約が効くので方針はそのまま必要**」）と論点2（「**チューニング定数の置き場を実装モジュールから leaf モジュールへ。合成ルート（di）→ worker 実装という逆向きの依存を作らない**」）は、`env.ts` の value-import がまさにその逆向き依存の実例である。design.md は「対象消滅」で閉じており、引き継がれた論点2 を受け取っていない。
    3. **新しい設計は同種のチューニング定数を再導入する** — 第7.4節の3階層予算（ジョブ25件 / 20チャンク / 1,000行）、最小再開間隔、`done` / `poison` の prune 保持期間、lease 期限。design.md は値の決定主体（#37 → #38）だけを書き、**置き場所（leaf モジュールか実装モジュールか、env で可変か固定定数か）を書いていない。** #37 が素直に「DO のジョブ実行部モジュールに `DEFAULT_*` を置いて `di/` から value-import する」と実装すると、#40 が指摘した逆向き依存を新構成でそのまま再生産する。
  - 提案: 3点。(i) 第11.2節の変更対象一覧に **削除** 区分で `packages/core/src/application/di/env.ts`（`TuningEnv` の `OUTBOX_*` スキーマと `../workers/*` からの value-import。Outbox 廃止に伴い全体が対象消滅）の行を足す。あわせて `packages/core/src/application/di/__tests__/serverCloudflare.test.ts` / `packages/core/src/application/identity/__tests__/eventDecoders.test.ts` / `packages/core/src/application/__tests__/helpers.ts` も同じ理由で道連れになるので、`apps/web/app/worker/cloudflare/__tests__/` に付けたのと同じ形の但し書きを添える。(ii) `:2430`「#40 の扱い」を「**#40 は 2026-07-29 に `NOT_PLANNED` でクローズ済み。真因（module-scope の乱数）は第7.3節の削除で消えるが、クローズコメントが #37 へ引き継いだ論点2（チューニング定数を leaf モジュールへ置き、`di/` → worker 実装の逆向き依存を作らない）は新構成でも有効である**」へ書き直す。(iii) 第7.4節の3階層予算の段落に「**定数は leaf モジュール（`packages/core/src/lib/` もしくはジョブ契約の型定義側）に置き、DI や実行部から value-import させない**」の1文を足す。値そのものは #37 → #38 の2段のままでよい。

### Notes

- **[N-001] `.adr/` 3件と design.md の食い違いは検出できなかった。** 参照方向を全数で当てた — `.adr/002` → 第3章 / 第4.8節 / 第3.1節・第5.1節、`.adr/003` → 第7.2節 / 第4.6節・第7.1節 / 第2.1節・第2.1.1節・第11.2節、`.adr/004` → 第7.4節 / 第7.7節 / 第7.3節 / 第8.2.1節 / 第8章・第9章 — いずれも節が実在し、断定の内容も一致する。**とくに懸念されていた「`.adr/002` の鍵の世代管理と design.md の #44 委譲の矛盾」は成立しない** — `.adr/002` は「正規化値を鍵付きハッシュした内部キーを使う」までしか書いておらず、世代・keyring・ローテーションに一切触れていない（`grep -c 'bucket' .adr/00[234]-*.md` = 0）。薄い台帳の粒度は保たれている。
- **[N-002] 委譲の3要件（何が決まっているか / 誰が残りを決めるか / それが決まる前に後続が着手できるか）は3つとも書かれている。** 第6.8節が4つの小見出しで (i) 方針8点、(ii) 委譲の宣言と根拠3点、(iii) #37 / #44 の境界表8行、(iv) retirement 証明の枠組みを持ち、第6.9節は締め出し経路20行のうち7行の**塞ぎ方だけ**を #44 へ渡して経路の列挙を残している。**最も落としやすい (iii) が特に厚い** — 「実装しないの射程を1点だけ限定する」段落が、#37 が落としてはならない4点（`credential_locators` の一意性 `(credentialId, generation)` / `record-credential-locator` が upsert / `credential_mappings.credentialId` の bucket 内 UNIQUE / 述語のキーを `credentialId` にする）を根拠つきで固定している。第6.1.1節の (A)/(B) 書き分け（(R1)〜(R5)+(R8) は単一世代でも成立 → #37、(R6)(R7)(R9) は移送前提 → #44）も、境界の引き方の実測上の根拠（R5 以降 (A) に指摘ゼロ）まで書いてある。
- **[N-003] #44 の入力としては十分である。** 論点6件が #44 の Issue 本文と一致し（正本が #44 側であることも明記）、各論点に「本書での現状」欄で残置物（(R6)(R7)(R9) / RPC 全数表からの除外理由 / `rotationKind` 列と置換キー / 空白の性質）が対応づけられている。#44 が引く `review-008.md` の B-001〜B-003、`review-007-do-boundary.md` B-001、`review-007-security.md` W-001 はすべて実在し、design.md の引用（R7/R8 の do-boundary / security の区別つき）のほうが Issue 本文より精確である。B-001 と W-001 が入力の精度を落とすが、決着させる対象の特定そのものは可能。
- **[N-004] #35 の受け入れ条件7項目は全項目に対応がある。** 受け入れ条件3（`SearchIndexPort` を query/upsert/remove へ）と受け入れ条件7 は design.md 側が Issue 本文の**訂正指示**として扱っており（3 → `query` 1本へ縮小 / 7 の照合対象に #13 を追加）、同じ形式の訂正指示が第9.1節（`new_sqlite_classes` → `exports`）と第11.2節（UoW 契約は「維持」ではなく「差し替え」）にも置かれている。これは望ましい形である — Issue 本文と設計が食い違ったときに、担当者が気づかず Issue に従う事故を防いでいる。
- **[N-005] 未確認項目に決定主体と時期の欠落は無い。** 第2.1節の `未確認` 6行（F-4b / F-13 / F-14 / F-26 / F-27b / F-32b）はすべて「#37 の着手時に spike で確定する」を持ち、第11.4節の10行はすべて「決める主体」「いつ」の両欄が埋まっている（#44 が1行、#37 の spike が9行、うち2行は #37 → #38 の2段）。「検討する」「TBD」「暫定」で終わる節は0件（確認項目7 手順3 が空）。
- **[N-006] 委譲であることが読み取れる書き方になっている。** `#44 待ち` が6箇所（第1.1節の #38 読者導線 / 第11.2節の `rotationKind` 行 / 第11.3節の4項目）で使われ、第11.3節は「**#38 はこの項目を「#44 の成果物を入力とする」ものとして扱い、#44 が完了する前に手順書を書き始めない**」「**その前提に立つ検知手順を #38 が先に書いてはならない**」と踏み込んでいる。未決の放置と読める箇所は見つからなかった。
- **[N-007] `testing.md` 確認項目16 の「実測」値だけが陳腐化している** — 「走査時点の実測 130件 / うち ADR-020 以降 111件」に対し現物は 136 / 117。ただし同項目は「判定は『19より大きい』であって特定の件数ではない。レビュー往復のたびに増える」と自ら断っているので**検査は成立している**。指摘としては挙げない。
- **[N-008] `spec/manual-tests/search.md` の行番号ラベルが1件ずれている。** 第11.1節が「`:5` / `:69` / `:266` の**非同期反映**前提」と書いているが、この3行はいずれもハイブリッド / ベクトル前提であり、実際の非同期前提は `:48` / `:125`（TC-07 の表題そのもの）/ `:128` / `:133` / `:137` / `:270` にある。ただし同じセルの追加・置換欄が「『投稿直後は検索にヒットしない場合がある』の確認項目は**必ずヒットする**に反転する」と指示しており、`:17` / `:247` が語彙走査に掛かってファイル自体は改訂対象に入るので、**#35 の作業結果は変わらない**。ラベルの精度の問題なので指摘としては挙げない。
