.DS_Store
node_modules/

# TanStack Start / Vite
.tanstack/
.output/
.nitro/
dist/

# Cloudflare Wrangler
.wrangler/
worker-configuration.d.ts
.dev.vars
# Rendered from `wrangler.<stage>.toml.tpl` by scripts/render-wrangler.ts.
# Edit the `.tpl` instead — these are regenerated on every deploy.
wrangler.staging.toml
wrangler.production.toml
# Per-worker variants rendered while splitting the app across Durable Objects.
wrangler.*.staging.toml
wrangler.*.production.toml

# Local env files
.env
.env.*

.envrc
.direnv

*.db
*.db-shm
*.db-wal
.claude/scheduled_tasks.lock

# Test run evidence / reports written by the verification scripts.
.artifacts/

# Fixture written by `lint/pluginWiring.test.ts` and removed in its `finally`.
# The test passes `--vcs-use-ignore-file=false` so Biome still lints it.
lint/*.tmp.ts
