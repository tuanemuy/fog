# PH-03 検証報告 — ナレッジ

- Verifier 実施: 2026-09-08T12:38〜12:51+09:00。対象 HEAD `8f5833b`（`2c4c8a0` core / `4b455cb` web / `8dc4d10` テスト / `c1ae41a` core / `8f5833b` web、spec `121ffe7`）、ブランチ `feat/spec-implement`。作業木の tracked ファイルに未コミット変更なし
- 実装コード・`spec/`・管理ファイルは変更していない。書き込みは本ファイルのみ

## 判定

| ID | 判定 | 根拠 |
|---|---|---|
| R-DT-01 | 合格 | B（dev 12:42〜12:43、preview 12:50）: 作成（名前空白のみで「追加」非活性、説明欄は「説明を追加」で展開、楽観行 → sqlite `topics` version 0、同一フレーム 2 回 submit で 1 行）、一覧（進行中の行に `ドキュメント数: n`）、P-07 で名前 / 説明の編集（空名は保存不可、説明を空にすると `NULL`、楽観反映 → version 2）、完了にする → バッジ「完了」/ `archived` → P-06 は下部「› 完了済み（1）」に畳まれ（既定閉、開くと行が出る）、完了を解除 → `active`（version 4）。完了済み 0 件のときトグルは出ない（`toggle` なし）。不在 / trashed は「トピックが見つかりません」+ `/topics`。§2 A-1〜A-3。T: `topics.integration.test.ts` (a)〜(e)、dom `topicList` 11 件 / `topicRow` 4 件 / `topicHeader` 6 件 / `topicDetailFeed` 2 件 |
| R-DT-02 | 合格 | B（dev 12:44〜12:46、preview 12:50）: P-09 新規（理由欄なし、タイトル空で非活性）の出典ピッカー（`<search>`）で `検証03 出典` → 候補 1 件（改行を畳んだ snippet）→ 追加 → 「追加済み」非活性 → 外す → 再追加 → `買い物` → メモ1 追加 → 空欄検索は直近 20 件。保存（`requestSubmit` ×2）→ 1 行、`/documents/<id>`、sqlite: rev 1「作成」、`source_links` 2 行、`search_entries.source_ids` が doc→memo / memo→doc の両向き、`topics.version` +1（touch）。編集: placeholder「手動編集」、出典は読み取り専用（外す / 追加ボタン 0）、変更なし保存は rev 不変で P-08 へ、理由 + タイトル変更 → rev 2「検証理由」+ `search_entries` 更新、空理由 → rev 3「手動編集」、別タブ競合 → `role=alert`「編集を開いた後にあなたが編集しました（12:45・t2 の理由）」+ 現在のタイトル / 本文 + 「そのまま保存」→ rev 5（t2 の rev 4 残存）、タイトル 201 文字 → 「タイトルは200文字以内で入力してください」+ 入力保持。§2 A-4〜A-7。T: `documents.integration.test.ts` (a)〜(g)（両向き projection・トピック touch・code point 境界 `𠮷`×400,000・不在 / trashed で何も書かない・conflict と再適用・RAISE(IGNORE) で OCC・重複 rev 番号 → OCC）、dom `documentEditor` 7 件、`schema.test.ts` |
| R-DT-03 | 合格 | B（dev 12:46〜12:47、preview 12:50）: 昇順 5 行（日時 / あなた · 理由 · リビジョン n）、1 回目クリックで「比較元」+「リビジョン 1 → リビジョン 5（最新） の差分」（タイトル行 + removed / added）+ 戻すボタン、2 回目で「リビジョン 1 → リビジョン 2 の差分」に切替、戻す → 確認 → P-08（タイトル・本文が rev 1）、rev 6「リビジョン1の内容に戻す」・1〜5 残存。最新を 1 点目に選ぶと差分なし、戻す → 「現在の内容は既にこのリビジョンと同じです」で rev 不変。1 件のみは `DIV` 行・戻すなし、不在は「ドキュメントが見つかりません」。§2 A-8。T: `documents.integration.test.ts` (h)〜(j)、dom `documentRevisionHistory` 7 件 |
| R-DT-04 | 合格 | B（dev 12:44〜12:48、preview 12:50）: P-04 のメモ1 に `<nav aria-label="出典になっているドキュメント">` で trashed の「削除済みのドキュメント」`span[aria-disabled]` と live の「検証03 ドキュメント」`<a>` が**並ぶ**（複数 + 削除済み非リンク）。P-08「元になったメモ」は `/?memo=<id>` の `<a>`（投稿日時付き）、出典メモを削除後は `div[aria-disabled]`「削除されたメモ」。P-07 の関連メモも同じ行。ハードデリート済みの不在は T で固定（`sourceLinks` (a)(c)、`topics` (e) が行を直接 DELETE）。編集中は導線を出さず、取り消しで戻る（P-6）。§2 A-5, A-9, P-3。T: `sourceLinks.integration.test.ts` (a)〜(c)、dom `sourceDocumentLinks` / `originRow` / `memoEntry`（+2） |
| R-DT-05 | 合格 | B（dev 12:47〜12:48、preview 12:50）: ドキュメント削除 → 「ドキュメントを削除しますか？」→ キャンセルで `active` → 削除で P-07、sqlite `trashed` / `trashed_with NULL` / `purge_after − trashed_at = 30.0 日` / `search_entries` 0 行 / `jobs` の `purge-trash` は早い既存起床のまま。P-06 の行メニュー（編集 / 削除の 2 項目 = 完了と並ばない）と右クリックで同じメニュー、確認 → キャンセルで残存 → 削除で即時除去（楽観）→ sqlite `trashed` / `was_archived 0`。P-07 の削除はメニューの `<hr class="fog-pop-separator">` の下（編集 / 完了にする / — / 削除）。セット削除: active 1 件 + 単独削除済み 1 件のトピック → `/topics`、topic `trashed`、active だった文書は `trashed_with = <topic id>` で同一 `purge_after`、単独削除済みは `trashed_with NULL` と元の期限のまま、両文書の `search_entries` 除去、出典メモ1 の `source_ids` が `[]`。以後 P-07 / P-08 は「見つかりません」、P-04 の導線は「削除済みのドキュメント」。§2 A-10〜A-12。T: `trashKnowledge.integration.test.ts` (a)〜(c)（(c) は `RAISE(IGNORE)` で 1 件の save を競合させセット全体が巻き戻る）、`service.test.ts`、dom `topicList` 3 件 / `topicHeader` / `documentActions` 4 件 |

### Manager の判断が必要な事項

1. **F-1 dom テストの不安定さ**（合否には含めない）: `pnpm test` の 1 回目で `memoEntry.dom.test.tsx` の 2 件（conflict の「そのまま保存」ラベル待ち / rejection の alert）が失敗、`pnpm test:unit` の再実行 1 回目で `timelineBoard.dom.test.tsx`「shows the optimistic entry…」が失敗、その後の 2 回は 73 files / 1,229 とも緑、失敗ファイル単体は 3/3 緑。PH-02 の timeline dom テストが 73 ファイル並列の負荷で `pending` 表示を取りこぼす timing 依存で、PH-03 の変更（`MemoEntry` 非編集側に `SourceDocumentLinks` 1 行）は編集フォーム側に触れていない。Implementer の報告（12:33 緑）と矛盾しない「ときどき落ちる」状態。CI で偽陽性になるので、`waitFor` 化か `--project dom` の並列度を下げる修正を issue 化するか判断を求める
2. **G-1 `insertSourceLinks` の 33 行 chunk が未固定**: 実装報告 R-DT-02 の T 欄「出典 100 件 chunk」に当たるテストは無い（`documents.integration.test.ts` の出典は最大 2 件、`grep insertSourceLinks|length: 100` で該当なし）。zod は `sourceMemoIds` ≤ 100、store は 33 行ずつ `INSERT`（100 パラメータ上限）。R-DT-02 の完了条件「出典リンクが生成される」は 2 件で満たしているので合格のままとし、34 件以上（≥ 2 chunk）の integration を追加する指示を推奨（追加観察 A-6 に再掲）
3. A-2（trashed ドキュメントの P-10 が読める）: spec/usecases/knowledge.md listDocumentRevisions 手順 2 のとおり `findByIdIncludingTrashed` なので**現状維持で妥当**。dev 12:47 で削除済み「検証03 単独」の `/history` が 1 行（戻りリンクは「ドキュメント」）を表示することを確認。P-08 / P-09 は「見つかりません」

## 1. 自動テスト（自分で実行）

- 2026-09-08T12:38:27〜12:38:39+09:00、HEAD `8f5833b`、tracked に未コミット変更なし
- `pnpm typecheck` 0 件、`pnpm lint` 0 件、`pnpm format:check` 差分なし
- `pnpm test`: integration **15 files / 69 tests** 緑。unit+dom は 1 回目 **72/73 files、1,227/1,229**（`memoEntry.dom.test.tsx` 2 件失敗）、`pnpm test:unit` 再実行 1 回目 1 件失敗（`timelineBoard.dom.test.tsx`）、再実行 2・3 回目（12:41:23 / 12:41:29）**73 files / 1,229 tests** 緑。件数は報告（73 / 1,229、15 / 69）と一致。失敗の性質は F-1
- 完了条件を固定しているテスト（名前と断定で確認）:
  - `topics.integration.test.ts`: (a) version 0・description 任意・名前境界、(b) rename / description null / archive 往復 / 空更新は `ValidationError`、(c) OCC、(d) `name, id` 順・archived は要求時のみ・配下 active のみ、(e) 配下 `updatedAt desc`・関連メモの重複除去と `deleted`・ハードデリート済み（行 DELETE）は不在
  - `documents.integration.test.ts`: (a) 行 + rev #1 + links + 両向き projection + topic touch、(b) 理由補完・空本文・archived 配下・`𠮷`×400,000 と `あ`×400,001（code point 判定）、(c) トピック / 出典の不在・trashed で何も書かない、(d) saved / unchanged / title のみ、(e) conflict と再適用、(f) 不変条件・NotFound・conflict 判定通過後の OCC（`RAISE(IGNORE)`）、(g) 重複 rev 番号 → `OPTIMISTIC_LOCK_FAILURE`、(h) summaries 昇順・本文なし・trashed でも可、(i) diff の順序 / 同一 / 不在、(j) rollback の新 rev と履歴不変・同内容 no-op
  - `sourceLinks.integration.test.ts`: (a) link 順・最新本文・`deleted`・ハードデリート済み不在、(b) `listDocumentsReferencingMemo`（trashed メモでも）、(c) timeline 3 経路の `sourceDocuments`（`isTrashed`・ハードデリート済みは落ちる）
  - `trashKnowledge.integration.test.ts`: (a) 単独削除 = `trashed_with NULL`・`purge_after`・entry 除去・出典メモ再射影・`purge-trash`、(b) セット削除 = `trashed_with = topic.id`・同一期限・単独削除済みは触らない・`jobs` は早い方、(c) 1 件の save 競合でセット全体が巻き戻る
  - dom 14 ファイル（`topicList` / `topicRow` / `topicHeader` / `topicDetailFeed` / `documentEditor` / `documentActions` / `documentRevisionHistory` / `originRow` / `knowledgeNotFound` / `sourceDocumentLinks` + `memoEntry` / `appShell` / `renderWithRouter` 拡張 + `schema.test.ts` ×2）: 所有関係（`TopicList` が追加 / 削除の楽観と N-1 ガード、`TopicHeader` が名前 / 説明 / 完了の `useOptimistic`、`DocumentEditor` の form が保存、`DocumentActions` が削除）、失敗で復帰 + 再試行、形検証を「wrong shape → system error」で固定

## 2. ブラウザ検証（自分で実行）

`agent-browser --session verify03`、`ph02@example.com`。dev = http://localhost:3000（Implementer の PID 16273 をそのまま使用）、preview = http://localhost:4173（**自分で `pnpm build`（12:41:32、exit 0、6 ビルド）→ `pnpm preview` PID 30797 に立て直し**。Implementer の 22427 は停止）。sqlite は `a36227cb….sqlite`。React の controlled input は native setter + `input` イベント、送信は `requestSubmit()`。

### dev

| # | 時刻 | 操作 | 期待 | 結果 |
|---|---|---|---|---|
| A-1 | 12:42:33 | `/topics`（既存「プレビュー確認」1 件）: 名前を空白 3 文字 → 追加ボタン | 非活性 | `disabled: true`。ナビ「トピック」に `aria-current`、`<h1>` トピック |
| A-2 | 12:42:33 | 名前「検証03 トピック」+「説明を追加」→ 説明 2 行 → submit。`requestSubmit` ×2 で「検証03 二重送信」。「検証03 空トピック」 | 行が出る、1 行 | 行「検証03 トピック / ドキュメント数: 0 / 説明」、sqlite version 0・説明は改行込みで保存、二重送信 1 行、4 行 |
| A-3 | 12:42:50 | `/topics/<検証03>`: 表示、メニュー、編集（空名 → 非活性、名前改 + 説明空 → 保存）、完了にする、`/topics` の折り畳み、完了を解除、不在 2 種 | P-07 仕様 | 名前 / 説明 / 「まだドキュメントがありません」+ `＋ 新しいドキュメント`（`/topics/<id>/documents/new`）/ 関連メモ非表示。メニュー = `編集 / 完了にする / <hr.fog-pop-separator> / 削除(.fog-pop-danger)`。編集: 非活性 ✓、送信直後に楽観で「検証03 トピック改」、sqlite `NULL` 説明 / version 2。完了にする → 即時バッジ「完了」/ `archived` v3 → `/topics` は 3 行 + 「› 完了済み（1）」（閉）→ 開くと `[aria-label=完了済みのトピック]` に 1 行。archived でも「新しいドキュメント」あり（△-6）。解除 → バッジなし / `active` v4。trashed「読書メモ」/ `nope` → 「トピックが見つかりません」+ `/topics` |
| A-4 | 12:44:28 | 投稿「検証03 出典メモ 一行目\n二行目の本文」→ `/topics/<id>/documents/new` | P-09 新規 | `<h1>` ドキュメント作成、form「ドキュメントを作成」、コンテキストにトピック名、保存非活性、理由欄なし、「出典メモはまだありません」+「＋ 出典を追加」。タイトル入力で活性 |
| A-5 | 12:44:40 | ピッカー: `検証03 出典` 検索 → 追加 → 外す → 再追加 → `買い物` → メモ1 追加 → 空欄検索 | △-2 / △-9 | 候補「2026/09/08 12:44 / 検証03 出典メモ 一行目 二行目の本文 / 追加」（改行畳み）→「追加済み」非活性 + 出典行「出典から外す」→ 外すと 0 件・ボタン「追加」→ メモ1 候補 1 件 → 空欄は 20 件。出典 2 件 |
| A-6 | 12:44:52 | 保存（`requestSubmit` ×2）→ P-08 | 1 件、リンク | `/documents/<DOC>`、documents 1 行 / rev 1「作成」user / `source_links` 2 行 / `search_entries`: memo1・出典メモの `source_ids` = [DOC]、DOC の `source_ids` = [memo1, 出典メモ] / topics.version 5（touch）。P-08: `<h1>` ドキュメント、タイトル、トピックリンク `/topics/<id>`、Markdown（`# 見出し` → `<h1>`、`**強調**`、`<li>` 2）、操作 = 編集 `/edit` / 履歴 `/history` / 削除、「元になったメモ」2 行とも `/?memo=<id>` の `<a>` |
| A-7 | 12:44:59 | P-04 `/?memo=<メモ1>`、P-09 編集: 変更なし → 理由 + タイトル変更 → 空理由 → 別タブ競合 → タイトル 201 文字 | R-DT-02 / R-DT-04 | メモ1 の `nav[aria-label=出典になっているドキュメント]` = `SPAN 削除済みのドキュメント(aria-disabled)` + `A 検証03 ドキュメント(/documents/<DOC>)`。編集: form「ドキュメントを編集」、placeholder「手動編集」、外す / 追加 0、出典 2 行（`<a>`）、「編集をやめる」。変更なし → P-08 / rev 1 v0 のまま。理由「検証理由」+ 改 → rev 2 v1、`search_entries.body` 更新。空理由 → rev 3「手動編集」。t2 で「t2 の理由」保存（rev 4 v3）→ t1 保存 → `/edit` に留まり `.fog-conflict`「編集を開いた後にあなたが編集しました（2026/09/08 12:45・t2 の理由）。」+ 現在のタイトル / 本文、ボタン「そのまま保存」、DB は rev 4 のまま → そのまま保存 → rev 5「手動編集」本文 = t1 の追記、rev 4 残存。201 文字 → 「タイトルは200文字以内で入力してください」、title 201 / 本文保持、rev 5 のまま |
| A-8 | 12:46:52 | `/documents/<DOC>/history` | R-DT-03 | 昇順 5 行 `BUTTON`、「比較元のリビジョンを選んでください」。rev1 → 「比較元」+「比較先を選ぶと二点の差分に切り替わります」+「リビジョン 1 → リビジョン 5（最新） の差分」+ `タイトル: 検証03 ドキュメント → 検証03 ドキュメント改` + context / removed / added 行 + 戻す。rev2 → 「リビジョン 1 → 2」/「… → リビジョン 2 の差分」（added `- 三つ`）。戻す → 「この内容に戻しますか？」→ P-08 でタイトル「検証03 ドキュメント」・`<li>` 2、rev 6「リビジョン1の内容に戻す」。最新（rev 6）を 1 点目 → 差分なし、戻す → notice「現在の内容は既にこのリビジョンと同じです」、rev 6 のまま。出典なし「検証03 単独」を作成（本文空 → 保存可、`.fog-origin` なし）→ `/history` は `DIV` 1 行・戻すなし。`/documents/nope/history` → 「ドキュメントが見つかりません」 |
| A-9 | 12:47:32 | 「検証03 単独」を P-08 から削除（キャンセル → 削除） | R-DT-05 / A-2 | キャンセルで `active`。削除で `/topics/<id>`（一覧に残るのは「検証03 ドキュメント」）、sqlite `trashed` / `trashed_with NULL` / 30.0 日 / v1、`search_entries` 0、`jobs` は `2026-09-09 11:27:12` のまま。`/documents/<単独>` → 見つかりません、`/history` → 1 行（戻りリンク「ドキュメント」） |
| A-10 | 12:47:50 | 出典メモ「検証03 出典メモ」をタイムラインで削除 | 削除済み表示 | memos `trashed`、DOC の `source_ids` は変わらず（出典メモは trashed でもリンクは残る）。P-08「元になったメモ」= `A メモ1` + `DIV[aria-disabled] 12:44 削除されたメモ`。P-07 関連メモも同じ 2 行（`postedAt` 降順） |
| A-11 | 12:48:03 | P-06「検証03 空トピック」: メニュー → 削除 → キャンセル; 右クリック → 削除 → 削除 | TC-32 / TC-19 | メニューは「編集 / 削除」の 2 項目。ダイアログ「トピックを削除しますか？ トピックと配下のドキュメントは…」→ キャンセルで 4 行・`active`。`contextmenu` で `[role=menu]` 1 → 削除 → クリック直後に 3 行（楽観）→ 3.5 秒後も 3 行・alert なし、sqlite `trashed` / `was_archived 0` / 30.0 日 |
| A-12 | 12:48:13 | P-07「検証03 トピック改」（active「検証03 ドキュメント」+ trashed「検証03 単独」）の削除 | セット削除 | `/topics` へ。topic `trashed` v7 `purge_after 2026-10-08 12:48:17`、「検証03 ドキュメント」`trashed` / `trashed_with = <topic id>` / 同一 `purge_after` / v6、「検証03 単独」は `trashed_with NULL` / 期限 12:47:37 のまま、両 `search_entries` 0、メモ1 の `source_ids = []`、`jobs` 不変。P-04 メモ1 の導線は `SPAN 削除済みのドキュメント` ×2。`/topics/<id>` / `/documents/<DOC>` → 見つかりません |
| A-13 | 12:48:32 | TC-39: 「プレビュー文書」の本文を `あ`×400,000 → 保存 / ×400,001 → 保存 | 境界 | 400,000: P-08 へ、rev 3 / `length(body) 400000`。400,001: `/edit` に留まり「本文は400,000文字以内で入力してください」、本文 400,001 保持、rev 3 のまま |

### preview（SSR ストリーミング経路、12:49:56〜12:50:59）

| # | 操作 | 結果 |
|---|---|---|
| P-1 | `/settings` → ナビ「トピック」（MutationObserver） | T+51ms `[class*=skeleton]` 4 / 行 0 → T+353ms skeleton 0 / `.fog-topic-row` 2（skeleton → 内容） |
| P-2 | `createTopicFn`「プレビュー検証03」 | 行が出る、sqlite 1 行 |
| P-3 | `updateTopicFn` 完了にする / 解除 | `archived` → `active` v2 |
| P-4 | `createDocumentFn`（出典 メモ1）→ P-08 | `/documents/<PDOC>`、`source_links` 1 行、「元になったメモ」`/?memo=<メモ1>` |
| P-5 | `editDocumentFn`（本文 v2・理由「preview 理由」） | rev 2 |
| P-6 | `diffDocumentRevisionsFn`（1 点目 = rev1）→ `rollbackDocumentFn` | 「リビジョン 1 → リビジョン 2（最新） の差分」removed / added → P-08、rev 3「リビジョン1の内容に戻す」= 本文 v1 |
| P-7 | P-04 `/?memo=<メモ1>` | 導線 = `SPAN 削除済み` ×2 + `A プレビュー検証03 文書`、ハイライトあり |
| P-8 | `trashDocumentFn` → P-07、`trashTopicFn` → `/topics` | `trashed` / `trashed` |
| P-9 | `errors` | なし。8 server fn すべて production マニフェスト経由で成功（`__root.tsx` の side-effect import） |

### PH-01 / PH-02 回帰（preview、12:50）

- インライン編集: メモ1 の編集を開くと `.fog-doc-links` 0（編集中は導線なし）、取り消しで 3 件に戻る
- `?date=2026-09-03` → `scrollY 3194 / 31 件 / 9月1日@0`（PH-02 再検証と同じ値。`section.fog-timeline article` への絞り込み後も同じ）
- 投稿「PH-03 検証 投稿」が先頭に出る

## 3. Implementer の検証結果を利用した範囲

- 利用なし。上表はすべて自分で再実行し、実装報告の B 欄と一致した。差異: 自動テストの 1 回目に dom 2 件が落ちた（F-1）。A-5（関連メモの順序）は報告「出典の登録順」に対し実装は `postedAt` 降順（`getTopic.ts` の sort）で、観察は A-10 のとおり降順 — spec に順序指定は無いので合否に影響なし

## 4. コードレビュー

### 指摘（不具合ではない）

- (a) 同期契約 / UoW: `application/knowledge/*.ts` の procedure に `await` なし、`run` のネストなし（`durableObjectBase.ts` の 2 か所のみ）。`trashTopicProcedure` は `findById` → `listActiveByTopic`（OCC トークン付き）→ `trashTopicSet` → `topicRepository.save` + 各 `documentRepository.save` → `armPurgeTrash(ctx)` を 1 callback で行い、(c) のテストが 1 件の save 競合でセット全体の巻き戻りを固定。`armPurgeTrash` は `application/trash/armPurgeTrash.ts` に抽出され、`softDeleteMemo` / `trashDocument` / `trashTopic` の 3 箇所から呼ばれる（`rosterGrep` の `enqueueJob({ … kind: "purge-trash" })` は 1 箇所）。facade は `idGenerator` / `clock` / `requireSelfLocator()` を `runUnitOfWork` の外で取る
- (b) spec 突き合わせ: `Topic`（`create` / `rename` / `changeDescription` / `archive` / `unarchive` / `softDelete` が `wasArchived` を記録 / `restore`）、`Document`（`create` の重複除去と rev #1、`edit` は title 同一 + `DocumentBody.equals` で `unchanged`、`rollback` の `RevisionDocumentMismatch`、`softDelete` の `TrashedWithMismatch`、`restore` / `moveToTopic`）、`DocumentRevision` / `SourceLink` の型、`TopicTrashService.trashTopicSet` / `restoreTopicSet` が spec/domains/knowledge.md と一致。ポート: `LiveTopicSummary` / `ActiveDocumentSummary` / `DocumentSummary` / `DocumentRevisionSummary` は `Pick` で `Versioned` を載せず、`listRevisionSummaries` は `REVISION_META_COLUMNS`（`body` 列を読まない）、`listRevisions` は無い（△-10）。`documents.topic_id` に FK なし（DDL 変更なし、ADR-001）。projection: `projectDocument` は `type: "document"` / `topicId` / `title` / `body` / `updatedAt` / `sourceIds = activeSourceMemoIds`、`save` は active なら再射影・trashed なら `removeSearchEntry`、どちらも出典メモを `reprojectMemo`（`source_ids` 両向き。A-6 / A-10 / A-12 で確認）。ADR-003: ハードデリート済みは `listByIdsIncludingTrashed` / `listSummariesByIdsIncludingTrashed` の結果に無いので落ちる（T で固定）。ADR-006 は memo 側の `editMemo` 全文置換のまま（PH-03 は触れない）。`createDocumentProcedure` の topic touch は `save({ ...topic, version + 1 }, token)` で内容不変 ✓。`updateTopicProcedure` は何も適用されなくても `version + 1` で `save`（testcases どおり）
- (c) Manager 決定: △-1 zod `.max(800_000)`（`documents/schema.ts`）+ VO が code point で判定（A-13、T (b)）✓ / △-2 `SourceMemoPicker` は `loadTimelinePageFn({ limit: 20, keyword })` ✓ / △-3 新規に理由欄なし、`CREATE_CHANGE_REASON`「作成」✓ / △-4 編集は `OriginList` 読み取り専用 ✓ / △-5 1 点目で `loadDiff(base, latestRevision)`、最新自身は差分なし ✓ / △-6 archived でも「新しいドキュメント」、`findById` は Live ✓ / △-7 `listDocumentsReferencingMemo` は facade / gateway / T まで、web から import なし ✓ / △-8 `application/identity/view.ts` に `ActorView` / `toActorView`、`memo/view.ts` は再 export ✓ / △-9 `snippetOf` が `\r?\n` → 空白（A-5 の候補行と P-08 で確認）✓ / △-10 ✓
- (d) 所有規則: `TopicList`（list owner: 追加は form の `useActionState` + 楽観行、削除は `ConfirmDialog` → `useOptimistic` remove → `trashTopicFn` → `removed` + `invalidate`、失敗は revert + `role=alert` + 再試行、N-1 ガード）、`TopicRow`（leaf、`onDelete` を上げるだけ、長押し / 右クリック）、`TopicHeader`（leaf: 名前 / 説明 / 完了を `useOptimistic` で即時反映 → `invalidate`、削除は `navigate("/topics")`）、`DocumentEditor`（form が保存、N-1 ガード、`expectedVersion` は開いた `version` を state に保持、conflict → `currentVersion` で再送）、`DocumentActions`（削除 → `navigate` to P-07）、`DocumentRevisionHistory`。結果はすべて `readServerFnResult(isXxxResult)` で形検証（dom「wrong shape → system error」）
- (e) レイヤー境界 / ban list: `snippetOf` は `lib/text`（core・web 両方から import 可）、`RetentionPolicy` は domain/trash、TZ は presentation。新しいエラークラスなし（`isUniqueViolation` は関数）、`banList.test.ts` 緑、`pnpm lint` 0 件
- (f) 細部: `trashTopicProcedure` の `throw new Error("unreachable")`（`documents[index]` の到達不能分岐）は plain `Error`。到達不能だが PH-01 B-1 の方針（plain Error → `SystemError`）とは揃っていない。`memoRepository.insertRevision` は `isUniqueViolation` を通していないが、spec/domains/memo.md L327 は memo 側の一意制約違反を `SystemError(DatabaseError)` と定めているので設計 §2.2 の「両方」の読みが過剰で、現状が spec どおり。`TopicList` の `input maxLength={100}` / `textarea maxLength={500}` は VO と一致、zod は 400 / 2,000（transport 上限のみ）

## 5. 追加観察

- A-1（ピッカーの `<form>` 入れ子を `<search>` + `type=button` に変更）: 妥当。dom 19 件と A-5 で「検索」が編集フォームを送信しないことを確認（A-5 の検索後も P-09 に留まる）
- A-2（trashed ドキュメントの P-10）: 冒頭「判断」3 のとおり現状維持で妥当
- A-3（`listDocumentsReferencingMemo` 未接続）: △-7 どおり。T (b) で固定
- A-4（セット削除で `jobs.next_run_at` が動かない）: 収束規則 (1)。A-9 / A-12 で確認、`trashKnowledge` (a) が早まる側を固定
- A-5（関連メモの順序）: 報告は「登録順」だが実装は `postedAt` 降順（`getTopic.ts`）。spec は順序を定めていないので合否に影響なし。報告の記述だけ実装に合わせて直すとよい
- **A-6（新規、G-1）**: `insertSourceLinks` の 33 行 chunk（≥ 34 件の出典）を固定するテストが無い。zod 上限 100 なので 100 件の integration を 1 本足すことを推奨
- **A-7（新規、F-1）**: dom テストの負荷依存の不安定さ（§1）
- A-8（新規）: `TopicHeader` の編集フォームは `onSubmit` + `useTransition` で、`DocumentEditor` / `TopicList` のような `inFlight` ガードは無いが、`saving` 中は `save()` が early return するので二重送信は起きない（別方式で同じ結果）

## 6. PH-01 / PH-02 受け入れ済み 13 項目への影響

- R-TL-02 / R-TL-03 / R-TL-08（`sourceDocuments`）: `getTimeline` / `jumpToDate` / `showMemoInTimeline` の `items.map(toTimelineItemView)` が `attachSourceDocuments(ctx, items)`（`listSourceLinksByMemos` + `listSummariesByIdsIncludingTrashed` の 2 クエリ / ページ）に置換。ドキュメント未作成のメモは `[]` のまま（`timeline` / `timelineWindow` の断定は不変で緑）。preview で `?date=2026-09-03` の見出し位置（9月1日@0、31 件）は PH-02 再検証と同値、`?memo=` のハイライトあり
- R-TL-01 / R-TL-04（`MemoEntry`）: 非編集側に `SourceDocumentLinks` が付くだけ。編集中は導線なし、取り消しで戻る（preview 12:50）。投稿は先頭に出る。dom `memoEntry` 12 件 + `timelineBoard` 32 件は据え置き（F-1 の timing 以外は緑）
- R-TL-05（`ActorView` 移設）: `memo/view.ts` の再 export で import 元不変、`actorLabel` は `MemoEntry` から document 側が import。`revisionHistory.dom` 8 件緑
- R-TL-06（`armPurgeTrash` 抽出）: `softDeleteMemo.integration.test.ts` (c) は `PURGE_TRASH_OPERATION_KEY` の import 元を追随して緑。A-9 / A-12 で `jobs` の収束を再確認
- R-TL-03 の `TimelineBoard` 先頭判定を `section.fog-timeline article` に狭めた（PH-02 再検証の観察どおり）: `?date=2030-01-01` 相当の「一覧先頭ならスクロールしない」は dom「does not scroll when the pivot already heads the list」が緑、preview の `?date=2026-09-03` は同値
- R-INF-02（ナビ）: 「タイムライン / トピック / 設定」の 3 件、`/topics` と `/documents/*` で「トピック」が current（A-1、`appShell.dom` 更新済み）。`snippetOf` の改行畳みは memo の既存テスト（`text.test.ts`）が緑
- identity 側（R-AC-01 / 03 / 04 / 08、R-INF-01）: `git diff --stat` に identity / signup / login のファイル無し。ログイン 12:41:40（dev）/ 12:49:56（preview）で変化なし

## 7. 検証環境の引き継ぎ

- 起動中: dev PID 16273（Implementer 起動のまま、http://localhost:3000）、preview PID 30797（自分で `8f5833b` を再ビルド、http://localhost:4173、ログ scratchpad `preview3.log` / `build3.log`）
- DO 状態（`a36227cb…`）: topics = 「プレビュー確認」active、「検証03 二重送信」active、「読書メモ」「検証03 トピック改」「検証03 空トピック」「プレビュー検証03」trashed。documents = 「プレビュー文書」rev 3（本文 `あ`×400,000）、「出典つきプレビュー」active、「検証03 ドキュメント」（rev 6、`trashed_with` あり）「検証03 単独」「プレビュー検証03 文書」「読書メモまとめ」「出典テストB」trashed。memos に「検証03 出典メモ」（trashed）「PH-03 検証 投稿」を追加。`jobs.purge-trash` は 2026-09-09 11:27:12 JST のまま、`trash_retention_days` 30
- agent-browser: session `verify03`（3000 / 4173 でログイン中、タブ t1 / t2）。Implementer の `ph03` はそのまま
- scratchpad: `checks3.log`（自動テスト全文、失敗 2 件の出力を含む）、`flaky.log`、`build3.log`、`ab3.sh` / `jump1.js`
