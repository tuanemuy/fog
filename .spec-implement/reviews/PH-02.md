# PH-02 検証報告 — タイムライン完成

- Verifier 実施: 2026-09-08T11:13〜11:33+09:00。対象 HEAD `f43ca7f`（`ef447d3` core / `1f3b3dd` web / `f43ca7f` テスト）、ブランチ `feat/spec-implement`
- 実装コード・`spec/`・管理ファイルは変更していない。書き込みは本ファイルのみ

## 判定

| ID | 判定 | 根拠 |
|---|---|---|
| R-TL-03 | **不合格（日付ジャンプの表示位置のみ。B-1）** | 絞り込み: 合格（`%` / `_` を文字として照合、ASCII 大小無視、0 件表示 + 解除導線、バーからの絞り込みと × 解除、`q` と `date` の両立。§2 A-1〜A-3, P-1）。日付ジャンプ: **データは正しい**（該当日 / 前後で近い側 / 範囲外 / 絞り込み継続。§2 A-4, A-5, P-2 と `timelineWindow.integration.test.ts` (b)〜(h)）が、**画面はその位置へ移動しない** — ジャンプ直後の viewport には window の最新側（9/8 のメモ）だけが見え、指定日の見出しは画面外（dev / preview とも。§2 A-4, P-2, §4 B-1）。`spec/manual-tests/timeline.md` TC-07 手順 1「その日付の見出し位置にタイムラインが移動し、その日のメモが表示される」と、完了条件「該当位置（無ければ最寄り）へ移動する」を満たさない |
| R-TL-04 | 合格 | 保存で rev +1 / version +1 / `search_entries` 更新 / `posted_at` 不変、保存中の楽観表示、空白のみは保存非活性、変更なしは何も書かない、取り消し、別タブ競合 → 警告（actor「あなた」+ 現在の本文）→ そのまま保存で両リビジョン残存（§2 A-7, A-8, P-3）。T: `editMemo.integration.test.ts` (a)〜(g)、`memoEntry.dom.test.tsx` |
| R-TL-05 | 合格 | 昇順 5 行、1 回目 = 比較元 + 「この内容に戻す」、2 回目 = 比較先 + 差分（逆順 3→1 も可）、戻す → 確認 → rev +1 = 比較元と同内容・履歴不変 → `/?memo=` にハイライトが viewport 内、最新を戻すと「既に同じ」、1 件のみは行が button でなく差分 / 戻すなし、不在は案内 + タイムラインへ（§2 A-6, A-9, P-4, P-6）。T: `revisions.integration.test.ts`、`diff.test.ts`、`revisionHistory.dom.test.tsx` |
| R-TL-06 | 合格 | 確認ダイアログ（キャンセルで残存・`jobs` 不変）→ 確認で即時消失（`presentImmediately: false`）→ sqlite `trashed`、`purge_after = trashed_at + 30d`、`search_entries` 除去、`jobs` の `purge-trash` は既存の早い起床のまま（規則 (1)）。保持 1 日にして 2 件目を削除すると `next_run_at` が新しい `purge_after` に早まる（§2 A-11, A-12, P-5）。T: `softDeleteMemo.integration.test.ts` (a)〜(c) |
| R-TL-08 | 合格 | `?memo=<最古>` で `.fog-memo-highlight` + viewport 内（top 339 / 633）、trashed / 不在は `role="status"` の案内 + 通常 50 件、履歴からのロールバック後も同じ（§2 A-6, A-9, P-4, P-6）。T: `timelineWindow.integration.test.ts` (i)(j)、`timelineBoard.dom.test.tsx` |
| N-1 | 対応済み | 同一フレーム 2 回の `requestSubmit()` → 1 行。1 回目が 422（10,001 文字）でも POST は 1 回、入力保持、直してから再送で 1 行（§2 A-10）。dom「does not re-send the body when the first of two same-frame submits fails」 |
| N-2 | 対応済み | `ENQUEUE_JOB_KIND` が `payload: {}` を挟んだ `kind:` を読み、`EVENT_TYPE_LITERAL` が `"index.md"` を拾わない。自己テスト 2 件（`rosterGrep.test.ts`） |
| T-1 | 対応済み | `editMemo.integration.test.ts` (g): `BEFORE UPDATE` トリガーで CAS を 0 行にし、request 側に `ConflictError("OPTIMISTIC_LOCK_FAILURE")`（`toSerialized().kind === "conflict"`）が届き、rev / projection が巻き戻る |

### Manager の判断が必要な事項

1. **B-1 の扱い**（R-TL-03）: ジャンプ後に指定日の位置へスクロールしない。修正案は §4 B-1。データ側（usecase / repository / integration テスト）は spec どおりで、直すのは `TimelineBoard` の presentation だけ。`?memo=` と同じ `scrollIntoView` を pivot に掛ければ済むが、**pivot をクライアントが知る手段が要る**（`TimelineWindowView` は pivot を返さない）。(i) `TimelineWindowView` に `pivotId` を足す（spec の `findTimelineAround` 戻り値にも無いので spec 改訂を伴う）か、(ii) クライアントが `initial.items` から同じ規則で再計算する（その日にメモがあればその日の最新、無ければ日の終端 / 開始からの距離で近い方）か。推奨は (i)（規則の二重実装を避ける）
2. A-2（絞り込み中に編集して一致しなくなった行が残る）を条件に足すか: 触れない（§5）。追跡は任意
3. A-1（design HTML の「現在の内容」欄）: P-05 の機能列挙に無く、判断は PH-10 送りで可（§5）

## 1. 自動テスト（自分で実行）

- 2026-09-08T11:13:40〜11:13:50+09:00、HEAD `f43ca7f`、作業木クリーン（`.claude/` `.codex/` `.spec-implement/` は untracked のまま）
- `pnpm typecheck` 0 件、`pnpm lint` 0 件、`pnpm format:check` 差分なし
- `pnpm test`: unit+dom **58 files / 1,144 tests**、integration **11 files / 48 tests** — 実装報告と一致
- 完了条件を固定しているテスト（名前で確認）:
  - `timelineWindow.integration.test.ts`: (a) `%` / `_` / `\` を文字として照合・ASCII 大小無視・NFKC なし、(b) 該当日の最新を pivot に両方向継続で重複欠落なし、(c) 空の日は近い方・同距離は過去、(d) 未来が厳密に近い場合、(e) 範囲外の過去 / 未来、(f) 0 件・keyword 0 件、(g) keyword 継続、(h) `dayEnd <= date` / Invalid Date / limit 境界、(i)(j) showMemoInTimeline の found / trashed / notFound
  - `editMemo.integration.test.ts`: (a) saved、(b) unchanged と末尾空白、(c) conflict は書かない + 再適用、(d) 別セッションの自分が actor、(e) 10,000 / 10,001 / 空、(f) NotFound（不在 / trashed / 空 ID）、(g) T-1
  - `revisions.integration.test.ts`: 昇順・本文なし・trashed でも可・NotFound、逆順・同一 → Validation・不在、rollback の新リビジョンと履歴不変・unchanged・trashed → NotFound
  - `softDeleteMemo.integration.test.ts`: (a) trashed + `purge_after` + projection 除去 + タイムラインから消える、(b) 二重削除 NotFound、(c) `purge-trash` 行（`pending` / `next_run_at = purge_after` / `payload '{}'`）、早い方へ収束、遅い期限では動かない、`done` からの復帰
  - dom: `timelineBoard.dom.test.tsx`「delete is owned by the board」3 件（キャンセル / 楽観除去 / 失敗で復帰 + 再試行）、「N-1」、絞り込み 0 件 + 解除、日付案内、両 sentinel、`.highlight` + `scrollIntoView`; `memoEntry.dom.test.tsx`「inline edit」6 件（`useOptimistic` 反映、started version で送信、unchanged で閉じる、conflict → `currentVersion` で再送、拒否で保持、取り消し）; `revisionHistory.dom.test.tsx` 8 件; `confirmDialog.dom.test.tsx` 4 件
  - unit: `diff.test.ts` 5 件（`ignoreNewlineAtEof` の意図「trailing newline を空行にしない」を含む）、`time.test.ts` 6 件、`search.test.ts` 8 件、`retentionPolicy.test.ts` 3 件、`memoKeyword.test.ts`、`jumpToDate.test.ts` / `showMemoInTimeline.test.ts` / `revisions.test.ts` / `editMemo.test.ts`

## 2. ブラウザ検証（自分で実行）

`agent-browser --session verify02`、`ph02@example.com`。dev = http://localhost:3000（`ssr: false`）、preview = http://localhost:4173（`pnpm build` 11:27:38〜42、HEAD `f43ca7f`）。sqlite は `apps/web/.wrangler/state/v3/do/…UserDataDurableObject/a36227cb….sqlite`。DOM の観測は `eval` で JSON を返す関数（scratchpad `tl1.js`）。

### dev

| # | 時刻 | 操作 | 期待 | 結果 |
|---|---|---|---|---|
| A-1 | 11:17:06 | 投稿「検証02 50%OFF Snake」（R-TL-01 回帰） | 先頭に出る | 先頭・50 件・見出し 1 つ |
| A-2 | 11:17:28 | `?q=50%25` / `?q=SNAKE` / `?q=snake` / `?q=50_OFF` / `?q=zz存在しないzz` | 1 / 1 / 1 / 0 / 0 件 | そのとおり。`50_OFF` が 0 件 = `_` も文字。0 件は「「zz存在しないzz」に一致するメモは見つかりませんでした」+ 絞り込みを解除ボタン |
| A-3 | 11:18:17 | 解除ボタン → `/` 50 件。バーで `Snake` 送信 → `?q=Snake`、× → `/` | — | そのとおり |
| A-4 | 11:18:17 | `?date=2026-09-03`（9/1 メモ1 と 9/5 メモ4 の間。過去 62h / 未来 63h） | 案内「…にメモはありません。前後で最も近いメモの位置…」、pivot = メモ1 | 案内は正しい。**pivot は network の最初の `newer` カーソルから推定**（`/_serverFn` の payload 内 base64url `{p,i}`）: 連番027 = window の最新 = メモ1 pivot の 25 件新しい側の端 → 過去側 ✓。`?date=2026-09-04`（過去 86h / 未来 39h）→ 連番028 → pivot = メモ4（未来側）✓。`?date=2026-09-05` は「移動しました」。`2020-01-01` / `2030-01-01` は案内 + 最古 / 最新付近。`2026-02-30` は `/` の通常表示（`.catch`）。**ただし viewport**: どの日付でも `scrollY: 0`、可視記事は 9/8 の 4 件（`date-0903.png`）。dev では上端 sentinel が viewport 内なので新しい側が自動で全部読まれ 61 件になる（A-3 の観察どおり） |
| A-5 | 11:18:17 | `?date=2026-09-06&q=買い物` | 絞り込み継続 | メモ1 のみ 1 件 + 案内 |
| A-6 | 11:19:31 | `?memo=<最古 連番001>` / `<メモ1>` / `<trashed メモ3>` / `does-not-exist` | ハイライト + viewport / 案内 + 通常 | highlight あり・top 339 / 272（633 中）・`scrollY` 3274 / 2971・上端に「新しいメモを読み込む」; trashed「指定されたメモはゴミ箱にあります。通常のタイムラインを表示しています」+ 50 件; 不在「指定されたメモは見つかりません。…」+ 50 件 |
| A-7 | 11:22:42 | 検証02 の 編集: 開く → 同じ本文で保存 → 別本文で保存 → 取り消し | unchanged は書かない / saved は rev 2 / 取り消しは戻る | unchanged: 閉じて rev 1 / v0。saved: 送信直後「保存中…」+ 非活性、3 秒後 rev 2 / v1、`memo_revisions` 2 行、`search_entries` 更新、`posted_at` 11:17:06 不変。取り消し: 本文不変 |
| A-8 | 11:22:55〜11:23:10 | 競合: t1 で編集を開いたまま `tab new` の t2 で「割り込み編集 (t2)」保存（rev 3 / v2）→ t1 で「自分の編集 (t1)」保存 | 警告 + そのまま保存で rev 4、rev 3 残存 | `role="alert"`「編集を始めた後にあなたがこのメモを編集しました（2026/09/08 11:23）。」+ 現在の本文「割り込み編集 (t2)」+ ボタン「そのまま保存」、DB は rev 3 のまま（書いていない）。そのまま保存 → rev 4 / v3、rev 1〜4 残存、`search_entries` = 自分の編集 (t1) |
| A-9 | 11:24:03〜11:24:26 | 空白のみ → 保存非活性。履歴 `/memos/<検証02>/history`: rev1 クリック → 「比較元」+ 戻すボタン; rev3 → 「比較先」+「リビジョン 1 → リビジョン 3 の差分」removed / added 各 1 行; 戻す → ダイアログ「この内容に戻しますか？」→ 戻す | `/?memo=` にハイライト、rev 5 = rev 1 | URL `/?memo=<id>`、highlight top 209、rev 5 / v4、本文 = rev 1。最新 rev5 を比較元にして戻す → 「現在の内容は既にこのリビジョンと同じです」・rev 不変。逆順 3→1 の差分も正しい向き。1 件のみ（メモ1）は `DIV` 行・戻すなし。`/memos/nope-id/history` → 「メモが見つかりません」+ `href="/"` |
| A-10 | 11:24:26〜11:26:11 | 二重送信: `requestSubmit()` ×2（同一 tick） | 1 行 | 1 行。10,001 文字で ×2 → `postMemoFn` POST は 1 回（422）、「メモは10,000文字以内で入力してください」、draft 10,001 文字保持、直して送信 → 1 行・draft 空 |
| A-11 | 11:26:11〜11:26:23 | 連番055 削除: メニュー → ダイアログ「メモを削除しますか？」（削除 `.fog-danger` / キャンセル）→ キャンセル → 再度 → 削除 | 楽観除去、sqlite、`jobs` | キャンセル: 残存・`active`・`jobs` 不変。削除: クリック直後に `present: false`（`aria-busy=true`）、3.5 秒後もなし・alert なし・50 件（次が繰り上がる）。sqlite `trashed` / v1 / `purge_after − trashed_at = 30.0 日`、`search_entries` 0 行、`jobs` = `purge-trash` pending `2026-10-08 11:05:01` のまま（遅い期限なので動かない）。reload 後も消えたまま |
| A-12 | 11:27:01〜11:27:15 | dev / preview 停止 → sqlite `trash_retention_days = 1` → `pnpm dev` 再起動 → 連番054 削除 | `next_run_at` が早まる | `purge_after = 2026-09-09 11:27:12`（1.0 日）、`jobs.next_run_at = 2026-09-09 11:27:12` = `min(purge_after)` ✓ |

### preview（SSR ストリーミング経路）

| # | 時刻 | 操作 | 結果 |
|---|---|---|---|
| P-0 | 11:27:38〜11:27:53 | sqlite で保持を 30 に戻す → `pnpm build`（exit 0、rsc / ssr / rsc / client 4 ビルド）→ `pnpm dev` + `pnpm preview` → 4173 でログイン | `/` 50 件。`curl -H Cookie` の SSR HTML 57,155 B: `<article>` 50 がインライン、`<!--$-->` 5、skeleton 記法 0、`$RC` 0（PH-01 検証 02:47:52 と同じ: 応答が速く fallback は emit されない） |
| P-1 | 11:28:39 | `?q=50%25` / `?q=SNAKE` / 0 件 → 解除 | 1 / 1 / 0 → `/` |
| P-2 | 11:28:39 | `?date=2026-09-03` / `04` / `05` | 案内は dev と同じ。preview では新しい側の自動読み込みが起きず 31 / 32 / 32 件（= 25 新しい側 + pivot + 古い側の残り）で、**可視の先頭は 連番027 / 028 / 028（9/8）**、`scrollY: 0` → 件数から pivot = メモ1 / メモ4 / メモ4 と確定（過去側 / 未来側 / 当日）。指定日の見出しは画面外（B-1） |
| P-3 | 11:28:39 | `?date=2026-09-06&q=買い物`、`?memo=<最古>` / `<trashed>` / `nope`、検証02 を「検証02 preview 編集」で保存 | 1 件 + 案内; highlight + 案内 2 種; rev 6 / v5 |
| P-4 | 11:28:39〜11:29:08 | 履歴 6 行 → rev2 / rev6 で「リビジョン 2 → リビジョン 6 の差分」→ rev2 へ戻す → `/?memo=`; 連番053 削除; 二重送信 ×2 | rev 7 = rev 2 本文; trashed 30.0 日・`jobs` は `2026-09-09 11:27:12` のまま; 1 行 |
| P-5 | 11:31:03〜11:31:31 | `/settings` → ナビ「タイムライン」（クライアント遷移、MutationObserver）; `/` → メニュー「履歴」 | T+37ms `.fog-skeleton-*` 4 / article 0 → T+977ms skeleton 0 / article 50。履歴: skeleton 4 / 行 0 → 1,002ms 後 skeleton 0 / `.fog-revision-list li` 8。skeleton → 内容の入れ替わりを確認 |
| P-6 | 11:32:20〜11:32:49 | 戻す（rev7 = 比較元）→ 4 秒待って観測 | `/?memo=<id>`、28 件、highlight top 271 / 633、本文 = rev 7。`errors` なし（console は vite の再接続ログのみ） |
| P-7 | 11:32:20 | sentinel: `scrollIntoView` / `scroll down 6000` 後 3 秒 | **自動読み込みは起きない**（dev でも同じ）。自前で張った `IntersectionObserver` も発火しない（`probe: []`）ので、この headless セッションではスクロール起因の IO が届かない環境要因。「過去のメモを読み込む」クリック → 61 件・見出し 8・重複なし・ボタン消失。dev の `?date=` では初期レイアウトで IO が発火して両方向を読んでいるので配線自体は生きている |

## 3. Implementer の検証結果を利用した範囲

- 利用なし。上表はすべて自分で再実行した。実装報告の B 欄と一致した項目: 絞り込み / 案内文言 / `?memo=` / 競合 / 履歴 / 削除 / `jobs`。**一致しなかった点**: 実装報告は日付ジャンプを案内文言だけで判定しており、viewport の位置（B-1）を見ていない

## 4. コードレビュー

### 不具合

- **B-1（R-TL-03）日付ジャンプ後、指定日の位置へスクロールしない。** 再現: 複数日のデータで `/?date=2026-09-03`（dev / preview とも）→ 案内「…前後で最も近いメモの位置を表示しています」は出るが、viewport は window の最新側（`items[0]`、9/8 のメモ）から始まり、指定日の見出しは 25 件下。期待（`spec/manual-tests/timeline.md` TC-07 手順 1、`spec/pages/index.md` L85「日付指定でその位置へジャンプ」）: 指定日（無ければ最寄りメモ）の見出し位置が見える。原因: `TimelineBoard` の `scrollIntoView` は `initial.target.state === "found"`（`?memo=`）だけに掛かり、date モードには無い（`apps/web/app/components/timeline/TimelineBoard/index.tsx` L281–288）。副作用: dev では上端 sentinel が viewport に入って新しい側が自動で全部読まれる（A-3 は本件の症状）。修正方針は冒頭「Manager の判断」1

### 指摘（不具合ではない）

- (a) 同期契約 / UoW: 7 procedure に `await` なし、`run` のネストなし（`durableObjectBase.ts` の 2 か所以外に `.run(` 無し）。`softDeleteMemoProcedure` は `save` → `trashQueryPort.findEarliestPurgeAfter()` → `enqueueJob` を同じ callback で呼ぶ（`softDeleteMemo.ts` L50–70）。facade は `clock.now()` を `runUnitOfWork` の外で取り、`envelope` で包む（`userDataDurableObject.ts`）
- (b) レイヤー境界: 日付の TZ 解釈は `apps/web/app/presentation/time.ts`（`dayRange`、固定 `+09:00`、`Asia/Tokyo` は DST なしの前提を JSDoc で宣言）に閉じ、`JumpToDateDto` は `Date` 2 点を渡す。`RetentionPolicy` は `packages/core/src/domain/trash/retentionPolicy.ts` に純関数（identity の `TrashRetentionDays` を受ける。spec/domains/trash.md「ドメインサービス」どおり）。`TrashQueryPort` は `findEarliestPurgeAfter` のみ宣言（J-A）で、実装は 3 表 `UNION ALL` の `min(purge_after)`。domain は I/O に触れない
- (c) spec 突き合わせ: `findTimelineAround` の date anchor は `{ from, toExclusive }`。`resolvePivot`（`memoRepository.ts` L220–262）は「`toExclusive` 以前で最新」を引き、それが `from` 以上ならその日の最新 = pivot、未満なら `posted_at >= toExclusive` で最古を引く（`before` が `from` 未満なら `[from, toExclusive)` は空なので `>= from` と等価）。距離は `toExclusive − before` と `after − from`、`futureDistance < pastDistance` のときだけ未来 = 同距離は過去。spec/domains/memo.md L334 と一致。`editMemo`: version 不一致で書かずに `conflict`（`latestRevision` が無ければ `SystemError(DataIntegrityError)`）、`Memo.edit` が `newRevision: null` なら `unchanged`、`save` + `insertRevision` で `saved`。`expectedVersion` は transport の zod `int().min(0)` のみ（J-F）。`rollbackMemo` は `expectedVersion` を受けず `found.expectedVersion` で `save`、同内容は `unchanged`。`listMemoRevisions` / `diffMemoRevisions` は `findByIdIncludingTrashed` / `findRevision`（trashed でも読める）、同一番号は usecase の `ValidationError("SAME_REVISION")`（J-G。transport は形だけ）。`softDeleteMemo` は `findById`（active のみ）→ 二重削除は `NotFoundError("MEMO_NOT_FOUND")`。`enqueueJob({ operationKey: "purge-trash", kind: "purge-trash", payload: {}, nextRunAt: earliest })` で `jobWriter` の規則 (1)(3) に任せる（早める方向のみ・`done` から復帰）。`purge-trash` の handler は未登録（J-B）
- (d) 所有規則: 編集は `MemoEntry` が `editMemoFn` + `useOptimistic(memo.body)` + 競合パネルを所有し、`onSaved` で owner に報告 → `router.invalidate()`。削除は `TimelineBoard` が `ConfirmDialog` → `useOptimistic` の `remove` → `softDeleteMemoFn` → `removed` セット + `loaded` から除去 → `router.invalidate()`、失敗は transition の revert で復帰 + `role="alert"` + 再試行。`MemoEntry.onDelete` は上げるだけ。`mergeTimeline` は `version` の大きい方で re-base
- (e) `validateSearch`: `q` / `date` / `memo` すべて `.optional().catch(undefined)`、`memo` 優先・`q` は `date` と両立（`timelineModeOf`）。`2026-02-30` / 型違いは通常表示（`search.test.ts`、ブラウザ A-4）
- (f) `presentation/diff.ts`: jsdiff `diffLines` + `ignoreNewlineAtEof: true`、chunk を `\n` で割って末尾の空要素を落とす行単位 unified（removed → added → context）。`ignoreNewlineAtEof` は「末尾に行を足すと直前行が −/+ 対になる」誤差分を防ぐ妥当な選択。限界は A-6
- (g) ban list / lint: 新しいエラークラスなし（`git diff` に `class … extends` の追加なし）、`banList.test.ts` / `pluginWiring.test.ts` を含む unit が緑、`pnpm lint` 0 件。`DiffView` の `noArrayIndexKey` は `biome-ignore` に理由付き
- (h) 細部: `rollbackMemoFn` は `Actor.user(UserId.create(userId))` を手書きしており `timeline/actions.ts` の `userActorOf` と重複（動作に影響なし）。`MemoHistoryFeed` の `try / catch` は `guardStreamedRender` の後で `notFound` だけを画面状態に変え、それ以外は再スロー — presentation の境界として許容範囲

## 5. 追加観察の判定と自分の追加観察

- A-1（「現在の内容」欄なし）: 妥当。P-05 の機能列挙（`spec/pages/index.md`）に無く、条件外。design HTML との差なので PH-10 で spec 側を揃えるか決める
- **A-2（絞り込み中に編集して一致しなくなった行が残る）: R-TL-03 / R-TL-04 の条件には触れない。** R-TL-03 は「絞り込みと解除ができ、0 件表示 + 解除導線」、R-TL-04 は「保存で新リビジョン / 空不可 / 変更なし / 警告」で、編集後の絞り込み一貫性はどちらの文言にも無い。原因は `onSaved` が `loaded` に re-base した行を `mergeTimeline` が `initial` に無くても保持すること。再読み込みで消えるので実害は小さい。条件に足すなら R-TL-03 に「絞り込み中の編集で一致しなくなった行は再取得後に消える」を追加する形になるが、推奨は追跡のみ（issue）
- A-3（`?date=` で上端 sentinel が自動で読む）: B-1 の症状。B-1 を直して pivot にスクロールすれば `?memo=` と同様に sentinel が viewport を外れる
- A-4（`<search>` 要素）: 妥当。React 19 / jsdom で描画され dom テストも通る
- A-5（dev は `ssr: false`）: 妥当。preview で P-5 / P-6 を確認済み
- **A-6（新規）**: `ignoreNewlineAtEof` のため、末尾改行の有無だけが違う 2 リビジョン（`"a"` と `"a\n"`。`Memo.edit` は別本文として rev を積む）の差分は空になる。表示の限界であり条件外。気になるなら差分が空のとき「改行のみの違いです」等の案内を出す
- **A-7（新規、環境）**: headless の agent-browser ではスクロール起因の `IntersectionObserver` が発火しない（自前の観測 IO も同様。P-7）。無限スクロールの自動読み込みはブラウザで検証できず、dom テスト + ボタン経路で代替した。次フェーズ以降も同じ

## 6. PH-01 受け入れ済み 8 項目への影響

- R-TL-01（投稿の楽観 UI / 空不可 / 失敗時保持 + リトライ）: **影響なし**。`TimelineBoard` の composer は plain list では `router.invalidate()`（従来どおり）、絞り込み / ジャンプ中は `goto({})` で `/` へ戻す追加のみ。N-1 の実装が `draftRef` から `inFlight` + `onSubmit` の `preventDefault` に変わったが、A-1（先頭に出る）、A-10（10,001 文字で 422 → 文言 + draft 保持 → 再送で成功、同一フレーム 2 回で 1 行）で条件を再確認。dom「shows the optimistic entry…」「keeps the draft and shows the message when the post fails」「posts once when the form is submitted twice in the same frame」は残っている
- R-TL-02（新しい順 + 日付見出し + 無限スクロール + 空案内）: **影響なし**。`getTimeline.ts` は `normalizeLimit` / `normalizeKeyword` / `toTimelineItemView` への分割のみで挙動同一（`timeline.integration.test.ts` は据え置きで緑）。`memoRepository.ts` の `instr` → `LIKE` は `keyword !== null` の枝だけ。`findTimelineAround` の limit 分割修正は `findTimelinePage` に触れない。ブラウザ: 8 見出しが新しい順（A-4）、ボタンで 50 → 61 件・重複なし（P-7）、`MemoEntry` の Markdown / ISO `datetime` は dom テスト継続
- R-INF-01 / R-INF-02 / R-AC-01 / R-AC-03 / R-AC-04 / R-AC-08: `appShell.dom.test.tsx` の変更は `/memos/:id/history` をナビ項目にしない判定の追加のみ、identity 側の変更なし（`git diff --stat` に identity / signup / login のファイル無し）。`__root.tsx` は `memoHistory/actions` の side-effect import 1 行。ログイン（11:16:13 dev / 11:27:53 preview）と `/settings` 表示（P-5）を経由して動作に変化なし

## 7. 検証環境の引き継ぎ

- 起動中: `pnpm dev` PID 88836（http://localhost:3000、ログ scratchpad `dev.log`）、`pnpm preview` PID 88734（http://localhost:4173、ビルド 11:27:42、HEAD `f43ca7f`）。Implementer の 81333 / 82466 は停止した。どちらも止めて再起動して構わない
- DO 状態（`a36227cb…`）: `user_settings.trash_retention_days = 30`（sqlite で 1 → 30 に戻してから両サーバーを再起動済み。キャッシュ不整合なし）。memos 66 行、うち trashed 5（メモ2 / メモ3 / 連番055 / 連番054 / 連番053）。**連番054 の `purge_after` は 2026-09-09 11:27:12 JST で `jobs.purge-trash` の `next_run_at` もそこ** — handler 未登録なので明日その時刻に dev / preview が起動していれば「No handler registered」→ backoff → `poison`（J-B の想定どおり。PH-05 の検証で `requeue` の材料になる）。追加したメモ: 「検証02 …」（rev 8、9/8 11:17 投稿）、「二重送信 verify02 A / B (リトライ)」「二重送信 preview C」
- agent-browser: session `verify02`（4173 と 3000 の両方でログイン済み、タブ t1 / t2）。今回の作法: `eval` は単独呼び出し、`const` を使うときは IIFE で包む（同じページで 2 回 `const f` を宣言すると SyntaxError で黙って何も起きる）、メニューのクリックと DOM 読みは別の `eval` に分ける（React の再描画は eval の戻り後）、React 制御の textarea へは native setter + `input` イベント、batch のセレクタは空白なし（`form.fog-inline-edit>textarea`）、preview の `/_serverFn` は本番でも `{file, export}` の base64url ID で payload は seroval 形式（カーソルは正規表現で拾う）
- scratchpad: `checks.log`（自動テスト全文）、`build.log`、`preview-ssr.html`、`date-0903.png`（B-1 の証跡）、`ab.sh` / `tl1.js` / `cursors.py`

## 再検証（2026-09-08T11:46〜11:51+09:00、HEAD `9c90d1b`）

対象: `57e6e54`（spec: `findTimelineAround` の戻り値と jumpToDate 出力に `pivotId`）、`1b34078` / `9c90d1b`（Implementer の B-1 / A-6 対応）。実装コード・spec・管理ファイルは変更していない。

### 判定

| ID | 判定 | 根拠 |
|---|---|---|
| R-TL-03 | **合格** | B-1 解消。dev / preview とも、`?date=` 直後（3.5 秒後に観測）に指定日または pivot の見出しが viewport 先頭にある（下表）。A-3 解消（上端 sentinel が viewport を外れ、自動読み込みなし: 32 / 31 / 32 / 26 / 25 件のまま。以前は 61 件）。絞り込み側は前回のまま。T: `timelineWindow.integration.test.ts` の `pivotId` 4 か所（(b) 該当日の最新 = pivot で「window の最新行ではない」を明示、(c) 同距離は過去、(d) 未来が近い、(i) showMemoInTimeline found）、dom `timelineBoard.dom.test.tsx` 4 件（日グループ先頭なら `section.fog-day` を `scrollIntoView({ block: "start" })`、先頭でなければ行自身、一覧先頭なら呼ばない、plain / 絞り込みでは呼ばない） |
| R-TL-04 / 05 / 06 / 08 | 合格（影響なし） | 新ビルドで再実行: 編集 2 回（rev 10 / 11）、履歴の二点差分（1→8 は removed / added）、rev1 へ戻す → `/?memo=` にハイライト top 271（rev 12 = rev 1）、連番052 削除 → 即時消失 → `trashed` 30.0 日 → `jobs.next_run_at` 不変（2026-09-09 11:27:12）、`?memo=<最古>` highlight top 339 / trashed は案内 + 通常 50 件（数値は前回と同一） |
| A-6 | 対応済み | rev 10 `'改行テスト'` と rev 11 `'改行テスト\n'` の差分: `context:改行テスト` + `note:末尾の改行の有無だけが異なります`（mark 空）。通常差分（1→8）に note なし。unit「says so when only the final newline differs」 |
| PH-01 の 8 項目 | 影響なし | 変更は `TimelineBoard` の `useEffect` 1 つ（`mode.kind === "date"` かつ `pivotId` 非 null のときだけ動く）と `TimelineWindowView` / `TimelineWindow` への `pivotId` 追加（`getTimeline` / `findTimelinePage` は触れない。`loadTimelineWindow` の list / フォールバックは `pivotId: null`）。新ビルドで投稿（先頭に出る、`requestSubmit` ×2 で 1 行）、「過去のメモを読み込む」で 50 → 61 件・id 重複なし・見出し 8。identity 側の diff なし |

### 自動テスト（11:46:44〜、自分で実行）

`pnpm typecheck` 0 件、`pnpm lint` 0 件、`pnpm format:check` 差分なし、`pnpm test`: unit+dom **58 files / 1,149**、integration **11 files / 48** — 報告と一致。

### ブラウザ（自分で実行。`eval` で viewport 内の `.fog-day h2` の top・先頭に見える記事・案内の top を返す）

dev = http://localhost:3000（Implementer の PID 93635 をそのまま使用）、preview = http://localhost:4173（**自分で `pnpm build`（11:48:24、exit 0）→ `pnpm preview` PID 97550 に立て直し**。Implementer の 94241 は停止）。

| URL | 期待 | dev（11:47:31〜11:48:04） | preview（11:49:09〜11:49:39） |
|---|---|---|---|
| `?date=2026-09-05`（該当日あり、pivot = メモ4） | 9/5 見出しが先頭 | scrollY 3194 / 32 件 / **9月5日@0**, 9月1日@187, … / 先頭記事 preview 編集後@38 | 同一 |
| `?date=2026-09-03`（無し、過去側 メモ1） | 9/1 見出し | 3268 / 31 / **9月1日@0** / メモ1 買い物リスト@38 | 同一 |
| `?date=2026-09-04`（無し、未来側 メモ4） | 9/5 見出し | 3194 / 32 / **9月5日@0** / preview 編集後@38 | 同一 |
| `?date=2020-01-01`（範囲外・過去、pivot = 連番001 = 一覧末尾） | 8/1 が viewport 内 | 3337（末尾）/ 26 / 8月2日@114, **8月1日@301** | 同一 |
| `?date=2030-01-01`（範囲外・未来、pivot = 一覧先頭） | スクロールせず案内が見える | 0 / 25 / 9月8日@234 / 案内 top 171 | 同一 |
| `?date=2026-09-06&q=買い物`（絞り込み継続、1 件） | 案内 + 9/1 | 0 / 1 / 9月1日@307 / 案内 top 244 | 同一 |
| `?date=2026-08-03`（該当日あり、途中の日） | 8/3 見出し | 3490 / 28 / **8月3日@0** / 連番003@38 | — |
| `?memo=<最古 連番001>` | 従来どおり | highlight top 339、scrollY 3274（前回と同一） | 同一 |
| `?memo=<メモ1>` / `<trashed メモ3>` | 従来どおり | highlight top 272 / 案内 + 50 件 | trashed 同一 |

### 観察

- `9c90d1b` のガード（pivot が一覧先頭なら scroll しない）は範囲外・未来と絞り込み 1 件で案内が見える状態を保つ。範囲外・過去は pivot が末尾なのでページ末尾で止まり、8/1 の見出しは viewport 内（top 301）だが先頭ではない — 期待どおり
- `document.querySelector("article")` を「一覧先頭」の判定に使っている。`apps/web/app` で `<article>` を描くのは `MemoEntry` だけなので現状は成立する。別の `<article>` をレイアウトに足すと判定がずれるので、足すときは `section.fog-timeline article` に狭める
- 前回の A-7（headless で IO のスクロール発火が観測できない）は変わらず。A-3 の解消は「自動読み込みが起きない」ことと `newerBtn: true`（上端ボタンが残る）で確認した

### 環境の引き継ぎ

- 起動中: dev PID 93635（Implementer 起動のまま、http://localhost:3000）、preview PID 97550（自分で `9c90d1b` を再ビルド、http://localhost:4173、ログ scratchpad `preview2.log` / `build2.log`）
- DO 状態: 検証02（`01a07ece-…`）は rev 12（rev 10 / 11 は改行のみ違い、rev 12 = rev 1 本文）、連番052 が trashed（計 6 件）、「再検証 投稿 D」を追加。`jobs.purge-trash` は 2026-09-09 11:27:12 JST のまま。`trash_retention_days` 30
- agent-browser: session `verify02`（3000 / 4173 でログイン中）。Implementer の `ph02` はそのまま
- 11:50:28 に `packages/core/src/adapters/cloudflare/stores/{occ,searchProjection}.ts` に未コミットの追加（`isUniqueViolation` / `projectDocument` 等、PH-03 の作業と見える）が現れた。自分の変更ではない。自動テスト（11:46:44、作業木クリーン）と preview ビルド（11:48:24）より後なので本再検証の結果には影響しない
