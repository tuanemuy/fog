# PH-05 検証報告 — ゴミ箱と保持期限

- Verifier 実施: 2026-09-08T17:02〜17:16+09:00。対象 HEAD `232bebe`（`3d34394` core / `b30e583` `75810f3` web / `232bebe` テスト）、ブランチ `feat/spec-implement`。tracked ファイルに未コミット変更なし
- 実装コード・`spec/`・管理ファイルは変更していない。書き込みは本ファイルのみ

## 判定

| ID | 判定 | 根拠 |
|---|---|---|
| R-TR-01 | **合格** | B（dev 17:05〜17:08、preview 17:14）: `/trash` = ナビ「ゴミ箱」が current、`<h1>` ゴミ箱。空は「ゴミ箱は空です」+「空にする（0）」`disabled`。5 件の一覧 = 種別バッジ（トピック / ドキュメント / メモ）・表示名・「残り30日」・「YYYY/MM/DD HH:MM に削除」、新しい順。セット削除の「ゴミ箱T5」は「ドキュメント1件・残り30日」+ 配下「セットD5a（トピックとセットで削除）」のインデント、個別削除の「セットD5b」は単独行（§2 A-1, A-2）。T: `trash.integration.test.ts` (a)（3 種別の降順 / 保存値の期限 / `deletedWithTopic` と `setDocumentIds` に個別削除分を含まない / ページング / 範囲外）、dom `trashBoard` 12 件（空 + 非活性、種別と残り日数、入れ子と親不在のメタ、もっと読む）、`appShell.dom`（5 項目 + タイトル） |
| R-TR-02 | **合格** | B（17:06〜17:09）: メモ復元 → 行が消え「メモを復元しました / タイムラインで見る」→ `/?memo=` ハイライト、sqlite `active` / `purge_after NULL` / `search_entries` 1 行、検索 `trashmemo1` 1 件（A-3）。ドキュメント単独復元（D3 → 検索テストA）→ `active` / `topic_id` = A、A の version 5 → 6（touch）、P-07 の一覧に出る（A-4）。セット配下 D5a の復元 → 「トピック「ゴミ箱T5」とセットで復元しますか？」→ キャンセルで何も変わらず → セットで復元 → T5 `active` + D5a `active`、個別削除の D5b は残る（A-5）。トピック T5 の復元 → 配下ごと `active`、D5b は残る（A-6）。ADR-001: T6 / T7 をハードデリート後、D6 の復元 → 「「選択D6」の復元先を選んでください」（既存 = Live 6 件、完了済みは「（完了）」、または新規）→ 検索テストA を選択 → `active` / `topic_id` = A / `trashed_with NULL`、A の version 6 → 7（touch）。D7 → 新規（空白名は送信されない → 「新規復元先T8」）→ 新トピック `active` 配下に復元、P-08 のトピックリンク = 新規復元先T8（A-7）。T: (b)〜(g)（元の位置と検索・`sourceMemoIds` に戻る / 単独 + touch・archived でも / 確認要求で書き込みなし → セット・当のドキュメントは個別削除でも戻る / 他の個別削除分は残る / 不在で選択要求・既存 / 新規 / セット復元の `wasArchived` / 並行 `trashTopic` との OCC）、dom 5 件 |
| R-TR-03 | **合格** | B（17:10〜17:11）: 出典になっている trashed メモの完全に削除 → 「完全に削除しますか？「…」は履歴ごと消え、元に戻せません。」→ キャンセルで残る → 確認で消え、sqlite `memos` / `memo_revisions` / `source_links` / `search_entries` すべて 0 行、引用ドキュメントの P-08 に「元になったメモ」領域なし（ADR-003）、検索 `hardmemo9` 0 件、`/memos/<id>/history` は「メモが見つかりません」（A-8）。トピックのセット削除の確認 = 「「ゴミ箱T5」と、セットで削除されたドキュメント1件が対象です。履歴ごと消え、元に戻せません。」→ トピック行 0、配下 D5a 消去、個別削除の D5b は残る（A-9）。空にする → 「ゴミ箱の2件をすべて完全に削除しますか？」→ キャンセルで 2 件のまま → すべて削除 → 「ゴミ箱を空にしました（2件）」+ 空状態 + 非活性、sqlite: trashed 0 / 孤児リビジョン 0 / 孤児リンク 0 / 本体の無い `search_entries` 0（A-10）。T: (h)（メモ: リビジョン・リンク消去、相手の `sourceMemoIds` から消える、検索・履歴に出ない、live / 不在は `TRASH_ITEM_NOT_FOUND`）、(i)（セットのみ、個別削除分は残る）、(j)（項目数で数える、空で 0）、(k)（`RAISE(IGNORE)` で 1 件失敗 → `failedCount 1`、残りは消える）、dom 3 件 |
| R-TR-04 | **合格** | B（17:13:28〜17:13:56）: dev 停止 → sqlite で trashed メモの `trashed_at` を 40 日前・`purge_after` を 30 日後（= 10 日前）に → dev 再起動 → メモ 1 件の削除で投入 → **3 秒以内に期限切れメモが消え**（本体 / リビジョン / entry 0）、`jobs` は `pending` / attempt 0 / `terminal_reason NULL` / `next_run_at` = 残る 1 件の `purge_after`（10-08 17:13:51）に再武装（A-11）。preview でその 1 件を復元後は `pending` のまま（次の起床で `done`。設計 §1.4 どおり）。T: (l)（`runDurableObjectAlarm` で期限切れを消し次の期限に再武装 → 空で `done` → 次のソフトデリートで `pending`）、(m)（`purge_after == now` は対象外・1ms 過去は対象・期限内には触れない・トピックはセットごと）、(n)（10×2 で打ち切り `hasMore`）、(o)（再計算残件があるうちは削除に進まず `yield`）、unit `usecases.test.ts`（再計算優先 / 予算 / 先送り） |
| R-ST-01 | **合格** | B（17:11:44〜17:12:34、preview 17:15）: P-13 の欄は `min=1 step=1`、`max` なし。0 / -1 / 1.5 は `reportValidity()` が false で送信されず値は保持（A-12）。`noValidate` で 1.5 を送ると「1 以上の整数を入力してください」、sqlite 30 のまま。7 で保存 → 送信直後に楽観「現在: 7 日」→ 「保存しました。既存のゴミ箱の項目にも適用されます」、sqlite `user_settings` 7、既存 trashed の `purge_after = trashed_at + 7 日`、`/trash` の行が「残り7日」。1（下限）→ 1.0 日・`jobs.next_run_at` が 09-09 17:11:56 に早まる。30 に戻す → 30.0 日、`next_run_at` は 09-09 のまま（規則 (1)。空振り後に張り直る）、`/trash` は「残り30日」（A-13）。T: (p)（3 表の再計算 / `next_run_at` = 新しい min / 延長は規則 (1) / 0・1.5 は `INVALID_TRASH_RETENTION_DAYS`）、(q)（空のゴミ箱では投入しない）、dom `retentionForm` 3 件、`schema.test.ts` |

### Manager の判断が必要な事項

- なし。A-1〜A-6 は §5 のとおり妥当。細部の指摘（§4 (h)）は不具合ではない

## 1. 自動テスト（自分で実行、17:02:42〜17:03:39）

- `pnpm typecheck` 0 件、`pnpm lint` 0 件、`pnpm format:check` 差分なし
- `pnpm test`: unit+dom **85 files / 1,288**、integration **17 files / 106** — 報告と一致。`pnpm test:unit` 追加 3 回（17:03:11 / 17:03:21 / 17:03:30）すべて 85 / 1,288 緑
- `trash.integration.test.ts` 18 ケースと完了条件の対応: R-TR-01 ← (a)。R-TR-02 ← (b)(c)(d)(d2)(e)(f)(g)。R-TR-03 ← (h)(i)(j)(k)。R-TR-04 ← (l)(m)(n)(o)。R-ST-01 ← (p)(q)。unit `usecases.test.ts`（ページング / 予算の検証、`collectTargets` の和集合と順序、`pruneExpiredTrashItems` の 3 ケース）、dom `trashBoard` 12 / `retentionForm` 3 / `currentUserPanel` / `appShell`

## 2. ブラウザ検証（自分で実行）

`agent-browser --session verify05`、`ph02@example.com`。dev = http://localhost:3000（Implementer の PID 3447 → 期限テストで自分が再起動、PID 15947）、preview = http://localhost:4173（**`lsof -t -i :4173 | xargs kill` → `pnpm build`（17:04:20、exit 0）→ `pnpm preview`**。dev 再起動時の `pkill -f "workerd serve"` が preview の workerd も止めたので 17:14:53 に `pnpm preview` を再起動、PID 16469）。sqlite は `a36227cb….sqlite`。DOM は `eval` で JSON を返す関数（scratchpad `tr1.js`）。

### dev（17:05:36〜17:13:56）

| # | TC | 操作 | 結果 |
|---|---|---|---|
| A-1 | 14 / 20 | ログイン → `/trash` | 空状態「ゴミ箱は空です」、「空にする（0）」非活性、ナビ「ゴミ箱」current、`<h1>` ゴミ箱 |
| A-2 | 01 / 02 | メモ「ゴミ箱検証メモ」削除、D3 単独削除、トピック T5（D5a・D5b、D5b は先に単独削除）のセット削除 → `/trash` | 「空にする（5）」。行 = トピック|ゴミ箱T5|ドキュメント1件・残り30日|17:06|配下 セットD5a（トピックとセットで削除）/ ドキュメント|セットD5b|残り30日 / ドキュメント|復元先選択用D3 / メモ|ゴミ箱検証メモ trashmemo1。sqlite: D5a `trashed_with = T5`、D5b・D3 `NULL` |
| A-3 | 03 / 09 | メモを復元 | 行が消え「空にする（4）」、通知「メモを復元しました タイムラインで見る」→ `/?memo=` ハイライト。sqlite `active` / `purge_after NULL` / entry 1 行。`/search?q=trashmemo1` 1 件 |
| A-4 | 04 | D3 を復元（所属 検索テストA は存命） | 「「復元先選択用D3」を復元しました」、sqlite `active` / `topic_id` = A、A の version 5 → 6、P-07 の一覧に D3 |
| A-5 | 05 / 17 | 配下 D5a を復元 → キャンセル → 再度 → セットで復元 | ダイアログ「トピック「ゴミ箱T5」とセットで復元しますか？」+「所属トピックもゴミ箱にあります。…」。キャンセル: T5 `trashed` のまま。確認: 「空にする（1）」、T5 `active`、D5a `active` / `trashed_with NULL`、D5b `trashed` のまま |
| A-6 | 08 | T5 を再度セット削除 → トピック行の復元 | 「トピック「ゴミ箱T5」を配下ごと復元しました」、T5 / D5a `active`、D5b 残る |
| A-7 | 06 / 07 / 18 / 11 | T6（D6 単独削除後にセット削除）/ T7 同様 → T6・T7 を完全に削除 → D6 復元 → 既存 検索テストA / D7 復元 → 新規 | T6・T7 の行 0、D6・D7 は `trashed` のまま。D6: ダイアログ「「選択D6」の復元先を選んでください」、`select` = ゴミ箱T5 / プレビュー確認 / 検索テストA / 検索テストC（完了）/ 検証03 二重送信 / 読書メモ、radio existing* / new → A を選択 → 「「選択D6」を復元しました」、sqlite `active` / `topic_id` = A / `trashed_with NULL`、A の version 6 → 7。D7: 新規 + 空白名 → 送信されない（`required`）→ 「新規復元先T8」→ 復元、`topics` に T8 `active`、P-08 のトピックリンク「新規復元先T8」 |
| A-8 | 10 / 15 | 出典メモ「ゴミ箱出典メモ hardmemo9」（D9 の出典）を削除 → 完全に削除 → キャンセル → 確認 | 「完全に削除しますか？「…」は履歴ごと消え、元に戻せません。」。キャンセルで残る（行 1）。確認: 通知「…を完全に削除しました」、sqlite 4 表とも 0 行、P-08（D9）に `.fog-origin` なし、`/search?q=hardmemo9` 0 件、`/memos/<id>/history` → 「メモが見つかりません」 |
| A-9 | 11 | T5 を再度セット削除 → トピック行の完全に削除 | 「「ゴミ箱T5」と、セットで削除されたドキュメント1件が対象です。履歴ごと消え、元に戻せません。」→ `topics` 0、D5a 消去（リビジョンは D5b の 1 件のみ）、D5b `trashed` のまま |
| A-10 | 12 / 16 | 2 件で「空にする」→ キャンセル → 確認 | 「ゴミ箱の2件をすべて完全に削除しますか？」「すべての項目が履歴ごと消え、元に戻せません。」。キャンセルで 2 件。確認: 「ゴミ箱を空にしました（2件）」+ 空状態 + 非活性。sqlite: trashed 0 / 孤児 `memo_revisions` 0 / 孤児 `document_revisions` 0 / 孤児 `source_links` 0 / 本体の無い `search_entries` 0。`jobs` は `pending` 09-15 のまま |
| A-11 | 13 / 23 / 24 | dev 停止 → メモ retA の `trashed_at` = 07-30、`purge_after` = 08-29 → 再起動 → メモ armB を削除（投入） | 投入直後 `next_run_at` = 10-08 17:13:51（armB）。**3 秒後に retA の行 0**、リビジョン 0 / entry 0、`jobs` `pending` / attempt 0 / `terminal_reason NULL` / `completed_at NULL` / `next_run_at` 10-08 17:13:51。`/trash` は armB 1 件。dev ログの error 1 行は Vite の client-entry HMR（再起動中のブラウザ側）で DO 側ではない |
| A-12 | 25 / settings 08〜10 | P-13: 0 / -1 / 1.5 | `input` は `min=1 step=1`、`max` なし。`reportValidity()` false（「値は 1 以上にする必要があります。」/「有効な値を入力してください。…」）、送信されず、sqlite 30。`noValidate` で 1.5 → `role=alert`「1 以上の整数を入力してください」、sqlite 30 |
| A-13 | settings 01 / 02 / 07、trash 24 | 7 → 1 → 30 | 7: 送信直後に楽観「現在: 7 日」→ 「保存しました。既存のゴミ箱の項目にも適用されます」、sqlite 7、retA の `purge_after − trashed_at = 7.0 日`、`/trash`「残り7日」、`jobs` 09-15 のまま（早くならないので規則 (1)）。1: 1.0 日、`next_run_at` 09-09 17:11:56 に早まる。30: 30.0 日、`next_run_at` 09-09 のまま、`/trash`「残り30日」 |

### preview（17:14:53〜17:15:17）

| # | 操作 | 結果 |
|---|---|---|
| P-1 | `/settings` → ナビ「ゴミ箱」（MutationObserver） | T+47ms `[class*=skeleton]` 4 / 行 0 → T+348ms skeleton 0 / `.fog-trash-row` 1（**skeleton → 内容**）。行 = メモ|期限起床トリガー armB|残り30日 |
| P-2 | 復元 | 空状態 + 「空にする（0）」非活性 + 「メモを復元しました タイムラインで見る」、sqlite `active` / `purge_after NULL`、`jobs` `pending` 10-08（ゴミ箱が空になったので次の起床で `done`） |
| P-3 | P-13 30 → 7 → 30 | 「保存しました…」「現在: 7 日」、sqlite 7 → 30。`errors` なし |
| P-4 | ナビ | 「タイムライン / トピック / 検索 / ゴミ箱 / 設定」の 5 項目 |

## 3. Implementer の検証結果を利用した範囲

- 利用なし。すべて自分で再実行し、報告の B 欄と一致した。期限切れの再現は報告の手順（`trashed_at` と `purge_after` の両方を過去へ）で成立した

## 4. コードレビュー

### 指摘（不具合ではない）

- (a) `run` の入れ子なし: `emptyTrash` facade は `envelope(async () => { await enterRpc(); provider = createUnitOfWorkProvider(); emptyTrashInDurableObject(fn => provider.run(fn)); if (takeRearmRequest()) rearm })` で `runUnitOfWork` と同じ並びを自前で書き、項目ごとに `provider.run` を逐次呼ぶ。`purge-trash` handler は `jobRunner.ts:173` の `await handler(...)` で claim の `transactionSync` の外から呼ばれ、中で `provider.run` を項目ごとに逐次。`grep "\.run("` は `durableObjectBase.ts` の 2 か所 + これらのみ。ハードデリートは `executeHardDeletePlan` が 1 tx（documents → memos → topics。`documentRepository.delete` の cascade = リビジョン / documentId 側リンク / `search_fts` delete コマンド + `search_entries` / 出典メモ再射影、`memoRepository.hardDelete` + `deleteSourceLinksByMemo`（影響ドキュメント再射影））で、`emptyTrash` / `prune` は項目ごとに `run` するので失敗は項目単位で巻き戻る（(k) と `deferred`）。`alarm()` は `durableObjectBase.ts` の既存 catch（L392〜473）の中で `runJobsPass` を呼ぶ
- (b) `pruneExpiredTrashItems`: 再計算フェーズが `chunksLeft` を共有して先行し、`pending || chunksLeft === 0` なら削除に進まず `hasMore: true` → handler が `yield`（`nextRunAt: now`）。削除は `listItemsToPurge(now, chunkLimit)`（`purge_after < ?` 厳密、`ORDER BY purge_after ASC`）、`lastChunkFull && chunksLeft === 0` で `hasMore`。空なら `finished` → `done`、`JOB_KIND_POLICY["purge-trash"].revivesFromDone` で次の `armPurgeTrash` から `pending` へ（(l)）。失敗は `logger.warn` + `failedCount` → `rearm` を `now + jobsBackoffBaseMs` 以降に（A-5）。`alarm()` からは投げない
- (c) ADR-001: `RestorePolicy.decideDocumentRestore` の 3 分岐、`restoreAlone` は `findById`（Live）が null なら再判定して書き込みなし、touch は `save({ ...live, version + 1, updatedAt: now }, token)`（(g) で並行 `trashTopic` が OCC）、`restoreWithTopic` は `confirmSetRestore` 無しで書かず、再取得が live なら `restoreAlone`・null なら選択要求、`skippedDocuments` の当のドキュメントは追加で復元、`restoreTo` は existing → `findById` null で `TOPIC_NOT_FOUND` + touch / new → `Topic.create` + `insert`、`moveToTopic` → `restore`。復元の projection: `documentRepository.save`（PH-03）が doc + 出典メモを再射影、`memoRepository.save` は更新前 `status` を 1 回読み trashed → active で `reprojectCitingDocuments`（(b) の `sourceMemoIds` に戻る）。ADR-003 は A-8 と (h)
- (d) `changeTrashRetentionDaysProcedure`: `TrashRetentionDays.create`（整数 ≥ 1、上限なし。zod `max(36_500)`、UI `max` なし = △-4）→ `User.changeTrashRetentionDays` → `save` → 3 表の `recalculatePurgeAfter` を `maxChunks` 回まで 1 tx で → ゴミ箱が非空なら `armPurgeTrash`（`enqueueJob` 規則 (1) で延長は早まらない: A-13、(p)）。残件は `purge-trash` の再計算フェーズが引き取る（(o)）
- (e) `TrashQueryPort` は `listTrashItems` / `findTrashItem` / `countTrashItems` / `listItemsToPurge` / `findEarliestPurgeAfter` の読み取り 5 メソッドのみ。書き込みは `memoRepository` / `documentRepository` / `topicRepository` の `save` / `hardDelete` / `delete` 経由（spec/domains/trash.md「書き込みポートについて」）
- (f) `TrashBoard` が list owner（復元 / 完全に削除 / 空にする / もっと読む を実行、`useOptimistic` で `busy` → 行の `aria-busy` + ボタン非活性、成功で行を外し `router.invalidate()` → `seed` で re-base、失敗は行内 `role=alert` + 再試行、`NotFound` は「既に処理済みです」）。ダイアログ文言: 「履歴ごと消え、元に戻せません」、トピックは「セットで削除されたドキュメント n 件が対象です」、空にするは「ゴミ箱の N 件をすべて…」（A-8〜A-10）。結果はすべて `readServerFnResult(isXxx)`。`RetentionForm` は `useActionState` + `useOptimistic`、controlled input で草稿保持
- (g) ban list / lint: 新しいエラークラスなし（`TRASH_ITEM_NOT_FOUND` / `INVALID_PAGE` / `INVALID_PRUNE_BUDGET` は既存クラスのコード）、`pnpm lint` 0 件、`banList.test.ts` 緑
- (h) 細部: (1) `restoreAlone` / `restoreTo` の touch は `updatedAt: now` も書く。spec は「内容を変えず `version` をインクリメント」で、PH-03 の `createDocument` の touch は `updatedAt` を変えない — 揃えるなら `version` だけにする（`updated_at` を見る画面はトピックに無い）。(2) `emptyTrash` facade は `provider.run` の合間に `await` を挟まないので同期的に走り切る（1 tx = 1 項目は保たれる）。(3) `listTrashItems` の `count` は 3 表の `count(*)` の和で毎回計算（100 件ページなら十分）

## 5. 追加観察の判定と自分の追加観察

- A-1（0 / 負数 / 小数の三重検査）: 妥当。A-12 で確認。P-13「項目ごとにエラー」を満たす
- A-2（配下 0 件のトピックは通常文言）: 妥当。A-7 の T6（個別削除の D6 は対象外）で通常文言、A-9 で「1 件が対象」
- A-3（`emptyTrash` の `deferred` は同一呼び出しで再試行しない）: 妥当。spec「残件は再実行で消化」どおり、P-12 の再試行が次の呼び出し
- A-4（何も更新しなかった probe を予算に数えない）: 妥当。spec の上限は「触る量」の有界性であり、0 行の probe は量を増やさない。`pending` が続く限りは数えるので無限には回らない
- A-5（先送り時は `now + backoffBase` 以降に再武装）: 妥当。過去の期限で即時に回り続けない
- A-6（lint fixture の pid 衝突）: `rosterGrep` が `.tmp.ts` を読み飛ばす対処で `pnpm test` 4 回とも緑。fixture 名に乱数を足す根本対処は PH-10 の整理候補
- **O-1（新規）**: §4 (h)(1) の `updatedAt` の touch 差
- **O-2（新規）**: preview でゴミ箱を空にした後も `jobs.purge-trash` は `pending`（10-08）で残る。次の起床で `finished` → `done` になる設計どおりだが、空のゴミ箱に対して 1 回空振りする。`restoreMemo` 等の復元経路でも `findEarliestPurgeAfter` を読み直して張り直す設計は spec に無いので現状維持でよい
- **O-3（新規、環境）**: dev を止めるときの `pkill -f "workerd serve"` は preview の workerd も巻き込む（自分の手順ミス）。次フェーズ以降は `lsof -t -i :3000` の pid とその子だけを止める

## 6. PH-01〜04 受け入れ済み 19 項目への影響

- **R-INF-02（ナビ）**: `NAV_ITEMS` に「ゴミ箱」（検索の後、設定の前）。preview で「タイムライン / トピック / 検索 / ゴミ箱 / 設定」の 5 項目（P-4）= plan.md の完了条件が全部そろった。`appShell.dom` の roster と `titleFor("/trash")` が固定
- **R-TL-01〜08、R-SE-01（`memoRepository.save`）**: 追加は `SELECT status` 1 回と trashed → active の遷移時だけの `reprojectCitingDocuments`。投稿 / 編集 / ソフトデリートの経路は不変（`timeline` / `timelineWindow` / `editMemo` / `search` integration が 17 / 106 の中で緑）。A-3 で復元後の `/?memo=` と検索を確認
- **R-AC-01 / 03 / 04 / 08、R-INF-01（`IdentityGateway`）**: `changeTrashRetentionDays` のメソッド追加と fake roster のみ。ログイン 17:05（dev）/ 17:14（preview）で変化なし
- **R-TL-06 / R-DT-05（`jobRegistry["purge-trash"]`）**: `armPurgeTrash` の投入経路は不変。handler が入ったので A-11 のとおり `poison` にならずに処理される。`softDeleteMemo` / `trashKnowledge` integration 緑
- **R-DT-01〜04**: knowledge 側のコード変更なし。A-4 / A-7 でトピック touch（version +1）と P-07 / P-08 の表示を確認
- **設定画面（`CurrentUserPanel` → `RetentionForm`）**: 表示の置き換えのみ。`currentUserPanel.dom` を router 化して緑。P-13 の他の節は不変

## 7. 検証環境の引き継ぎ

- 起動中: dev PID 15947（自分で再起動、http://localhost:3000、ログ scratchpad `dev5.log`）、preview PID 16469（`232bebe` のビルド、http://localhost:4173、ログ `preview5b.log`）
- DO 状態: ゴミ箱は空（trashed 0）、保持 30 日、`jobs.purge-trash` は `pending` / `next_run_at` 2026-10-08 17:13:51 JST（次の起床で `done`）。追加 = トピック「ゴミ箱T5」「ゴミ箱T6」「ゴミ箱T7」はハードデリート済み、「新規復元先T8」（active、配下「選択D7」）、検索テストA 配下に「復元先選択用D3」「選択D6」「ゴミ箱引用D9」（出典メモはハードデリート済み）、メモ「ゴミ箱検証メモ trashmemo1」「期限起床トリガー armB」（active）。「保持期限用メモ retA」は Alarm で消去済み
- agent-browser: session `verify05`（3000 / 4173 でログイン中）。Implementer の `ph05` はそのまま
- scratchpad: `checks5.log`（自動テスト全文 + unit 3 回）、`build5.log`、`ab5.sh` / `tr1.js`
