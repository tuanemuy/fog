# PH-01 検証報告 — walking skeleton

- Verifier 実施: 2026-09-08T02:39〜02:51+09:00（JST。以下の時刻はすべて JST）
- 対象: ブランチ `feat/spec-implement`、HEAD `5d36a89`（`git status` に実装コードの未コミット変更なし。未追跡は `.claude/` と `.spec-implement/` のみ）
- 実装報告: `.spec-implement/phases/PH-01.md`
- 検証環境: `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test`、`pnpm dev`（http://localhost:3000、状態 `rm -rf apps/web/.wrangler/state` 後に起動）、`pnpm build && pnpm preview`（http://localhost:4173）、`agent-browser --session verify01`、`sqlite3` で `apps/web/.wrangler/state/v3/do/**/*.sqlite`

## 結果表

| ID | 判定 | 根拠（詳細は「自分で実行した検証」「コードレビューの指摘」） |
|---|---|---|
| R-INF-01 | 合格 | T: `pnpm test:integration` 5 files / 16 tests 通過（自分で再実行）。`schema.integration.test.ts` は両 DO の `_meta.schema_version = 1` と 17 + 7 表・24 + 13 索引の**名前**を固定。DDL 突き合わせは表・列・CHECK・索引・部分索引・FTS5 まで一致（差分は下記 G-1〜G-3 の 3 点、いずれも spec に矛盾しない）。`alarm.integration.test.ts` は 1 回の `alarm()` で job が `done`・event が `published` になることを固定。`occ.integration.test.ts` は `ConflictError("OPTIMISTIC_LOCK_FAILURE")` を DO 内で固定。**限界**: OCC が RPC 封筒を越えてリクエスト側へ届く経路、Alarm の「各 pass ちょうど 1 回」「lease 行での再武装」は未固定（観察 T-1〜T-3。判断事項 M-4） |
| R-INF-02 | 合格（判断事項 M-2 あり） | B: 未ログインで `/settings` → `/login?redirect=%2Fsettings` → ログイン → `/settings` に戻る（dev 02:42:27〜02:42:52、preview 02:48:28〜02:49:20）。ナビは「タイムライン / 設定」の 2 つ。完了条件の文言「ナビ（タイムライン / トピック / 検索 / ゴミ箱 / 設定）が出る」に対し 3 つが無い。`appShell.dom.test.tsx` が「5 つの行き先それぞれについて、ナビにある ⇔ ルートが存在する」を固定しているので、後続フェーズでルートと同時に出る設計 |
| R-AC-01 | 合格（判断事項 M-3 あり） | B: 登録 → `/`（02:41:37）。重複（`Verify01@Example.com`、大文字混じり）→「このメールアドレスは既に登録されています ログインする」リンク付き（02:43:24）。パスワード 5 文字 → パスワード欄に項目別エラー + `aria-invalid=true`（02:43:38）。メール形式不正はネイティブ検証を `noValidate` で外してサーバー経路を通し「メールアドレスの形式が正しくありません」+ `aria-invalid=true`（02:43:40）。saga: 登録直後 `resume-signup` pending（02:42）→ 02:48:47 に `done`、User Data `operations.phase = done`、`credential_locators` 1 行、`account.caller_token` 32 文字。spec との差分は C-1〜C-4 |
| R-AC-03 | 合格 | B: 誤パスワード / 未登録メールとも「メールアドレスまたはパスワードが正しくありません」のみ、URL は `/login?redirect=%2Fsettings` のまま（02:42:40）。正しい資格情報で `/settings` へ（02:42:52）。ロックアウトの値（閾値 5、30s×2^n、上限 900s、減衰 900s）は `tuning.ts` と `signupSaga.integration.test.ts` (d)(e) / `loginWithPassword.test.ts` で spec どおり（レビュー E-1〜E-2） |
| R-AC-04 | 合格 | B: 設定画面「ログアウト」→ `/login`、直後 `/settings` → `/login?redirect=%2Fsettings`（dev 02:42:27、preview 02:48:28）。`account.session_epoch` は 0 のまま = spec（`session_epoch` を進める操作は 4 つだけで logout は含まない。domains/identity.md:556） |
| R-AC-08 | 合格 | B: 設定画面に `verify01@example.com`、「メールアドレス / ログインに使用」、「30 日」、AI 接続の案内、ログアウト（dev 02:42:15、preview 02:48:07）。DTO は `{userId, email, credentials[{credentialId, kind, label, usableForLogin}], trashRetentionDays}` で検証材料・subject 無し（`getCurrentUser.test.ts` / `signupSaga (g)` が `toEqual` で全形固定）。差分 F-1 は観察 |
| R-TL-01 | **不合格** | B: 楽観表示（「投稿中…」+ `article[aria-busy]`、02:41:58）→ 確定、同一 DO の `memos` / `memo_revisions` / `search_entries` / `search_fts MATCH` が各 1 行（02:42:01）。空文字は非活性。通信断（`/_serverFn/` を abort）では「エラーが発生しました」+ 入力保持 + 再送で成功（02:45:13〜02:45:36）。**しかし A-10 を単独で再現した結果、`postMemoFn` だけが JSON 本文の HTTP 500（`{"status":500,"unhandled":true}`）を返すと、楽観エントリが静かに消え、入力欄が空になり、エラー表示も無く、行も無い**（02:47:26。詳細は「A-10 の判定」）。完了条件「失敗時は入力保持 + リトライ」を満たさない失敗経路が 1 つ残る。修正は小さい（判断事項 M-1） |
| R-TL-02 | 合格 | B: 35 件で初回 30 件 + 「過去のメモを読み込む」、sentinel を可視化すると 35 件・重複なし・ボタン消失（02:44:45〜02:44:55）。日付見出し「2026年9月8日(火)」の下に新しい順。複数日のグルーピングは同日データしか作れないためブラウザ未確認、`timelineBoard.dom.test.tsx` の `groupByDay` / 「newest-first day headings」で固定。空状態「最初のメモを残そう」（02:41:37）。preview: クライアント遷移で `TimelineSkeleton` 3 行 → 30 件に置換（02:49:44、T0 skel=3/articles=0 → T1 skel=0/articles=30）。SSR 応答（curl、14 ms）は内容がインラインで skeleton は出ない（速すぎて Suspense fallback が emit されない。異常ではない） |

### Manager の判断が必要な事項

- **M-1 R-TL-01 / A-10（不合格の根拠）**: `TimelineBoard/index.tsx:136-140` は `await post(...)` が**解決すれば成功**とみなし `router.invalidate()` → `setDraft("")` へ進む。`@tanstack/start-client-core` の `serverFnFetcher.js:181-186` は `content-type: application/json` かつ `x-tss-serialized` 無しの応答を `response.ok` を見ずに JSON として返すので、封筒でない JSON 500 は reject されない。実装報告の A-03 はこの経路で実際に発生した（マニフェスト漏れは直したが、経路自体は残る）。修正案: 解決値の形（`result.memo.id` の存在）を検証し、満たさなければ `SystemError` 相当として `displayError` へ落とす。`timelineBoard.dom.test.tsx` (d) は `{ memoId: "new" }` という実際には返らない形で resolve しているので、実形 `{ memo }` に直し、非封筒応答のケースを足す。**PH-01 で直すか、PH-10 の監査へ送るかの判断が要る**（私の意見: 経路が実在し修正が小さいので PH-01 で直す）
- **M-2 R-INF-02 のナビ**: 完了条件は 5 つの行き先を列挙するが、現状 2 つ。「ルートが存在するものだけ出す」設計と dom test の固定は妥当。**完了条件の文言をこの解釈で読んでよいか**（読めるなら合格のまま。読めないなら PH-03〜05 まで review 継続）
- **M-3 `saga_committed` の書き時期（R-AC-01、spec 逸脱）**: `credentialMappingStore.ts:190-193` は `saga_committed = 1` を `status = 'active'` と**同じ UPDATE**（phase 3）で書く。掃除の述語は `status = 'reserved' AND saga_committed IS NULL AND reserved_until < ?`（`sweepReservations.ts:15`、spec/database:753）なので `active` 行は印の有無に関係なく掃除対象外 — つまり**印は現状なにも守っていない**。spec/recovery/index.md:257 は「phase 2 の戻り値を受けてコーディネーターが印を書く」= `reserved` のまま印を書き、印が失われる窓を「phase 2 commit 後に応答が失われた場合」だけに限定している。実装は窓を phase 2 → phase 3 の全区間に広げ、`cm_reservation_idx` の部分述語も無意味になっている。今日の登録は完走するので完了条件は満たすが、**予約 TTL の下限計算（C-2）も過小評価**なので、TTL 既定 3,600 s に守られているだけの状態。**PH-01 で直すか（`activateReservation` の前に `reserved` 行へ印を書く 1 文）、PH-09（R-REC-01）へ送るか**
- **M-4 JSDoc が存在しないテストを保証として名指ししている**: `schema/plan.ts:95-99,107-115`、`alarmSchedule.ts:39-40`、`rowRunner.ts:197-198` が `__tests__/alarmSchedule.integration.test.ts` / `__tests__/rowRunner.integration.test.ts` の `EXPLAIN QUERY PLAN` 検証を根拠として書くが、**どちらのファイルも無い**（`EXPLAIN QUERY PLAN` を含むテストはリポジトリに 0 件）。`delivery/__tests__/jobKinds.test.ts:14-15` も「`event.type` が roster 外に現れないことを grep するテストの対」と書くが、その grep テストは無い。CLAUDE.md「保証には test か type、無ければ限界を明記」に照らして、テストを足すか JSDoc を限界の記述に直すか、issue で追跡するかの判断
- **M-5 catch 方針の宣言済み例外**: `loginWithPassword.ts:84-90` は `Email.create` / `PlainPassword.create` の `BusinessRuleError` を捕まえて `ValidationError("INVALID_CREDENTIALS")` に翻訳する。spec/usecases/identity.md（応答を一様にする規則）どおりだが、CLAUDE.md「domain → application: 再翻訳しない」に反するので、**宣言済み例外として CLAUDE.md か docs に 1 文置く**か、判断を記録する

## 自分で実行した検証

### 自動テスト（2026-09-08T02:39:09〜02:39:17+09:00、HEAD `5d36a89`）

| コマンド | 期待 | 結果 |
|---|---|---|
| `pnpm typecheck` | 0 件 | exit 0 |
| `pnpm lint` | 0 件 | exit 0 |
| `pnpm format:check` | 差分なし | exit 0 |
| `pnpm test`（unit + dom） | 44 files / 1047 tests | **44 passed / 1047 passed**（報告と一致） |
| `pnpm test`（integration、Miniflare DO プール） | 5 files / 16 tests | **5 passed / 16 passed**（報告と一致） |

### テストが完了条件を固定しているか（レビュー）

- `schema.integration.test.ts`: 両 DO の `_meta.schema_version === 1`、`sqlite_master` の表名集合 = `*_TABLE_NAMES`（17 / 7）、明示索引名集合 = `*_INDEX_NAMES`（24 / 13）、`search_fts` が `CREATE VIRTUAL TABLE … fts5`。**固定していないもの**: 列・型・NOT NULL・CHECK・PK 構成・索引の列順 / DESC・部分索引の述語・FTS5 の `content=` / `content_rowid=` / `tokenize=`。比較対象がプラン自身の定数配列なので、DDL と配列を同時に改名しても通る（spec の独立転記ではない）
- `timeline.integration.test.ts` (a): **1 回の `postMemo` の直後**に `memos` / `memo_revisions` / `search_entries` / `search_fts MATCH` を読む（4 表の共存を固定）。「同一 tx」= 途中失敗で 4 表とも巻き戻ることは未固定（失敗注入なし。(b) は `Memo.create` で落ちるので文が 1 つも走らない）
- `occ.integration.test.ts`: `repository.save` を `transactionSync` 内で呼び `ConflictError("OPTIMISTIC_LOCK_FAILURE")` を固定。**DO 内のみ** — `envelope` / `callDurableObject` / `rebuildRpcError`（`rpcErrors.ts:47-48` に conflict 分岐はある）を通っていない。現フェーズには OCC を RPC 越しに起こせるユースケースが無い（`postMemo` は insert のみ）
- `alarm.integration.test.ts`: 期日 job 1 + 期日 outbox 1 → `runDurableObjectAlarm` 1 回 → job `done`・event `published`・runnable 空・alarm 削除・2 回目 `false`。**未固定**: 各 pass が「ちょうど 1 回」（カウンタ / spy 無し）、pass ごとの独立した件数上限、lease 行 `leaseUntil` での再武装、`alarm()` が投げないこと（migration gate の fail-closed 分岐）
- `timelineBoard.dom.test.tsx` (d): 送信直後に `role=status`「保存中…」+ `article[aria-busy=true]` が先頭、`postMemoFn` 呼び出し後・解決前は `invalidate` 未呼出、解決後 `invalidate` 1 回・欄クリア。(e): `AppServerError(business, EMPTY_BODY)` の reject → alert、楽観エントリ消失、欄に本文保持、`invalidate` 未呼出。**リトライ（失敗後の再送信）は未テスト**。(d) の resolve 値 `{ memoId: "new" }` は実装が返す形（`{ memo }`）ではない — コンポーネントが解決値を見ていないから通る（M-1 と同根）
- `appShell.dom.test.tsx`: `NAV_ITEMS.to` が production `routeTree` に存在、5 つの行き先について「ナビにある ⇔ ルートがある」、ラベル `["タイムライン","設定"]`、`aria-current`、`<h1>`、skip link。堅い

### ブラウザ dev（http://localhost:3000、02:41〜02:47。事前に Implementer の dev サーバー（02:10 起動、PID 67991）を停止し `rm -rf apps/web/.wrangler/state` 後に再起動）

| 時刻 | 操作 | 期待 | 結果 |
|---|---|---|---|
| 02:41:21 | 未ログインで `/` | ログイン画面 | `/login`。メール / パスワード / ログイン / アカウント登録リンク |
| 02:41:37 | `/signup` に `verify01@example.com` / `password123` | `/` へ | `/`。ナビ「タイムライン / 設定」、「最初のメモを残そう」、投稿ボタン `disabled` |
| 02:41:49 | 投稿欄に入力 | ボタン活性 | `is enabled` = true |
| 02:41:58 | `requestSubmit()` 直後 snapshot | 楽観表示 | 見出し「2026年9月8日(火)」出現、textbox `disabled`、ボタン「投稿中…」 |
| 02:42:01 | 2.5 s 後 | 確定・欄クリア | 「02:41 今日の気づきメモ」表示、欄 `""`、ボタン `disabled`。sqlite（User Data）: `memos` 1 / `memo_revisions` 1 / `search_entries` 1 / `search_fts MATCH '気づき'` 1 |
| 02:42:10 | sqlite（Directory `dir:g1:b11`） | saga の行 | `jobs`: `sweep-reservations` pending（`operation_key = sweep-reservations`、`next_run_at` = 予約期限）、`resume-signup:<opId>` pending。`credential_mappings` active、`saga_committed = 1`。User Data: `operations(signup, done)`、`account` active / `session_epoch 0` / `caller_token` 32 文字、`credential_locators` 1 行 |
| 02:42:15 | ナビ「設定」 | P-13 の内容 | `verify01@example.com`、「メールアドレス / ログインに使用」、「30 日」、AI 接続の案内、ログアウト |
| 02:42:27 | 「ログアウト」→ `/settings` | `/login` → `/login?redirect=%2Fsettings` | どちらも期待どおり |
| 02:42:40 | 誤パスワード / 未登録メール（`nobody@example.com`） | 同一文言、理由非特定 | 両方「メールアドレスまたはパスワードが正しくありません」のみ。URL は `?redirect=` 保持。未登録メールは別 bucket `dir:g1:b10` の DO を初期化するだけで行は作らない |
| 02:42:52 | 正しい資格情報 | `/settings` へ戻る | `/settings`。`failed_attempts` 0（成功でリセット） |
| 02:43:13 | ログイン中に `/signup` | — | `/` へ跳ねる（観察 O-4） |
| 02:43:24 | cookie 消去後 `/signup` に `Verify01@Example.com` | 重複エラー + 導線 | 「このメールアドレスは既に登録されています ログインする」（リンク） |
| 02:43:38 | `verify02@example.com` / `short` | 項目別エラー | パスワード欄「パスワードは8文字以上128文字以下で入力してください」、`aria-invalid`: email null / password true |
| 02:43:40 | `form.noValidate = true` で `not-an-email` / `password123` | サーバー側 INVALID_EMAIL | 「メールアドレスの形式が正しくありません」、`aria-invalid`: email true / password null |
| 02:44:14〜36 | ログイン → 34 件を `fill` + `requestSubmit()` で連続投稿 | 35 行 | `memos` 35 / distinct 35、`search_entries` 35、`memo_revisions` 35、`errors` なし |
| 02:44:45 | リロード | 30 件 + 読み込みボタン | `article` 30、先頭「メモその34」、末尾「メモその5」、「過去のメモを読み込む」あり |
| 02:44:55 | sentinel を `scrollIntoView` | 残り追加・重複なし | 2.5 s 後 35 件 / distinct 35、末尾「今日の気づきメモ」、ボタン消失 |
| 02:45:13 | `network route "**/_serverFn/**" --abort` で投稿（TC-17 手順 2） | エラー + 入力保持 + リトライ手段 | alert「エラーが発生しました」、欄 `"通信失敗テスト"` 保持、楽観エントリ消失、ボタン再活性 |
| 02:45:36 | `unroute` → 再送 | 成功 | 先頭「02:45 通信失敗テスト」、欄クリア、sqlite 1 行 |
| 02:45:39 | `--body '{"status":500,"unhandled":true}'`（HTTP 200） | — | alert 表示・入力保持（この形は reject される。A-10 の再現にはならない） |
| 02:47:26 | **A-10 単独再現**: `window.fetch` を差し替え、`postMemoFn` の URL だけ HTTP 500 + JSON 本文 | 入力保持 + エラー表示 | **`articles=30 pending=0 value="" alerts=""`** — 楽観エントリ消失、欄クリア、エラーなし、行なし |

### ブラウザ preview（`pnpm build`（exit 0、02:45〜02:46）→ `pnpm preview` 4173。02:47:29〜02:50:40。dev と同じ DO 状態を共有）

| 時刻 | 操作 | 期待 | 結果 |
|---|---|---|---|
| 02:47:29 | `/`（cookie 共有） | 内容表示 | 30 件、先頭「通信失敗テスト」 |
| 02:47:52 | `curl -H Cookie` で `/` の SSR HTML | ストリーム | 200 / 24,441 B / 14 ms。`<article>` 30 が本文にインライン、`$_TSR.e()` 1、skeleton 記法 0（応答が速く fallback が emit されない） |
| 02:48:00 | 「preview投稿A」を投稿 | 永続化 | `memos` 36 → 37、`search_fts MATCH` 1、500 応答 0 件。リロード後も先頭 |
| 02:48:07 | `/settings` | P-13 | dev と同じ内容 |
| 02:48:28 | ログアウト → `/settings` | `/login` → `/login?redirect=%2Fsettings` | 期待どおり |
| 02:49:44 | ログイン後 `/settings` からナビ「タイムライン」（クライアント遷移、MutationObserver で監視） | skeleton → 内容 | T0（60 ms）: `.fog-skeleton-entry` 3 / article 0 → T1（3 s）: skeleton 0 / article 30 |
| 02:50:17 | ナビ「設定」 | skeleton → 内容 | 60 ms 時点で既に email 表示（skeleton は観測できず。配線は `settings.tsx:40-42`） |
| 02:50:14 | TC-18: `requestSubmit()` × 3 を同期で連続 | 1 件 | **3 行**（観察 O-1） |
| 02:50:37 | TC-18: `button.click()` × 2 を同期で連続 | 1 件 | `disabled` は 3 回とも `false`、**2 行**（観察 O-1） |

### 既存の検証根拠が HEAD `5d36a89` に適用できるか（確認項目 5）

- 実装報告のブラウザ検証は `717c1c2` 以前 + `5d36a89` 後の preview 3 回。`6db586e`（TokenGenerator ポート、`streamingRouteOptions`）と `5d36a89`（`__root.tsx` の side-effect import）は登録 / 投稿 / ストリーミングの経路に触れる変更なので、上記のとおり dev・preview の全経路を `5d36a89` で再実行した。結果は報告と一致（A-03 の修正も preview で確認: 500 応答 0 件、投稿が永続化）
- `caller_token` が `TokenGenerator` 経由で 32 文字生成されていることは sqlite で確認

## Implementer の検証結果を利用した範囲

- FTS5 trigram の実測（design D-09）は再実行していない。`timeline.integration.test.ts` (a) の `MATCH` と、ブラウザ投稿後の `search_fts MATCH` で間接的に確認
- `resume-signup` の Alarm 実行は自分の環境でも `done` を確認（02:48:47）
- 320 文字メールの登録は未実施（unit の境界テストで固定されている）

## コードレビューの指摘

不具合（項目 ID 付き）を先に、次いで spec / 規約との差分。

### R-TL-01

- **D-1（不合格の根拠、M-1）** `apps/web/app/components/timeline/TimelineBoard/index.tsx:136-144`: `post()` の解決値を検証せず成功扱い。封筒でない JSON 応答（HTTP 500 でも）が成功に見える。再現: 上記 02:47:26。期待: alert + 入力保持。実際: 楽観エントリ消失・欄クリア・alert 無し・行無し
- **O-1（追加観察、TC-18）** `disabled` は再レンダー後にしか反映されないので、同一フレーム内の 2 回目のクリックが 2 件目の投稿になる（`button.click()` × 2 で 2 行、02:50:37）。R-TL-01 の完了条件には二重送信が無いので観察扱い。対策候補: `useRef` の in-flight フラグで action 冒頭で弾く。条件に足すなら spec/manual-tests/timeline.md TC-18（S-TL-01「重複・競合」）

### R-INF-01

- **T-1** OCC の `ConflictError` が RPC 封筒（`envelope` → `callDurableObject` → `rebuildRpcError`）を越えることを固定するテストが無い。現フェーズに起こせるユースケースが無いので PH-02（editMemo）で足すのが自然
- **T-2** `alarm.integration.test.ts` に「各 pass 1 回」「pass ごとの上限が独立」「lease 行の `leaseUntil` で再武装」「gate 失敗で alarm を残す」が無い
- **T-3** `timeline.integration.test.ts` に巻き戻しの原子性（失敗注入）が無い
- **M-4** の JSDoc（存在しないテストの名指し）
- **G-1** `operations.kind` に impl だけ `CHECK (kind IN ('signup','link','unlink','credential-change','withdrawal'))`（`userDataPlan.ts:239`）。spec:633 の DDL 列には無く、prose の 5 値と整合。差分として記録
- **G-2** `password_reset_tokens.token_hash`: spec:770 は列に `UNIQUE` + 索引 `prt_token_hash_uq` の両方、impl は名前付き索引のみ（`identityDirectoryPlan.ts:52,62`）。機能等価、spec:415 の「無名 autoindex を避ける」理由に沿う
- **G-3** `credential_mappings.failed_attempts` の「既定 0」（spec:717）は DB DEFAULT ではなく impl も無し。prose のみ
- それ以外の 24 表・37 索引・部分述語・FTS5（`content='search_entries'`, `content_rowid='rowid'`, `tokenize='trigram'`）・`_meta` は spec と一致。Alarm 再武装（`alarmSchedule.ts:42-51,71-78,112-125`）は spec/database:538-552 の 4 つの最小値と `deleteAlarm` 条件に一致。`enqueueJob` の `operationKey`: `sweep-reservations` は定数、`resume-signup:<operationId>`、いずれも spec/async:40,42 に一致。`jobs.kind` の roster は `application/delivery/types.ts`（`jobKinds.test.ts` が独立転記で固定）、`event.type` は `domain/identity/passwordResetRequested.ts` に分かれている（roster が 2 ファイル）

### R-AC-01（新規登録 saga）

- **C-1（M-3）** `saga_committed` の書き時期（上記）
- **C-2** `application/identity/tuning.ts:328-349` の予約 TTL 下限 = `2 × backoffTotal + 2 s` = 62 s。spec/recovery:250-258 の不等式に対し、前進の初回待ち `signupResumeDelayMs`（60 s）と後始末の初回待ち（`backoffDelayMs(0)`）を含めていない。既定 3,600 s は満たすが、ガードとして過小評価（`tuning.test.ts:600-603` が 62,000 を固定）
- **C-3** `credentialMappingStore.ts:322-375` `reserveCredential` は SELECT → INSERT。spec/domains/identity.md:439「各操作は単一の CAS 文」。`transactionSync` 内なので原子的だが形が違う。期限切れ未掃除の `reserved` 行も競合扱い（spec:430「有効な行」）
- **C-4** 後始末 S1〜S4（`terminalStage`）は未実装 — `resumeSignup.ts:265-273` に「recovery slice で足す」と明記。PH-09（R-REC-01）の範囲で妥当。ただし S1 の「材料が無ければ即 `poison`（`cleanup-material-lost:`）」は現状 plain `Error` の 5 回リトライ後 `poison`
- 手順の順序（予約 → initialize-account → activate → 逆引き）、ID の採番位置、phase 2 の digest（`callerToken` を含めない）、phase 4 の `updateOperation` 同 tx、冪等な再駆動（`account` 既存で短絡、`activate` の CAS が `active AND user_id` でも真）、`resume-signup` のコーディネーターだけへの投入、`sweep-reservations` の述語と再武装は spec どおり。`signupSaga.integration.test.ts` は spec/testcases/identity/registerWithPassword.md の 7/8/9/12〜15/17/19 を固定、18（SSO 所有メール）/20（真のレース）/21〜24 は未固定
- **B-1** `jobs/resumeSignup.ts:55,59,63,89`、`jobRunner.ts:168`、`stores/accountStore.ts:51` が plain `Error` を投げる。`failureLabel`（`rowRunner.ts:87-95`）は `terminal_reason` に `"Error"` と書くので、operator へ届く理由が区別できない。`SystemError` の別コードにすべき

### R-AC-03（ログイン）

- **E-1** spec/usecases/identity.md:145 のダミー導出は `hasher.hash(DUMMY)`（`loginWithPassword.ts:244`）。`verify` との等コストは未検証。形式不正は導出前に返る（spec の手順 1 と同じ位置なので一致）
- **E-2** `loginWithPassword.test.ts` は testcases 31〜39 / 42 / 43 / 45（正しいパスワード側）/ 46（一部）を固定。40 / 41（DB / verify 失敗 → SystemError）、45 の誤パスワード側、46 の同時 CAS、47 の 2 世代 locator は未固定
- **M-5** の catch

### R-AC-08（getCurrentUser）

- **F-1** `identityGateway.ts:170-179` が `NotInitialized` → `null`、`getCurrentUser.ts:411-414` が `NotFoundError("USER_NOT_FOUND")` に畳む。spec:687,695-696 は `SystemError(NOT_INITIALIZED)` を認証ミドルウェアまで通し、`USER_NOT_FOUND` は「初期化済み DO に settings 行が無い」場合に限る。今日の経路（cookie は初期化後にしか発行されない）では観測できないが、spec と違う

### 規約（CLAUDE.md）

- 同期契約: `run` の条件型（`unitOfWork.ts:42`）で async を型拒否。全コールバック（`userDataDurableObject.ts:70-139`、`identityDirectoryDurableObject.ts:94-176`、`durableObjectBase.ts:198,229`）が同期で、非同期ポート（`PasswordHasher` / `SessionCodec` / `TokenGenerator`）は `RequestContainer` にしか無く DO 設定には無い。`run` のネスト無し。**限界**: `enqueueJob` / `enqueueEvent` は `ctx` を持ち出せば tx 外でも呼べる（型・実行時ガード無し、今は呼び手なし）
- レイヤー: domain に I/O / 時刻 / 乱数無し。application は `di/` 以外で `crypto` 直呼び無し（`callerToken.ts` は `TokenGenerator` 経由）。`application/ports/clock.ts` の `SystemClock`（`new Date()`）と `ports/logger.ts` の `ConsoleLogger`（`console.*`）が application 木に置かれている（ポート定義と同じファイルの既定実装。配線は DO 側）— 様式の差
- adapter の翻訳: `unitOfWork.ts:45-59`、`doStubs.ts:42-57`（stub 失敗 → `SystemError(DatabaseError)`、封筒 error → `rebuildRpcError` で再構築、wire 値の再 throw 無し）、両 DO の全公開メソッドが `envelope(...)`
- catch: 許可された境界（adapter 翻訳・DO RPC 入口・`alarm()` の 5 箇所・relay / job の行単位・server fn 直列化・クライアント表示）に収まる。例外は M-5
- ban list: 全 12 エラークラスが `lint/no-instanceof-error.grit:92-106` に載っている。`instanceof` は `errorResponse.ts:60` / `rowRunner.ts:90` の `instanceof Error`（組み込み）のみ
- Frontend 三層: `useOptimistic` / `useActionState` / `useTransition`、追加は list owner の form action、`router.invalidate()` で再基底化。loader は `renderServerComponent` を await せず返す。`streamingRouteOptions` = `pendingComponent: () => null` + `ssr: !DEV`。`inputValidator` は `registerFn` / `loginFn` / `postMemoFn` / `loadTimelinePageFn` に付く（`logoutFn` は入力なし）
- **O-2** `userDataDurableObject.ts:124-133` `postMemo` は `userId` を self locator から取るが、RPC 入力の `actor.userId` を照合しない（request 側が両方をセッションから作るので今は問題なし）
- **O-3** `components/timeline/schema.ts` の `body.max(100_000)` は domain の 10,000 code points より緩い（transport は DoS 上限のみ、業務上限は VO — 方針どおり）

## 追加観察（完了条件外）

- O-1 二重送信（上記）。条件に足すなら S-TL-01 の TC-18
- O-4 ログイン中に `/signup` / `/login` を開くと `/` へ跳ねる。spec/pages に記述は見つけていない（妥当な挙動）
- O-5 web のタイムライン初回 / 追加ページは `TIMELINE_PAGE_LIMIT = 30`（spec/usecases/memo.md:78 の「既定 50」は limit 省略時の既定。web は明示的に 30 を渡すので範囲内。manual-tests/timeline.md TC-04 は「既定 50 件」と書くので、マニュアルテストの文面と食い違う）
- O-6 preview の SSR 応答が 14 ms のため Suspense fallback が HTML に出ない。スケルトンはクライアント遷移でのみ観測。設定画面のスケルトンは 60 ms 時点で既に内容が出ていて観測できず
- O-7 `agent-browser batch` は空白で分割するので `eval` は単独呼び出しにする。`find text <ラベル> click` はサイドバーに覆われて失敗、`@ref` の `click` を使う。Devtools ボタンは `fill` + `eval requestSubmit()` で回避（報告の引き継ぎどおり）
- O-8 `credential_mappings` の OCC 列名は `credential_version`（`version` ではない）— spec:439 どおり `version` CAS を持たない設計。sqlite で `version` を引くとエラーになる

## 検証環境の引き継ぎ状態

- 起動中: `pnpm dev`（PID 80655 / vite 80812、http://localhost:3000、ログ `scratchpad/dev.log`）、`pnpm preview`（PID 82622 / 82659、http://localhost:4173、ログ `scratchpad/preview.log`）。両方とも私が起動。Implementer の dev サーバー（PID 67905 / 67991）は停止済み
- agent-browser: セッション `verify01`（`--restore`）、最後は http://localhost:4173/ にログイン中（`verify01@example.com` / `password123`）。`window.fetch` の差し替えはリロードで消えている
- DO 状態: `apps/web/.wrangler/state/v3/do/` を 02:41 に消して作り直した。User Data 1 DO（`verify01@example.com`、`memos` 42 行: 気づき 1 + メモその 34 + 通信失敗 1 + preview投稿A 1 + 連打テスト 3 + 連打クリック 2）、Identity Directory 2 bucket（`dir:g1:b11` にマッピング 1 行、`dir:g1:b10` は空）。`resume-signup` は `done`、`sweep-reservations` は pending（03:41 頃に起床）
- 実装コード・`spec/`・plan / status / brief / design は変更していない。書いたのは本ファイルのみ

## 再検証（差し戻し 3 項目、HEAD `bb3a1d0`）

- Verifier 実施: 2026-09-08T03:18〜03:28+09:00（JST）
- 対象: `8737aed`（Manager: CLAUDE.md の M-5 宣言済み例外、spec の jumpToDate 契約。docs / spec のみ）、`ffecad9`（core）、`bb3a1d0`（web）。`git status` に実装コードの未コミット変更なし
- 検証環境: Implementer の dev / preview（PID 89830 / 90080）を停止し、`rm -rf apps/web/.wrangler/state` 後に `pnpm dev`（3000）と `pnpm build && pnpm preview`（4173、`bb3a1d0` のビルド、exit 0）を立て直した。agent-browser session `verify01`

### 結果表

| ID | 判定 | 根拠 |
|---|---|---|
| R-TL-01 | **合格** | dev（03:25:19）: `postMemoFn` の URL だけ JSON 500（封筒なし）を返す注入 → 「システムエラーが発生しました」、下書き「500注入中の投稿」保持、楽観エントリ消失、ボタン再活性、行なし。fetch 復元 → 再送信で先頭に表示・欄クリア・行 1（03:25:2x）。preview（03:27:29、本番ビルドは server fn id がハッシュなので network log から実 URL `b0e86d…` を取って注入）: 同じく alert + 下書き保持 + 行なし → 復元 → 再送信で行 1・`search_fts MATCH` 1。二重送信（03:25:24）: `requestSubmit()` ×2 → POST 1 回・行 1、`button.click()` ×2 → 行 1。ページサイズ: dev で 54 件 → 初回 50 + 「過去のメモを読み込む」→ 54 件 distinct（03:26:28）、preview で 55 → 50 → 55 distinct（03:26:45）。dom: `timelineBoard.dom.test.tsx` が `{status:500,unhandled:true}` の resolve → alert・下書き保持・`invalidate` 未呼出、(d) は実形 `{ memo }`、同一フレーム 2 回 submit → `postMemoFn` 1 回、`limit: 50` を固定。`authForm` / 新設 `logoutButton.dom.test.tsx` も形検証を固定。`useServerFn` の全 5 呼び出し（TimelineBoard ×2 / AuthForm / LogoutButton）が `readServerFnResult` を通る |
| R-AC-01 | **合格** | `credentialMappingStore.ts:190-222`: `commitSaga` が `operation_id = ? AND status IN ('reserved','active')` に `saga_committed = 1` を CAS、`activateReservation` は印を書かない（diff で除去を確認）。`registerWithPassword.ts:49-103`: 予約 → initializeAccount → `commitSignupSaga`（false → `ConflictError(EMAIL_ALREADY_REGISTERED)`）→ activate → 逆引き。`resumeSignup.ts:88-114` も initializeAccount → commit → activate、再駆動は commit の WHERE が印を見ないので冪等。spec/recovery/index.md L251「phase 2 の戻り値を受けて印を書く」に一致。integration (h): reserved 行に印を書き `reserved_until` を過去にして Alarm で sweep → 行が `reserved` / `saga_committed=1` のまま残り、その後 `resume-signup` で `active`・ログイン可。(i): 印なしの期限切れ予約は削除。ブラウザ（03:25:12）: 登録直後の sqlite は `active` / `saga_committed=1` / `operations.phase=done`（phase 2 → 3 の窓は手動では捕まえられない。(h) が固定）。TTL 床: `tuning.ts:68-80` = `signupResumeDelayMs`(60,000) + 前進バックオフ総和(2+4+8+16 s = 30,000。`jobsMaxAttempts=5` で失敗 1〜4 回目の待ち) + `backoffDelayMs(0)`(1,000。終端モード突入時の初回待ち = 後始末曲線の内側) + 後始末総和(30,000) + マージン 2,000 = 123,000 ms。spec/recovery L250-258 の「前進の総経過時間 + 後始末の総経過時間 + マージン」（後始末の初回待ちは後始末の項の内側）に一致。`tuning.test.ts:25-34,78-90` が 123,000 と境界（`<=` で throw）を固定 |
| R-AC-08 | **合格** | `identityGateway.ts`: `isNotInitialized` と 2 箇所の try/catch を除去、`readAccountState` / `readCurrentUser` は `callDurableObject` を素通し（`isSystemError` import も消えた）。`getCurrentUser.ts` は catch なし。畳むのは `apps/web/app/presentation/currentUser.ts:38-50` の `getCurrentUserId` だけ（`NotInitialized` → `null`、他の SystemError は再 throw）。`requireUserId` は `null` → `/login` へ redirect、他は error-response middleware へ。新設 `currentUser.test.ts` 6 件（active 一致 / cookie なし / epoch 超過 / null / NotInitialized → null / DatabaseError 透過）、`getCurrentUser.test.ts`「passes SystemError(NotInitialized) through unchanged」 |

### 自動テスト（2026-09-08T03:18:49〜03:18:58+09:00、HEAD `bb3a1d0`）

`pnpm typecheck` / `pnpm lint` / `pnpm format:check` すべて exit 0。`pnpm test`: unit+dom **47 files / 1,067 tests**、integration **7 files / 23 tests**（報告と一致）。

### 受け入れ済み 5 項目への影響（確認項目 5）

`git diff 5d36a89..bb3a1d0 --stat` に `schema/`、`alarmSchedule.ts`、`outboxRelay.ts`、`components/layout`、`routes/`、`loginWithPassword.ts`、`logout.ts`、`timeline/actions.ts` の変更なし。実行経路に触れたのは `rowRunner.ts`（claim / finalize 文を同一文字列のまま export へ抽出。`rowRunner.integration.test.ts` が実行時の文で EXPLAIN）、`jobRunner.ts`（ハンドラ欠落分岐のエラー種別のみ）、`identityDirectoryDurableObject.ts`（`commitSaga` RPC の追加）、`credentialMappingStore.ts`（R-AC-01 そのもの）、`TIMELINE_PAGE_LIMIT` 30 → 50（`timelinePageSchema` の上限 100 内、R-TL-02 はブラウザで再確認済み）。`8737aed` は CLAUDE.md と spec のみ。**R-INF-01 / R-INF-02 / R-AC-03 / R-AC-04 / R-TL-02 の完了条件に影響なし**（ログイン / ログアウト / 認証ガードは 03:25 の登録 → 投稿 → preview の cookie 共有で経路が生きていることも確認）。

### B-1 / M-4 / O-5 の規約適合（確認項目 6）

- B-1: `resumeSignup.ts:60,67,103,110` → `SystemError(DataIntegrityError)`、`:74` → `ConfigurationError`、`jobRunner.ts:168` → 新コード `JOB_HANDLER_MISSING`（`errors.ts:291-294`、`SystemErrorCode` に WHY 付きで追加）、`accountStore.ts:53` → `DataIntegrityError`。`failureLabel`（`rowRunner.ts:87-95`）が `code` を `terminal_reason` へ書くので plain `Error` の `"Error"` は消えた。**限界**: `resumeSignup.ts` の 4 箇所は同じ `DATA_INTEGRITY_ERROR` なので相互には区別できない（`CONFIGURATION_ERROR` / `JOB_HANDLER_MISSING` とは区別できる）。`JOB_HANDLER_MISSING` を通すテストは無い
- M-4: `alarmSchedule.integration.test.ts` / `rowRunner.integration.test.ts` は `runInDurableObject` で両 DO クラスの実 SQLite に `EXPLAIN QUERY PLAN`（`planAssertions.ts:21-32` は `INDEX <name>` 行の存在 + `SCAN` 行の不在）。**負の対照あり**: `alarmSchedule.integration.test.ts:105-116` が冗長述語を落とした文で `jobs_runnable_idx` に乗らないことを確認し、ヘルパーが落ちうることを示す。`expectKeySeek`（claim / finalize の行キー seek）には負の対照が無い。`rosterGrep.test.ts` は `packages/core/src` / `apps/web/app` の非テストソース 50 ファイル超を走査し、`enqueueJob({ kind: "bogus" })` は `outside` に入って落ちる。JSDoc の参照先（`schema/plan.ts`、`alarmSchedule.ts`、`rowRunner.ts`、`jobKinds.test.ts`）はすべて実在ファイルに直った
- O-5: `TIMELINE_PAGE_LIMIT = 50`（`schema.ts:11`）、`TimelineFeed` / `TimelineBoard` の両方で使用、dom が `limit: 50` を固定。ブラウザで 50 → 全件を dev / preview とも確認

### 追加観察（完了条件外）

- N-1 二重送信ガードは成功経路でのみ `draftRef` を空にする（`TimelineBoard/index.tsx:142-174`）。同一フレームで 2 回 submit され 1 回目が**失敗**した場合、直列実行される 2 回目が同じ本文を再送する（サーバーが実際には書いていた 500 なら二重書き込み）。テストは「2 回 submit + 1 回目成功」のみ。`posting` ref は `useActionState` の直列化により常に false で冗長（害はない）
- N-2 `rosterGrep.test.ts:44` の `EVENT_TYPE_LITERAL = /"([a-z]+\.[a-z][A-Za-z]+)"/` は `"index.md"` のような二重引用符付きファイル名リテラルにも当たる（今は該当なし。将来の偽陽性）。`ENQUEUE_JOB_KIND` は `payload:` が `kind:` より前にある呼び出しを取りこぼす（既知 2 kind は非空断定で守られる）
- N-3 spec/usecases/identity.md L40-56 の手順は 5（初期化）→ 6（activateReservation）で印を書く独立した手順が無い。印の時期は spec/recovery/index.md だけが定めており、コードは recovery に従う。spec の文面を揃えるかは Manager 判断（spec-revise の範囲）
- N-4 signupSaga (h) は sweep が「走った」ことを `next_run_at` の前進などで直接は断定しない（`poison` でないことと行の残存のみ）。(i) が同じ設定で削除するので、対で成立している
- N-5 Implementer の引き継ぎにある `docs/test.md` の DOM 節が名指しする 5 ファイルの不在は未確認（PH-01 以前からの記述。docs の扱いは Manager 判断）

### 検証環境の引き継ぎ状態（03:28）

- 起動中: `pnpm dev`（PID 93234 / vite 93444、http://localhost:3000、ログ `scratchpad/dev2.log`）、`pnpm preview`（PID 94466 / vite 94496、http://localhost:4173、`bb3a1d0` のビルド、ログ `scratchpad/preview2.log`）。Implementer の 89830 / 90080 は停止済み
- agent-browser: session `verify01`、http://localhost:4173/ に `verify02@example.com` / `password123` でログイン中。`window.fetch` の差し替えは復元済み（リロードでも消える）
- DO 状態: 03:24 に `apps/web/.wrangler/state` を消して作り直した。User Data DO `0796e96d…`（`verify02@example.com`、`memos` 57 行）。他 2 つの User Data sqlite（`48f4…`、`f623…`）は古い cookie が指した未初期化 DO（表は `_meta` のみ）。Identity Directory は 1 bucket、`resume-signup` は登録直後 pending
- 実装コード・`spec/`・plan / status / brief / design は変更していない。書いたのは本ファイルの本節のみ
