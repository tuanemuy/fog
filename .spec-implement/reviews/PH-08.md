# PH-08 検証報告（Verifier、2026-09-08 22:52〜23:00 JST）

対象 HEAD `28653c9`（`f77883a` / `7135721` / `e52f2bc` / `48bc003` / `28653c9` + Manager `1d2ce92` `3f108df`）。`3e041c2..28653c9` は 79 ファイル。作業ツリーは追跡ファイル clean。

## 判定

| ID | 判定 | 根拠（詳細は下） |
|---|---|---|
| R-ST-02 エクスポート / P-13 | **合格** | dev / preview とも P-13「データ」節（「ゴミ箱の保持期限」の直後）→「エクスポート」→ 生成 → `fog-export-20260908.zip` がダウンロードされ、行は「ダウンロード」+「もう一度生成」に。展開: `index.md` + `memos/2026-09-08.md` + `topics/{slug}/index.md` + 文書 3 = 7 ファイル、manifest の counts = memos 3 / topics 2 / documents 3（ゴミ箱のメモ・文書・トピックは数えない）、本文は最新のみ（`旧バージョン08` / `初版08` / ゴミ箱の語 0 件）、完了トピック `archived: true`、`description` 本文、出典 `file: ../../memos/2026-09-08.md` が実在。出典メモをゴミ箱へ → 再エクスポートで `deleted: true`・memos 2。0 件ユーザーは `index.md` のみ。同じ入力の 2 回は `index.md` の `exportedAt` 1 行と zip の `mtime` 以外バイト同一。curl 405 / 401 / 403（他 origin・`Sec-Fetch-Site: cross-site`）。セッション切れは行内 alert + `/login?redirect=%2Fsettings` |
| PH-07 O-2 / O-3 / O-4 対応 | **妥当** | refresh に `client` を束縛し交換時に `client_id` と照合（別 client → `invalid_grant`「not issued to this client」、旧形式 → 検証失敗）。`AiToolContainer`（3 gateway）+ `Needs<…>` で AI 到達 usecase の依存を絞り、`tools.test.ts` が gateway メンバー / 型名の出現を禁止。MCP `Origin` は `new URL(appUrl).origin` と比較 |

**R-ST-02 合格。** PH-01〜07 の 32 項目への影響なし（§6）。

### Manager の判断が必要な事項

- **O-1（軽微・決定性の環境依存）**: zip の DOS タイムスタンプは `fflate` が `Date` のローカル時刻ゲッターで書くため、同じ `exportedAt` でも **プロセスのタイムゾーンが違えば zip のバイトが変わる**（内容と `index.md` は不変）。workerd は UTC で動くので dev / preview / 本番の間では一致し（実測: `exportedAt 22:55:54+09:00` の zip の mtime は `13:55:54` = UTC）、Node の unit（`zipArchiveWriter.test.ts` の決定性）も同一プロセス内比較なので通る。「同一入力 → バイト同一」の契約は満たすが、限界（プロセス TZ が入力に含まれる）を JSDoc に一文足すかは任意。
- **O-2（軽微）**: 設計 §4.1 は「`Origin` があれば `config.appUrl` と一致」だったが `28653c9` でリクエスト自身の origin に変更（preview で `APP_URL` = :3000 のため 403 になったのが理由）。CSRF 防御としては等価以上（`Origin` が自サイト以外なら 403、無ければ `Sec-Fetch-Site` で判定、非ブラウザは cookie が無いので 401）。ただし MCP 側（O-4 対応）は `APP_URL` の origin と比較しており、2 つの bare handler で基準が違う。統一するかは任意（MCP は非ブラウザ前提なのでどちらでも動く）。
- **O-3（スラッグ規則の細部）**: 手順 6 の 50 コードポイント切り詰めは手順 4（先頭末尾の `.`/`-` 除去）の後なので、切り詰め結果が `-` や `.` で終わることがある（例: 50 文字目が `-`）。spec の手順順どおりで、テストも「50 で切る」を固定している。ファイル名として問題は無いが、末尾 `.` は Windows で作れない名前になる。spec の範囲外なので判断材料としてのみ。
- 参考: 24 MiB の上限超過は integration（小さい上限）で固定されており、ブラウザでは踏んでいない（UI で 24 MiB を作る手段が無い。Implementer 報告と同じ限界）。

## 1. 自動テスト（自分で実行、`checks8.log`）

`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を HEAD `28653c9` で実行: 全て exit 0（lint warning 3 は既存）。unit+dom **118 files / 1,502 passed + 2 skipped**、integration **25 files / 143 passed** — 報告と一致。

新テストと完了条件の対応（`spec/testcases/export/exportAllData.md` 41 行）:

- `valueObject.test.ts`（178 行）: `ExportRequest`（正規名化・空 / 不明 / 末尾空白の拒否）、`ExportFile` の path 規則 4 種、`ExportArchive` のソート・重複、`slugOf` の `it.each`（禁止文字・空白 / NFC・`untitled`・先頭末尾 `.`/`-`・日本語）+ 50 / 51 コードポイント（サロゲートペア）、`resolveSlugs`（`-2` `-3`、切り詰め後衝突と `x`/`x-2` 並存、`createdAt` 同値のタイブレーク）。
- `exportRenderer.test.ts`（403 行）: オフセット付き ISO、YAML 引用、構成、manifest 全文、topic の frontmatter + description / null、document の sources と本文、`deleted: true` / ハードデリート不在、同日 3 メモ（同一分）、UTC 同日 / JST 別日、`##` 本文と CRLF → LF、0 件、メモ 0 件 / 文書 0 件トピック、archived、階層ごとの衝突番号、決定性、OrphanDocument。期待は完全一致文字列。
- `zipArchiveWriter.test.ts`: 往復（パス・内容・`mtime`）、決定性、注入した `zip` の throw → `ArchiveEncodingError`。
- `exportAllData.test.ts`: 読み 1 回 → render → writer、`InvalidTimezone` は gateway 未呼出、`userId` 不正は `ValidationError`、gateway 失敗の素通し（render / writer 未呼出）。
- `exportAllData.integration.test.ts`（実 DO）: 範囲（ゴミ箱除外・最新のみ・archived 含む・ハードデリート出典なし・ゴミ箱出典 `deleted: true`・`postAt` で複数日）、0 件 + 他ユーザー不可視、同一スナップショット 2 回でバイト同一、上限超過は本文を読まず `EXPORT_TOO_LARGE` + `jobs` / `outbox_events` 行数不変。
- web `handler.test.ts`: 200 のヘッダ（`content-type` / `content-disposition` / `content-length` / `no-store` / `nosniff`）と zip 署名、405 / 403（`Origin`・`Sec-Fetch-Site`）/ 401、timezone 欠落 400、不明 zone 422、`EXPORT_TOO_LARGE` は code 保持・他 system は `code: null`。
- dom `exportPanel.dom.test.tsx`: hidden `timezone`、生成中はボタン無し、Blob → `<a download>` click 1 回 + リンク、「もう一度生成」で `revokeObjectURL`、失敗文言、401 の文言 + `/login?redirect=%2Fsettings`、ネットワーク失敗。`currentUserPanel.dom.test.tsx` に「データ」節。
- testcases の 41 行のうち「`SystemError(DatabaseError)`」（DB 障害）は fake の素通しテストで代替、「AI トークンで呼べない」は `tools.test.ts` の禁止断片 `application/export/` / `exportAllData` + `/export` が `isAiRoute` 外であることで担保。

## 2. コードレビュー

- **(a) DO 側 `readExportSourceDto`**: `userDataDurableObject.readExportSource()` は `envelope → runUnitOfWork(() => readExportSourceDto(sql, cap, logger))` で 1 つの `transactionSync`。中身は SELECT 5 文（集計 1 + memos / topics / documents / source_links）だけで INSERT / UPDATE / `enqueueJob` / `enqueueEvent` は無し（UoW ctx を受け取らないので登録点に触れない。`takeRearmRequest()` は false のまま）。集計は `SUM(length(CAST(body AS BLOB)))` = **UTF-8 バイト**で memos.body + documents.body/title + topics.name/description を数え、`> maxBytes` なら本文を読む前に `SystemError(ExportTooLarge, "The export exceeds the size limit")`（件数・バイト数はログの warn のみ）。範囲: memos / documents は `status = 'active'`、topics は `IN ('active','archived')`、本文は行の `body`（= 最新リビジョン）、`source_links` は `JOIN documents d … d.status='active'` で `document_id, created_at, memo_id` 順。`source_links.memo_id … ON DELETE CASCADE`（`userDataPlan.ts` L211）なのでハードデリート済み出典は行が無く、ゴミ箱内の出典は残って renderer が `deleted: true` にする。ADR-002 / spec と一致。実走: ph08v の DO は `jobs=1`（PH-07 由来の done 行）/ `outbox=0` のまま、poison 0。
- **(b) `ExportRenderer.render`**: 純関数（`Intl.DateTimeFormat` のキャッシュ以外に状態なし、`new Date()` を呼ばない）。構成（`index.md` / `memos/YYYY-MM-DD.md` / `topics/{slug}/index.md` / `topics/{slug}/{slug}.md`、メモ 0 件で `memos/` 無し、文書 0 件でも `index.md`）、manifest（spec の YAML と本文どおり）、memo 見出し `## HH:mm (memoId)` + 本文そのまま + LF 正規化 + 末尾 LF 1 つ、topic / document の frontmatter（`yamlString` でダブルクォート + `\"` `\\` `\n` `\r` `\t` `\uXXXX`）、`sources` の順は DTO（`source_links` 順）、相対パス `../../memos/{day}.md`、`deleted: true`、日別は `dayKeyOf(postedAt, timezone)`、オフセットは壁時計との差から算出（`timeZoneName` に依存しない）、`postedAt` 昇順 → `memoId` で安定化、`resolveSlugs` は階層ごと（topics 全体 / 各 topic 内の documents）、`ExportArchive.create` で辞書順ソート + 重複検査、`OrphanDocument` は最初に検査。実走で `トピックA/08:検証*?` → `トピックA08検証`、同タイトル 2 件 → `ドキュメントA1` / `ドキュメントA1-2`、`トピックB 完了08` → `トピックB-完了08`。
- **(c) `createZipArchiveWriter`**: 同期、`zipSync`、`{ level: 6, mtime: archive.exportedAt }`、パスは `${rootDirName}/${path}`、`filename = ${rootDirName}.zip`、例外は `SystemError(ArchiveEncodingError, cause)`。決定性は同一プロセスでバイト同一（O-1）。
- **(d) `POST /export`**: `server.cloudflare.ts` で TanStack より前に `isExportRoute` → `storage.run(container, handleExport)`。405（`allow: POST`）→ 同一オリジン（`Origin` = `new URL(request.url).origin`、無ければ `Sec-Fetch-Site` が `same-origin` / `none` / 不在）→ `readSessionUserId`（`requestSession.ts` に移動。署名・期限・`account.status`・`sessionEpoch`・`NOT_INITIALIZED` を全部 `null` に畳む。SSO handler は import 先を変えただけ）→ 401 `{ kind: validation, code: UNAUTHENTICATED }` → `formData().timezone`（string・≤64）→ 400 `INVALID_INPUT` → `exportAllData(…, createZipArchiveWriter())` → 200 `application/zip` + `content-disposition: attachment; filename="fog-export-YYYYMMDD.zip"` + `content-length` + `no-store` + `nosniff`。失敗は `toClientErrorBody`（`serializeError → redactForClient`、`PUBLIC_SYSTEM_CODES = {EXPORT_TOO_LARGE}` のときだけ `code` を元に戻す。**message は固定文言のまま**なので redaction の意図（内部情報を出さない）は保たれる）+ `httpStatusFor`（`INVALID_TIMEZONE` は business → 422）。AI 経路の `toAiErrorBody` は同関数の再 export で挙動不変（AI から `EXPORT_TOO_LARGE` は到達不能）。
- **(e) `ExportPanel`**: `<form method="post" action="/export">` + hidden `timezone`（SSR は `UTC`、`useEffect` でブラウザの zone に。実走 `Asia/Tokyo`）で JS 無しでも POST できる。JS あり: `preventDefault` → `fetch(form.action, { body: FormData, credentials: "same-origin" })` → `generating` の間は submit ボタンを描かず `role="status"`「生成中…」+ `aria-busy` → `blob()` → `createObjectURL` → `<a download>` を 1 回 click → `done`（「ダウンロード」リンク + 「もう一度生成」）。`useEffect` の cleanup で `revokeObjectURL`（phase 変更・unmount）。失敗は `role="alert"`（401 は専用文言 + `/login?redirect=/settings`、`EXPORT_TOO_LARGE` は `renderSystemMessage` の文言、ネットワーク例外は固定文言）。実走で 401 の行内表示とリンクを確認。
- **(f) 依存と `_meta`**: `fflate ^0.8.3` は `packages/core/package.json` のみ（`apps/web/package.json` に無し、web は `adapters/fflate/zipArchiveWriter` 越し）。`exportSourceReader.ts` / `domain/export/*` に `_meta` / `self_locator` の参照なし（grep 0 件）。アーカイブにも出ない（zip 全体を走査して 0 件）。spec `1d2ce92` の「アーカイブに載せない」と一致。
- **(g) PH-07 対応**: O-2 — `AiRefreshToken.client` を追加し `issueRefresh(uid, cid, client, now)`、`verifyRefresh` は `client` 必須、`handleToken` の refresh 分岐で `form.client_id !== refresh.client` → `invalid_grant`。access も refresh も発行元 `client_id` を持つ。O-3 — `ServiceArgs<T, C = UsecaseContainer>` + `Needs<K> = Pick<UsecaseContainer, K>`、AI 到達 16 usecase が `Needs<"memoGateway">` 等を宣言、`AiToolContainer = Needs<"memoGateway" | "knowledgeGateway" | "searchGateway">`、`toAiToolContainer` は `application/di/` に置き `presentation/ai/**` には gateway 名が現れない（`tools.test.ts` の新 `it` が `\.\w*Gateway\b` と `\b\w+Gateway\b(?!:)` を禁止）。`auth.ts` / `oauth.ts` は `identityGateway` を必要とする `authorizeAiClient` / `consumeAuthorizationCode` に `deps.container`（RequestContainer）を渡すが、gateway 名は書かないので走査を通り、tool 実行側は `AiToolContainer` だけを持つ。O-4 — `origin !== new URL(deps.appUrl).origin`。
- **(h) ban list / lint**: 新しい `*Error` クラスなし（`SystemErrorCode` に `EXPORT_TOO_LARGE` / `ARCHIVE_ENCODING_ERROR` の 2 値のみ、`RETRYABLE_SYSTEM_CODES` 外）、lint warning 増なし。
- その他: `exportAllData` は `Needs<"exportGateway" | "clock">` で `archiveWriter` を第 2 引数に取る（container に載せない = fixture に `fflate` を持ち込まない）。`ExportSourceDto` は epoch ms で運び request 側で `Date` に戻す。`renderWithRouter.BARE_HANDLER_PREFIXES` に `/export`。`SettingsSkeleton` 6 節。

## 3. ブラウザ / スクリプト検証（自分で実行、dev = :3000、preview = :4173）

環境: `lsof -t -i :3000 -i :4173 -i :8765 -i :8766 | xargs kill` → `pnpm build`（22:54:15、exit 0）→ `pnpm dev` / `pnpm preview`（22:54:27 up）。Implementer の `.ai-client.json` は `scratchpad/ai-client.impl8.json` に退避。agent-browser `verify08`（`ph08v@example.com` / `verify08pass`）、`verify08e`（`ph08e@example.com` / `verify08empty`、0 件）、`verify08s`（SSO ログイン）、`verify08p`（preview）。zip は `agent-browser download '<selector>' <path>` で取得し Python `zipfile` で検査（`scratchpad/export8/`）。

### 3.1 投入データ（22:55、テストクライアント `Verify08` で `ph08v` に）

メモ 4 件（`メモ一 蒸留08`（`##` 行を含む複数行）、`メモ二 旧バージョン08` → `update_memo` で `メモ二 新しい本文08`、`メモ三 08`、`削除済みテキスト08` → ゴミ箱）、トピック A `  トピックA/08:検証*?  `（description あり、文書 `ドキュメントA1`（出典 m1 + m2、`replaceAll` で `初版08` → `最新08`）と同タイトル `ドキュメントA1`（出典なし）、`削除される文書08` → ゴミ箱）、トピック B `トピックB 完了08`（文書 1、出典 m3、`archived: true`）、`削除されるトピック08` → ゴミ箱。

### 3.2 dev（`s8b.log`）

| 時刻 | 操作 | 結果 |
|---|---|---|
| 22:55:50 | `/settings` | h2 順: アカウント / ログイン手段 / パスワードの変更 / ゴミ箱の保持期限 / **データ** / AI クライアント接続 / セッション。行: 「エクスポート | Markdown形式。ゴミ箱・履歴は含まれません | [エクスポート]」、hidden `timezone=Asia/Tokyo`、`aria-busy=false` |
| 22:55:54 | 「エクスポート」を `download` | `e1.zip` 2,913 B 保存。行は「ダウンロード[download=fog-export-20260908.zip] / もう一度生成」 |
| | `e1.zip` 検査 | 7 entries、全 `mtime` = 13:55:54（UTC）、非 ASCII 名は UTF-8 flag。`index.md`: `exportedAt: 2026-09-08T22:55:54+09:00` / `timezone: Asia/Tokyo` / counts memos 3, topics 2, documents 3 + 本文 2 行。禁止語（`削除済みテキスト08` / `旧バージョン08` / `削除済み文書本文08` / `初版08` / `削除されるトピック08` / `_meta` / `self_locator`）0 件、CR 無し、全ファイル末尾 LF、辞書順。topic A: `name: "トピックA/08:検証*?"` / `archived: false` / 本文 `トピックAの説明08`。topic B: `archived: true`、本文なし。`ドキュメントA1.md`: `sources` 2 件（`memoId` / `postedAt` / `file: ../../memos/2026-09-08.md`、実在）、本文 `A1 本文 最新08`。`ドキュメントA1-2.md`: `sources: []`。`memos/2026-09-08.md`: `## 22:55 (id)` ×3、`##` 行を含む本文がそのまま |
| 22:55:55 | 「ダウンロード」リンクを `download` | `e1b.zip` = `e1.zip`（`cmp` 一致、同じ Blob） |
| 22:55:56 | 「もう一度生成」→「エクスポート」 | `e2.zip`: 7 ファイル中 6 がバイト同一、`index.md` は `exportedAt` 1 行だけ差、`mtime` 13:55:54 → 13:55:56 |
| 22:55:58 | m2 をゴミ箱へ（`delete`）→ 再生成 | `e3.zip`: counts memos 2、`ドキュメントA1.md` の 2 件目が `memoId` + `deleted: true`（`file` 無し）、1 件目は従来どおり |
| 22:56:10 | `ph08e`（0 件）で `/settings` → エクスポート | `empty.zip` 456 B、`index.md` のみ、counts 0 / 0 / 0 |
| 22:58 | `ph08e` の別タブでログアウト → 元タブで「エクスポート」 | 行内 `role=alert`「ログインし直してください。セッションが切れています ログインする」（`/login?redirect=%2Fsettings`）+ ボタン復帰 |

curl（22:54）: `GET /export` 405、POST 未ログイン 401 `{ kind: validation, code: UNAUTHENTICATED }`、`Origin: https://evil.example` 403、`Sec-Fetch-Site: cross-site` 403、preview 他 origin 403 / 自 origin cookie 無し 401。

### 3.3 preview（22:57、`s8c.log`）

`ph08v` で :4173 ログイン → `/settings` の h2 順は dev と同じ → 「エクスポート」を `download` → `preview.zip` 2,878 B、7 entries、`exportedAt: 2026-09-08T22:57:09+09:00`、counts memos 2 / topics 2 / documents 3（m2 ゴミ箱後）、禁止語 0 件、`deleted: true` あり。行は「ダウンロード / もう一度生成」。

### 3.4 受け入れ済みの再実行

- **R-AI-01（refresh の交換）**: `Verify08`（`f77883a` 後に発行）で `refresh` 200。`clientId` を別値に書き換えて `refresh` → 400 `invalid_grant`「The refresh token was not issued to this client」。PH-07 時点の旧形式 refresh（`ai-client.impl.json`）→ 400 `invalid_grant`「The refresh token is not valid」（`client` 欠落で検証失敗 = 再認可が要る。dev ローカルの接続のみ）。Implementer の PH-08 状態（`PH-08 Seeder`）は 200。`tools/list` 11 本、`list_topics` が `AiToolContainer` 経由で動く。
- **R-AC-02 / R-AC-09（SSO、`requestSession.ts` 切り出し後）**: `ph08v` で SSO 連携 `ph08v-sso` → `sso=linked` → 別セッションで SSO ログイン → `/`、`/settings` に `ph08v@example.com` → 解除 → 外部アカウントの解除ボタン 0 件（残る「Verify08 の接続を解除」は AI 接続）。
- poison 0 件、dev / preview ログに `schema gate` / `poison` / error なし。

## 4. Implementer の検証結果を利用した範囲

自動テスト件数は自分で再実行して一致を確認。ブラウザ / curl は全て自分で実施（Implementer の zip・スクリーンショットは見ていない）。多日分割（△-9）と上限超過は integration の内容を読んで確認し、ブラウザでは踏んでいない。JS 無しの form POST は markup（`method=post action=/export` + hidden `timezone`）と handler の form-encoded 経路（curl）で確認し、JS を切ったブラウザでは実行していない。

## 5. 追加観察の妥当性

- macOS `unzip` の非 ASCII 問題: 妥当。zip 側は UTF-8 flag 付き（`zipfile` で `flag_bits & 0x800` = True）で、Python / `ditto` で展開できる。manual-tests の手順への補足（PH-10）で足りる。
- `EXPORT_TOO_LARGE` の公開: 妥当。許可リストは 1 件で `code` のみ通し message は固定文言のまま、AI 経路からは到達不能。spec/usecases/export.md に code 名を足すかは Manager。

## 6. PH-01〜07 受け入れ済み 32 項目への影響

| 変更 | 影響 | 根拠 |
|---|---|---|
| `ServiceArgs<T, C = UsecaseContainer>` / `Needs` | なし | 既定型で既存呼び出し不変、typecheck 0、unit 全通過 |
| `AiToolContainer` | なし | 11 ツール実走（`tools/list` / `list_topics`）、`handlers.test.ts` |
| refresh token の形（`client`） | 既存 refresh は無効 | dev ローカルの接続だけが該当（deployed 環境は未発行）。access は 1 時間で切れるので再認可 1 回で復帰（3.4） |
| `requestSession.ts` 切り出し | なし | SSO 連携 / ログイン / 解除の実走（3.4）、`session.test.ts` / `ssoHandlers.test.ts` |
| `RequestContainer.exportGateway` | なし | fake roster 8 箇所 + `createTestGateways` 更新、全 unit 通過 |
| `CurrentUserPanel` の「データ」節 / `SettingsSkeleton` 6 節 | なし | P-13 の他 6 節の見出しと機能は不変（3.2 の h2 順）、`currentUserPanel.dom.test.tsx` |
| `errorBody.ts` 共通化（`toAiErrorBody` 再 export） | なし | AI の業務エラー / system の形は PH-07 と同じ（`handlers.test.ts` の `-32000` 文言不含テスト） |
| `SystemErrorCode` +2 | なし | retryable 外、`errorDisplay.test.ts` |

## 7. 検証環境の引き継ぎ

- dev（:3000、`scratchpad/dev8.log`）と preview（:4173、`preview8.log`）は起動したまま。loopback 8765 停止済み。
- `apps/web/.ai-client.json` は `Verify08`（:3000、`ph08v`、active、refresh 済み）。Implementer の元ファイルは `scratchpad/ai-client.impl8.json`。
- 検証アカウント: `ph08v@example.com` / `verify08pass`（メモ 2 + ゴミ箱 2、トピック 2 + ゴミ箱 1、文書 3 + ゴミ箱 1、AI 接続 `Verify08`）、`ph08e@example.com` / `verify08empty`（0 件）。zip は `scratchpad/export8/{e1,e1b,e2,e3,empty,preview}.zip`。
