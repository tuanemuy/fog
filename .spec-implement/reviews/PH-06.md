# PH-06 検証報告 — 認証の残り

- Verifier 実施: 2026-09-08T20:09〜20:30+09:00。対象 HEAD `0f3868e`（`7744a92` `eb2c636` core / `671bb4a` web / `46436e5` テスト / `352fc1d` `0f3868e` fix、Manager spec `96951e7` `f333936`）、ブランチ `feat/spec-implement`。tracked ファイルに未コミット変更なし（`.dev.vars` は一時変更後に復元済み）
- 実装コード・`spec/`・管理ファイルは変更していない。書き込みは本ファイルのみ

## 判定

| ID | 判定 | 根拠 |
|---|---|---|
| R-AC-07 | **合格** | B（dev 20:13〜20:19 / preview 20:21〜20:22）: 依頼フォームは誰にでも出て、登録済み / 未登録とも `?sent=1` の同一応答。Alarm relay → Queue → consumer が `[dev-mail]` を 1 行（登録済み分のみ。未登録分は `nothing-to-send` で ack、`QUEUE … 2/2`）。リンク → 弱いパスワードはトークンを消費せず項目別エラー → `newpass456` → `/password-reset/done`（クレデンシャル一覧 + 「すべて失効」→「失効しました（0 件）」+ タイムラインへ）。同じリンクの再利用 → 「リンクが無効か期限切れです もう一度依頼する」。期限切れ（sqlite で `expires_at` を過去に → 再起動）→ 同じ文言、`credential_version` 不変。旧パスワードは `INVALID_CREDENTIALS`、新パスワードでログイン。P-13 の変更: 誤り → 「現在のパスワードが正しくありません」、5 回目で「試行回数の上限に達しました…」（照合せず、正しい現在パスワードでも同じ = 隠さない）、解除後に「変更しました」、再読み込みでも認証維持。sqlite: `session_epoch` / `reset_version` / `credential_version` が前進、`change_state` / `pending_verifier` クリア、`change_auth_token` NULL、`resume-credential-change` は `done`。preview でも relay → Queue → consumer → `[dev-mail]` → 完了画面。§2 A-1〜A-5, P-1。T: `passwordReset.integration.test.ts`（窓ごと 1 event・ガード・完走、SSO 専用 / 未登録の decoy と沈黙、`sweep-reset-tokens`、`resume-credential-change` の `pending` 前進、`changePassword` のカウンタ、`revokeAll`）、unit `requestPasswordReset` / `executePasswordReset` / `changePassword` / `mailSenders` / `queueHandlers`、dom `passwordReset` / `passwordChangeForm` / `aiConnectionsPanel`。**ただし B-2（§4）を Manager 判断へ** |
| R-AC-02 | **合格** | B（dev 20:19〜20:20）: 「Google で続行」→ `/__dev/sso/google/authorize`（state cookie `fog_sso_state; Path=/auth/sso; HttpOnly; SameSite=Lax; Max-Age=600`）→ 許可 → `redirect` 尊重で `/settings`。ログイン手段 = 「外部アカウント（google）ログインに使用」+「メールアドレス 一意性の予約のみ」、パスワード変更節なし。sqlite: sso / email の 2 行 `active`、email は verifier なし。2 回目は同じ subject でログイン（sso 行数不変）。`/signup` から既存パスワードアドレス → `/signup?sso_error=email_registered` + 「ログインする」、sso 行は増えない（予約が戻る）。キャンセル → `/login?sso_error=cancelled`、フォームは空・再試行可（**O-1**: `role=alert` の文言が出る。P-01「エラー扱いしない」/ TC-17「エラーメッセージは表示されない」との差）。state 不一致（curl）→ 302 `/login?sso_error=failed`。§2 A-7〜A-9。T: `sso.integration.test.ts`（2 行 active / 再ログイン / 既存アドレスで sso 予約が戻る / 2 locator の resume-signup 再駆動が冪等）、unit `registerOrLoginWithSso` / `ssoStateCodec`（セッション鍵と相互不可）/ `devStubSsoProvider` / `ssoHandlers`、dom `authForm` |
| R-AC-09 | **不合格（B-1）** | 条件の文言はすべて満たす — B（dev 20:18〜20:19, 20:23）: P-13「SSO 連携を追加（Google）」→ `/settings?sso=linked` + 行「外部アカウント（google）|解除」、同じ subject を再連携 → `?sso_error=already_used`「既に別のアカウントに連携されています」、解除 → 行が即時に消え再読み込みでも認証維持（`session_epoch` 前進 + `startSession`）、SSO 専用アカウントの唯一の連携を解除 → 「最後のログイン手段は解除できません」で一覧不変、email 行に解除ボタンなし。sqlite: `credential_locators` / `credential_mappings` から消える、`operations` `link:done` `unlink:done`、`resume-link` / `sweep-orphan-mapping` `done`。**しかし連携を 1 回行うごとに SSO bucket に `resume-signup:<linkOp>` が投入され、60 秒後に 4 回失敗して `poison`（`DATA_INTEGRITY_ERROR`）になる**（§4 B-1。連携を保持したまま観測: attempt 4 → poison。解除後も同じ）。`poison` は prune されず operator へエスカレーションされる行なので、正常系の連携が必ず 1 行残すのは受け入れられない。T: `sso.integration.test.ts`（link / unlink / `resume-link` / `sweep-orphan-mapping`）は `resume-link` の行しか見ておらず、SSO bucket の `resume-signup` を検査していない |
| R-INF-03 | **合格** | 4 kind が両 DO の registry に登録され、T で前進を固定: `sweep-reset-tokens`（2 表の掃除 → `finished`）、`resume-credential-change`（`pending` から apply → advance → promote）、`resume-link`（予約後に落ちた link を activate + complete）、`sweep-orphan-mapping`（bucket 行の削除 + `done`）。dev で `resume-credential-change` / `resume-link` / `sweep-orphan-mapping` が `done`、`sweep-reset-tokens` が窓末尾（20:15 / 20:30）に `pending`。`poison` + `terminal_reason` は既存 runner（`352fc1d` の場面と B-1 / B-2 で実際に `DATA_INTEGRITY_ERROR` が残ることを dev で観測）。**G-1（§5）**: 4 kind のどれも「失敗 → `terminal_reason`」を PH-06 のテストで固定していない（`resume-credential-change` の `'advanced'` 分岐も未固定） |

### Manager の判断が必要な事項

1. **B-1（R-AC-09 差し戻し推奨）**: `linkSsoCredential` の予約（`coordinator` role）で `reserveSignupCredentialProcedure` が `resume-signup:<op>` を投入し、`resume-signup` handler が既存アカウントに対して登録 saga を再駆動して失敗 → `poison`。spec/async の `resume-signup` 投入点は「新規登録 saga の予約行」で、連携の予約は `sweep-reservations` の 3 箇所目としてのみ挙がっている。修正候補: (a) 予約 DTO に「saga の種類」を持たせ link では `resume-signup` を投入しない、(b) `activateReservation` / `completeLink` が `resume-signup:<op>` を `done` に閉じる（`352fc1d` と同じ手）、(c) handler が `operations` の `link` 記録を見て `finished`。テストは「連携後に SSO bucket の `resume-signup` が `done`（または存在しない）」を固定する
2. **B-2（R-AC-07、同じ根）**: `beginCredentialChange` が `credential_mappings.operation_id` を変更 saga の op で上書きし、`promoteVerifier` も戻さない（`change_state` / `change_origin` / `pending_verifier` は NULL に戻るが `operation_id` は残る）。登録直後 60 秒以内（`resume-signup` が走る前）にリセット / 変更を完走すると、`commitSignupSaga` の CAS（`operation_id = ?`）が外れて `resume-signup` が `poison` になる。preview の `ph06v-two`（20:21:5x 登録 → 20:22:3x リセット完走）で再現: bucket `f0a2901d` の mapping `operation_id` は変更 op `01a080c1…`、`resume-signup:01a080c0…` は `poison`。稀だが正常系。修正候補: `operation_id` を saga 種別ごとに分ける、または `promoteVerifier` で元の値へ戻す / `resume-signup` が `saga_committed = 1` の行を `finished` にする。spec/database の `operation_id` 列の契約（どの saga が書くか）の確認も
3. **O-1（R-AC-02、軽微）**: SSO キャンセルで `/login?sso_error=cancelled` に `role=alert`「外部アカウントでの認証が中断されました」を出す。P-01「SSO中断: 初期状態に戻る（エラー扱いしない）」/ TC-17「エラーメッセージは表示されない」と食い違う。文言を出さないか `role=status` の中立表示にするか
4. **G-1（R-INF-03）**: 4 kind の失敗 → `terminal_reason` と `resume-credential-change` の `'advanced'` 分岐をテストに足すか（本フェーズ受け入れ条件の末尾「OCC 競合は `terminalReason` に残る」を直接固定するテストは無い）

## 1. 自動テスト（自分で実行、20:09:26〜20:09:44）

- `pnpm typecheck` 0 件、`pnpm lint` 0 件（warning 3 は既存）、`pnpm format:check` 差分なし
- `pnpm test`: unit+dom **102 files / 1,395 passed + 2 skipped**、integration **19 files / 120** — 報告と一致
- 契約テストの skip は分かる形: `resendMailSender.contract.test.ts` / `googleSsoProvider.contract.test.ts` が `it.skipIf(!configured)` で本体を skip し、同じ describe に「is skipped without MAIL_PROVIDER_API_KEY and MAIL_CONTRACT_TEST_TO」/「…without GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET」が常に走って理由を名乗る（2 skipped の内訳）
- 新テストと完了条件: R-AC-07 ← `passwordReset.integration`（窓 / 決め / ガード / 完走 / sweep / resume-credential-change / changePassword / revokeAll）、`requestPasswordReset.test`（`claimWindow` だけで分岐、同一窓は何も書かない、sweep は必ず投入）、`executePasswordReset.test`（消費前の検証、`RESET_TOKEN_INVALID`、`PasswordNotSupported`）、`changePassword.test`（照合前の `TOO_MANY_ATTEMPTS`、カウンタ、SSO 専用、進行中、`OPTIMISTIC_LOCK_FAILURE`）、`queueHandlers.test`（send + ack / nothing-to-send + ack / 未知 type・routing key は retry / 例外は retry かつ token を log しない / DLQ は id・type のみで ack）、`mailSenders.test`（console 1 行、Resend の `Idempotency-Key`、非 2xx / 通信失敗の `SystemError`）、dom `passwordReset` / `passwordChangeForm`。R-AC-02 ← `sso.integration` (a)(b)(c)、`registerOrLoginWithSso.test`、`ssoStateCodec.test`、`ssoHandlers.test`、dom `authForm`。R-AC-09 ← `sso.integration` (d)〜(g)、`linkUnlinkSso.test`、dom `credentialList` / `currentUserPanel`（**B-1 は未固定**）。R-INF-03 ← 上記の 4 handler ケース（**G-1**）

## 2. ブラウザ検証（自分で実行）

`agent-browser --session verify06`。dev = http://localhost:3000、preview = http://localhost:4173 — **両方とも自分で立て直し**（`lsof -t -i :3000 -i :4173 | xargs kill` → `pnpm build`（20:12:15）→ `pnpm dev` / `pnpm preview`、ログは scratchpad `dev6*.log` / `preview6.log`）。`[dev-mail]` は consumer の `max_batch_timeout = 30` 秒後にログへ出る（最初の 6 秒待ちでは出ず、35 秒後に出た）。sqlite は Identity Directory の bucket 別ファイル（`…IdentityDirectoryDurableObject/<hash>.sqlite`、先頭 8 桁で表記）と User Data。

### dev

| # | 時刻 | 操作 | 結果 |
|---|---|---|---|
| A-1 | 20:13:19 | `/signup` で `ph06v-pw@example.com` / `password123`（R-AC-01 回帰） | `/` へ。`/settings` = メールアドレス行「ログインに使用」、パスワード変更節あり、「SSO 連携を追加（Google / Apple）」。bucket `adcb2e43` に email `active` + verifier、`resume-signup` `pending`（60 秒後に `done`） |
| A-2 | 20:13:40 | `/password-reset` で登録済み → 未登録 `nobody-ph06v@example.com` | どちらも `?sent=1`「登録されていれば、リセット用のメールを送信しました…」。`outbox_events` は両 bucket に 1 行ずつ `published`（payload = `{"tokenId","mailKind":"password-reset"}` のみ、`owner_token` あり）、`reset_request_windows` 1 行ずつ、`sweep-reset-tokens` `pending` 20:15:00（窓末尾）、`password_reset_tokens` は登録済み bucket だけ 1 行。35 秒後の dev ログ: `[dev-mail] to=ph06v-pw@example.com url=…/password-reset?token=1.8.<secret>` 1 行 + `QUEUE tanstack-start-template-events 2/2` |
| A-3 | 20:15:35 | リンクを開く → `short` → `newpass456` → done → すべて失効 | `<meta name="referrer" content="no-referrer">`。`short` → 「パスワードは8文字以上128文字以下で入力してください」、トークン未消費のまま。`newpass456` → `/password-reset/done`「パスワードを再設定しました。他の端末のセッションはすべて終了しています。」+「ログイン手段の確認」（email 行、連携追加なし）+「AI クライアント接続の確認」→ 「失効しました（0 件）」+「タイムラインへ」。sqlite: mapping `credential_version` 1 → 2、`change_state` / `pending_verifier` NULL、`failed_attempts` 0、token `used_at` あり / `change_auth_token` NULL、`resume-credential-change` `pending`（→ 60 秒後 `done`）、account `session_epoch` 1 / `reset_version` 1、locator `credential_version` 2 |
| A-4 | 20:16:1x | 同じリンクを再利用 → `anotherpass789` | 「リンクが無効か期限切れです もう一度依頼する」（`/password-reset` へ） |
| A-5 | 20:16:2x | ログアウト → `password123` / `newpass456` | 旧: 「メールアドレスまたはパスワードが正しくありません」、新: `/` |
| A-6 | 20:17:34 | P-13 パスワード変更: `wrongpass` ×5 → 6 回目 → 正しい現在パスワードでも → 16 秒待って正しい | 1〜5 回目「現在のパスワードが正しくありません」（`failed_attempts` 1→5、`next_attempt_allowed_at` 20:18:25）、6 回目「試行回数の上限に達しました。しばらくしてからお試しください」、制限中は正しい現在パスワードでも同じ文言（照合しない）。解除後: 「変更しました」、`credential_version` 3、`failed_attempts` 0、`/settings` 再読み込みで認証維持 |
| A-7 | 20:18:3x | `SSO 連携を追加（Google）` → スタブ `ph06v-link` 許可 | `/settings?sso=linked`「外部アカウントを連携しました」+ 行「外部アカウント（google）|ログインに使用|解除」。bucket `132deaae` に sso `active`、User Data `operations` `link:done`、`resume-link` `done` |
| A-8 | 20:18:5x | 同じ subject を再連携 → 解除 → 再読み込み | `?sso_error=already_used`「この外部アカウントは既に別のアカウントに連携されています」、行数不変。解除 → 即時 1 行 → 4 秒後も 1 行・alert なし → 再読み込み `/settings` で 1 行（認証維持）。mapping 行消去、`operations` `unlink:done`、`sweep-orphan-mapping` `done`、`credential_locators` は email のみ |
| A-9 | 20:19:47 | ログアウト → `/login?redirect=%2Fsettings` → Google → `ph06v-sso` / `ph06v-sso@example.com` | `/settings`（redirect 尊重）。h2 に「パスワードの変更」なし、行 = 「外部アカウント（google）|ログインに使用|解除」「メールアドレス|一意性の予約のみ」。sqlite: sso / email 各 `active`、email は verifier なし。解除 → 「最後のログイン手段は解除できません」、2 行のまま。ログアウト → 同じ subject → `/`（sso 行数 2 のまま = 新規作成なし） |
| A-10 | 20:20:0x | `/signup` から subject `ph06v-other` + 既存 `ph06v-pw@example.com` | `/signup?sso_error=email_registered`「このメールアドレスは既に登録されています。パスワードでログインしてください ログインする」、sso 行 2 のまま（予約が戻る）、`reserved` 行なし |
| A-11 | 20:20:3x | キャンセル | `/login?sso_error=cancelled`、email / password 欄空、「Google で続行」再試行可。**`role=alert`「外部アカウントでの認証が中断されました」（O-1）** |
| A-12 | 20:20:5x | curl: `callback?state=bogus` / `apple/start` / `github/start` / `google/start` | 302 `/login?sso_error=failed` / 302 スタブ / **404** / `set-cookie: fog_sso_state=…; Path=/auth/sso; HttpOnly; SameSite=Lax; Max-Age=600` |
| A-13 | 20:23:26〜20:25:07 | **B-1 の確認**: `ph06v-link2` を連携して保持 → 80 秒待つ | 連携直後 bucket `41763742` に `resume-signup:01a080c2…` `pending`（`next_run_at` +60 秒）→ 80 秒後 `attempt 4`（→ `poison`）。dev ログ `Job execution failed { kind: 'resume-signup', … cause: 'DATA_INTEGRITY_ERROR' }` ×4。P-13 の行は正常 |
| A-14 | 20:26:14〜20:27:35 | 新しい窓で再依頼 → リンク取得 → dev 停止 → `expires_at` を過去に → 再起動 → リンク | 「リンクが無効か期限切れです もう一度依頼する」、`credential_version` 3 のまま |
| A-15 | 20:28:23〜20:29:07 | `.dev.vars` の `SSO_DEV_STUB="false"`（`GOOGLE_*` 未設定）で再起動 → 復元して再起動 | `/login` / `/signup` に SSO 節（「または」区切りとボタン）なし、P-13 に連携導線なし、`/auth/sso/{apple,google}/start` と `/__dev/sso/google/authorize` が 404。復元後は Google / Apple の両ボタン |

### preview（20:21:56〜20:22:46）

| # | 操作 | 結果 |
|---|---|---|
| P-1 | `/signup` で `ph06v-two@example.com` | `/` へ。`/settings` にパスワード変更節と連携導線 |
| P-2 | ログアウト → `/password-reset` で依頼 | `?sent=1`。28 秒後に preview ログ `[dev-mail] to=ph06v-two@example.com url=http://localhost:3000/password-reset?token=1.7.…` + `QUEUE tanstack-start-template-events 1/1`（**relay → Queue → consumer が preview ワーカーで動く**。URL の host は `APP_URL` = :3000 なので :4173 に読み替えて開いた） |
| P-3 | 新パスワード `previewpass1` | `/password-reset/done`「パスワードを再設定しました…」+ 2 節。`errors` なし |

## 3. Implementer の検証結果を利用した範囲

- 利用なし。すべて自分で再実行し、報告の B 欄と一致した。差異: 報告は連携後の SSO bucket の `resume-signup` を見ていない（B-1）。報告の「dev の既存 poison 行 1 件」は 2 件（`49061e34` / `78c82d94`）あり、いずれも Implementer の作業由来

## 4. コードレビュー

### 不具合

- **B-1（R-AC-09）正常系の SSO 連携が `resume-signup` の `poison` 行を残す。** 再現: P-13 で連携 → 60 秒待つ → SSO bucket の `jobs` に `resume-signup:<linkOp>` が `attempt 4` → `poison` / `terminal_reason = DATA_INTEGRITY_ERROR`（A-13、解除の有無に関係なし）。原因: `application/identity/reserveSignupCredential.ts` L67–73 が `dto.coordinator.role === "coordinator"` なら無条件に `resume-signup` を投入し、`linkSsoCredential` の予約も `coordinator` role で行う（L64）。`jobs/resumeSignup.ts` は既存アカウントに `initializeAccount` を打ち、`commitSignupSaga` の CAS（`operation_id = ?` / `status IN ('reserved','active')`）が外れて「reservation lost before the saga mark」→ backoff → `poison`。spec/async の `resume-signup` 投入点は「新規登録 saga の予約行」のみ。CLAUDE.md「poison は prune されず operator へエスカレーション」に照らして受け入れ不可
- **B-2（R-AC-07）`beginCredentialChange` が mapping の `operation_id` を変更 saga の op で上書きし、`promoteVerifier` が戻さない。** `stores/credentialMappingStore.ts` の `beginCredentialChange`（`SET … operation_id = ?`）と `promoteVerifier`（`SET password_verifier = pending_verifier, pending_verifier = NULL, change_state = NULL, change_origin = NULL, credential_version = ?, failed_attempts = 0, next_attempt_allowed_at = NULL` — `operation_id` を含まない）。登録から 60 秒以内にリセット / 変更を完走すると、その後に走る `resume-signup:<signupOp>` の `commitSignupSaga` CAS が外れて `poison`。preview の `ph06v-two` で再現（bucket `f0a2901d`: mapping `operation_id = 01a080c1…`（変更 op）、`resume-signup:01a080c0…` `poison`）。`resume-credential-change` 側の `markAdvanced` / `promote` も `operation_id` 一致を CAS 条件にしているので、列の共用は設計上の衝突

### 指摘（不具合ではない）

- (a) Outbox: `requestPasswordResetProcedure` は `claimWindow` の戻り値だけで分岐（L41）、`findByLocator` はその内側、`tokenId` は `issue()` / `mintDecoyTokenId()` の同じ `opaque()`（16 byte CSPRNG → base64url 22 文字）、`enqueueEvent` は同じ procedure = 同じ `runUnitOfWork` tx、payload は `{ tokenId, mailKind }` のみ（A-2 で sqlite 確認。PII / 生トークン / userId なし）、`sweep-reset-tokens` は 4 ケース共通で窓末尾に投入
- (b) 送信材料 RPC `readResetMailMaterials`: 引数長 < 22 → `nothing-to-send`、行不在 / `quarantined` / `owner_token NULL` → `nothing-to-send`、`timingSafeEqual` で一致、`status` は条件に無い（`published` でも `send`）。応答は `send` / `nothing-to-send` の 2 分岐で理由なし。`providerIdempotencyKey` は DO の `PROVIDER_IDEMPOTENCY_KEY`（≥ `MIN_KEYRING_SECRET_LENGTH`）で HMAC 導出、`IDENTITY_RESET_TOKEN_KEY` も DO 内で読むだけ。RPC は migration ゲートの内側（`enterRpc`）で書き込みなし
- (c) consumer `queueHandlers.ts`: 業務判断なし（type と routing key の形だけ）、失敗は `message.retry()`、成功 / `nothing-to-send` は `ack()`、ログは `eventId` / `type` / `error.name` まで（token / 宛先 / 本文なし）、DLQ handler は `warn("dlq", { eventId, type })` + `ack()`（batch 例外は `ackAll()`）、転送なし。Queue メッセージは PH-01 の `OutboxQueueMessage` 5 項目のまま
- (d) 衛生: `createQueueContainer` は `MAIL_DEV_SINK` が設定されているときだけ console sink（`"console"` 以外は `ConfigurationError`）、それ以外は Resend（分岐なし）。`wranglerConfig.test.ts` が deployed 3 config に `MAIL_DEV_SINK` / `SSO_DEV_STUB` が無いことと `MAIL_FROM_ADDRESS` を固定。spec/async `96951e7` の宣言済み例外どおり
- (e) `executePasswordReset`: `PlainPassword.create` → `consumeResetToken`（`verifyAndConsume` の条件付き UPDATE で `used_at` / `change_auth_token`）→ `hasVerifier` → `hash` → `runCredentialChange`（begin: `change_auth_token` CAS → NULL / apply: `advanceSessionEpoch` + `advanceCredentialVersion` + `advanceResetVersion` → `revokeCreatedAtResetVersion(after − 1)` / markAdvanced / promote）→ presentation が `startSession`。A-3 の sqlite どおり
- (f) `changePassword`: `nextAttemptAllowedAt > now` なら照合前に `TOO_MANY_ATTEMPTS`（P-13 は表示、P-01 は従来どおり `INVALID_CREDENTIALS`）、不一致は `recordAttemptOutcome` + `CURRENT_PASSWORD_MISMATCH`。A-6
- (g) SSO: `ssoStateCodec` は `SESSION_SECRET` から HMAC-SHA256 label `fog:sso-state` で派生（unit で相互不可）、cookie 属性は A-12、callback は cookie と `state` の一致 + 署名 + `provider` 一致を要求。初回登録は `runSignupSaga` の 2 クレデンシャル（sso coordinator / email member `usableForLogin: false`）、既存アドレスは `EMAIL_ALREADY_REGISTERED` で `cancelReservation` により sso 予約を戻す（自動リンクなし）、キャンセルは `"cancelled"` で例外を投げない（O-1 は表示だけ）。link: `beginLink`（記録 + `resume-link`）→ 予約 → activate → `completeLink`、敗北は `finishLink` + `SSO_IDENTITY_ALREADY_REGISTERED`。unlink: `User.removeCredential`（`LAST_CREDENTIAL_REMOVAL` / email 拒否は domain）→ 全世代の locator 退避 → `deleteByCredentialId` → `advanceSessionEpoch` → `sweep-orphan-mapping` → 各 bucket `deleteMapping`（`user_id` + `caller_token` 束縛）
- (h) ジョブ: 4 handler は入力の状態から冪等に判定（行なし / `done` / 他 op → `finished`）、失敗は throw → runner の backoff → `poison` + `terminal_reason`。`352fc1d`: `cancelReservation` が coordinator 行なら `resume-signup:<op>` を `done` に（A-10 で sso 行数不変・`reserved` なし）。ただし B-1 / B-2 の 2 経路が同じ穴として残る
- (i) `configuredSsoProviders`: `SSO_DEV_STUB="true"` → `["google","apple"]`、`GOOGLE_*` → `["google"]`、無ければ `[]`。P-01 / P-02 / P-13 と bare handler（一覧外は 404）が同じ一覧を見る（A-15）
- (j) `.dev.vars.example` の帰属表: `IDENTITY_RESET_TOKEN_KEY` = state、`MAIL_*` / `SSO_*` / `GOOGLE_*` = request、`MAIL_FROM_ADDRESS` は `[vars]`（render 時の環境変数）。`resetTokenKey()` / `providerIdempotencyKey()` は ≥ 32 文字を要求、`ssoStateCodec` は session floor
- (k) ban list / lint: 新しいエラークラスなし、`pnpm lint` 0 件（warning 3 は既存）、`banList.test.ts` 緑

## 5. 追加観察

- Implementer の観察（`Alarm stopped at the schema gate { cause: 'CONFIGURATION_ERROR' }`）: 妥当。再起動直後の dev ログに出て（`dev6b.log` 1 回、`dev6c.log` 0 回、`dev6.log` では `Alarm re-arm failed` を伴う）、名前付きアクセス後は出ない。PH-06 の変更前からの挙動で、fail-closed 再武装が働いている。ただし dev では「誰も触らない bucket の `pending` ジョブは次の名前付きアクセスまで走らない」ので、`058f6cd0` / `50463eca` の `resume-signup` が 19:5x から `pending` のまま — 環境要因
- **O-1**: SSO キャンセルの `role=alert`（冒頭 3）
- **G-1**: R-INF-03 の「`terminalReason` に残る」と `resume-credential-change` の `'advanced'` 分岐が未固定（冒頭 4）
- O-2: dev と preview が同じ `.wrangler/state` を別プロセスで開く（PH-05 と同じ限界）。B-2 の観測は preview 側で起きたが、原因は sqlite の `operation_id` の値で説明でき、二重プロセスの影響ではない
- O-3: `[dev-mail]` の URL は `APP_URL`（`wrangler.toml` = :3000）で組み立てるので preview の consumer も :3000 の URL を出す（△-8 / MailSender の「base URL は設定値だけから」どおり）
- O-4: リンクの再依頼は 15 分の窓単位（`resetRequestWindowMs`）なので、同じアドレスでの再検証は窓をまたぐ必要がある（A-14 は 20:15 の窓で成立）

## 6. PH-01〜05 受け入れ済み 24 項目への影響

- **R-AC-01（登録 saga）**: `registerWithPassword` は `runSignupSaga`（1 クレデンシャル）へ委譲、`InitializeAccountDto.credential` → `credentials[]`、`resume-signup` が全 locator を扱う。A-1 / P-1 で登録 → `/`、`resume-signup` `pending` → `done`（`92c10456` / `944a67ed` で確認）。`signupSaga.integration` 緑。**B-2 は登録直後 60 秒以内の変更という条件付き**で R-AC-01 の完走そのものには影響しない
- **R-AC-03 / R-AC-04**: `loginWithPassword` / `logout` は不変。A-5 / A-9 でログイン・ログアウト
- **R-AC-08**: `CurrentUserPanel` が `CredentialList` / `PasswordChangeForm` / `AiConnectionsPanel` に分割。email / 一覧 / 保持期限は不変（A-1 / A-9 の `/settings`）。`currentUserPanel.dom` 更新済み
- **R-INF-01 / R-INF-02**: `AuthForm` に SSO 節と「パスワードをお忘れの方」が増えるだけ。ナビ 5 項目不変
- **R-TL-* / R-DT-* / R-SE-01 / R-TR-* / R-ST-01**: memo / knowledge / search / trash のコード変更なし。`RequestContainer` は不変（`ssoIdentityProvider` / `mailSender` は presentation / queue の DI）、`UserDataUnitOfWorkContext.aiClientConnectionRevoker` の追加はテストコンテナのみ。`IdentityGateway` +17 は fake roster 更新。`PROVIDER_IDEMPOTENCY_KEY` の ≥ 32 文字要件は `vitest.config.do.ts` と `.dev.vars` を延長済み（送信材料 RPC が要求。既存の relay は影響なし）。integration 19 / 120 緑
- `wrangler.toml` に `MAIL_FROM_ADDRESS`、`wrangler.state.toml` の自己参照 bindings（`resume-link` / `sweep-orphan-mapping` / `resume-credential-change` が使う）— `wranglerConfig.test.ts` 緑

## 7. 検証環境の引き継ぎ

- 起動中: dev PID 68162（`.dev.vars` は元どおり `SSO_DEV_STUB="true"`、ログ scratchpad `dev6e.log`）、preview PID 62596（`0f3868e` のビルド、`preview6.log`）。Implementer の dev / preview は停止済み
- 追加したアカウント: `ph06v-pw@example.com`（パスワード `brandnew789`、`ph06v-link2` を連携中 = B-1 の poison 行が bucket `41763742` に残る）、SSO `ph06v-sso`（`ph06v-sso@example.com`）、`ph06v-two@example.com`（preview、パスワード `previewpass1`、B-2 の poison 行が bucket `f0a2901d`）。既存の poison 行: `49061e34` / `78c82d94`（Implementer 由来）、`132deaae`（B-1、解除済み）
- agent-browser: session `verify06`（dev は `ph06v-pw` でログイン中、preview は `ph06v-two` の新セッション）
- scratchpad: `checks6.log`、`build6.log`、`dev6.log`〜`dev6e.log`、`preview6.log`、`.dev.vars.bak`

---

# 再検証（差し戻し対応 HEAD `9ac3024`、2026-09-08 20:45〜21:12 JST）

対象: `0f3868e..9ac3024`（Manager の spec 修正 `b0bbd6f` を含む 19 ファイル）。作業ツリーは追跡ファイル clean。

## 判定

| ID | 初回 | 再検証 | 根拠（詳細は下） |
|---|---|---|---|
| R-AC-09 SSO 連携・解除 | 不合格（B-1） | **合格** | dev で連携 2 件（`ph06r-link3`→b3、`ph06r-link5`→b5）とも 80 秒後に bucket の `jobs` は `sweep-reservations` のみ、`resume-signup` 無し。連携→解除→再連携→当該主体で SSO ログイン成功。preview も連携・解除・キャンセル動作 |
| R-AC-07 パスワード変更 | 合格（B-2 付き） | **合格（B-2 解消）** | 登録 9 秒後にパスワード変更 → 60 秒後 `resume-signup` が `done` / `attempt 0` / `terminal_reason NULL`、`resume-credential-change` も `done`。新パスワードでログイン成功 |
| R-AC-02 ログイン | 合格（O-1 付き） | **合格（O-1 解消）** | `/login?redirect=%2Fsettings` からのキャンセルは同 URL に戻り `role=alert/status` 無し・`sso_error` 無し（dev / preview とも）。`/signup` 起点・`/settings`（link intent）起点も文言無し |
| R-INF-03 jobs/poison | 合格（G-1 付き） | **合格（G-1 解消）** | `jobTerminal.integration.test.ts` は 4 kind すべてを実 Alarm（`runDurableObjectAlarm`）で天井まで回し `poison` + `terminal_reason` + `attempt = jobsMaxAttempts-1` を検証（後述 5 節） |

**PH-06 全 4 項目 合格。** 受け入れ済み項目への影響も検出せず（6 節）。

### Manager の判断が必要な事項

- **O-2（新規観察・9ac3024 の変更ではない）: ローカル workerd で Alarm 起床した DO は `ctx.id.name` が `undefined` になり、`requireSelfLocator()` → schema gate が `CONFIGURATION_ERROR` で止まる。しかもその name 無しインスタンスがそのまま常駐するため、以後そのユーザー/bucket への通常 RPC も `CONFIGURATION_ERROR` になる。** 実害として dev で `ph06v-pw` の `/settings` が「読み込めませんでした」（`getCurrentUser` → `Durable Object was reached through a stub that carries no name`、20:52:29、`dev6r.log` L136）になり、dev 再起動まで回復しなかった。また Implementer が dev を止めた時点で期限を過ぎていた `resume-signup`（b0 / b15、`next_run_at` 19:51〜19:52、`attempt 0`）と `sweep-reset-tokens`（b2 / b6 / b11、20:00）は起動直後から gate ループ（`Alarm stopped at the schema gate` ×12 → 再起動後さらに ×20、preview 側ログにも ×5）に入り、21:11 時点でも `pending` のまま一度も実行されていない。
  - 判断材料: `durableObjectBase.ts` L96〜113 の JSDoc は「`idFromName` 経由なら constructor でも読める」と断言し、`docs/runtime_cloudflare.md` にこの限界の記述は無い。Cloudflare 公式 docs（`durable-objects/api/id/`）は「alarm handler 内でも `ctx.id.name` は保持される。ただし 2026-03-15 以前に作られた alarm では `undefined`」としており、本番では設計どおり動く可能性が高いが、手元の `workerd@1.20260508.1 / miniflare@4.20260508.0` では再現しなかった（＝少なくともローカルでは保証が破れている）。CLAUDE.md の「保証は test か型で守るか、限界を併記する」に照らすと、docs/JSDoc への限界の明記（またはローカル向けの name フォールバック）が要るかは Manager 判断。コードは `16e36ce` 由来で PH-06 の差分ではないため、本フェーズの合否には含めていない。
  - 副次: 同じ `.wrangler/state` を dev と preview の 2 プロセスが共有しており、両者が同じ DO の Alarm を奪い合う（`preview6r.log` にも gate ログ）。preview の SSO コールバックは `APP_URL` の :3000 に着地する（初回と同じ）。

## 1. 自動テスト（自分で実行、20:45:30〜20:45:48）

`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を HEAD `9ac3024` で実行（`checks6b.log`）: typecheck 0 / lint 0（warning 3 件は初回と同じ既存の `${MAIL_FROM_ADDRESS}` テンプレート指摘）/ format:check 0 / test 0。unit+dom **102 files / 1,395 passed + 2 skipped**、integration **20 files / 126 passed** — Implementer 報告と一致。

## 2. ブラウザ検証（自分で実行、dev = :3000、preview = :4173）

両サーバーとも自分で起動（`lsof -t -i :3000 -i :4173 | xargs kill` → `pnpm build`（20:46:00、exit 0）→ `pnpm dev` / `pnpm preview`、20:46:11 up）。`.dev.vars` は `MAIL_DEV_SINK="console"` / `SSO_DEV_STUB="true"` のまま変更していない。DO 状態は `.wrangler/state/v3/do/…/<hash>.sqlite` を `sqlite3` で読み取り（`__miniflare_do_name` で bucket 名を解決）。

### B-1（R-AC-09）dev

| 時刻 | 操作 | 結果 |
|---|---|---|
| 20:48:40 | `ph06v-pw` でログイン → `/settings` | 連携 0 件（Implementer の掃除後） |
| 20:48:55〜20:49:02 | 「SSO 連携を追加（Google）」→ stub に `ph06r-link3` / 許可 | `/settings?sso=linked`「外部アカウントを連携しました」、解除ボタン「外部アカウント（google）を解除」出現 |
| 20:49:02 / 20:50:22 | bucket **b3**（`e5d5dbf3`）の `jobs` | 直後・80 秒後とも `sweep-reservations pending attempt=0 next=+1h` の 1 行のみ。`resume-signup` 無し。mapping は `sso active user_id=candidate_user_id=01a080b9…` |
| 20:50:37 | 同じ主体で再度「連携を追加」 | `sso_error=already_used`「既に別のアカウントに連携されています」（`spec/testcases/identity/linkSsoCredential.md` L16 のとおり自アカウント重複も `ConflictError`） |
| 20:52:29 | `/settings` 再表示 | **「読み込めませんでした」= O-2**（上記）。dev を 20:55:14 に再起動して続行 |
| 21:02:20〜21:02:38 | `ph06r-v2`（B-2 で作成）でログイン → `ph06r-link5` を連携 | `sso=linked`。credential は bucket **b5**（`41763742`、`01a080e6-07d0 sso active`） |
| 21:03:59 | b5 の `jobs`（80 秒後） | `sweep-reservations pending`（既存行）+ 初回ラウンドの `resume-signup done`（20:21 完了）のみ。**新規 `resume-signup` 無し** |
| 21:06:27 | 解除ボタン | 一覧から消える。リロード後も 0 件 |
| 21:06:44 | 同主体 `ph06r-link5` を再連携 | `sso=linked`、解除ボタン再出現 |
| 21:08:05 | b5 の `jobs`（再連携 80 秒後） | 変化なし（`resume-signup` 追加されず） |
| 21:05〜 | 別セッションで `/login` → SSO `ph06r-link5` | `/` に着地（ログイン成立） |

（21:00:06 の最初の「解除」失敗は私のスクリプトが RSC ストリーミング完了前にボタンを探した誤操作。待ち時間を入れた 21:06:27 で成功。）

### B-2（R-AC-07）dev

| 時刻 | 操作 | 結果 |
|---|---|---|
| 20:56:29 | `/signup` で `ph06r-v2@example.com` / `firstpass123` 登録 | `/` に着地。bucket **b4**（`14d29974`）に `resume-signup pending attempt=0 next=1788868645718`（= 登録 +60 秒） |
| 20:56:38 | `/settings` でパスワード変更（`secondpass456`） | 「変更しました」（登録 9 秒後） |
| 20:58:11 | b4 の `jobs` / `credential_mappings` | `resume-signup … done attempt=0 completed_at=1788868645736 terminal_reason=NULL`（予定時刻の 18 ms 後）、`resume-credential-change … done attempt=0`。mapping: `email active saga_committed=1 operation_id=01a080e0-85aa…`（= 変更 saga の id に上書き済み）`change_state=NULL credential_version=2` |
| 20:57:5x | 新セッションで新パスワードでログイン | `/` に着地 |

spec の解釈: `spec/database/index.md` L726 は `operation_id` を「予約を取った操作。`activateReservation` では消さない。**次に上書きされるのはクレデンシャル変更 saga の開始時であり、したがって可変列である**」と定義しており、`beginCredentialChange` が上書きすること自体は spec どおり。よって修正箇所を store ではなく `resume-signup` handler（`status='active' AND saga_committed=1 AND user_id=candidate` なら CAS フェーズをスキップし冪等フェーズだけ再実行）に置いた Implementer の解釈は **spec と整合**。`b0bbd6f` の spec 変更は OAuth `jti` テーブル名の追記で、この列の定義は変わっていない。

### O-1（R-AC-02）dev / preview（20:55:42〜20:56:20、新規セッション）

- `/login?redirect=%2Fsettings` → SSO ボタン（`/auth/sso/google/start?redirect=%2Fsettings`）→ stub で「キャンセル」→ **`/login?redirect=%2Fsettings`** に戻り `[role=alert],[role=status]` 空、`sso_error` 無し、Cookie 空（state cookie 消去済み）。dev / preview とも同じ。
- `/signup` 起点 → `/signup` に戻り文言無し（両環境）。
- preview `/settings`（link intent）起点 → `/settings` に戻り文言無し（21:11）。

### preview（21:10:12〜21:11）

`ph06v-two` でログイン → 連携 `ph06r-plink`（`sso=linked`、解除ボタン 2 件）→ 解除（1 件に減少）→ 再連携 → `already_used` → キャンセル → 文言無し。コールバックは `APP_URL` の :3000 に着地するので、ハンドラ本体は dev と共通、preview 側で確認できたのは start エンドポイント・設定画面のレンダリング・`/login` キャンセル復帰。

### R-AC-01 影響確認 dev（21:08:47〜21:10:07）

`ph06r-v3@example.com` を通常登録（パスワード変更なし）→ 70 秒後 bucket b6 に `resume-signup done attempt=0 terminal_reason=NULL`、`/settings` にメール表示。handler 変更後も通常経路は `commitSaga` を経て `done`。

### poison 走査

21:11:19 時点で Identity Directory 14 bucket / User Data 全 DO に `status='poison'` 行なし。dev / preview ログに `poison` 出現なし。

## 3. Implementer の検証結果を利用した範囲

自動テストの件数は自分で再実行して一致を確認。ブラウザ手順は全て自分で実施。Implementer の sqlite 掃除（poison 5 行削除）は結果（走査で 0 件）だけを確認した。

## 4. コードレビュー（`git diff 0f3868e..9ac3024`）

- **B-1**: `ReserveCredentialDto.saga: "signup" | "link"` を追加し、`reserveCredentialProcedure`（旧 `reserveSignupCredentialProcedure`）は `dto.saga === "signup" && coordinator` のときだけ `resume-signup` を enqueue。`linkSsoCredential` は `saga: "link"` を渡す。型で経路を分けており、`spec/async/index.md` の「`resume-signup` の入口は登録予約だけ」と一致。`sso.integration.test.ts` に「link の bucket に `resume-signup` 無し・`sweep-reservations` 有り」のテスト追加。
- **B-2**: `resumeSignup.ts` が `saga_committed` を SELECT に追加し `coordinatorCompleted` で phase 2/3（`commit` / coordinator `activate`）を条件付きスキップ。非コーディネーター locator の `activate` はこれまでどおり実行、`initializeAccount` と逆引き行も再実行（冪等）。store は変更なし（spec L726 と整合）。`signupSaga.integration.test.ts` に「変更完走後の再駆動が `done`」テスト追加。
- **O-1**: `cancelTarget()` が `intent=link` → `/settings`、それ以外 → `origin` + `redirect` 保持。`"cancelled"` を `SsoErrorCode` / `ssoErrorSchema` / `renderSsoError` / `SsoNotice` から削除し、URL に `sso_error=cancelled` が来ても schema で弾かれる（不正値は無視）。`ssoHandlers.test.ts` / `authForm.dom.test.tsx` 更新。
- **G-1**: `sweepOrphanMapping.ts` は `DataIntegrityError` のみ rethrow（他は従来どおり再アーム）。`jobTerminal.integration.test.ts` 追加、`passwordReset.integration.test.ts` に `advanced` 分岐追加（`change_state='advanced'` から `promoteVerifier` → `done`、`session_epoch` 1）。
- 不変条件の逸脱なし: UoW 内に非同期なし、`instanceof` なし、`resume-signup` / `sweep-reservations` 以外に新しい kind/event 無し。

## 5. G-1 テストが 4 kind の失敗経路を本当に通しているか

`runUntilTerminal` は各 wake-up で `runDurableObjectAlarm(stub)` を呼び、`next_run_at` を過去に引き戻して `jobsMaxAttempts + 2` 回まで回す。終端しなければ throw（＝再アームし続ける旧 `sweep-orphan-mapping` はここで落ちる）。各 kind の失敗の作り方:

| kind | 失敗の作り方 | 到達する throw | 検証 |
|---|---|---|---|
| `sweep-reset-tokens` | bucket で `DROP TABLE reset_request_windows` | handler の SQL 自体が失敗 | `poison` / `attempt = max-1` / `terminal_reason not null`（コードは固定していない — 妥当だが弱い） |
| `resume-credential-change` | `beginCredentialChange` 後に payload の `userId` を未初期化 id に書き換え | phase 2 の User Data RPC が `NOT_INITIALIZED` | `expectPoison(row,"NOT_INITIALIZED")` + 行が `pending`/`pending_verifier` のまま（ロールバックは PH-09） |
| `resume-link` | `operations` に `target_locators NULL` の `link` 記録 | handler が `DataIntegrityError` | `expectPoison(row,"DATA_INTEGRITY_ERROR")` |
| `sweep-orphan-mapping` | `target_locators` に `mapping:"not-a-mapping"` の `unlink` 記録 | locator 解析が `DataIntegrityError` → 新設の rethrow | `expectPoison(row,"DATA_INTEGRITY_ERROR")` + 記録が残る |

いずれも handler 本体の失敗（payload パースやテスト側の細工ではない）が Alarm → job runner → backoff → `poison` を実際に通っており、`terminal_reason` にコードが入ること、`attempt` が天井の一つ手前で止まることを固定している。**十分と判断。**

## 6. 受け入れ済み項目への影響

| ID | 影響 | 根拠 |
|---|---|---|
| R-AC-01 登録 | なし | 2 節「R-AC-01 影響確認」: 新 handler でも通常登録の `resume-signup` が `done attempt=0`。`signupSaga.integration.test.ts` 既存ケースも通過 |
| R-AC-02 ログイン | なし（O-1 で改善） | パスワードログイン（`ph06r-v2` 新パスワード、`ph06v-two` preview）と SSO ログイン（`ph06r-link5`）成功。キャンセル復帰の変更は `cancelled` 分岐のみ |
| R-INF-03 jobs | なし（G-1 で強化） | `sweep-orphan-mapping` の変更は `DataIntegrityError` の扱いだけ。poison 走査 0 件 |
| PH-01〜05 の 24 項目 | なし | 差分は identity / SSO / jobs に閉じており、memo・timeline・search・trash のファイルは未変更。unit+dom 102 files 全通過 |

## 7. 検証環境の引き継ぎ

- dev（pid `lsof -t -i :3000`、ログ `scratchpad/dev6r2.log`）と preview（`:4173`、`preview6r.log`）は**起動したまま**。
- ローカル DO 状態に O-2 の「詰まった」pending 行が残っている: b0 / b15 `resume-signup`、b2 / b6 / b11 `sweep-reset-tokens`（いずれも `attempt 0`、期限過去）。実装コードでは触っていない。
- 追加した検証アカウント: `ph06r-v2@example.com` / `secondpass456`（SSO `ph06r-link5` 連携中）、`ph06r-v3@example.com` / `plainpass123`、`ph06v-pw` に `ph06r-link3` 連携、`ph06v-two`（preview）に `ph06r-plink` 連携。
- agent-browser セッション: `verify06`（`ph06v-pw`）、`verify06c/d`（`ph06r-v2`）、`verify06f`（`ph06r-v3`）、`verify06p`（`ph06v-two`）、`verify06o/e`（未ログイン）。
