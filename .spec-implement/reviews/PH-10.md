# PH-10 検証報告（Verifier、2026-09-11 14:10〜14:55 JST）

対象 HEAD `de888b7`（Implementer 最終 `c1f3366` + Manager の spec 修正 `c2d36f0` / `de888b7`）。範囲は `9bec53e..de888b7`（57 ファイル +1,033 / −352）と、それ以前の PH-10 コミット `02676d3` / `90281ae` / `9f5671e` / `9bec53e`。実装コード・`spec/`・管理ファイルは変更していない。書き込んだのはこのファイル、補助の `reviews/PH-10-verify/{docs-audit,search,document}.md`、scratchpad だけ。検証のために `.dev.vars` と `apps/web/wrangler.state.toml` を一時的に書き換え、sqlite 4 ファイルを編集した。設定ファイルは戻してあり（`cmp` でバックアップと一致、`git status` clean）、引き継ぎ状態は末尾に書いた。

## 判定

| 完了条件（R-INT-01） | 判定 | 根拠 |
|---|---|---|
| 1. 全マニュアルテストをブラウザで実行して合格 | **合格**（TC-47 の部分実施は条件付き。Manager 判断 M-2） | 7 カテゴリから計 94 ケースを自分と補助で再実行し、すべて合格（§2）。PH-10 で直したケースと sqlite シードのケースを優先し、配送異常系 TC-45 / TC-46 は本物の publish 失敗・consumer 失敗で通した。preview（SSR ストリーミング）でも主要 8 画面と 4 操作を確認した。Implementer の 207 合格の記録は、抜き取りの範囲で観測と食い違わなかった |
| 2. `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` が成功 | **合格** | HEAD `de888b7` で 2 回連続して exit 0。unit+dom 129 files / 1,598 passed + 2 skipped、integration 32 files / 191 passed。報告の件数と一致し、flaky は出なかった（§1） |
| 3. アーキテクチャ監査に重大な違反なし | **合格** | 11 観点を抜き取りで確かめ、重大な違反は無かった。軽微は Implementer の issue 候補と同じもの（§5） |
| 4. docs が現状を述べる | **不合格** | 存在しないテスト・述語を名指しして保証を主張する文が 2 つ残っている（§6 D-1 / D-2）。`CLAUDE.md` の原則「docs の保証には test か type、無ければ限界を明記」に反し、PH-10 §1.1 が消すと決めた「存在しないファイルへの参照」にも当たる。ほかに事実の誤りが 4 件ある（軽微、§6 D-3〜D-6） |

**R-INT-01 は不合格（差し戻し）。原因は docs の文 2 つだけで、実装・テストに受け入れ条件を割る欠陥は見つからなかった。** 直すのは `docs/runtime_cloudflare.md:390` と `docs/test.md:52` の 2 文（軽微の 4 件も同時に直すのを推奨）。コード変更は不要で、再検証は docs の差分確認と `pnpm lint && pnpm format:check` で足りる。受け入れ済みの 36 項目の完了条件に影響する変更は無かった（§4）。

### Manager の判断が必要な事項

- **M-1（差し戻しの範囲）**: docs の重大 2 件（D-1 / D-2）を直すと条件 4 を満たす。直し方は 2 通りある。存在するテストの記述に置き換えるか（D-1 は `alarmSchedule` / `rowRunner` の 2 本だけを残し、タイムラインの seek / scan には計画テストが無いと限界として書く。D-2 は「route の params は zod で検証し、表示できない id は feed の `KnowledgeNotFound` に落ちる。述語の unit test は無い」）、タイムラインの `EXPLAIN QUERY PLAN` テストを実際に足すか。前者で足りる
- **M-2（TC-47 の部分実施）**: bucket 単位で cross-DO RPC を落とす手段がローカルに無い。代わりに sqlite シード + operator の list / requeue で観測し、巻き戻しの実経路は integration（`recovery` / `jobTerminal`、PH-09A で受け入れ済み）で担保している。この代替は妥当だと考える（§3）。ただ条件 1 の文言は「全マニュアルテストをブラウザで実行して合格」なので、部分実施を合格として数えるかは Manager が決めてほしい
- **M-3（issue の起票）**: issue はこのリポジトリに 0 件（`gh issue list --state all` で確認）。docs §14「Known limits」の 6 行と、監査の軽微（application の 2 procedure が `SqlStorage` を直接読む）は、起票されるまで `CLAUDE.md`「gap は閉じるか issue で追跡」を満たさない。R-INT-01 の完了条件の外だが、PH-10 を閉じる前に起票を済ませるのが望ましい（Manager 判断 (1) は「ユーザー確認のうえ起票」のまま）
- **M-4（spec の記述漏れ・情報）**: `spec/database/index.md` の `oauth_consumed_codes` の節は書き手を「認可コード交換の RPC だけ」としている。実装では `finalize-withdrawal` の最終トランザクションも同じ表を全削除する（`jobs/finalizeWithdrawal.ts`、退会で到達性を消す義務の一部。PH-09A で受け入れ済み）。spec に 2 人目の書き手として足すのが望ましい
- **M-5（テストを緩めた変更の確認・情報）**: `3d23270` は `recovery.integration` の「abandon-account 直後」の期待を `status ∈ {deleting, deleted}`・job `∈ {pending, running, done}` に広げた。pool が Alarm を即時に起こす競合への対処で、epoch がちょうど +1 であることの固定は残っている。完走後の状態（接続の失効 = version +1、既失効は不変）は `ebbac54` の追加テストが固定しているので、緩めても意味は失われていないと判断した

## 1. 自動テスト（自分で実行）

| 回 | 時刻（+09:00） | typecheck / lint / format | unit + dom | integration |
|---|---|---|---|---|
| 1 | 2026-09-11T14:10:26 〜 14:10:47 | exit 0（lint warning 3 は既存: `currentUserPanel.dom` の non-null ×2、`wranglerConfig.test` の template 文字列） | 129 files / 1,598 passed + 2 skipped | 32 files / 191 passed |
| 2 | 2026-09-11T14:10:47 〜 14:11:07 | exit 0（同上） | 129 files / 1,598 passed + 2 skipped | 32 files / 191 passed |

- dev を止めて実行した（Implementer と同じ条件）。ログは `scratchpad/v10/final-{1,2}.log`
- skip 2 件は、資格情報が無いときの契約テスト（`resendMailSender.contract` と `googleSsoProvider.contract` の `it.skipIf(!configured)`）で、brief の「契約検証」のとおり
- 補助 (a) が、監査用の scan テストと AI スコープのテスト 6 ファイル（32 件）を個別に再実行し、すべて緑だった
- `pnpm build` は 14:44 に exit 0

## 2. マニュアルテストの抜き取り再実行（dev :3000 / preview :4173、HEAD `de888b7`）

使ったユーザー（パスワードは全員 `V10verify#Pass1`。例外は注記）:

- `v10-main@example.com`: 主要シナリオ。TC-10 以降のパスワードは `V10chg#Pass3`
- `v10-sso-new@example.com`: SSO 専用。dev スタブの subject は `v10-sso-new`
- `v10-q45` / `v10-q46`: 配送の異常系
- `v10-trexp`（保持 1 → 30 日）/ `v10-tredge` / `v10-trext`: 期限まわり
- 補助 (b) が使ったユーザー: `v10-sd-doc@example.com`、既存の `ph10-search-main`

agent-browser の session は `v10-main` / `v10-main2` / `v10-sso` / `v10-tr` / `v10-pv`、補助 (b) は `v10-sd` / `v10-sd2`。AI クライアントは ai-client の複製（`scratchpad/v10/aic`、loopback 8770、クライアント名 `V10 Verifier Client`）。

| カテゴリ | 件数 | 結果 | 実行したケース |
|---|---|---|---|
| account | 24 | 合格 23 / 部分 1 | 02 / 03 / 04 / 05 / 06 / 07 / 08 / 09 / 10 / 11 / 17 / 18 / 19 / 20 / 27 / 31 / 32 / 33 / 41 / 42 / 43 / 45 / 46 / 47（部分） |
| timeline | 14 | 合格 14 | 01 / 02 / 03 / 05 / 07 / 12 / 13 / 17 / 18 / 21 / 22 / 24 / 33 / 34 |
| document | 14 | 合格 14（補助 b） | 05 / 06 / 09 / 10 / 11 / 18 / 19 / 20 / 21 / 22 / 23 / 25 / 26 / 35 |
| search | 10 | 合格 10（補助 b。チップは自分でも dev / preview で確認） | 01 / 04 / 05 / 06 / 07 / 11 / 14 / 20 / 22 / 23 |
| trash | 15 | 合格 15 | 01 / 02 / 06 / 12 / 13 / 14 / 15 / 16 / 18 / 19 / 20 / 21 / 22 / 23 / 24 |
| ai | 10 | 合格 10 | 01 / 03 / 04 / 06 / 07 / 08 / 13 / 14 / 15 / 22（`update_memo` / `get` の分） |
| settings | 7 | 合格 7 | 03（単日・多日）/ 04 / 05 / 06 / 08 / 09 / 10 |
| **計** | **94** | **合格 93 / 部分 1** | |

### 2.1 観測の要点（PH-10 で直したケースと sqlite シードのケースを中心に）

- **timeline TC-21（`9bec53e`）** 14:14: `<script>alert(1)</script> <b>bold</b> <img src=x onerror=alert(2)>` と、ブロック HTML `<div onclick=…>` を含む本文を投稿した。`.fog-markdown` の innerHTML はすべてエスケープ済みのテキストで、`script` / `b` / `img` / `div` 要素は 0、`alert` の呼び出しも 0。リロード後と preview の SSR（`&lt;script&gt;`）も同じ。TC-05 の箇条書きとコードブロックは `ul` / `pre`（monospace、block）で描かれる
- **確認ダイアログ（`1210ec5`）**: メモ削除・接続解除・ロールバックのダイアログは modal `<dialog>` で、rect を測るといずれも left 480 / width 320（ビューポート 1280×633 の中央）。完全削除・空にするの確認は文言とボタンの動作だけを確かめ、位置は測っていない（同じ `ConfirmDialog` コンポーネント）。復元先ダイアログは rect（430 / 420 幅 = 中央）とスクリーンショットで確認した。危険ボタンは白地（`oklch(1 0 0)`）に `oklch(0.55 0.19 27)` の文字。preview でも同じ値だった。補助 (b) の測定では、ドキュメント / トピック削除のダイアログは中心差 [0, 0]、コントラストは約 5.3:1
- **削除済みのメモ（`c1f3366`、timeline TC-34 / document TC-26）** 14:16: 出典メモをゴミ箱へ入れると、P-08 の「元になったメモ」が `div.fog-origin-row.deleted[aria-disabled=true]`「2026/09/11 14:14 削除済みのメモ」になる（リンク 0、クリックしても URL 不変）。P-07 の関連メモも「削除済みのメモ」。復元すると `a[href=/?memo=…]` に戻り、ハイライトへ遷移する。AI の `delete` でも同じ表示だった（14:20）
- **検索チップ（`c844897`）** 14:17: トピックチップを選ぶと、そのチップだけが `aria-current="page"`（背景 `oklch(0.63 0.13 292)`、白文字）になり、「すべて」は null。preview（14:45）でも同じ
- **保持期限フォーム（`c734ddb`、settings TC-08〜10）** 14:17:
  - `0` はネイティブ検証で止まる（「値は 1 以上にする必要があります。」）
  - `noValidate` で `0` / `-1` / `1.5` / 空欄を送ると「1 以上の整数を入力してください」、`36501` は「36,500 日以下で入力してください」。いずれも入力値は残り、保存されない
  - 送信本文を改竄して `retentionDays` を `0` / `36501` に書き換えると、サーバーが拒否して「1 以上の整数を入力してください」が出る。現在値は 7 日のまま
  - リロード後も変更前の値
- **trash TC-21 / TC-22（`ce6501a`）** 14:28:
  - TC-21: タブ A で完全削除した行を、古いタブ B で復元すると role=alert「「v10 ゴミ箱テストM1」はゴミ箱に見つかりません。完全に削除されたか、別の画面で復元されています」と「一覧を読み直す」が出る。押すとアラートが消えて一覧を取り直し、リロード後も行は無い
  - TC-22: セット復元の確認を開いたまま別タブでトピックを復元し、確認を承認すると同じアラートになる。P-07 の配下は D2a / D2b が 1 件ずつで、重複も消失も無い
- **trash TC-18 / TC-19（`ce6501a`）** 14:27:
  - 復元先ダイアログはカード（白地、角丸 26px、幅 420、中央）で出る。候補にゴミ箱内の T2 は出ない
  - 新規トピック名が空欄ならネイティブ検証。空白 3 つなら「トピック名を入力してください」（`aria-invalid=true`）
  - 101 字は「トピック名は100文字以内で入力してください」（「候補を読み直す」は出ない）。100 字なら復元され、100 字のトピックが作られる
- **sqlite シードのケース**（14:33 に dev を止めて編集。N = 1789104852816）:
  - timeline TC-03: `v10-main` のメモ 4 件を −3 / −5 / −10 日へずらした。見出しは 9/11 → 9/8 → 9/6 → 9/1 の順で、各見出しの下はその日のメモだけ
  - timeline TC-07: `?date=2026-09-08` で「2026年9月8日(火)に移動しました」と出て、9/8 の見出しが top 0
  - timeline TC-24: `2026-09-07` を指定すると「…にメモはありません。前後で最も近いメモの位置を表示しています」と出て、9/6 の見出しが top 9。エラーも空表示も無い
  - trash TC-13（`v10-trexp`）: 期限切れ行は「まもなく削除」。発火用メモを削除して Alarm を前倒しすると、6 秒後に期限切れ行だけが消え、期限内の行は残る。検索でも 0 件。保持期限を 30 に戻すと「残り30日」
  - trash TC-23（`v10-tredge`）: 期限の 1 時間前の行が「残り1日」で表示され、復元すると元の位置（前 > テスト > 後の間）に戻る
  - trash TC-24（`v10-trext`）: 40 日前に削除した行は「まもなく削除」。保持期限を 60 にすると「残り20日」になり、保存直後の再武装で `purge-trash` が走っても 6 秒後に行は残る。30 に戻すと期限が過去になり、自動削除された（停止中の sqlite で、`trashed` 0 件・`purge-trash done`）
  - settings TC-03〜06: `memos/2026-09-01.md` / `09-06` / `09-08` / `09-11` の 4 ファイルに分かれる。完了済みトピックは `archived: true`、ゴミ箱内のメモと旧リビジョンの本文は含まれない（`counts.memos` 7）。`sources` に `memoId` / `postedAt` / `file: ../../memos/2026-09-11.md` が入る。非 ASCII 名のエントリは flag `0x800` 付き
  - macOS `/usr/bin/unzip` は exit 2（非 ASCII 名）で、`ditto -x -k` なら全ファイルを展開できる
- **account TC-10 / TC-31** 14:21〜14:23:
  - 依頼から約 29 秒で `[dev-mail]` が届く
  - 完了画面はログイン済みで表示され、ログイン手段の一覧・AI 接続の一覧・「すべて失効」が出る
  - 事前に別 session（`v10-main2`）でログインしていたセッションは、リロードで `/login` に落ちる
  - 旧パスワードは「メールアドレスまたはパスワードが正しくありません」
  - 使用済みリンクは「リンクが無効か期限切れです もう一度依頼する」
- **account TC-41 / 42 / 43 / 18 / 17 / 03 / 04（SSO_DEV_STUB）** 14:23〜14:26:
  - TC-41: 連携すると `?sso=linked`「外部アカウントを連携しました」になり、その subject の SSO で `v10-main` にログインできる。解除すると行が消える
  - TC-42: SSO 専用アカウントで唯一の SSO を解除しようとすると「最後のログイン手段は解除できません」
  - TC-43: 他アカウントの subject を連携しようとすると `?sso_error=already_used`、一覧は変わらない
  - TC-18: パスワード登録済みのメールで SSO すると `?sso_error=email_registered`
  - TC-17: スタブでキャンセルすると `/login` に戻り、alert は出ない
  - 解除（`unlinkSsoCredential` の手順 5）で `sessionEpoch` が進み、SSO で入っていた別 session は `/login` に落ちる（spec どおり）
- **ai（`425f36b` の AiTokenCodec 移設後）** 14:19〜14:21:
  - 認可: P-14 はクライアント名、許可 / できないことの一覧を出し、押すと「処理中…」で disabled になる。そのあと loopback に code が届き、token は 200
  - `tools/list` は 11 個。`list_trash` / `hard_delete` / `restore` / `empty_trash` / `list_revisions` / `rollback` は MCP で `-32601`、REST で 404 `UNKNOWN_TOOL`。`edit_document {mode: rollback}` は `-32602`
  - TC-01 / 06: `post_memo` / `update_memo` のリビジョンは P-05 で「V10 Verifier Client · リビジョン n」。人間のロールバックは「あなた · リビジョン 3」
  - TC-07: `edit_document` の patch で、P-10 に「V10 Verifier Client · 誤字の修正（事→こと）」が残る
  - TC-15 / 22: ゴミ箱内のメモは `search` 0 件、`get` / `update_memo` は `MEMO_NOT_FOUND`（存在しない id と同じ応答）
  - TC-13: P-13 で解除すると、`call` は 401 `invalid_token`、`refresh` は 400 `invalid_grant`
- **account TC-45** 14:35〜14:40:
  - `wrangler.state.toml` の producer binding を外して起動し、`v10-q45` のリセットを依頼した。応答は `?sent=1` で、失敗は現れない
  - backlog は `dir:g1:b11` pending 1。ログには `Outbox publish failed { eventId, type, cause: 'TypeError' }` だけが出る
  - 約 30 秒で `list-quarantined-events` に 1 行が現れた。列は `eventId / type / attempt / createdAt / completedAt / terminalReason("TypeError")` の 6 つだけ
  - binding を戻して `requeue-quarantined-event` を打つと 30 秒で `[dev-mail]` が届き、再設定を完了できた（依頼から約 5 分、TTL 内 =「届く」側）
  - 停止中の sqlite で、行は `published`・attempt 0・`terminal_reason` は残存（`spec/database` の requeue の 4 列と一致）
- **account TC-46** 14:41〜14:44:
  - `MAIL_DEV_SINK` を不正値にして起動した。`Mail consumer is not configured` が 4 回出たあと「Moving message … to dead letter queue … after 4 failed attempts」
  - DLQ ハンドラは `dlq { eventId, type, outcome: 'failed' }` の 1 行を出して ack する
  - 発行元 `dir:g1:b6` の行は `published` / attempt 0 のまま
  - 手順 4（DLQ の中身）はメッセージを開かず、型（`OutboxQueueMessage` = `eventId / type / payload / routingKey / ownerToken`）で確かめた
- **account TC-47（部分）** 14:38: `v10-main` の User Data DO にある完走済み `resume-link` 2 行を、(a) 終端モード（`pending`、`forward-exhausted <opId>`）と (b) `poison`（`cleanup-exhausted:forward-exhausted <opId>`）に書き換えた
  - `list-poisoned-jobs` には (b) だけが出る。列は 5 つで、`payload` は無い
  - `requeue-poisoned-job` を打つと、(a)(b) とも `done`・理由は残存
  - 手順 1・2・5〜7（RPC 障害の注入と、実際の巻き戻し）はローカルでは実行できない（§3）
- **preview（SSR ストリーミング）** 14:44〜14:46: `/`・`/topics`・`/topics/:id`・`/documents/:id`・`/search?q=…`・`/trash`・`/settings`・`/memos/:id/history` を cookie 付きの `curl` で取った。いずれも 200 で、`<main>` に本文が SSR 済み（スケルトンは無い）。未ログインは 307 → `/login?redirect=%2Ftrash`。ブラウザでは次の 4 操作を確認した
  - 投稿の楽観 UI（「保存中…」→ 確定）
  - 削除ダイアログの中央表示と危険ボタンの配色
  - 検索チップの current
  - P-09 のタイトル空エラー（`aria-invalid`、本文保持）

## 3. 部分実施と代替検証の妥当性（brief「外部依存と検証段階」との照合）

| 対象 | 代替 | 判断 |
|---|---|---|
| 実 IdP（SSO） | `SSO_DEV_STUB` による契約検証 + 資格情報があるときだけ走る Google の契約テスト（skip） | brief の段階「契約検証」どおり。妥当。Apple の実アダプターは無い（issue 候補に既にある） |
| 実 mail provider | console sink の `[dev-mail]` + Resend の契約テスト（skip） | 同上。妥当 |
| 実 LLM アプリ | ai-client で MCP `tools/call` / REST を直接呼ぶ | brief どおり。「依頼文からツールを選ぶ」振る舞いは requirements の範囲外（brief 対象外: AI の振る舞い）で、除外は妥当 |
| account TC-47（bucket 単位の RPC 障害） | sqlite シード + operator の list / requeue + integration（`recovery` 12 件 / `jobTerminal`） | brief の外部依存表には無い「ローカルで注入できない障害」。Miniflare では bucket DO だけを落とせない（binding を外すと request 経路まで止まる）。終端モード・poison の見え方と requeue は dev で観測でき、巻き戻しの実経路は PH-09A で受け入れた integration が固定している。**代替は妥当。受け入れの数え方は M-2** |
| macOS `unzip` | `ditto -x -k` / Python `zipfile` | zip は正しい（非 ASCII 名に UTF-8 フラグ `0x800`）。Apple 版 UnZip 6.00 の制約で、README / docs §13 に限界として書かれている。妥当 |
| 24 MiB 超のエクスポート | integration（`ExportTooLarge`） | ブラウザでは踏めない。docs/test.md の Limits に記載済み。妥当 |
| 10 GB 逼迫 | なし（`spec` の設計と JSDoc の限界記述） | ローカルで作れない。docs に限界が書かれている。妥当 |

## 4. PH-10 の修正の影響とコードレビュー

受け入れ済み 36 項目の完了条件に照らして、各コミットを読んだ。**完了条件を割る変更は無い。**

- `9bec53e`（R-TL-01 周辺）: `skipHtml` を外しただけ。`rehype-raw` を入れず `urlTransform` も既定のままなので、HTML は常にテキストとして描かれる。上の実測で、インラインとブロックの両方がエスケープされていた
- `87a1f46`（R-DT-01 / 02）: 空欄のときは submit で `preventDefault` し、`aria-invalid` と `aria-describedby` で文言を出す。文言は `blankFieldMessage` に 1 つだけ置かれ、サーバー側の VO コードからも同じ文言に届く。`inFlight` の二重送信ガードとの順序も正しい
- `c844897`（R-SE-01）: `activeOptions: { exact: true }` を使い、CSS は `aria-current="page"` に当たる。`keyword` は URL の `q` なので、チップの `search` と URL が一致する
- `b7deeb4`（R-DT-01 / R-DT-05）: 完了はヘッダーのボタン、メニューは 編集 / 削除。「削除は完了と並ばない別階層」がむしろ強くなった。dom テストはメニューに「完了」が無いことを固定している
- `ce6501a`（R-TR-01〜03）: notFound の文言が成功と読めていたのを、失敗として両方の結果を名指す形に直した（P-12「操作失敗: エラー＋リトライ」）。復元先エラーは `{ message, stale }` で、stale のときだけ再取得のボタンが出る
- `c734ddb`（R-ST-01）: `<form action>` の自動リセットを避けるため、自前で `startTransition(() => action(data))` を呼ぶ。`useOptimistic` + `useActionState` の三層構造は保たれている。細部は §8 O-1
- `1210ec5`: `.fog-danger` の配色をデザインの dialog-confirm に揃え、`.fog-dialog { margin: auto }` を足した。`.fog-danger` は `ConfirmDialog`、ゴミ箱の行ボタン（`.fog-secondary.fog-danger`）、`DocumentActions` で共用で、確認した範囲ではどれも読める配色になった（上の実測）
- `425f36b`（R-AC-05 / R-ST-02）: `application/ports/aiTokenCodec.ts`（presentation 専用 port）、`presentation/ai/pkce.ts`、zip writer は entry point から注入。OAuth の全経路（DCR → authorize → code → token → refresh → 失効後の 401 / `invalid_grant`）とエクスポートを dev と preview で実行し、振る舞いは変わっていなかった
- `ebbac54`（R-REC-01）: `finishWithdrawalProcedure(ctx, now)` は store / repository / entity 経由で、同期の UoW callback の中にある。adapter-owned の `oauth_consumed_codes` の削除だけが adapter に残る。integration は「active 1 件の失効（version +1）、既失効は不変」を固定している
- `f14fcd8`: `alarm.integration` の 2 件は、上限 +2 件の backlog が各 pass でちょうど自分の上限分だけ消費されること、claimed 行だけの DO が最早の `leaseUntil` に再武装すること（`getAlarm() === jobLease`）を assert している。`CLAUDE.md`「非同期実行契約 3」の文と対応する
- `3d23270`: M-5 のとおり
- `9f5671e`（前任）:
  - `findActiveAiClientProcedure` が `account.status !== 'active'` なら null を返す。`authorizeAiClient` と `consumeAuthorizationCode` の両方がこの procedure を通るので、PH-07 O-1 は両経路で閉じている
  - `operatorBuckets` は keyring が壊れていると 500 を返す（値はログにも応答にも出ない）
  - `restoreDocument` の touch は version だけ
  - trash のジョブのログは `error.name` だけ
- `c1f3366`: 表示と JSDoc の文言だけで、dom テストも追随している。`apps/web/app` / `packages/core/src` / docs に「削除されたメモ」は残っていない
- テストの削除: 範囲内で消えた `it(...)` は 6 件で、どれも改名か期待の置き換え（`treats a not-found as already handled` → 新しいアラートの期待、PKCE のベクトルは `pkce.test.ts` へ移動）。検証から外れたものは無い

## 5. 監査（抜き取り。補助 (a) の `reviews/PH-10-verify/docs-audit.md` と自分の確認）

| 観点 | 判定 | 確認したこと |
|---|---|---|
| 依存方向 | 合格 | 補助 (a) が `adapterImports.test.ts` の射程を評価した。`apps/web/app` の `.ts(x)` 全部から `from "@repo/core/adapters/…"` を探し、除外は `server.cloudflare.ts` と `worker/cloudflare/` だけ。見逃すのは動的 `import()`、副作用 import、相対パス（現状の実例は 0）。独立の grep でも許可リストの内側に収まった。`domain/` → 外、`application/`（`di/` 以外）→ adapters はともに 0。**packages/core 内の依存方向を固定する scan は無い**（軽微、情報） |
| 書き込み口 | 合格 | `storageWriters.test.ts` は大文字の `INSERT` / `UPDATE` / `DELETE` / `REPLACE` を、`packages/core/src/adapters/` の外から探す。大文字小文字を無視した広い正規表現で独立に grep しても、adapters の外の書き込み SQL は 0。application の 2 procedure の `SqlStorage` 読み取りは既知の issue 候補 |
| UoW の同期性 | 合格 | `run` / `UnitOfWorkRunner` の条件型が async callback を拒否することを、補助 (a) が scratchpad で `TS2322` として実証した。`run(async` は 0。`transactionSync` の呼び手は adapters の 11 ファイルだけ。自分でも `userDataDurableObject.ts` の `authorizeAiClient` / `consumeAuthorizationCode` / `approveAiClientAuthorization`、`finalizeWithdrawal.ts` の callback が同期であることを確認した。この型の退行を `@ts-expect-error` で固定するテストは無い（情報） |
| AI スコープ | 合格 | 型: `AiToolContainer = Needs<"memoGateway" \| "knowledgeGateway" \| "searchGateway">`。許可リスト: `AI_TOOL_NAMES` 11 個。固定テスト: `tools.test.ts` と `handlers.test.ts`。dev でも非公開ツールの拒否を実測した（§2.1） |
| ログ衛生 | 合格 | 自分の dev / preview のログ 941 行で、`[dev-mail]` 以外にメールアドレス・`token=`・owner / caller token は 0 件。64 hex は 14 件あったが、すべて preview の serverFn ハッシュ id。relay / DLQ / operator の監査ログの形は §2.1 のとおり。補助 (a) の注記: `errorResponseMiddleware` は system / unknown のエラーでオブジェクト全体を logger に渡す（既存、PII は未観測。情報） |
| エラー契約 | 合格 | `9bec53e..de888b7` で新しい error class は 0、broad catch の追加も 0（補助 (a)）。`CLAUDE.md` の catch 境界の列挙は実装の catch と一致する |
| その他（入力検証・全数表・非同期 port・Retry） | 合格（Implementer の判定を抜き取りで確認） | `CLAUDE.md` の `AiTokenCodec` 追記（`425f36b`）は、presentation 専用 port 群の説明として実装と一致する |

## 6. docs（抜き取り）

| ID | 重大度 | 場所 | 記述 | 実際 |
|---|---|---|---|---|
| D-1 | **重大** | `docs/runtime_cloudflare.md:390` | `EXPLAIN QUERY PLAN` のテストとして `alarmSchedule` / `rowRunner` と並べて「`stores/__tests__/memoRepository.integration.test.ts` for the timeline seeks and scans」 | ファイルは存在せず、git 履歴にも無い（`3f7c501` = 前回ビルドから継承した文）。`planAssertions` を使うのは `alarmSchedule` と `rowRunner` の 2 本だけ。タイムラインの seek / scan の計画を固定するテストは無い |
| D-2 | **重大** | `docs/test.md:52` | 「routes' own refusals of an undisplayable id (`isDisplayableDocumentId` / `isDisplayableTopicId` → …) have a unit test for the predicate」 | どちらのシンボルもコードに無い（`grep` で 0 件）。route は params の zod 検証と、feed 側の `KnowledgeNotFound` で拒否している |
| D-3 | 軽微 | `docs/runtime_cloudflare.md:624` | 「the events consumer failed it three times」 | `max_retries = 3` なので 4 回失敗してから DLQ に入る（dev ログ「after 4 failed attempts」を 14:43 に観測） |
| D-4 | 軽微 | `docs/backend_implementation_example.md:99` | ディレクトリ木で `scripts/` が `apps/web/app/` の直下にある | 実際は `apps/web/scripts/` |
| D-5 | 軽微 | `docs/test.md:13` | 命名例の `eventTypeRoster.test.ts` | 存在しない |
| D-6 | 軽微 | `docs/backend_implementation_example.md:384-407` | 「shipped one」とする `save` の抜粋 | 実コードと 2 箇所違う（`now` と inline の `ConflictError` / 実際は `user.updatedAt.getTime()` と `occConflict()`。補助 (a)） |

問題が無かったもの（補助 (a) + 自分の確認）:

- 件数: DOM 29 ファイルと docs の 29 行が 1 対 1、integration 32 files / 191 tests はファイル別の件数まで全行一致
- operator の 15 entry の名前・対象・引数・監査 id。`OPERATOR_TOKEN` の判定順は 404（未設定）→ 404（未知 entry）→ 405 → 401 → 400
- DLQ の自動 1 回 → ack
- secret の帰属表と env 型、README の必須 secret 6
- issue 番号・`.thread/` の残り 0（`#` 付き番号の grep）
- `CLAUDE.md` の差分 4 文（Retry の DLQ 例外、非同期契約 5、catch 境界、非同期 port の群分け）はいずれも実装と一致する

## 7. issue 候補一覧（PH-10.md §5）の妥当性

- 受け入れ条件を割る欠陥は、候補一覧に紛れていない
- 最重の「request Worker をデプロイできない / `pnpm start` が起動しない」は、デプロイが brief の対象外なので R-INT-01 の外。README に事実として記録されている
- 「40 万字のドキュメントの編集画面が `chrome-error` になった」（document TC-39 の観察）は再現しなかった
  - dev: `open` + `networkidle` を 3 回
  - preview: `curl` 3 回（200、2.43 MB、0.06〜0.17 s）とブラウザ 3 回
  - いずれも表示でき、textarea は 400,000 字。**欠陥ではなく一過性と判断し、候補から外すか「再現せず」と注記するのを推奨**
- 追加を推奨する候補（いずれも低）:
  - (a) packages/core 内の依存方向の scan テスト（§5）
  - (b) UoW の async 拒否の型テスト（`@ts-expect-error`）
  - (c) 長さエラー時の `aria-invalid` 欠落（§8 O-2）
  - (d) spec の `oauth_consumed_codes` の書き手（M-4、spec 側）
- 「既存ドキュメントに出典を後から足す経路が無い」は D-10 △-4（出典は作成時のみ、`spec/domains/knowledge.md`）で決まった仕様どおりなので、issue ではなく spec の確認事項のままでよい

## 8. 追加観察（合否に影響しない）

- **O-1**（`c734ddb`）: サーバーが `retentionDays` の transport 検証（36,500 超）で拒否した場合も、文言は「1 以上の整数を入力してください」になる。クライアントが先に上限を判定するので、送信本文を改竄したときにしか届かない
- **O-2**: 空欄エラーには `aria-invalid` が付くが、長さ超過エラー（トピック名 101 字、復元先の新規名 101 字）には付かない（補助 (b) と自分の観測が一致）
- **O-3**（見た目）: modal ダイアログのカード下端の角に、四角い薄い帯が見える（`.fog-dialog` の `overflow: auto` がカードの影を dialog の矩形で切っているように見える）。dev と preview の両方で出る（`scratchpad/v10/preview-dialog.png`）
- **O-4**: 「削除済みのメモ」の行にも薄い ↗ アイコン（`.fog-origin-jump`）が残る。遷移はしない（補助 (b)）
- **O-5**: 復元先ダイアログで、select と「キャンセル」ボタンの間に余白が無い（`scratchpad/v10/trash-dest-dialog.png`）
- **O-6**: SSO 連携の解除には確認ダイアログが無い（手順書は確認を要求していない。Implementer の観察と同じ）
- **O-7**: 補助 (b) の session が 14:23 に cookie 空へ戻った（Implementer も 3 回観測）。agent-browser 側の要因と見られ、結果には影響しない

## 9. Implementer の結果を利用した範囲

- 抜き取りに入らなかった 114 ケースは、`reviews/PH-10-manual/*.md` の記録（TC ごとの観測値・時刻）に依拠した
- 補助 (b) の search は、Implementer が作ったデータ（`ph10-search-main`）の上で実行した。TC-01 で、データが手順書の表どおりであることを確認している
- 監査の「入力検証 2 点」「全数表」「非同期 port の列挙」「Retry」は Implementer の判定を採った。関連する実装は読んだが、全数の再列挙はしていない
- `pnpm start` が起動しないこと（README）は再実行していない。デプロイは brief の対象外

## 10. 検証環境の引き継ぎ状態（14:55 時点）

- **dev**: :3000 で起動中（14:48 に再起動、ログ `scratchpad/v10/dev-v10-final.log`）。preview は停止
- **設定**: `.dev.vars` と `apps/web/wrangler.state.toml` はバックアップ（`scratchpad/v10/{dev.vars.bak,wrangler.state.toml.bak}`）と `cmp` で一致。`git status` は未追跡の `.claude/` `.codex/` `.spec-implement/` だけ
- **DO 状態**: 14:33 に編集した sqlite 4 ファイルの編集前のコピーを `scratchpad/v10/sqlbak/` に置いた
  - `v10-main`: メモ 4 件の日付をずらし、resume-link 2 行を書き換えた（現在は `done`）
  - `v10-trexp` / `v10-tredge` / `v10-trext`: 期限をシードした
- **追加したデータ**:
  - `v10-main`: トピック `v10 検証トピック` / `v10 T1` / `v10 T2`（完了済み）/ 100 字の「あ」のトピック、ドキュメント 7 件（`v10 40万字` を含む）、ゴミ箱に `連打テスト v10`。AI 接続 `V10 Verifier Client` は有効（`scratchpad/v10/aic/.ai-client.json`、Implementer の `apps/web/.ai-client.json` には触れていない）
  - SSO 専用アカウント 2 つ: `v10-sso-new@example.com`（subject `v10-sso-new`）と、スタブの既定値で作られた `sso-user@example.com`（subject `dev-subject-1`。既存ならログインしただけ）
  - パスワードを変えたユーザー: `v10-q45`（`V10q45new#1`）
- **agent-browser**: 自分の session（`v10-main` / `v10-main2` / `v10-sso` / `v10-tr` / `v10-pv`）は 14:55 に閉じた。補助 (b) も `v10-sd` / `v10-sd2` を閉じた。ai-client の loopback（8770）は残っていない

## 0. 進捗ログ

- 14:10 開始。入力（CLAUDE.md / brief / design / plan / PH-10.md / PH-10-manual 7 本 / PH-09B.md）を読了。dev（:3000、13:57 起動の Implementer のもの）が稼働中、`git status` clean（未追跡は `.claude/` `.codex/` `.spec-implement/` のみ）
- 14:10 dev（PID 60470）を停止。14:10:26〜14:11:07 に `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を 2 回連続（`scratchpad/v10/final-{1,2}.log`）: 2 回とも exit 0、unit+dom **129 files / 1,598 passed + 2 skipped**、integration **32 files / 191 passed**、lint warning 3（既存: `currentUserPanel.dom` non-null ×2、`wranglerConfig.test` template 文字列）。skip 2 は資格情報が無いときの契約テスト（`resendMailSender.contract` / `googleSsoProvider.contract` の `it.skipIf(!configured)`）。**報告の件数と一致、flaky なし**
- 14:11 dev を再起動（ログ `scratchpad/v10/dev-v10.log`）
- 14:14 補助 2 本を起動: (a) docs + 監査の抜き取り（読み取りのみ、`reviews/PH-10-verify/docs-audit.md`）、(b) search / document のブラウザ抜き取り（session `v10-sd`、`reviews/PH-10-verify/{search,document}.md`、dev の再起動・sqlite 編集は禁止）。自分は timeline / trash / settings / account / ai、sqlite 窓・dev 再起動を伴うケース、preview、コードレビューを担当
- 14:13〜14:17 主要シナリオを新規ユーザー `v10-main@example.com` / `V10verify#Pass1`（session `v10-main`、dev）で: account TC-02、timeline TC-22 / 01 / 02 / 21 / 05 / 17 / 18 / 13 / 33 / 34 / 12 合格（詳細は §3.1）
- 14:17〜14:19 同ユーザーで検索（チップの current）、settings TC-08 / 09 / 10 相当（ネイティブ検証 + `noValidate` + 送信本文の改竄で transport 拒否）、エクスポート（`agent-browser download` → Python `zipfile` / `ditto`。macOS `unzip` は exit 2 を再現）合格
- 14:19〜14:21 AI 経路（ai-client の複製 `scratchpad/v10/aic`、loopback 8770、クライアント `V10 Verifier Client`）: account TC-07 / 08 / 09 / 27、ai TC-01 / 04 / 06 / 07 / 08 / 13 / 14 / 15 / 22（一部）合格。`tools/list` = 11 個
- 14:21〜14:26 account: TC-06 / 10（`[dev-mail]` 約 30 秒、別 session `v10-main2` の失効）/ 31 / 19 / 20 / 05 / 32 / 11 / 41（`SSO_DEV_STUB`、subject `v10-sso-link`）/ 03 / 04 / 33 / 42 / 43 / 18 / 17 合格（v10-main のパスワードは `V10chg#Pass3` に変わった。SSO 専用 `v10-sso-new@example.com` = subject `v10-sso-new`）
- 14:26〜14:30 trash（v10-main。データは AI クライアント経由で作成、ハードデリートは UI）: TC-01 / 02 / 15 / 06 / 18 / 19 / 20 / 21 / 22 / 16 / 12 / 14 合格（`ce6501a` / `1210ec5` の表示を確認）
- 14:30〜14:40 コードレビュー（`9bec53e` / `87a1f46` / `c844897` / `ce6501a` / `c734ddb` / `1210ec5` / `425f36b` / `ebbac54` / `f14fcd8` / `3d23270` / `9f5671e` / `c1f3366`）。詳細は §4。sqlite 窓用のユーザーを準備中
- 14:33 補助 (b) 完了: search 10 件 / document 14 件すべて合格（`reviews/PH-10-verify/{search,document}.md`）。補助 (a) 完了: 監査に重大な違反なし、docs に「存在しないテストを名指しする保証文」2 件（`docs/runtime_cloudflare.md:390`、`docs/test.md:52`）を自分でも確認（§5）
- 14:33 sqlite 窓 + 配送異常系のため dev を停止する（準備済みユーザー: `v10-tredge` / `v10-trexp`（保持 1 日）/ `v10-trext` / `v10-q45` / `v10-q46`、パスワードは全員 `V10verify#Pass1`）
- 14:33〜14:38 sqlite 窓（dev 停止中に 4 ファイルを編集、バックアップ `scratchpad/v10/sqlbak/`）→ R1（`wrangler.state.toml` の `[[queues.producers]]` を一時コメントアウト）で起動: account TC-45 手順 1〜4（`dir:g1:b11`、本物の publish 失敗 → 約 30 秒で quarantined）、timeline TC-03 / 07 / 24、trash TC-13 / 23 / 24 合格
- 14:38〜14:40 R1 のまま account TC-47 の代替観測（operator `list-poisoned-jobs` / `requeue-poisoned-job`）と settings TC-03〜06（多日エクスポート）。R2（binding を戻す、`git status` clean）で TC-45 手順 5〜6: requeue → 30 秒で `[dev-mail]` → 再設定完了（「届く」側）
- 14:41〜14:44 R3（`.dev.vars` の `MAIL_DEV_SINK` を不正値）で account TC-46: `v10-q46`（`dir:g1:b6`）→ consumer 失敗 ×4 → DLQ → `dlq { eventId, type, outcome: 'failed' }` で ack。停止中の sqlite で発行元行は `published` / attempt 0（quarantined ではない）。R4 で `.dev.vars` / `wrangler.state.toml` をバックアップと同一に戻した（`cmp` 一致、`git status` clean）
- 14:44〜14:46 `pnpm build`（exit 0）→ `pnpm preview`（:4173、dev 停止中）: 保護 8 画面を cookie 付き `curl` で SSR 本文確認（スケルトンなし、HTML タグのメモはエスケープ済み）、未ログイン 307、ブラウザで投稿（楽観 UI）/ 削除確認ダイアログ（中央・危険ボタンの配色）/ 検索チップの current / P-09 タイトル空エラー。14:46 preview 停止、dev 再起動（ログ `scratchpad/v10/dev-v10-final.log`）
- 14:46〜14:48 issue 候補「40 万字の編集画面が chrome-error」の再現確認: `v10 40万字`（REST で 400,000 字）を dev で `open` + `wait --load networkidle` ×3、preview で `curl` ×3（200、2.43 MB、0.06〜0.17 s）+ ブラウザ ×3 — すべて表示、textarea 400,000 字。再現せず。14:48 dev を再起動（引き継ぎ状態）
- 14:55 報告を記入。自分の agent-browser session を閉じ、dev（:3000）は起動したまま引き継ぐ

## 再検証（Verifier、2026-09-11 16:15〜16:24 JST）

対象 HEAD `fef0e3b`（`de888b7` からの差分: `f3b09af` spec、`eb1a084` / `f47777c` / `fef0e3b`）。

### 進捗ログ

- 16:15 開始。`git status` clean（未追跡は `.claude/` `.codex/` `.spec-implement/` のみ）
- 16:16:22〜16:17:04 自動テスト 2 回連続（dev 停止中、`scratchpad/v10/re-final-{1,2}.log`）: 2 回とも exit 0、unit+dom 130 files / 1,601 passed + 2 skipped、integration 33 files / 195 passed、lint warning 3（既存）
- 16:17 dev 起動 → AI REST で `v10-main` に `v10page 01`〜`70` を投稿 → 16:18 dev 停止、sqlite で 01〜30 を −2 日（9/9）、16〜30 の `posted_at` を同一値に（同時刻 15 件の塊がページ境界 50 を跨ぐ形）。編集前のコピー `scratchpad/v10/sqlbak/0f66-before-re.sqlite`、`PRAGMA integrity_check` ok、WAL 0 byte。正解の並び（`posted_at DESC, id DESC`）を sqlite から `truth-all.txt`（77）/ `truth-kw.txt`（70）に書き出し
- 16:19〜16:21 dev（session `v10-re`）: 通常ページング 50 → 77、キーワード `v10page` 50 → 70、日付ジャンプ 9/9（両方向の読み込み）→ 77、キーワード + 日付ジャンプ → 70、空の日 9/10（過去側）・9/5（未来側が近い）— いずれも正解の並びと完全一致、重複 0
- 16:21〜16:24 D-1 のコードとテスト、D-2〜D-6 と `knowledgeFeeds.dom`、docs の機械的な突き合わせ（自作 `scratchpad/v10/docrefs_v.py`）、`gh issue view` #3〜#8 / #21 を確認

### 判定（再検証、HEAD `fef0e3b`、2026-09-11T16:24+09:00）

| 完了条件（R-INT-01） | 最終判定 | 根拠 |
|---|---|---|
| 1. 全マニュアルテストをブラウザで実行して合格 | **合格** | 前回の抜き取り 94 ケース（合格 93 / 部分 1）はそのまま有効。部分実施の TC-47 は、Manager の判断（M-2: brief の契約検証の方針に沿い、integration + sqlite シードの代替で受け入れる）で合格として数える。前回以降の変更のうち実行経路に触れるのは `eb1a084`（タイムラインの読み取り文）だけで、R-TL-02 / R-TL-03 の該当ケースを dev で再実行し、全件合格した（下の R-2） |
| 2. `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` | **合格** | 16:16:22〜16:17:04 に 2 回連続して exit 0。unit+dom 130 files / 1,601 passed + 2 skipped、integration 33 files / 195 passed。報告と一致し、flaky は出なかった |
| 3. アーキテクチャ監査に重大な違反なし | **合格** | 前回の判定から変わらない。`eb1a084` は adapter 内の SQL の組み立てを関数に切り出しただけで、層・書き込み口・UoW に変化は無い。軽微の既知事項（application の `SqlStorage` 読み取り）は #10 で追跡されている |
| 4. docs が現状を述べる | **合格** | D-1〜D-6 はすべて解消した（R-3）。docs 7 ファイルのバッククォート内のパスとシンボルを機械的に突き合わせ、残ったものは全件、実在するか、外部 API・例示・git-ignored の生成物だった（R-4）。issue 番号は #3〜#8 / #21 だけで、7 件とも実在（OPEN）し、docs の行と題名が一致する |

**R-INT-01 は合格。** 条件 1〜4 をすべて満たす。受け入れ済み 36 項目への影響も無い（`eb1a084` の読み取り文は、旧 OR 形と同じ結果を同じ順序で返すことを dev で全件照合した）。

### R-1. 自動テスト

| 回 | 時刻（+09:00） | 結果 |
|---|---|---|
| 1 | 16:16:22 〜 16:16:43 | exit 0。lint warning 3（既存）、unit+dom 130 / 1,601 + 2 skipped、integration 33 / 195 |
| 2 | 16:16:43 〜 16:17:04 | 同上 |

ログは `scratchpad/v10/re-final-{1,2}.log`。dev は止めて実行した。

### R-2. D-1（`eb1a084`）: 実行計画テストとカーソル条件

- **テストはリポジトリが実行する文そのものを検査している**: `createMemoRepository` のタイムライン読み取りは、`timelinePageStatement({ positioned, direction, inclusive, keyword })` と `pivotStatement("memo" | "before" | "after", keyword)` の戻り値をそのまま `sql.exec` に渡す。`memoRepository.ts` の中で `posted_at` を条件にする文は、この 2 関数の中にしか無い。`memoTimelinePlan.integration.test.ts` は同じ 2 関数を import して `EXPLAIN QUERY PLAN` に掛けている。bind の順序（カーソル `(p, i)` → keyword → `LIMIT`）も文の `?` の順序と一致する
- **テストの中身**:
  - カーソル無しの 1 ページ目 4 形（older / newer × keyword 有無）は `expectOrderedIndexWalk`（`memos_timeline_idx` の SCAN のみで、TEMP B-TREE も表の SCAN も無い）
  - カーソル後の 8 形（older / newer × inclusive × keyword）は `expectIndexSeek`（SCAN 行が無い）
  - 日付アンカーの 2 文は index の SEARCH、メモアンカーはキー SEARCH
  - 負の対照は、OR 形の文で `expectIndexSeek` が throw することの確認。builder を OR 形に戻せば 2 本目の it が赤くなる
  - 限界: `expectIndexSeek` は「index を使う行があり、SCAN 行が無い」までしか見ず、SEARCH の範囲条件の中身は assert しない。ただ、OR 形の実際の plan は `SCAN … USING INDEX` なので、この assertion で退行は捕まる
- **独立の確認**: sqlite3 CLI 3.51.0 で、実 DO ファイルの複製（`v10-main`）に `EXPLAIN QUERY PLAN` を掛けた
  - 行値比較: `SEARCH memos USING INDEX memos_timeline_idx ((posted_at,id)<(?,?))`。`>` 側と keyword 付きも SEARCH
  - OR 形: older / newer とも `SCAN memos USING INDEX memos_timeline_idx`
  - テストの主張と一致する（workerd の SQLite とは版が違うので参考値）
- **意味の同値性**: 旧 older `posted_at < p OR (posted_at = p AND id <tie> i)` は新 `(posted_at, id) <tie'> (p, i)` と、旧 newer は `(posted_at, id) > (p, i)` と同値。`inclusive` の `<=` も保たれている
- **keyword の扱い**: `spec/database/index.md` L202 / L1041 は、keyword（`body LIKE`）を `memos_timeline_idx` の範囲内で評価すると定める。実装は同じ文の WHERE に `body LIKE ? ESCAPE '\'` を足すだけで、plan は keyword 無しと同じ index の SEARCH / SCAN（テストと CLI の両方で確認）。spec どおり
- **dev での再確認**（受け入れ済み R-TL-02 / R-TL-03 の読み取り文）: `v10-main` に `v10page 01`〜`70` を投稿した。sqlite で 01〜30 を 9/9 に移し、16〜30 の 15 件を同一の `posted_at` にした（同時刻の塊がページ境界 50 を跨ぐ形）。正解の並び（`posted_at DESC, id DESC`）を sqlite から書き出し、UI の `article` の id 列と `diff` した（session `v10-re`）

| 確認 | 時刻 | 結果 |
|---|---|---|
| 通常ページング（51 件以上の過去ページ） | 16:19 | 初期表示 50 件は正解の 1〜50 行目と一致。「過去のメモを読み込む」で 77 件になりボタンが消える。**全 77 件が正解の並びと完全一致、重複 0**（同時刻の塊の途中でページが切れても欠落も重複も無い） |
| 絞り込み継続（`?q=v10page`） | 16:19 | 50 → 70 件。一致しない行 0、**正解の 70 件と完全一致、重複 0**（境界が同時刻の塊の中にある） |
| 日付ジャンプ（`?date=2026-09-09`） | 16:20 | 「2026年9月9日(水)に移動しました」。9/9 の見出しが top 0。初期の窓は正解の 19〜68 行目（アンカーの前後 25 件ずつ）と完全一致し、「新しいメモを読み込む」「過去のメモを読み込む」の両方が出る。両方を押すと **77 件が正解と完全一致、重複 0** |
| キーワード + 日付ジャンプ（`?q=v10page&date=2026-09-09`） | 16:20 | 窓 50 件（`v10page 55`〜`06`）、両方向の読み込みで **70 件が正解と完全一致、重複 0** |
| メモの無い日（前後で近い方） | 16:20 | `2026-09-10`: 過去側の 9/9 が近く、9/9 の見出しが top 0。`2026-09-05`: 未来側の 9/6 が近く（過去側 9/1 は遠い）、9/6 の見出しが top 10。どちらも「…にメモはありません。前後で最も近いメモの位置を表示しています」で、エラー 0 |

### R-3. D-2〜D-6 と `knowledgeFeeds.dom`

- **D-2**（`docs/test.md`）: 新しい記述は実装と一致する
  - 各 knowledge route の `paramsSchema` は `z.string().min(1).max(200)` を `validateInput` 経由で `inputValidator` に渡す（`documents_.$documentId*.tsx` / `topics_.$topicId*.tsx`。トピック詳細の変数名は `topicParamsSchema`）
  - `validator.test.ts` は実在し、「各 route の schema は固定しない」「URL から leaf までは manual（document TC-30 / TC-31）」という限界の記述もある
- **`knowledgeFeeds.dom.test.tsx`**（3 件）: `DocumentFeed` / `TopicDetailFeed` に loader の `NotFoundError` を与えると、「〜が見つかりません」の h2 と `/topics` へのリンクが出る。`SystemError` は reject のまま error boundary へ通す。loader は `serverData` の mock の生成順（index 0 / 3）で取っている点が脆いが、`toHaveBeenCalledWith(USER_ID, "missing")` を assert しているので、順序がずれれば黙って通らず赤になる（情報）
- **D-3**: 「failed it four times (the first delivery and `max_retries = 3` more)」。前回の dev 観測（after 4 failed attempts）と一致
- **D-4**: 木から `scripts/` を外し、`apps/web/scripts/`（`app/` の隣）と本文に書いた。実在と一致
- **D-5**: 命名例は `rosterGrep.test.ts`、Aim は `jobKinds.test.ts` / `rosterGrep.test.ts` / `EventId`。いずれも実在する
- **D-6**: `save` の抜粋は `userSettingsRepository.ts` L39〜50 と一致（`user.updatedAt.getTime()`、`if (!matched) throw occConflict();`）。`occConflict()` の説明は `stores/occ.ts` の実装（`ConflictError("OPTIMISTIC_LOCK_FAILURE", "The row was modified by another operation")`）と一致する
- **D-1 の docs**（`runtime_cloudflare.md:390`）: `alarmSchedule` / `rowRunner` / `memoTimelinePlan` の 3 本を挙げる。3 本とも実在し、`planAssertions` の使用箇所とも一致する

### R-4. docs の機械的な突き合わせと issue

自作のスクリプト（`scratchpad/v10/docrefs_v.py`）で確認した。

- 対象は `docs/*.md` 4 本、`README.md`、`CLAUDE.md`、`apps/web/.dev.vars.example`
- パス: バッククォート内の `/` を含む語と拡張子つきの語を、`git ls-files` のファイル・ディレクトリに、完全一致または末尾一致させた
- シンボル: バッククォート内の camelCase / PascalCase の単語を、追跡している ts / tsx / toml / json / css / grit の識別子の集合と突き合わせた
- 未解決として残ったものを全件、手で判定した:
  - パッケージ名: `@repo/*`、`@tanstack/*`、`@testing-library/*`、`@cloudflare/*`、`@vitejs/plugin-rsc`
  - git-ignored の生成物・ローカル状態: `apps/web/.dev.vars`、`.ai-client.json`、`.wrangler/state`、`.wrangler/deploy/config.json`、`wrangler.{staging,production}.toml` と `wrangler.state.*.toml`（`.tpl` から生成。`git check-ignore` で確認）
  - 実在するが git 管理外: `metadata.sqlite`（`.wrangler/state/v3/do/*/` に実在）
  - 「無い」と述べている文: `packages/core/src/adapters/d1/`
  - URL・コマンド・語句: `Asia/Tokyo`、`create/update`、`organization/`、`"/login"`、`Symbol.for(…)` の文字列
  - DOM の短縮名 30 件: 全件を `components/<dir>/__tests__/<name>.dom.test.tsx` として実在確認
  - integration の短縮名 9 件: 全件を `find` で 1 ファイルずつ実在確認
  - 例示: `FooId` / `BarId` / `fooProcedure` / `findPage`
  - 外部 API: `createCompositeComponent` / `CompositeComponent` / `cloneElement` / RSC の低レベル API / vitest の `testTimeout`
  - `useServerAction`: 文脈上「使わない」側の記述
  - `CLAUDE.md` の `leaseUntil`: 列 `lease_until` の概念名。コードに同名の識別子は無い。情報
- **存在しないファイル・テスト・シンボルを名指す文は無い**
- issue: docs / README / CLAUDE.md が名指す番号は #3 / #4 / #5 / #6 / #7 / #8 / #21 の 7 つだけ（`issues/N` のリンクと `(#N)` の両方の形で抽出）。`gh issue view` で 7 件とも OPEN で実在する。各行と題名の対応は次のとおり、すべて一致した
  - #3「request Worker をデプロイできず、`pnpm start` も起動しない」: runtime §0 / 2 / 3 / 4.2 / 4.5 / §14 と README の 2 箇所
  - #4「Pulumi の resources stack に使われない D1 と render の placeholder」: 1 章 / 2 章 / §14
  - #5「DLQ ハンドラの 1 行ログが本番で保持されない」: 8.6 / 8.7 / §14
  - #6「PITR の必須 4 手順に maintenance entry が無い」: 11.3 / §14
  - #7「配送の滞留を能動的に通知する手段が無い」: 7.3 / §14
  - #8「origin 単位の rate limit が無い」: 8.8 / 9.4 / 12.4 / §14
  - #21「ローカルで bucket 単位の cross-DO RPC 障害を起こす手段が無い」: 13 章 row 21 / §14
- 起票済みの #3〜#21 は 19 件。前回の issue 候補一覧（PH-10.md §5）と監査の軽微（#10 `SqlStorage` 直読み）を含む。前回 §7 で追加を勧めた低優先の候補 3 件（packages/core 内の依存方向の scan、UoW の async 拒否の型テスト、長さエラー時の `aria-invalid`）は起票されていない（受け入れ条件の外。任意）

### R-5. 検証環境の引き継ぎ（16:24 時点）

- **dev**: :3000 で起動中（16:19 起動、ログ `scratchpad/v10/dev-re2.log`）。preview は停止
- **設定と作業ツリー**: `git status` は未追跡の `.claude/` `.codex/` `.spec-implement/` だけ。`.dev.vars` / `wrangler.state.toml` は無変更
- **DO 状態**: `v10-main` に `v10page 01`〜`70` を追加した。01〜30 は 9/9 で、16〜30 は同一時刻。編集前のコピーは `scratchpad/v10/sqlbak/0f66-before-re.sqlite`（WAL は 0 byte、`integrity_check` ok）。ほかは前回の引き継ぎのまま
- **agent-browser**: session `v10-re` は下で閉じる
- 16:24 報告を記入。session `v10-re` を閉じた。dev（:3000）は起動したまま
