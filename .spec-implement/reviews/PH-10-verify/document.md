# PH-10 Verify 補助: document.md 抜き取り再実行

- 対象 HEAD: `de888b7`、環境 dev http://localhost:3000、agent-browser session `v10-sd`（2 つ目が要るときは `v10-sd2`）
- 開始: 2026-09-11T14:13+09:00
- スクリーンショット: `/private/tmp/claude-501/-Users-hikaru-github-com-tuanemuy-fog/dd13c819-2fe3-40f6-8885-afaf8719e7bd/scratchpad/v10/sd/`

## 集計

- 合格 14 / 不合格 0 / 実行不能 0（実行: TC-20, TC-21, TC-05, TC-06, TC-22, TC-23, TC-09, TC-11, TC-26, TC-10, TC-18, TC-19, TC-35, TC-25）
- 使用ユーザー: 14:20 に /signup で新規登録した `v10-sd-doc@example.com` / `V10verify#Pass1`（データはすべて自分で作成: メモA/B/C、トピック `補充後トピック`・`読書ノート`・`日々の作業メモ`（完了済み）・100 文字名のトピック・`削除テスト用`（ゴミ箱）、ドキュメント `タイトル補充`・`読書メモまとめ`・`下書き`（ゴミ箱）・`配下ドキュメント1`（ゴミ箱））
- PH-10 修正の確認:
  - b7deeb4: P-07 のヘッダーに `完了にする` / `完了を解除` ボタン、`トピックの操作` メニューは `編集 | 削除` のみ（完了前後とも）— TC-06
  - 87a1f46: 空のトピック名（作成 / 編集）・空のタイトル（作成 / 編集）でボタンは押せ、送信で role=alert `トピック名を入力してください` / `タイトルを入力してください` + 入力欄 `aria-invalid="true"` + `aria-describedby` がエラー要素を指す。他の入力値は保持 — TC-20〜23
  - 1210ec5: 削除確認ダイアログ（ドキュメント / トピック）がネイティブ modal `<dialog>` で画面中央（中心差 [0,0]）、危険ボタンは白地に `oklch(0.55 0.19 27)` の文字（コントラスト約 5.34:1）— TC-18 / TC-19
  - c1f3366: ゴミ箱内の出典メモが P-08 で `削除済みのメモ`（`div[aria-disabled=true]`、リンクでない）、復元でリンクに戻る — TC-26
- 観察（合否に関係なし）:
  - TC-35: `maxLength` を回避して 101 文字の名前を送ると `トピック名は100文字以内で入力してください` の role=alert は出るが、名前欄に `aria-invalid` / `aria-describedby` が付かない（空の名前のエラーでは付く）。通常入力は `maxLength=100` で止まる
  - TC-26: `削除済みのメモ` の行にも右端の ↗ アイコン（`.fog-origin-jump`、薄色）が残る。遷移はしない
  - TC-21: `agent-browser fill <ref> ""` では React の状態が空にならず、元の値のまま保存された（ツール側の挙動。native setter + input イベントで空にすると期待どおりのエラー）
  - 14:23 台に `v10-sd` のブラウザが cookie 空の状態に戻った（外部要因のブラウザ再起動と見られる）。再ログイン後に継続し、結果への影響はない

## 結果

| TC | 判定 | 時刻 | ユーザー | 観測 |
|---|---|---|---|---|
| TC-20 | 合格 | 2026-09-11T14:21:02+09:00 | v10-sd-doc@example.com（14:20 に /signup で新規登録。メモA/B/C を投稿済み） | P-06 はトピック 0 件で `最初のトピックを作ろう`。`説明を追加` → 説明欄に `説明のみ`、名前欄は空。`追加` ボタンは disabled でなく押せる。押下 → 作成されず、role=alert `トピック名を入力してください`（id `_r_1_`）が名前欄の下に出て、名前欄は `aria-invalid="true"` / `aria-describedby="_r_1_"`。説明欄の値 `説明のみ` は保持。スクリーンショット `sd/doc-tc20-error.png`。名前欄に `補充後トピック` を入力した時点で alert は消え、`追加` → 14:21:10 に一覧へ `補充後トピック ドキュメント数: 0 説明のみ`（alert 0） |
| TC-21 | 合格 | 2026-09-11T14:22:21+09:00 | v10-sd-doc | P-07（`補充後トピック`、`/topics/01a08ee9-9609-76fa-a9bc-f5573ca1d632`）のトピックの操作 → `編集` で `名前=補充後トピック` / `説明=説明のみ` のフォーム。名前欄を空にして（native setter + input イベント）`保存`（disabled でない）→ 14:22:11 に保存されずフォームが残り、role=alert `トピック名を入力してください`（id `_r_4_`）、名前欄 `aria-invalid="true"` / `aria-describedby="_r_4_"`、説明 `説明のみ` は保持。スクリーンショット `sd/doc-tc21-error.png`。`取り消し` → 再読み込み後も見出し下の名前は `補充後トピック`。※1 回目は `agent-browser fill @e14 ""` で空にしようとしたが React の状態が空にならず（ツール側の問題）、元の名前のまま保存されてフォームが閉じた — 名前は不変で、アプリの不具合ではない |
| TC-05 | 合格（準備を兼ねて実施） | 2026-09-11T14:22:48+09:00 | v10-sd-doc | P-07（`作業メモ`、`/topics/01a08ee9-be03-72b8-b86b-aecd0e3eef5a`）のトピックの操作 → `編集` で現在値 `作業メモ` / 空の説明。`日々の作業メモ` / `日次の作業記録` にして保存 → P-07 の表示が `日々の作業メモ | 完了にする | 日次の作業記録` に更新。P-06 側の反映は TC-06 手順3 の一覧（完了済みセクション内の `日々の作業メモ … 日次の作業記録`）で確認 |
| TC-06 | 合格（改訂後の期待で判定） | 2026-09-11T14:24:09+09:00 | v10-sd-doc | P-07 のヘッダー `.fog-topic-head` = `日々の作業メモ | 完了にする | 日次の作業記録`。`完了にする` はタイトル行（`.fog-topic-title-row`）のボタンで、`トピックの操作`（…）メニューは `編集 | 削除` の 2 項目だけ（`<div role=menu>` に menuitem 2 つ。完了系の項目なし）。本文に「アーカイブ」なし。スクリーンショット `sd/doc-tc06-p07-before.png` / `sd/doc-tc06-menu.png`。`完了にする` 押下 → 14:23:07 にヘッダーが `日々の作業メモ | 完了 | 完了を解除 | 日次の作業記録`（完了バッジ + ボタンが `完了を解除` に変化、alert 0）、メニューは完了後も `編集 | 削除`。スクリーンショット `sd/doc-tc06-p07-after.png`。P-06（14:24:09）: 進行中は `補充後トピック`・`読書ノート` のみ、下部に `完了済み（1）`、開くと `日々の作業メモ ドキュメント数: 0 日次の作業記録`。「アーカイブ」なし。スクリーンショット `sd/doc-tc06-p06.png`。確認ポイント: 完了（ヘッダーのボタン）と削除（… メニュー内）は別の場所 |
| TC-22 | 合格 | 2026-09-11T14:24:55+09:00 | v10-sd-doc | P-07（`読書ノート`、`/topics/01a08ee9-b3fc-764b-bbcb-66a1b6056652`）→ `新しいドキュメント` → `/topics/01a08ee9-b3fc-…/documents/new`（見出し `ドキュメント作成`、タイトル / 本文 / 出典）。タイトル空・本文 `本文のみ` で `保存`（disabled でない）→ 14:24:49 に URL は `/documents/new` のまま、role=alert `タイトルを入力してください`（id `document-title-error`）、タイトル欄 `aria-invalid="true"` / `aria-describedby="document-title-error"`、本文 `本文のみ` 保持。スクリーンショット `sd/doc-tc22-error.png`。タイトルに `タイトル補充` を入れると alert は 0 件になり、`保存` → `/documents/01a08eed-0139-777e-8c41-5610183f711f`（`読書ノート | タイトル補充 | 2026/09/11 14:24 更新 | 編集 | 履歴 | 削除 | 本文のみ`） |
| TC-23 | 合格 | 2026-09-11T14:25:21+09:00 | v10-sd-doc | P-08（`タイトル補充`）→ `編集` → `/documents/01a08eed-…/edit`（タイトル `タイトル補充` / 本文 `本文のみ` / 変更理由 空）。タイトルを空にして（native setter + input）`保存` → 14:25:09 に URL は `/edit` のまま、role=alert `タイトルを入力してください`（`document-title-error`）、タイトル欄 `aria-invalid="true"`、本文 `本文のみ` 保持。スクリーンショット `sd/doc-tc23-error.png`。`タイトル補充` を入れ直して `保存` → `/documents/01a08eed-…`（P-08、`タイトル補充 | … | 本文のみ`） |
| TC-09 | 合格 | 2026-09-11T14:26:07+09:00 | v10-sd-doc | `/topics/01a08ee9-b3fc-…/documents/new` でタイトル `読書メモまとめ`、本文 3 行（`## 要点` / `打ち合わせの決定事項を整理した。` / `# 補足はまだ書いていない`、textarea に native setter で入力し値が 3 行のまま）。`出典を追加` → `メモを検索` に `打ち合わせ` → `検索` → 候補 `2026/09/11 14:20 メモA: 打ち合わせの決定事項 追加` → `追加` で出典一覧に `メモA … 出典から外す`、候補側は `追加済み`。`保存` → 14:25:53 に `/documents/01a08eed-e46b-7439-96d6-f9963498a9cd`。P-07 のドキュメント一覧に `読書メモまとめ` / `タイトル補充`、関連メモに `メモA`。P-04 のメモA（`#memo-01a08ee9-2ac1-…`）に出典導線リンク `読書メモまとめ` → `/documents/01a08eed-e46b-…` |
| TC-11 | 合格（改訂後の期待で判定） | 2026-09-11T14:25:53+09:00 | v10-sd-doc | P-08（`読書メモまとめ`）の本文 DOM = `<h2>要点</h2><p>打ち合わせの決定事項を整理した。</p><h1>補足はまだ書いていない</h1>`、本文領域の innerText に `#` なし。`section[aria-label="元になったメモ"]` は本文とは別の要素（互いに包含しない）で、`2026/09/11 14:20` + `メモA: 打ち合わせの決定事項` のリンク行（`/?memo=01a08ee9-2ac1-…`）。所属トピック導線 `読書ノート` → `/topics/01a08ee9-b3fc-…`。スクリーンショット `sd/doc-tc11-p08.png` |
| TC-26 | 合格 | 2026-09-11T14:27:14+09:00 | v10-sd-doc | P-04 でメモA（`01a08ee9-2ac1-…`、出典導線 `読書メモまとめ` 付き）のメモの操作 → 削除 → ダイアログ `メモを削除しますか？ / メモはゴミ箱に移動し、保持期限を過ぎると完全に削除されます。` → 削除（14:26:39）でタイムラインから消える（残りはメモC・メモB）。P-08（`/documents/01a08eed-e46b-…`、14:26:42）の `元になったメモ` = `2026/09/11 14:20 | 削除済みのメモ`、行は `<div class="fog-origin-row deleted" aria-disabled="true">`（href なし、セクション内の `<a>` 0 件、cursor `auto`、文字色 `oklch(0.69 0.01 275)`）。スクリーンショット `sd/doc-tc26-deleted.png`。その行を実クリック → URL は `/documents/01a08eed-…` のまま（遷移なし）。P-12 に `メモA: 打ち合わせの決定事項 / メモ / 残り30日` → `復元`（14:27:08）→ `メモを復元しました` + `ゴミ箱は空です`。P-08 を開き直すと行は `<a class="fog-origin-row" href="/?memo=01a08ee9-2ac1-…">` で `メモA: 打ち合わせの決定事項` 表示、押下 → `/?memo=01a08ee9-2ac1-…` でメモA が `.fog-memo-highlight` かつビューポート内 |
| TC-10 | 合格（TC-18 の準備を兼ねて実施） | 2026-09-11T14:27:37+09:00 | v10-sd-doc | `読書ノート` の新規ドキュメントでタイトル `下書き` のみ・出典なしで保存 → `/documents/01a08eef-7a51-72b9-a890-4a26d499c74f`（alert 0）。P-08 = `読書ノート | 下書き | 2026/09/11 14:27 更新 | 編集 | 履歴 | 削除`、`section[aria-label="元になったメモ"]` なし |
| TC-18 | 合格 | 2026-09-11T14:28:13+09:00 | v10-sd-doc | P-08（`下書き`）の操作 `削除`（`編集` / `履歴` と並ぶボタン）→ ネイティブ `<dialog>`（`open` かつ `:modal`）`ドキュメントを削除しますか？ / ドキュメントはゴミ箱に移動し、保持期限を過ぎると完全に削除されます。ゴミ箱から元に戻せます。 / 削除 / キャンセル`。位置: `getBoundingClientRect` = left 480 / top 154.2 / width 320 / height 324.6（viewport 1280×633）、ダイアログ中心とビューポート中心の差 [0, 0]（`position: fixed`、`inset: 0`、`margin: 154.203px 480px`）。backdrop `oklch(0.21 0.006 270 / 0.28)`。危険ボタン `削除`（`.fog-danger`）: color `oklch(0.55 0.19 27)`（≒ rgb(201,48,45)）/ background-color `oklch(1 0 0)`（白）/ border `oklch(0.875 0.006 278)`、rect [510,327,260,54]、白地とのコントラスト比 約 5.34:1（計算値）。`キャンセル`（`.fog-secondary`）: color `oklch(0.38 0.008 275)` / 背景 transparent。スクリーンショット `sd/doc-tc18-dialog.png` で中央表示・赤文字が白地で読めることを目視確認。`削除` 実行 → 14:28:04 に P-07 `/topics/01a08ee9-b3fc-…` へ戻り、一覧は `読書メモまとめ` / `タイトル補充` のみ（`下書き` なし）。P-12（14:28:13）に `下書き / ドキュメント / 残り30日 / 2026/09/11 14:28 に削除` |
| TC-19 | 合格 | 2026-09-11T14:29:00+09:00 | v10-sd-doc | 準備: `削除テスト用`（`/topics/01a08ef0-599f-71aa-ba09-3cc95419035d`）と配下 `配下ドキュメント1`（`/documents/01a08ef0-779f-…`）を作成。P-06 の `削除テスト用` 行に contextmenu イベント（右クリック相当）→ メニュー `編集 | 削除`（完了の項目なし。P-07 では完了はヘッダーのボタン）→ `削除` → `<dialog>`（`:modal`）`トピックを削除しますか？ / トピックと配下のドキュメントはゴミ箱に移動し、保持期限を過ぎると完全に削除されます。`。位置は TC-18 と同じ left 480 / top 154.2 / 320×324.6、中心差 [0, 0]。`削除` ボタン color `oklch(0.55 0.19 27)` / background-color `oklch(1 0 0)`。スクリーンショット `sd/doc-tc19-dialog.png`。実行 → P-06 から `削除テスト用` が消え、P-12 に `削除テスト用 / トピック / ドキュメント1件・残り30日` と子行 `配下ドキュメント1 / トピックとセットで削除` |
| TC-35 | 合格（101 文字は「入力欄で 100 文字までに制限」側、制限を回避した送信でもエラー） | 2026-09-11T14:29:32+09:00 | v10-sd-doc | P-06 の名前欄は `maxLength=100`。100 文字（`境界百` + `あ`×97）を fill → 値 100 文字 → `追加` → 14:29:24 に一覧へ 100 文字の名前で作成（alert 0）。101 文字（`境界百一` + `い`×97）を fill → 値は 100 文字で止まる。native setter で 101 文字を入れて `追加` → 作成されず（`境界百一…` の行 0）、`.fog-error` role=alert `トピック名は100文字以内で入力してください`、入力値 101 文字は保持。スクリーンショット `sd/doc-tc35-101.png`。観察: この文字数超過 alert は名前欄に `aria-invalid` / `aria-describedby` が付かない（空の名前のとき＝TC-20 は両方付く） |
| TC-25 | 合格 | 2026-09-11T14:30:37+09:00 | v10-sd-doc（セッション1 = `v10-sd`、セッション2 = `v10-sd2` に同じユーザーでログイン） | S1 で `/documents/01a08eed-e46b-…/edit` を開いたまま（14:30:08、本文 3 行）。S2 で同じ編集画面を開き本文末尾に `セッション2の追記`、変更理由 `競合テスト用の編集` で保存 → 14:30:22 に S2 は P-08 へ戻り本文に `セッション2の追記`。S1 で本文末尾に `セッション1の追記` を足して `保存` → 14:30:30 に URL は `/edit` のまま、`.fog-conflict`（role=alert）`編集を開いた後にあなたが編集しました（2026/09/11 14:30・競合テスト用の編集）。 / 現在のタイトル 読書メモまとめ / 現在の本文 … セッション2の追記 / そのまま保存すると、あなたの内容が最新の内容として新しいリビジョンに積まれます。`、ボタンは `そのまま保存` に変化。スクリーンショット `sd/doc-tc25-conflict.png`。`そのまま保存` → P-08 の本文末尾が `セッション1の追記`。P-10: `リビジョン 1 作成` / `リビジョン 2 競合テスト用の編集` / `リビジョン 3 手動編集`（いずれも `あなた`、14:25 / 14:30 / 14:30）。リビジョン 2 を比較元にすると `リビジョン 2 → リビジョン 3（最新）` の差分で `削除: セッション2の追記` / `追加: セッション1の追記` — 介在編集はリビジョン 2 として残っている |

## 進捗ログ

- 14:13 開始。手順書と Implementer 記録を読んだ
- 14:21 新規ユーザー v10-sd-doc で準備（メモA/B/C、トピック 補充後トピック・読書ノート・作業メモ）。TC-20 を記録
- 14:22 TC-21 を記録
- 14:24 TC-05 / TC-06 を記録。14:23 台に v10-sd のブラウザが一度ログアウト状態（cookie 空）になった — ブラウザが外部要因で再起動したと見られる。再ログインして継続
- 14:25 TC-22 / TC-23 を記録
- 14:26 TC-09 / TC-11 を記録。次は TC-26
- 14:27 TC-26 を記録。次は TC-18（ドキュメント削除ダイアログの位置と配色）
- 14:28 TC-10 / TC-18 を記録。必須ケースは完了。任意の TC-35 / TC-25 に進む
- 14:29 TC-19 を記録
- 14:30 TC-35 を記録。次は TC-25（v10-sd2 を v10-sd-doc でログインし直す）
- 14:31 TC-25 を記録。document.md の抜き取りはここまで
- 14:31 完了。ブラウザセッション v10-sd / v10-sd2 を close
