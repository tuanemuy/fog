# PH-07 検証報告（Verifier、2026-09-08 22:00〜22:18 JST）

対象 HEAD `3e041c2`（`aa64fcb` / `a0df215` / `50fd460` / `e9cf421` / `57f14df` / `a282bb7` / `3e041c2` + Manager `b0bbd6f` `3f108df`）。`9ac3024..3e041c2` は 98 ファイル。作業ツリーは追跡ファイル clean。

## 判定

| ID | 判定 | 根拠（詳細は下） |
|---|---|---|
| R-AC-05 OAuth 2.1 認可 / P-14 | **合格** | DCR 201 → `authorize` → P-14 にクライアント名・メールアドレス・許可される操作 4 / できないこと 3 → 許可 → loopback に `code` + `state` → token 200。拒否 → `error=access_denied`（行は増えない）。改竄 `client_id` / `redirect_uri` 不一致 / 偽 blob / **期限切れ blob（10 分経過後の実 URL）** → alert + 許可ボタンなし。PKCE なし → `redirect_uri?error=invalid_request`。未ログイン → P-01 → 戻って許可成功。preview も同じ |
| R-AC-06 一覧・解除・全失効 / P-13 / P-03 | **合格** | P-13 に名前 / 接続日 / 最終利用（ツール呼び出しで `last_used_at` が進むのを sqlite と画面で確認）→ 解除 → `ping` / REST 401 `invalid_token` + `WWW-Authenticate`、`refresh` 400 `invalid_grant` → 再認可で復帰。P-03 で 2 件 → 「失効しました（2 件）」→ 0 件 → 再度「失効しました（0 件）」 |
| R-AI-01 11 ツール / 許可リスト | **合格** | dev で 11 ツール全部を MCP と REST で実走（下表）。リビジョンの「誰が」= `Verify07`、「なぜ」= `changeReason`。ゴミ箱内は get / update / delete / search / recent / 出典 / 作成先すべて「存在しない」。不在の出典・トピックは原子的に失敗。禁止ツール 7 種は MCP `-32601` / REST 404。型（`typeGuard.test.ts`）+ 配線（`tools.test.ts` の import 許可リスト）+ 列挙（`AI_TOOLS`）の三層をコードで確認 |
| R-AI-02 ガイダンス | **合格** | `initialize.instructions`（983 字）= `GET /api/ai.instructions`、11 ツール名と推奨行動を含む。`tools/list` の各 `description` に要点 |

**PH-07 全 4 項目 合格。** PH-01〜06 の 28 項目への影響なし（§6）。

### Manager の判断が必要な事項

- **O-1（軽微・設計 §1.4 との差）**: 設計 §1.4 は「`account.status !== "active"` も 401」と書くが、`authorizeAiClient` / `consumeAuthorizationCode` の facade は `ai_client_connections` だけを見て `account.status` を見ない（`userDataDurableObject.ts`）。`account.status` が `deleting` / `deleted` になる経路は退会（後続フェーズ）で、現時点で到達不能のため合否に含めない。退会フェーズで「退会中のアカウントの AI トークン」をどう扱うかを設計に持ち越すか、今 1 行足すかは Manager 判断。
- **O-2（OAuth 2.1 の細部）**: `refresh_token` の交換で `client_id` を照合しない（refresh の payload は `uid` / `cid` のみ）。OAuth 2.1 §4.3 は公開クライアントの refresh に `client_id` を要求し、発行時のクライアントへ束縛する。`client_id` は誰でも DCR で作れる署名付きメタデータなので束縛の実効性は薄く、失効は接続の `status` で即時に効く（△-2）ため実害は小さい。仕様準拠として直すか、限界として JSDoc に書くか。
- **O-3（許可リストテストの限界）**: `tools.test.ts` の走査は `@repo/core/application/...` の import 指定子だけを見る。`AiToolContext.container` は `RequestContainer` 全体なので、将来 `presentation/ai/**` が `ctx.container.trashGateway.…` のように gateway を直接呼べば usecase を経ずに到達でき、テストは検知しない。現状のコードは gateway を直接呼んでいない（`grep -rn "Gateway\|container\.(memo|knowledge|identity|trash|search)" presentation/ai/*.ts` = 0 件）。`container` を必要な usecase 呼び出しに閉じた型に狭めるか、走査に `Gateway` 参照の禁止を足すかは任意。
- **O-4（軽微）**: MCP の `Origin` 検査は `origin !== deps.appUrl` の文字列比較。`APP_URL` に末尾 `/` やパスが付く設定だと同一オリジンのブラウザ呼び出しが 403 になる。`new URL(appUrl).origin` との比較が堅い。現行 `APP_URL="http://localhost:3000"` では問題なし。
- **O-5（環境・PH-06 O-2 の後始末）**: `aa64fcb` の fallback により、PH-06 再検証で「詰まっていた」Alarm ジョブは dev 再起動（22:04）で全部動いた（b15 `resume-signup` done、b2 / b6 / b11 `sweep-reset-tokens` done、`schema gate` ログ 0 件）。ただし b0 の `resume-signup`（Implementer が 19:52 に登録したアカウント）は 2 時間放置のあいだに予約行が TTL で掃除されていたため `attempt 4 → poison DATA_INTEGRITY_ERROR`（`resume-signup: reservation lost before the saga mark`）で終端した。**「材料が既に無い」ケースの正しい終端であり不具合ではない**が、ローカル DO 状態に poison 行 1 件が残っている（削除するかは Manager / Implementer）。
- 参考: MCP 仕様の例示では未知ツールは `-32602`（Invalid params）だが、本実装は設計どおり `-32601`。クライアント互換上どちらでも動く。

## 1. 自動テスト（自分で実行、22:0x、`checks7.log`）

`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を HEAD `3e041c2` で実行: typecheck 0 / lint 0（warning 3 は既存）/ format:check 0 / test 0。unit+dom **112 files / 1,448 passed + 2 skipped**、integration **24 files / 139 passed** — 報告と一致。

新テストが完了条件を固定しているか:

- `tools.test.ts`: `AI_TOOL_NAMES` と `Object.keys(AI_TOOLS)` が spec の 11 本と同順で一致、各 `description` が `TOOL_DESCRIPTIONS`、JSON Schema 化、`destructiveHint` は `delete` のみ、`AI_GUIDANCE` に 11 ツール名 + 8 フレーズ。**import 許可リスト**: `presentation/ai/**`（`__tests__` 除く）の `from "…"` を走査し、`application/{memo,knowledge,search,trash,identity}/` 配下は 17 モジュールのみ許可、`application/trash/` / `Revision` / `rollback` / `IncludingTrashed` / `getTimeline` / `jumpToDate` / `showMemoInTimeline` / `getTopic` / `editMemo` / `knowledge/editDocument"` / `softDeleteMemo` / `hardDelete` / `emptyTrash` を含む import を禁止。限界は O-3。
- `typeGuard.test.ts`: `AiToolContext.actor` を `EditMemoInput` / `RollbackMemoInput` / `EditDocumentInput` / `RollbackDocumentInput` の `actor` に代入する 4 行に `@ts-expect-error`。`UserActor` が `Actor` に広がれば未使用 directive で typecheck が落ちる。有効。
- `handlers.test.ts`（564 行）: MCP 401 ×3 / initialize / ping / tools/list / notification 202 / batch 400 / 未知 method `-32601` / GET 405 / Origin 403 / tools/call の actor が `{ kind: aiClient, connectionId, clientName }` / NotFound → `isError` / `-32602` / 未知ツール `-32601` / System → `-32000` で message に `SQLITE` を含まない、REST 200 / 404 / 401 / index、OAuth DCR 正常 + 不正 metadata、authorize 正常 → blob / 改竄 → page error / redirect 不一致 → page error / PKCE なし → client へ、token: verifier / client / redirect 不一致 → `invalid_grant` で `consume` 不呼出、正常 + `no-store`、二重交換、refresh 正常 / 失効。
- integration: `aiClientConnections`（approve → 行、guard、revoke 不可逆、revokeAll と OCC 競合、リセット完了で前 reset_version の行だけ revoked、`consumeAuthorizationCode` 一回性 / 失効接続）、`aiMemo`（revision の `actor_type='ai_client'` + `actor_client_name` + projection、saved / unchanged、recent の順序と limit、delete → `listTrash` に出て AI から消える）、`editDocumentByAi`（patch → replaceAll → unchanged で `change_reason`、失敗 patch 3 態で不変、blank changeReason 4 態が DO に届かない、trashed → NotFound）、`selfLocator`（後述 (h)）。

## 2. コードレビュー

- **(a) 権限の非対称性の三層**: 型 — `AiToolContext.actor: AiClientActorDto`、`editMemo` / `rollbackMemo` / `editDocument` / `rollbackDocument` の入力は `UserActor`（typeGuard で固定）。配線 — `tools.ts` の import は 14 usecase + `actorDto`、`auth.ts` / `oauth.ts` は `authorizeAiClient` / `consumeAuthorizationCode` のみ。列挙 — `AI_TOOLS` の 11 本を `isAiToolName` で引き、MCP / REST とも登録外は `-32601` / 404（実走で確認）。`deleteMemoByAi` は `memoGateway.softDeleteMemo` の薄い包み、`trashDocument` / `trashTopic` は人間と同じソフトデリート。gateway を直接呼ぶ箇所は無し（限界は O-3）。
- **(b) OAuth 2.1**: `code_challenge_method=S256` 固定 + `isPkceChallenge`（43 文字）、`redirect_uri` は登録配列との完全一致（`includes`）、`client_id` は `fog_` + 署名付き `{ name, redirectUris, iat }` を `verifyClientId` で検証、認可コードは `{ jti, uid, cid, client, redirect, challenge, exp }` を署名（TTL 600 s）、交換時に `client` / `redirect` / `S256(verifier)` を照合してから `consumeAuthorizationCode` — DO 内 1 tx で `findActiveById` → `DELETE expires_at < now` → `INSERT … ON CONFLICT DO NOTHING RETURNING`（PK 衝突 = 使用済み）。access 3,600 s / refresh 30 日、ラベル `fog:ai-access` / `fog:ai-refresh` / `fog:ai-code` / `fog:ai-client` / `fog:ai-authz` で `deriveHmacKey` の派生鍵が別（`aiTokenCodec.test.ts` が相互拒否を固定）。token 応答 `Cache-Control: no-store` + `Pragma: no-cache`。401 は `WWW-Authenticate: Bearer error="invalid_token", resource_metadata="…/.well-known/oauth-protected-resource"`（実走で確認）。差は O-2（refresh の `client_id` 束縛）。
- **(c) MCP**: stateless、POST のみ（GET / DELETE 405 `Allow: POST`）、`Origin` 検査（実走: `https://evil.example` → 403、自オリジン → 200、無し → 200）、配列 body → `-32600` 400、通知 → 202、JSON-RPC `{ jsonrpc, id, error: { code, message, data? } }`、業務エラー（business / notFound / conflict / validation）は `isError: true` + `content[0].text` の JSON、system は `-32000` で `redactForClient` の固定文言（テストが `SQLITE` 不含を固定）。
- **(d) 認可ミドルウェア**: `authorizeAiRequest` は毎回 `verifyAccess` → `authorizeAiClient` → DO で `findActiveById`（`status = 'active'`）→ null なら 401、`NOT_INITIALIZED` も 401。`recordUsage` は UoW の外で単独 `UPDATE … WHERE status = 'active' AND (last_used_at IS NULL OR last_used_at < ?)`、失敗は `logger.warn`（connectionId のみ）で握る。`AiClientActorDto = { kind: "aiClient", userId, connectionId, clientName }`（`clientName` はその時点のスナップショット）→ `rebuildActor` → `Memo.edit` / `Document.edit` / `postMemoProcedure` → revision 行（P-05 / P-10 で `Verify07` を確認、integration が `actor_client_name` を固定）。
- **(e) ゴミ箱と原子性**: `memoRepository.findById` / `documentRepository.findById` / `findTimelinePage` / `topicRepository.listByUser` はすべて `status = 'active'`（or `IN ('active','archived')`）で絞る。`create_document` は `createDocument` 既存 usecase（出典・トピック不在で throw、同一 tx）。`edit_document` は zod（`changeReason: min(1)`）+ usecase（trim 空 → `CHANGE_REASON_REQUIRED`、DO 側でも再検査）の二層、patch 失敗は `DocumentPatch.apply` の throw で `save` に到達しない（実走で `latestRevision` 不変）。`replaceAll` は `mode` で明示、`update_memo` は常に全文置換（ADR-006）。
- **(f) リセット完了の自動失効**: `applyCredentialChangeProcedure` が `aiClientConnectionRepository.listByUserId()` から `active && createdAtResetVersion === advancedTo - 1` を絞って `findById` → `AiClientConnection.revoke` → `save(expectedVersion)`。`spec/usecases/identity.md` L273 の手順と一致。revoker の store / usecase は削除済み。E3 の実走: `created_at_reset_version 0` の接続が `reset_version 0 → 1` で `revoked`。
- **(g) 秘密**: `RequestSecrets.aiClientTokenSecret`（≥32、`requireAiClientTokenSecret`）は `readRequestServerConfig` だけが読む（`serverCloudflare.ts` L106、queue 側に無し）。`.dev.vars.example` に帰属表 + 本文 + `secret put`、staging / production tpl のチェックリストに `wrangler secret put AI_CLIENT_TOKEN_SECRET`、`wranglerConfig.test.ts` が「帰属表に request Worker」「request tpl 2 本に `secret put`」「全 config に `[vars]` として現れない」を固定。`.dev.vars` に設定済み（値は見ていない）。`.gitignore` に `apps/web/.ai-client.json`。
- **(h) `requireSelfLocator()` の `_meta` fallback**: `ctx.id.name` が空のとき `readSelfLocator(sql)`（`_meta` が無ければ null）→ 無ければ `NotInitialized`。`spec/database/index.md` L21 / L669 の「DO 名が使えない経路のフォールバック」に該当し、「行データの絞り込みには使わない」も守られている（locator は gate と relay の routing key にだけ使う）。`runMigrationGate` L135 の `meta.self_locator !== input.selfLocator` 一致検査は残っている（name があるときは両者を比較、無いときは `_meta` 由来なので自明に一致）。JSDoc に限界（ローカル workerd の Alarm 起床）を明記。`selfLocator.integration.test.ts` が「name を消したインスタンスへの RPC 読み + `alarm()` で due job が done」「name も `_meta` も無い → `NOT_INITIALIZED`」を固定。dev 再起動後の実挙動は O-5。
- **(i) ban list / lint**: 新しい `*Error` クラスなし、`instanceof` なし（lint 0 warning 増なし）。
- その他: `oauth_consumed_codes` は UoW ctx に出さず facade 直下の adapter 関数 1 つから書く（CLAUDE.md「表に store も登録点も無ければ adapter-owned」）。`userDataPlan.ts` v1 に in-place 追加（初回デプロイ前）。`ssoStateCodec` は `deriveHmacKey` 共通化のみで挙動不変（F の SSO 実走で確認）。`runtime.ts` の `Symbol.for` global slot は `containerStore` と同じ理由。

## 3. スクリプト + ブラウザ検証（自分で実行）

環境: `lsof -t -i :3000 -i :4173 -i :8765 -i :8766 | xargs kill` → `pnpm build`（22:04:34、exit 0）→ `pnpm dev`（:3000）/ `pnpm preview`（:4173）を自分で起動（22:04:46 up）。Implementer の `.ai-client.json` は退避（`scratchpad/ai-client.impl.json`）。agent-browser `verify07`（`ph07v@example.com` / 最終パスワード `verify07reset`）、`verify07x`（未ログイン → 許可）、`verify07s`（SSO）、`verify07w`（`ph07w@example.com` / `verify07w-reset`）、`verify07p`（preview）。スクリプトは `node scripts/ai-client.ts` と `scratchpad/ai7.py`（同じ token で MCP / REST を直接叩く）。

### 3.1 dev: 認可（R-AC-05）

| 時刻 | 操作 | 結果 |
|---|---|---|
| 22:05:14 | `/signup` で `ph07v` 登録 | `/` |
| 22:05 | `register --base :3000 --name Verify07` | 201 `client_id = fog_…`、metadata 2 本 200（`issuer` :3000、`S256`、`none`） |
| 22:05 | `authorize` → URL をブラウザで開く | `/oauth/authorize` → 302 → `/ai-clients/authorize?request=<blob>`。P-14: 「Verify07 が、ph07v@example.com として接続することを求めています。」+ 許可される操作 4 + できないこと 3 + 「許可する」「拒否する」、`<meta name=referrer content=no-referrer>` |
| 22:05 | 「許可する」 | `http://127.0.0.1:8765/callback?code=…&state=<一致>` → スクリプトが token 200 `{ Bearer, expires_in 3600 }` |
| 22:09 | 「拒否する」 | `…/callback?error=access_denied&state=…`、`ai_client_connections` に行が増えない |
| 22:09 | `client_id` を改竄した URL | 302 `/ai-clients/authorize?error=invalid_request` → alert「認可リクエストが正しくありません…」+ 「タイムラインへ」、許可ボタンなし |
| 22:09 | `redirect_uri` を `:9999` に | 302 `/ai-clients/authorize?error=invalid_request` |
| 22:09 | `code_challenge` を外す | 302 `127.0.0.1:8765/callback?error=invalid_request&error_description=PKCE+with+code_challenge_method%3DS256+is+required&state=…` |
| 22:09 | `request=<偽 blob>` | 同じエラー状態 |
| 22:17 | 22:05 発行の blob（exp 22:15:25）を再度開く | **期限切れ**: 同じエラー状態、許可ボタンなし |
| 22:09 | 未ログイン（`verify07x`）で認可 URL | P-01（ログイン）→ ログイン → `/ai-clients/authorize` に戻り P-14 表示 → 許可 → token 200 → `ping` 200 |

### 3.2 dev: 11 ツール（R-AI-01 / R-AI-02、22:06〜22:08、`s7b.log`）

| ツール | 実走 | 結果 |
|---|---|---|
| `initialize` | MCP | `protocolVersion 2025-06-18`、`serverInfo { fog, 0.0.0 }`、`capabilities.tools.listChanged false`、`instructions` 983 字 |
| `tools/list` / `GET /api/ai` | 両方 | 11 本（spec 順）+ `inputSchema` + `annotations`（`destructiveHint` は delete のみ）、`instructions` が initialize と同一 |
| `post_memo` | MCP / REST | M1 / M2 作成。空白のみ → `isError` `EMPTY_BODY`、10,001 字 → REST 422 `BODY_TOO_LONG`、10,000 字 → 200 |
| `update_memo` | MCP | `saved` `latestRevisionNumber 2` → 同じ本文 → `unchanged` |
| `create_topic` / `update_topic` / `list_topics` | MCP | T1 作成。101 字 → `TOPIC_NAME_TOO_LONG`、空 → `EMPTY_TOPIC_NAME`。`archived: true` → `status archived`、`list_topics` に archived として出る → `archived: false` + description 更新。引数なし → `-32602` |
| `create_document` | MCP | D1（出典 M1）。出典に不在 ID を混ぜる → `MEMO_NOT_FOUND`、不在トピック → `TOPIC_NOT_FOUND`、いずれも `list_topics` の配下件数 1 のまま（何も作られない） |
| `edit_document` | MCP / REST | `changeReason` 省略 → `-32602` / REST 400 `INVALID_INPUT`（`path: ["changeReason"]`）、空白 → `CHANGE_REASON_REQUIRED`、patch → `latestRevision 2`、不在 `oldText` → `PATCH_TARGET_NOT_FOUND`、複数一致 → `PATCH_TARGET_AMBIGUOUS`、2 件目で失敗 → 1 件目も適用されず `get` で本文 / `latestRevision 2` 不変、`replaceAll` → 3、同じ本文 → `changed false`、`replaceAll` で `body` 無し → `-32602`、不在 ID → `DOCUMENT_NOT_FOUND` |
| `get` | MCP / REST | memo / document とも全文。REST と MCP で同じ結果、`cache-control: no-store` |
| `search` | MCP | `蒸留器` → M1（`sourceOfDocumentIds` に D1）、`topicId` 絞り込み、T1 を archived にしても D1 がヒット（完了済み包含）、`topicName` は出ない |
| `recent_memos` | MCP | 新しい順、`limit 0` → `-32602` |
| `delete` memo | MCP | M2 → `trashed`。以後 `get` `MEMO_NOT_FOUND`、`search` 0 件、`recent_memos` から消える、二重 delete / `update_memo` / 出典指定 → `MEMO_NOT_FOUND` |
| `delete` topic | MCP | T2（配下 D2）→ `trashedDocumentIds [D2]`。`get D2` / `update_topic T2` / `create_document in T2` / `edit D2` / 二重 delete → NotFound、`list_topics` に無し |
| `delete` document | MCP | D3 → `trashed`、二重 → `DOCUMENT_NOT_FOUND` |
| 禁止ツール | 両方 | `list_trash` / `restore` / `rollback_document` / `get_memo_revisions` / `empty_trash` / `hard_delete` / `rollback_memo` → MCP `-32601 Unknown tool`、REST 404 `UNKNOWN_TOOL` |
| プロトコル | MCP | 未知 method `-32601`、batch 400 `-32600`、通知 202、GET 405、`Origin: https://evil.example` 403、自オリジン 200、不正引数 `-32602`（`issues`） |
| 未認証 | REST / MCP | 401 `{ error: "unauthorized" }` + `WWW-Authenticate: Bearer resource_metadata=…`、偽 token → `error="invalid_token"` |

人間 UI（22:08）: P-04 に M1 の更新後本文（M2 は無し）。P-05（`/memos/<M1>/history`）「Verify07 · リビジョン 1 / 2」。P-10（`/documents/<D1>/history`）「Verify07 · 作成 · 1」「Verify07 · AI が温度を訂正 · 2」「Verify07 · 全面書き直しの検証 · 3」。P-08 の「元になったメモ」に M1。P-12 に「削除される文書」「Verify07 使い捨て（ドキュメント 1 件）」「配下ドキュメント（トピックとセットで削除）」「Verify07 二本目」= 4 件、残り 30 日。

### 3.3 dev: P-13 / P-03（R-AC-06）

- P-13: 「Verify07 | 接続済み: 2026年9月8日(火) / 最終利用: 2026/09/08 22:06」。`call recent_memos` 1 回 → sqlite `last_used_at 1788872811676 → 1788872897238`、再表示で「最終利用: 22:08」。「接続を解除」→ 確認 →「解除する」→ 「接続はありません。」+ MCP URL 案内。sqlite `status revoked version 1`。`mcp ping` 401 `invalid_token`、`call recent_memos` 401、`refresh` 400 `invalid_grant`「The connection is no longer active」。再認可 → `ping` 200 → `refresh` 200 → `ping` 200。
- P-03（`/password-reset/done` を直接、active 2 件）: 一覧 2 行 → 「すべて失効」→ 「失効しました（2 件）」+ 「接続はありません。」→ token 401 → sqlite 3 行 revoked → もう一度 → 「失効しました（0 件）」。

### 3.4 preview（22:12〜22:13、`s7g.log`）

`ph07v` で :4173 にログイン → `register --base :4173 --port 8766` 201 → `authorize`: `:4173/oauth/authorize` → 302 は **`APP_URL` の :3000 の P-14**（「Verify07 Preview が、ph07v@example.com として…」）→ 許可 → loopback → `:4173/oauth/token` 200 → `initialize` / `tools/list` 11 / `post_memo` / `create_topic` / `create_document` / `edit_document`（patch）/ `search` 2 件 / `delete` / 禁止ツール `-32601` / 未認証 401、すべて :4173。P-13（:4173）に「Verify07 Preview」→ 解除 → `ping` 401 / `refresh` `invalid_grant`。

### 3.5 受け入れ済み項目の再実行

- **R-AC-07（リセット完了の自動失効、E3、22:15〜22:17）**: `ph07w` 登録 → 認可（`created_at_reset_version 0`、`ping` 200）→ ログアウト → `/password-reset` 依頼 → 30 秒後 `[dev-mail]` → 新パスワード設定 → `/password-reset/done` に「接続はありません。」→ sqlite `status revoked`, `revoked_at` あり, `reset_version 0 → 1` → `ping` 401 / `refresh` `invalid_grant`。（最初の試行 `ph07v` は全失効済みで active が無く、2 回目はリセット依頼のスロットル窓に当たって送信されず — 手順の不備で、`ph07w` でやり直した。）
- **R-AC-02 / R-AC-09（SSO、F、22:11）**: `ph07v` で SSO 連携 `ph07v-sso` → `sso=linked` → 別セッションで SSO ログイン → `/`、`/settings` に `ph07v@example.com` → 解除 → 0 件。`derivedHmac` 共通化後の state cookie が往復した。
- **R-AC-01 / 登録 saga**: `ph07w` の bucket b1 に `resume-signup done attempt 0`。
- **人間の投稿（`PostMemoDto.actor` の拡張）**: `ph07w` で P-04 から投稿 → 表示。
- poison 走査（22:17）: 全 DO で 1 件（O-5 の b0、PH-07 の操作由来ではない）。dev / preview ログに `schema gate` / `poison` 出現なし。

## 4. Implementer の検証結果を利用した範囲

自動テストの件数は自分で再実行して一致を確認。スクリプト / ブラウザ手順は全て自分で実施（Implementer のスクリーンショットは見ていない）。dom テスト（`authorizeSheet` / `aiConnectionsList` / `aiConnectionsPanel` / `currentUserPanel`）の内容はテスト名と通過で確認し、個別の分岐（楽観除去の失敗で行が戻る等）はブラウザでは再現していない。

## 5. 追加観察の妥当性（Implementer の判断 3 点）

- preview の metadata が `APP_URL`: 妥当。PH-06 の SSO と同じ構造で、preview では `/oauth/authorize` の 302 先も :3000 の P-14 になる（3.4）。deployed では `APP_URL` = 実 URL。
- refresh の非一回性: △-2 の Manager 決定どおりで、失効は `status` で即時（3.3 で確認）。JSDoc に限界あり。O-2（`client_id` 束縛）は別論点。
- `AI_CLIENT_TOKEN_SECRET` 必須: 妥当。`readRequestServerConfig` が要求し queue 側は読まない。無いと request Worker の全リクエストが `CONFIGURATION_ERROR` になる点は `.dev.vars.example` と tpl のチェックリストに書かれている。infra 側の `secret put` は本フェーズ対象外。

## 6. PH-01〜06 受け入れ済み 28 項目への影響

| 変更 | 影響 | 根拠 |
|---|---|---|
| `aiClientConnectionRevoker` → `aiClientConnectionRepository` | なし | R-AC-07 実走（3.5）+ `passwordReset.integration` / `aiClientConnections.integration` |
| `PostMemoDto.actor: ActorDto` | なし | 人間投稿（3.5）、memo unit / dom / integration 全通過 |
| gateway 追加（Identity +6 / Memo +3 / Knowledge +1） | なし | fake roster 更新済み、typecheck 0 |
| `requireSelfLocator()` fallback | なし（改善） | 全 RPC 経路（本検証全体）+ Alarm 経路（O-5 の done 行、`ph07w` の `resume-signup done`）、`selfLocator.integration` |
| `ssoStateCodec` の `derivedHmac` 共通化 | なし | R-AC-02 / R-AC-09 実走（3.5）、`ssoStateCodec` unit 不変 |
| `userDataPlan` v1 に `oauth_consumed_codes` | なし | `schemaPlans.test.ts` 更新、既存 DO に対する初期化（新規 `ph07v` / `ph07w`）成功。**既存 DO（v1 適用済み）にはテーブルが増えない** — 初回デプロイ前なので許容（D-05）。dev の既存ユーザーで token 交換を試すと `oauth_consumed_codes` 不在で失敗しうる点は、ローカル状態を作り直せば消える |
| `server.cloudflare.ts` の `isAiRoute` 先取り | なし | 通常ルート（P-04 / P-13 等）は従来どおり |
| `errorDisplay` 追加コード | なし | `errorDisplay.test.ts` |

## 7. 検証環境の引き継ぎ

- dev（:3000、`scratchpad/dev7.log`）と preview（:4173、`preview7.log`）は起動したまま。loopback 8765 / 8766 は停止済み。
- `apps/web/.ai-client.json` は Verify07（:3000、`ph07w` の失効済み token）。Implementer の元ファイルは `scratchpad/ai-client.impl.json`。
- 検証アカウント: `ph07v@example.com` / `verify07reset`（SSO なし、接続 3 件 revoked + preview 1 件 revoked、リセット依頼のスロットル窓に注意）、`ph07w@example.com` / `verify07w-reset`（接続 1 件 revoked）。dev DO に `Verify07 自作蒸留器` トピック・D1・ゴミ箱 4 件、preview 用トピック 1 件 + ゴミ箱 1 件。
- ローカル DO 状態に b0 の poison 行 1 件（O-5）。
