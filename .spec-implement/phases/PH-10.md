# PH-10 計画 — 統合検証 + docs の現状化（文書のみ）

- 作成: 2026-09-11T02:40+09:00（PH-09B の検証待ちの間。HEAD `5667348` のソースと docs を読むだけで変更しない）
- 対象: **R-INT-01**（plan.md: `spec/manual-tests/*.md` 7 カテゴリ 208 ケースをブラウザで実行して合格、`pnpm typecheck && lint && format:check && test` が成功、アーキテクチャ監査に重大な違反なし、docs が現状を述べる）
- 入力: `docs/{test,runtime_cloudflare,backend_implementation_example,frontend_implementation_example}.md`、`README.md`、`CLAUDE.md`、`apps/web/.dev.vars.example`、`spec/manual-tests/*.md`、`plan.md` 履歴、`reviews/PH-01〜09A.md`、`phases/PH-02〜09.md`
- 前提: `spec/` / `infra/` は変更しない（spec 側の反映は Manager）。docs は「現状を断定で述べる」（CLAUDE.md「gap は docs に記録しない」— 未着手の規則は issue へ）

## 1. docs の乖離一覧（現状の記述 → 正しい記述）

### 1.1 `docs/test.md`

| 箇所（行） | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| L10, L46 | fakes は `application/__tests__/fakes/` の 4 つ（`FakeIdGenerator` / `FakeLogger` / `FakePasswordHasher` / `trippingIdentityGateway`） | `application/__tests__/fakes.ts`（1 ファイル）+ `fakes/` 5 ファイル。roster = `FakeIdGenerator` / `FakeTokenGenerator` / `FakeLogger` / `FakePasswordHasher` / `trippingIdentityGateway` + `tripping{Memo,Knowledge,Search,Trash,Export}Gateway`（10）。加えて `identity/__tests__/unitContainer.ts`（`recordingGateway` / `trip` / `makeContainer`）、`memo/__tests__/{memoContainer,memoFixtures}.ts` | `packages/core/src/application/__tests__/fakes.ts`、`unitContainer.ts` |
| L51 | `trippingIdentityGateway` の使い手は `loginWithPassword.test.ts` / `logout.test.ts` / `apps/web/app/presentation/__tests__/` | identity unit 8 本 + `knowledge/__tests__/{usecases,editDocumentByAi}` + `memo/__tests__/aiMemo` + web `presentation/__tests__/{currentUser,session,errorResponseMiddleware}` / `presentation/ai/__tests__/handlers` / `presentation/export/__tests__/handler`。`logout.test.ts` は無い | grep |
| L18 | DOM 節: `no setupFiles` | `vitest.config.dom.ts` は `setupFiles: ["apps/web/app/components/__tests__/domSetup.ts"]` | `vitest.config.dom.ts` L37 |
| L23–26 | DOM が名指す 5 ファイル（`document/__tests__/{documentNotFound,documentBoard,documentFeed}`、`documentEdit/__tests__/newDocumentFeed`、`timeline/__tests__/memoEntry`）、`DocumentFeed` の catch、`topicDetailFeed` は無い | 実在は `timeline/__tests__/memoEntry.dom.test.tsx` のみ。DOM テストは **29 ファイル**（aiClients 1 / auth 2 / documents 3 / knowledge 2 / layout 1 / memoHistory 1 / search 1 / settings 9 / timeline 3 / topics 4 / trash 1 / ui 1）。`topics/__tests__/topicDetailFeed.dom.test.tsx` は実在。節を 29 ファイルの射程（分岐ごとに何を固定するか）で書き直す | `find apps/web/app -name "*.dom.test.ts*"` |
| L30–32, L61–66, L79, L89 | integration は D1 + DO の 2 project、`vitest.config.d1.ts`、`adapters/d1/__tests__/setup.ts`、`_occ_guard`、`occGuard.integration.test.ts`、「Workers pool: D1 + Durable Objects」 | DO project のみ（`vitest.config.integration.ts` → `vitest.config.do.ts`）。`d1/` も `setupFiles` も無い。OCC は `updateMatchedRow`（`rowRunner.integration` / `occ.integration`） | `vitest.config.integration.ts` |
| L34, L50, L81 | 例示ファイル `loginWithPassword.integration` / `helpers.integration` / `outboxRelay.integration` / `registerWithPassword.integration` / `getCurrentUser.integration` | いずれも無い。実在 32 ファイル / 188 件: `adapters/cloudflare/__tests__/` 10（alarm, alarmSchedule, migrationGate, occ, operatorEntries, rotation, rotationLifecycle, rowRunner, schema, selfLocator）、`jobs/__tests__/` 3（jobTerminal, recovery, rotateEncryption）、`application/export` 1、`identity` 5（aiClientConnections, lookupGeneration, passwordReset, signupSaga, sso）、`knowledge` 5、`memo` 6、`search` 1、`trash` 1 | `find … -name "*.integration.test.ts"`、`scratchpad/test3.log` |
| L36–42, L70–74 | Property-based（fast-check）節、`*.property.test.ts` | 1 ファイルも無い。`fast-check` は未使用の devDependency。節を落とす（または「未使用」と断定） | `find … -name "*.property.test.ts"` |
| L62–63, L68 | `nextBucket()`、`createTestContainer()` は `application/identity/__tests__/helpers.ts`、`markSearchEntry` / `PROJECTION_MARK` は `application/memo/__tests__/helpers.ts` | `createTestContainer` / `registerTestUser` / `bucketOfEmail` は `adapters/cloudflare/__tests__/testContainer.ts`、`nextUserId` / `uniqueEmail` / `directoryStubOf` / `in{UserData,Directory}Storage` / `overrideDirectoryEnv` / `overrideUserDataPlan` / `twoGenerationKeyring` / `commitmentJsonFor` は `helpers.ts`。memo fixture は `memoFixtures.ts`（`post` / `postAt` / `setPostedAt` / `searchHits`） | 両ファイルの export |
| （追加） | — | 「テスト不能な限界」を 1 節にまとめる: tx の原子性（巻き戻しの失敗注入なし。PH-01 T-3）、`rotate-encryption` の読み書き割り込み（keyRotation 037）、チャンク途中の DO リセット（028）、s4 再読の割り込み（038）、24 MiB 超過のブラウザ再現（PH-08）、sagaRecovery 26（予約 TTL の不等式 = 運用値）、`aiConnectionsList.dom.test.tsx` の並列負荷 flaky（PH-09A O-6。再現しなければ限界） | reviews |

### 1.2 `docs/runtime_cloudflare.md`

| 箇所（行） | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| L63–70 | D1 binding が request config と Pulumi に残る、`adapters/d1/` は正当 | D1 は無い（`d1_databases` / `adapters/d1/` / drizzle いずれも 0 件。D-05 で PH-01 削除）。表 2 行を削除 | grep |
| L174–211 | 秘密は 5 つ、「表は完全ではない（#12 / #67）」、`wrangler secret put` 5 行 | 帰属表は `.dev.vars.example` の 15 secret + `[vars]` 3（§1.6）。`secret put` は `.dev.vars.example` L60–72 の 11 行。「gap」段落を削除 | `apps/web/.dev.vars.example` |
| L221–222 | ローテーションは #67 | 実装済み。写像鍵: `DIRECTORY_ROUTING_KEYRING` + `DIRECTORY_KEY_COMMITMENT` を対で配布 → `remap-chunk --inject-keyring` × 退役世代の全 bucket → `read-rotation-checkpoint {remap, g}` 全 0 → previous 除去。暗号鍵: `IDENTITY_MAIL_ENCRYPTION_KEYRING` に previous → `start-rotate-encryption` × active 写像世代の全 bucket → `{encryption, g}` 全 0 → previous 除去。順序規範（暗号完了後に写像開始、逆も）。dev の実走（1 チャンク 121〜146 ms）は PH-09.md | `spec/rotation`、PH-09B 報告 |
| L106–113 | `selfLocator` に workerd の限界記述なし | 追記: ローカル workerd は再起動後の Alarm 起床で復元した DO に `ctx.id.name` を渡さない。`requireSelfLocator()` が `_meta.self_locator` にフォールバックするので relay / jobs は動く。起動直後の `Alarm stopped at the schema gate` はこの経路の未初期化 DO によるもの | `durableObjectBase.ts` L106–147、reviews/PH-06 O-2、aa64fcb |
| L352, L367–404 | fail-closed の検知は `GET /__diagnostics/schema-version`（local only）、本番に `read-schema-version` の呼び手が無い | `POST /__operator/read-schema-version`（`OPERATOR_TOKEN`、両クラス、ゲート外）が本番でも到達可能。`/__diagnostics` は dev 限定のまま | `operatorHandlers.ts` |
| L482–494 | `read-delivery-backlog` / `list-bucket-user-ids` 未実装、`sweep-reset-tokens` Reality None | 3 つとも実装済み。`list-bucket-user-ids` はゲートの外（fail-closed / 未初期化 bucket でも答え、未初期化は `[]` で初期化しない） | `identityDirectoryDurableObject.ts`、3c79194 |
| L517–531 | §8.2「Reality: None (#80)、7 entry unreachable」、`OPERATOR_TOKEN` は「Not adopted on its own」 | `POST /__operator/<entry>` が `apps/web/app/worker/cloudflare/operatorHandlers.ts`、`server.cloudflare.ts` が先取り。`OPERATOR_TOKEN`（request Worker、≥ 32 文字、定数時間比較）: 未設定 404（面が存在しない）/ 不一致 401 / GET 405 / 未知 entry 404 / locator・body 400。Cloudflare Access は前段の運用（`infra/` 外）。監査ログは `entry` / `locator` / `outcome` + 任意の `id`（eventId / operationKey / userId / afterCredentialId / credentialId）だけ | `operatorHandlers.ts`、`operatorHandlers.test.ts` |
| L543–601 | §8.3〜8.5: backlog 未実装、quarantine の 2 つは unreachable / delete 未実装、poison の 3 つは存在しない | すべて実装済み・到達可能。`list-*` は keyset / page 50（`listQuarantinedEventsLimit` / `listPoisonedJobsLimit`）、`requeue-*` は 4 列 + rearm、`delete-*` は `{ deleted }` で rearm しない | `durableObjectBase.ts`、`operatorEntries.integration` |
| L608–636 | §8.6「DLQ は記録して捨てる、再駆動は無い（#83）」 | `handleDlqBatch` は各メッセージを **1 回だけ** `deliverOnce` で自動再配送し、`sent` / `nothing-to-send` / `unserved` / `failed` のいずれも ack（`warn("dlq", { eventId, type, outcome })` 1 行）。手動再駆動は無い | `queueHandlers.ts` |
| L719–747 | §10 表: 15 entry のうち 12 が Implemented no、`read-schema-version` local only、脚注「#67 が書く」、「3 entry は名前だけ」 | 15 entry すべて Implemented yes / Reachable yes（`OPERATOR_TOKEN` 設定時）。Target: 8 entry は両クラス、`purge-user-mappings` / `list-bucket-user-ids` / `start-rotate-encryption` / `remap-chunk` / `import-remapped-mappings` / `read-rotation-checkpoint` は Directory、`record-remapped-locator` は User Data。読み専用一覧に `list-bucket-user-ids` の「ゲート外」を追記 | `operatorHandlers.ts` L114–238 |
| L753–761 | §11.1 export Reality None（#15）、上限未定 | `POST /export`（P-13「データ」節、同一 origin 検査）、上限 `EXPORT_MAX_SOURCE_BYTES = 24 MiB`（超過は `SystemError(ExportTooLarge)`） | `stores/exportSourceReader.ts` |
| L767–776 | §11.2 Reality None（#74）、「saga が 2 object を順に削除」 | 起点は `abandon-account`（登録 saga の後始末）のみ、利用者向け退会は範囲外。消すのは到達性（mapping 全世代 / `credential_locators` / `ai_client_connections` revoke / `oauth_consumed_codes`）、`operations(kind='withdrawal')` 行を初回に作り no-op 確定後 `done`、User Data DO は tombstone で削除しない。**`purge-user-mappings` の運用注意**: active なアカウントに打つとセッションは生きたまま P-13 が「読み込めません」（ログアウト導線は残る、3c79194）。退会残渣の最後の砦にだけ使う | `finalizeWithdrawal.ts`、reviews/PH-09A M-2 |
| L782–796 | §11.3 Reality None（#81）、`ai_client_connections` / `password_reset_tokens` は無い、sweep 未実装 | 両表は v1 DDL に実在。blast radius は `list-bucket-user-ids` × 全 bucket → `read-schema-version`（両方 operator 経路、ゲート外）。復元 bucket の checkpoint 無効化は「`remap-chunk` を再駆動してから `read-rotation-checkpoint` を読み直す」 | schema plans |
| L843 | `jobs.kind` handler は 1 つも登録されていない | 11 kind すべて登録済み（User Data 6 / Directory 5） | 両 DO の `jobRegistry` |
| L845–875 | 運用値の表に `listQuarantinedEventsLimit` のみ | +`listPoisonedJobsLimit` 50、`deleteNoopReissueDelayMs` 60,000（判断値、限界は JSDoc）、`failClosedRearmIntervalMs` | `tuning.ts` |
| L900–906 | 12.4「読み手の無い値」: throttle window / export cap / WAF | throttle は `IdentityTuning` が読む、export cap は `EXPORT_MAX_SOURCE_BYTES`。残るのは WAF のみ | 同上 |
| L955–984 | §13 表の行 8–14 / 16–17 / 19–20 / 22 / 24 / 26–30 / 33 / 35 が None | 8–14 Available（`node apps/web/scripts/operator.ts`）、16 None（設計どおり ack）/ 17 自動 1 回のみ、19 / 22 `MAIL_DEV_SINK="console"`（`[dev-mail]` ログ）、20 table 実在（sqlite で消せる、Local only）、24 / 26–28 `scripts/ai-client.ts`（実 LLM アプリだけ None）、29–30 `SSO_DEV_STUB="true"`（実 Google / Apple は None）、33 Available、35 Available。行 1–6（seeding / clock / forced Alarm）は sqlite 直接編集 + Alarm 手動武装で Local only | `.dev.vars.example`、両 scripts |
| L1004–1015 | §14 の open issues: #79 D1、#80 operator、#74 poison / fail-closed 発見、#83 DLQ、#81 PITR、#15 / #12 | 削除。残すのは #73（`pnpm start`、§3.3 で再確認）、#22（`[observability]` はどの config にも無い）、#23、#18、#83（手動再駆動なし） | grep |
| （追加） | — | 環境の限界: dev と preview が同じ `.wrangler/state` を共有し Alarm を奪い合う（PH-05 O-3 / PH-06 O-2）；preview の `[dev-mail]` URL は `APP_URL`（:3000）；`read-rotation-checkpoint` / `remap-chunk` が未初期化 bucket を初期化する；`rotate-encryption` で 1 行も復号できないチャンクは `DataIntegrityError` → backoff → `poison`（spec 無記述、Manager 判断待ち） | reviews / PH-09.md |

### 1.3 `docs/backend_implementation_example.md`

| 箇所（行） | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| L312–325 | `TransactionalRepository` / `FooRepository.findPage` が `Promise` を返す | 4 メソッドとも同期（`void` / `Versioned<T> \| null`）。読み取りも同期（`transactionSync` 内で await 不可） | `domain/common/transactionalRepository.ts` |
| L268–283 | `RequestContainer` = `config / identityGateway / identityTuning / passwordHasher / sessionCodec`、`QueueContainer` = `tuning / bindings / mailSender` | `RequestContainer` +`memoGateway / knowledgeGateway / searchGateway / trashGateway / exportGateway / tokenGenerator / clock / idGenerator / logger`；`QueueContainer` = `mailSender / logger / bindings` | `application/di/types.ts`、`serverCloudflare.ts` L156–160 |
| L212 | User Data ctx は `recordOperation / updateOperation` を足す、Directory は何も足さない | User Data: 4 repository + `searchIndex` / `trashQueryPort` / `accountStore` / `credentialLocatorStore` / `aiClientConnectionRepository` / `recordOperation` / `updateOperation` / `setMigrationCursor`；Directory: `credentialMappingReader` / `Writer` / `credentialAttemptRecorder` / `resetTokenStore` / `resetThrottleStore` / `rotationCheckpointStore`（spec「8 ストア 9 メソッド」） | `execution/unitOfWork.ts` |
| L35–85 | ディレクトリ一覧（`ports/` 4、`di/` 4、`stores/` 7、`adapters/cloudflare/` に gateway 1、`d1/`、`console/mailSender.ts`、`resetMailMaterials.ts`、presentation 5、worker 2） | `ports/` +`mailSender / ssoIdentityProvider / tokenGenerator`；`di/` +`aiToolContainer`；`stores/` 24；gateway 6 + `jobs/`（11 handler + `cleanup/`）+ `rotation/` + `terminalReason.ts`；`d1/` `console/` は無く `mail/{consoleMailSender,resendMailSender,resetMailContent}`、`sso/{devStubSsoProvider,googleSsoProvider}`、`fflate/zipArchiveWriter`、`webcrypto/` 7；`stores/sendMailMaterials.ts`；presentation 22、worker +`operatorHandlers / ssoHandlers / diagnostics / stateEnv.check`、`scripts/{operator,ai-client}.ts` | `ls` |
| L377, L392 | `mapDbError`、「D1 adapter に残るのは driver-error classification と `pendingBatch.ts`」 | `mapDbError` は無い（各 store が `SystemError(DatabaseError)` へ翻訳）。D1 adapter は無い | grep |
| L445 | job handler の戻りは `finished` / `rearm` / `yield` | +`{ kind: "poison", reason: "material-lost" }`、`finished.commit?` / `yield.commit?`、`TerminalStageSelector(row, sql)` | `jobRunner.ts` L79–87 |
| L460, L472 | quarantine は 2 entry、「DLQ は記録して ack」 | +`deleteQuarantinedEvent`、poison 3 entry、HTTP 面 15 entry；DLQ は 1 回再配送してから ack | `durableObjectBase.ts`、`queueHandlers.ts` |
| L226–243, L254 | `beginSignup` が `resume-link` を投入する例、request 側 identity usecase 4 つの列挙 | 実装は `reserveCredentialProcedure`（Directory）が `sweep-reservations` + `resume-signup` を投入。例示は「型が通る例」と断り、列挙は「例」に | `reserveSignupCredential.ts` |
| （追加） | gateway / procedure 並置の説明が無い | 実装の形: usecase → `*Gateway`（request 側、stub 選択 + envelope unwrap）→ DO facade（`envelope(() => runUnitOfWork(ctx => *Procedure(ctx, …)))`）→ `application/**/*.ts` の `*Procedure(ctx, sql?, dto)`（純同期）。ジョブは `jobs/*.ts` が同じ procedure を呼ぶ | `identityGateway.ts`、`userDataDurableObject.ts` |

### 1.4 `docs/frontend_implementation_example.md`

| 箇所（行） | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| L432–454 | `packages/core/src/lib/server/currentUser.ts` の `getCurrentUser` / `requireCurrentUser`、`container.authProvider` | `apps/web/app/presentation/currentUser.ts` の `getCurrentUserId()` / `requireUserId()`（cookie → `sessionCodec.verify` → `readAccountState` の epoch 照合、`redirect({ to: "/login", search: { redirect } })`）。`authProvider` は無い | `presentation/currentUser.ts` |
| L103–107 | `Deferred` は `return use(promise)` | `use(useDeferredValue(promise))`（`router.invalidate()` の差し替えでフォールバックに落ちない） | `components/ui/Deferred/index.tsx` L29 |
| L79–92, L112 | streaming route は `staleTime` のみ、「`/settings` は route-level pending を起こさない」 | `...streamingRouteOptions`（`ssr: !import.meta.env.DEV`、`pendingComponent: () => null`）を spread。client navigation の `/_serverFn/` 往復が `defaultPendingMs` を超えると route-level pending が先に出るため（CLAUDE.md） | `presentation/streamingRoute.ts` |
| L559–719 | 呼び出し側が server fn の戻りをそのまま信用 | `readServerFnResult(value, guard, name)` で形を検査、想定外は `UNEXPECTED_SERVER_FN_RESULT` | `presentation/serverFnResult.ts` |
| L24, L38, L79, L826 | `routes/index.tsx`、`/todo/`、`/posts/$postId` | 実 route は `routes/_app/*.tsx`（pathless layout）+ `login / signup / password-reset`。todo / posts は例示（冒頭で宣言） | `ls apps/web/app/routes` |
| L114–162, L744–819 | TanStack Query / Conform / sonner の例 | いずれも依存に無い。「採用していないパターン」と明記するか節を落とす | `apps/web/package.json` |
| L110 | Skeleton は 2 つ | `Timeline / Topics / TopicDetail / Document / Search / Trash / MemoHistory / Settings` の各 Skeleton + `RoutePendingFallback` | `ls components/**` |
| L5, L264–275, L856 | 例示の宣言、`serverData` / `loadServerDeps`、`appServerErrorAdapter` | 一致 | — |

### 1.5 `README.md`

| 箇所（行） | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| L1–3 | `# tanstack-start-template` / reference template | fog（メモ / トピック / 文書 / 検索 / AI API。`spec/requirements.md`）。Worker 名は `tanstack-start-template` のまま | `spec/index.md`、`wrangler.toml` |
| L15 | `components/auth/{LoginForm,SignupForm}`、`settings/LogoutButton` | `components/auth/AuthForm`（mode = login / signup）。optimistic list の実物は `timeline/TimelineBoard`、`trash/TrashBoard`、`topics/TopicList` | `ls components/auth` |
| L19 | two-tier Vitest（unit / integration） | unit / dom / integration の 3 project | `vitest.config.*.ts` |
| L41, L88–110 | `scripts/` は `render-wrangler.ts` のみ、operator / ai-client / export の使い方なし | +`operator.ts`（`pnpm --filter @repo/web operator <entry> --locator … [--json] [--inject-keyring] [--limit] [--after] [--base]`）、`ai-client.ts`（OAuth 2.1 + MCP / REST のテストクライアント、状態 `.ai-client.json`）、エクスポート（P-13「データ」→ `POST /export`） | `apps/web/package.json`、両 scripts |
| L44 | `spec/` は `/spec` workflow の入口 | `spec/` は要件〜テストケースの正本 | `spec/index.md` |
| L63–72 | `SESSION_SECRET` だけで `pnpm dev` が動く | 必須 secret 6（`SESSION_SECRET / DIRECTORY_ROUTING_SECRET / IDENTITY_MAIL_ENCRYPTION_KEY / PROVIDER_IDEMPOTENCY_KEY / IDENTITY_RESET_TOKEN_KEY / AI_CLIENT_TOKEN_SECRET`、各 ≥ 32 文字）+ `MAIL_DEV_SINK="console"` / `SSO_DEV_STUB="true"`。`OPERATOR_TOKEN` は operator 面を使うときだけ | `di/secrets.ts`、`durableObjectBase.ts` |
| L80–117 | `pnpm start` は #73 で起動しない | 記述は `start:cf` と整合。**現状は未確認** → §3.3 で 1 回実行して確定し、結果で書き換える | `apps/web/package.json` |
| L51, L131–133 | D1 の記述 | D-05 と整合。差分なし | — |
| （追加） | — | 対象外の宣言: 退会の起点 usecase、本番デプロイ、実 IdP（Apple adapter 未実装 / Google は資格情報が要る）、実 mail provider、実 LLM アプリは契約検証のみ（plan.md「実接続の検証手順」） | plan.md |

### 1.6 `CLAUDE.md`

| 箇所 | 現状の記述 | 正しい記述 | 根拠 |
|---|---|---|---|
| Reference runtime | `PasswordHasher` / `MailSender` が唯一の非同期ポート | 非同期ポートは 4 つ: `PasswordHasher`（domain）、`MailSender` / `SessionCodec` / `SsoIdentityProvider`（application）。「domain ポートの中では `PasswordHasher` だけ」に限定するか 4 つを列挙 | `domain/identity/ports/passwordHasher.ts`、`application/ports/*` |
| Error handling（broad catch の列挙） | 6 箇所 | 実装には更に `/__operator` HTTP 面、`remap-chunk` / `import-remapped-mappings` の per-row、`sweep-orphan-mapping` の per-record、`emptyTrash` / `pruneExpiredTrashItems` の per-item、`revokeAllAiClientConnections` の per-connection、signup / link saga の巻き戻し catch、SSO callback / AI / export の bare handler、`session.ts`。「代表例」に改めるか全数を書く（**Manager 判断**: CLAUDE.md の変更） | grep |
| Retry strategy | queue の先は platform の retry と DLQ | DLQ handler が 1 回だけ再配送してから ack（△-8、spec 反映済み）。「委譲される唯一の場所」に例外を 1 文 | `queueHandlers.ts` |
| Asynchronous execution contract | 削除 saga の再発行に触れない | 2 世代退避の no-op 削除は `deleteNoopReissueDelayMs` 後にもう 1 回発行してから確定（rotation「削除の no-op 確定」）。runner の backoff とは別 | `deletionConfirmation.ts` |
| Frontend | `docs/frontend_implementation_example.md` が list-owner の worked example | docs 側（§1.4）の修正で解消（実物は `TimelineBoard`） | — |
| 名指しファイル / Workspace layout / UoW roster | — | すべて実在・一致。差分なし | `ls` |

### 1.7 `apps/web/.dev.vars.example` 帰属表

| 変数 | 判定 |
|---|---|
| request 10（`SESSION_SECRET / MAIL_PROVIDER_API_KEY / DIRECTORY_ROUTING_SECRET / DIRECTORY_ROUTING_KEYRING / AI_CLIENT_TOKEN_SECRET / OPERATOR_TOKEN / GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET / MAIL_DEV_SINK / SSO_DEV_STUB`）、state 5（`IDENTITY_MAIL_ENCRYPTION_KEY / IDENTITY_MAIL_ENCRYPTION_KEYRING / DIRECTORY_KEY_COMMITMENT / PROVIDER_IDEMPOTENCY_KEY / IDENTITY_RESET_TOKEN_KEY`）、`MAIL_FROM_ADDRESS`（vars） | 記載あり・帰属一致（`ServerEnv` / `StateWorkerEnv` / `wrangler.toml`） |
| `APP_URL` | 表に無い（L8 の注記のみ）。request Worker の `[vars]`（`wrangler.toml` L35）。`MAIL_FROM_ADDRESS` と同様に 1 行足す |
| `DIAGNOSTICS_ENABLED` | 表にも本文にも無い。request Worker の `[vars]`（ローカルのみ `"true"`、`/__diagnostics/*`）。1 行足す |
| 表にあってコードが読まない変数 | なし |
| `wranglerConfig.test.ts` | secret 15 行は固定済み。`[vars]` 2 変数の帰属は未固定 → 表に足すなら test も 2 行足す |

## 2. 受け入れ済み項目の持ち越し観察（未対応で PH-10 の監査対象になるもの）

解決済み（コミットで閉じたもの）は除く。区分: **直す** = PH-10 で修正、**限界** = docs / JSDoc に限界として記述、**issue** = 候補タイトルだけ（`gh issue` は作らない）、**Manager** = spec / design の判断。

| ID | 出典 | 内容 | 扱い |
|---|---|---|---|
| PH-01 N-1 | reviews/PH-01 | 1 回目が失敗した直列 2 回目が同じ本文を再送（サーバーが書いていた 500 なら二重投稿） | issue「TimelineBoard: 失敗後の再送で二重投稿になりうる」 |
| PH-01 T-2 | reviews/PH-01 | `alarm.integration` に「各 pass 1 回 / 上限独立 / `leaseUntil` 再武装」の固定が無い（gate 失敗の保持は `operatorEntries` で固定済み） | 監査で確認。無ければ issue「alarm() の 2 pass 独立上限と leaseUntil 再武装の固定」 |
| PH-01 T-3 | reviews/PH-01 | 巻き戻しの原子性の失敗注入テストが無い | 限界（docs/test.md） |
| PH-01 C-3 | reviews/PH-01 | `reserveCredential` が SELECT → INSERT（spec は単一 CAS 文。tx 内で原子的） | issue「reserveCredential を単一 CAS 文に」（低） |
| PH-01 G-1〜G-3 | reviews/PH-01 | DDL の差（`operations.kind` CHECK、`token_hash` 索引形、`failed_attempts` 既定値） | Manager（spec の DDL に反映するか） |
| PH-01 O-2 | reviews/PH-01 | User Data facade が RPC 入力の `actor.userId` を self locator と照合しない | issue「facade で actor.userId と self locator の一致確認」（低） |
| PH-01 O-6 / O-7、PH-02 A-7、PH-09B 観察 | reviews | preview の SSR が速くスケルトンが見えない / headless で IntersectionObserver が発火しない / `Proxy` で DO stub を包めない / logout 直後の cookie 反映遅れ | §3 の手順に注記（dom テスト + ボタン経路で代替） |
| PH-01 N-5 = PH-04 O-3、PH-09A M-5 | reviews | docs/test.md の DOM 節・`setupFiles`、docs §8 / §10 / §11 が古い | 直す（§1） |
| PH-02 A-1 | phases/PH-02 | P-05「現在の内容」欄（design にあり spec/pages に無い） | 限界（spec/pages 優先）。要るなら issue「memo-history に現在の内容欄」 |
| PH-02 A-2 | reviews/PH-02 | 絞り込み中に編集して不一致になった行が再取得後も残る | issue「絞り込み中の編集後に不一致行が残る」 |
| PH-02 観察 / (h) | reviews/PH-02 | `document.querySelector("article")` で一覧先頭を判定 / `rollbackMemoFn` の `userActorOf` 重複 | 直す（小） |
| PH-03 (f) | reviews/PH-03 | `trashTopicProcedure` の到達不能分岐が plain `Error` | 直す（`SystemError(DataIntegrityError)`） |
| PH-04 O-1 | reviews/PH-04 | 半角キーワードで全角原文がヒットしたとき `<mark>` が付かない | 直す（`Highlighted` を NFKC 書記素照合に） |
| PH-04 O-2 | reviews/PH-04 | search cursor に MAC が無い（AI 経路で外部入力） | issue「search cursor に HMAC 署名」（低）。JSDoc に限界あり |
| PH-04 O-4 | reviews/PH-04 | `instr()` 経路（3 文字未満）は ASCII の大小を区別 | 限界（docs） |
| PH-04 O-5 = PH-03 △-4 | reviews/PH-04 | design `search.html` のメモ行トピック表示と DTO の差 | Manager（design） |
| PH-05 O-1 | reviews/PH-05 | 復元の touch が `updatedAt` も書く（`createDocument` は書かない） | 直す（`version` のみに揃える）または issue |
| PH-05 A-6 | phases/PH-05 | lint fixture 名が `process.pid` のみ（worker 衝突） | 直す（乱数を足す） |
| PH-05 O-3 = PH-06 O-2 | reviews | dev と preview が `.wrangler/state` を共有し Alarm を奪い合う | 限界（docs）+ §3 手順（片方ずつ止める） |
| PH-06 判断 2 | phases/PH-06 | Apple の実 IdP adapter 未実装 | 限界（README / docs）+ issue「Apple SSO adapter」 |
| PH-06 O-2（再検証）= PH-07 O-5 | reviews/PH-06 | workerd の Alarm `id.name` 欠落（コードは aa64fcb で解決、docs 未記述） | 直す（docs §1.2） |
| PH-06 O-3 | reviews/PH-06 | preview の `[dev-mail]` URL は `APP_URL`（:3000） | §3 手順に注記 |
| PH-07 O-1 | reviews/PH-07 | `authorizeAiClient` / `consumeAuthorizationCode` が `account.status` を見ない（`deleting` 中の窓） | 監査で決める: 直す（facade に `status !== 'active'` → 401）か限界 |
| PH-08 O-1 | reviews/PH-08 | zip の DOS タイムスタンプがプロセス TZ 依存（workerd は UTC） | 直す（JSDoc 1 文） |
| PH-08 O-2 | reviews/PH-08 | `/export` はリクエスト自身の origin、MCP は `APP_URL` の origin | 限界（両方 CSRF としては成立）。統一は issue（低） |
| PH-08 O-3 | reviews/PH-08 | スラッグ 50 切り詰めが `-` / `.` 終わりになりうる | Manager（spec 変更）→ issue「スラッグ末尾の記号」 |
| PH-08 判断 1 / 2 | phases/PH-08 | macOS `unzip` が非 ASCII 名を展開できない（zip は正しい）/ `EXPORT_TOO_LARGE` の公開 code | Manager（spec/manual-tests / spec/usecases）。§3 では環境依存として記述 |
| PH-08 参考 | reviews/PH-08 | 24 MiB 超過はブラウザで踏めない | §3「実行不能」（integration で固定済み） |
| PH-09A O-1 | reviews/PH-09A | `finalizeWithdrawal.ts` が `account` を生 SQL で更新（`version` を進めず epoch を 2 回進める） | 直す（`AccountStore` 経由 / `version + 1`） |
| PH-09A O-2 | reviews/PH-09A | S1 で `user_id ?? candidate_user_id` NULL も `material-lost`（spec の 2 類の外） | 限界（JSDoc） |
| PH-09A O-5 | reviews/PH-09A | `resumeLink.ts` の前進に `account.status` 判定が無い | 限界 |
| PH-09A O-6 = PH-03 A-7 | reviews/PH-09A | `aiConnectionsList.dom.test.tsx` の並列負荷 flaky（PH-09B の 3 回では再現せず） | 直す（`waitFor` 化を確認）。再現しなければ限界（docs/test.md） |
| PH-09A O-7 | reviews/PH-09A | `operatorEntries.integration` の「reserved or active」がテスト名だけ | 直す（`reserved` 行を足す） |
| PH-09A 追加観察 | reviews/PH-09A | 監査ログの `id` が docs §8.2 (c) より広い / purge 後の旧 DO が dev に残る | 直す（docs §8.2）/ 対応なし |
| PH-09A 未固定 20 行 | reviews/PH-09A §1 | sagaRecovery.md の 26（運用値、テスト不能）ほか 19 行が未固定 | 26 は限界（docs）。他は監査で列挙し issue「sagaRecovery 未固定行のテスト」 |
| PH-09B 観察 1 / 2、判断 3 | phases/PH-09 | 未初期化 bucket の初期化 / 復号不能チャンクの `poison` / `deleteNoopReissueDelayMs` 判断値 | docs §1.2 に記述。観察 2 は Manager（spec） |
| plan 対象外 | plan.md | 退会の起点 usecase / 本番デプロイ / 実 IdP・provider・LLM | 限界（README / docs）。実接続手順は plan の節のまま |

重複の統合: PH-01 O-1（解決済み）と N-1（失敗経路、未対応）は別項目；PH-03 A-7 / F-1（解決）と PH-09A O-6（別ファイル、未対応）；PH-05 O-3 と PH-06 O-2（環境）；PH-06 O-2（再検証）と PH-07 O-5；PH-01 N-5 と PH-04 O-3；PH-08 O-2 と PH-07 O-4（O-4 は解決）；PH-09A M-5 と phases/PH-09 判断 4 と監査ログ `id` の観察（すべて docs §8）。

## 3. 統合検証の計画

### 3.1 環境

- dev（:3000、`pnpm dev`）を主、preview（:4173、`pnpm build && pnpm preview`）は各カテゴリの主要経路 2〜3 ケースの再実行に使う。**両方を同時に起動しない**（`.wrangler/state` の共有で Alarm を奪い合う。PH-05 O-3）。preview の `[dev-mail]` URL は :3000 を指すので、リンクは host を読み替える
- `.dev.vars`: 現状は PH-09B の phase 4（3 変数とも active g2）。**統合検証はこの状態のまま行う**（単一変数に戻すと全ユーザーが到達不能）。作り直すなら `.wrangler/state` を消して `scratchpad/dev.vars.bak` に戻し、ユーザーを登録し直す（推奨: 作り直す。PH-07 O-5 の poison 行や purge 残渣も消える）
- agent-browser session は `ph10`（+ 競合ケース用に `ph10-b`）。ref は直前の snapshot でのみ有効、`eval` は `batch` に入れない。headless では IntersectionObserver が発火しない（無限スクロールはボタン経路 + dom テストで代替）
- DO の実値は dev を止めて `sqlite3 apps/web/.wrangler/state/v3/do/**/*.sqlite`（稼働中は読めない）。ユーザーの DO ファイルは登録順 / `_meta.self_locator` で特定
- 補助: `node apps/web/scripts/operator.ts`（`OPERATOR_TOKEN` は `.dev.vars`）、`node apps/web/scripts/ai-client.ts`（register → authorize（loopback）→ call / mcp）、`agent-browser network route "**/_serverFn/**" --abort`（通信失敗）、`agent-browser download`（zip）

### 3.2 マニュアルテストの回し方（7 カテゴリ 208 ケース）

順序は `spec/manual-tests/index.md` に 2 点の調整: (a) `document.md` TC-28 と `settings.md` TC-11 は「データ 0 件」なので新規ユーザーで最初に、(b) `trash.md` TC-10〜12（ハードデリート）は search / ai の後に、`settings.md`（エクスポート）は全データ揃った最後に。

| 順 | ファイル | 件数 | 前提データ | 実行不能 / 代替 | 既走（優先度） |
|---|---|---|---|---|---|
| 1 | account.md | 47 | `test@example.com` / `password123`、SSO 登録済み + 別主体、AI 接続 1 件（TC-08/09/27/28/38/39）、`[dev-mail]` ログ | SSO は `SSO_DEV_STUB`（subject / email 入力、許可 / キャンセル）で全件代替（IdP 内部エラーは spec 対象外）。TC-07〜09 / 24〜28 は ai-client + ブラウザ。TC-30 期限切れは sqlite で `expires_at` を過去へ。TC-44 は `read-delivery-backlog`、TC-45 は sqlite で quarantined をシード → `list/requeue-quarantined-event`、TC-46 は `MAIL_DEV_SINK` を外して DLQ、TC-47 は sqlite で `terminal_reason` シード → `list/requeue-poisoned-job`（突入は integration 担保） | 未走: 01 / 12〜16 / 19〜23 / 26〜28 / 34〜37 / 39 |
| 2 | timeline.md | 37 | 3 日以上 × 10 件、51 件以上（TC-04）、キーワード 2+2、10,000 / 10,001 文字、出典付き文書 2、AI actor（TC-09） | 過去日付は dev 停止中に sqlite `memos.posted_at`。51 件は ai-client `post_memo` ループ。TC-17 は route abort。TC-29 は別 session | 未走: 04 / 05 / 10〜14 / 19〜22 / 28 / 30 / 31 / 33〜37 |
| 3 | document.md | 41 | メモ A/B/C、100/101・200/201・500/501・400,000/400,001 文字、トピック 0 件ユーザー（TC-28） | TC-39 は `eval` で value 設定。TC-25 は別 session | PH-03 で全件。共有コンポーネント変更（PH-05 / PH-09）後の再走 |
| 4 | search.md | 23 | `fogsearch` M1〜M6 / TP-A〜E / D-*（一部ゴミ箱）、`fogpage` 21 件、専用ユーザー | 全件 UI。TC-23 は `tab new`。TC-16 は `maxLength=500` | PH-04 で全件。再走 |
| 5 | ai.md | 23 | AI 接続、メモ 3（`蒸留器の温度管理`）、トピック `自作蒸留器`、完了済みトピック + 文書、境界文字数 | 実 LLM アプリ不要（spec が REST 直叩きを許容）: ai-client `call <tool>` / `mcp`。「依頼文からツールを選ぶ」は検証対象外と備考に | 未走: 14〜23 |
| 6 | trash.md | 25 | M1 / M2 / T1 / D1 … の呼称データ、準備 A〜D、専用ユーザー、保持 30 日 | TC-13 / 23 / 24 は sqlite で `purge_after` と `purge-trash` の `next_run_at` を過去へ + Alarm 起床。TC-21 / 22 は別 session | PH-05 で全件。再走 |
| 7 | settings.md | 12 | メモ 3（2 件は別日 = sqlite）、履歴ありメモ、トピック A（文書 2）/ B（完了）、ゴミ箱、0 件ユーザー、`Asia/Tokyo` | TC-03〜06 の展開は macOS `unzip` 不可 → Python `zipfile` / `ditto -x -k`。ダウンロードは `agent-browser download`。多日分割は sqlite シードで実走 | 未走の形: TC-03 手順 4（多日） |

- 環境依存で実行不能なもの（FAIL にせず備考に記録）: 実 IdP の内部エラー、実 LLM のツール選択、TC-45 の producer binding 障害、TC-47 の RPC 障害による突入、macOS `unzip`、24 MiB 超過、無限スクロールの自動発火
- 既知の未対応観察がそのまま結果に出るもの: timeline の失敗後再送（PH-01 N-1）、絞り込み中編集（PH-02 A-2）、`<mark>` 欠落（PH-04 O-1）。§2 の扱いに従い、直したものは再走で確認
- 記録: 各ファイルの結果を `.spec-implement/reviews/PH-10-manual/<category>.md` に TC ごと（合格 / 不合格 / 実行不能 + 備考、ISO 8601、session、dev / preview）。不合格は再現手順と原因の当たりを添えて Manager へ

### 3.3 最終実行と `pnpm start`

- `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を HEAD で 2 回（flaky の判定。PH-09A の実績では 1 回目のみ dom 1 件失敗）
- `pnpm start`（#73）を 1 回実行して起動可否を確定し、README / docs §4.5 / §14 をその結果で書く

### 3.4 アーキテクチャ監査の観点（CLAUDE.md の規約ごと）

| 規約 | 確認方法 |
|---|---|
| 依存方向（presentation → application → domain、adapters は port の内側に） | `grep -rn "@repo/core/adapters" packages/core/src/{domain,application}` は di / 明示された例外だけか；`apps/web` から `application/**/*Procedure` を直接呼ぶ箇所が無いか |
| Unit of Work（callback 同期、`run` の入れ子なし、非同期 port を ctx に載せない） | `UnitOfWorkRunner` / `provider().run` の呼び手を列挙し、`await` を含む callback と `run` 内 `run` を grep（`transactionSync` 直接使用の全箇所も） |
| 書き込み口の全数（非集約ストア 8 ストア 9 メソッド、adapter-owned の `_meta` / `oauth_consumed_codes`） | 各表への `INSERT / UPDATE / DELETE` の grep → 出所が UoW ctx / 宣言済み adapter だけか（`rotationCheckpointWriters.test.ts` の手法を他表に適用） |
| Retry（OCC 不再試行、runner と relay 以外で retry しない、DLQ 自動 1 回） | `ConflictError` の catch 箇所、ループ再試行の grep |
| 入力検証 2 点（transport / VO）、RPC は primitives | facade signature に branded 型が無いこと；`serverAction` の `inputValidator` 欠落；`serverData` に外部入力を渡していないか |
| jobs.kind / event.type の全数表（`spec/async` 11 + 1） | `JOB_KIND_POLICY` と handler 登録の一致、`lint/` の roster grep |
| エラー契約（`instanceof` 禁止、`toSerialized`、wire 値の re-throw 禁止、catch の境界列挙） | `lint/banList.test.ts` 緑；broad `try/catch` の全数を列挙して CLAUDE.md の列挙と突き合わせ（§1.6） |
| 非同期 port の列挙（`PasswordHasher` / `MailSender` + `SessionCodec` / `SsoIdentityProvider`） | port ファイルの `Promise` grep → CLAUDE.md の文を確定 |
| AI スコープ（ハードデリート / ゴミ箱 / 履歴が AI に無い） | `AiToolContainer` の型と許可リスト、MCP / REST の handler が gateway を直接呼ばないこと |
| ログ衛生（queue message 全体 / 鍵 / canonical / HMAC を出さない） | logger 呼び出しの meta を grep、operator 監査の `id` 射程 |
| docs の断定（gap を docs に書かない） | §1 の反映後に「未実装 / #NN / TODO」の grep が 0 |

`architecture-audit` / `implement-audit` スキルは呼ばない（指示の制約）。上の観点を手で回し、違反は「重大（修正）/ 軽微（issue 候補）」に分ける。

## 4. 実装順

1. **docs の現状化**（§1、`docs/*` / `README.md` / `.dev.vars.example` + `wranglerConfig.test.ts` の `[vars]` 2 行。`CLAUDE.md` の 4 点は Manager の承認後）。コミット単位: docs/test.md / docs/runtime_cloudflare.md / 2 example / README + .dev.vars.example
2. **監査**（§3.4）と §2「直す」の修正（小さい順: PH-02 querySelector / userActorOf、PH-03 (f)、PH-05 A-6 / O-1、PH-08 O-1、PH-09A O-1 / O-7、PH-04 O-1、PH-09A O-6 の `waitFor`、PH-07 O-1 は監査結果次第）。各修正はテスト付き、`pnpm typecheck && lint:fix && format && test`
3. **マニュアルテスト実走**（§3.2 の順。docs / 修正の後に行う。dev → preview の順で片方ずつ）。不合格は原因の当たりを付けて Manager へ判断依頼、修正は 2 の粒度で
4. **最終実行**（§3.3）→ **最終報告**（PH-10.md に「実装報告」: 項目 ID ごとの結果、docs の変更一覧、監査結果（違反と処置）、マニュアルテスト集計（合格 / 不合格 / 実行不能）、issue 候補一覧（§2 の issue 化分 + 監査で出たもの）、PH-01〜09 への影響）

Manager の判断待ち（着手前）: (a) `CLAUDE.md` の 4 点（§1.6）を PH-10 で直してよいか、(b) §2 の「Manager」行（G-1〜3 / O-5 / PH-08 O-3 / 判断 1・2 / PH-09B 観察 2）、(c) `.wrangler/state` を作り直して統合検証を始めてよいか（推奨）、(d) issue の起票は PH-10 の最後に Manager が行う前提でよいか

## docs 実装報告（Implementer、2026-09-11T02:45〜02:30+09:00 → 02:28Z 完了。HEAD `90281ae`）

- コミット: `02676d3`（`docs/{test,runtime_cloudflare,backend_implementation_example,frontend_implementation_example}.md`）、`90281ae`（`README.md`、`CLAUDE.md`、`apps/web/.dev.vars.example`）。7 ファイル +662 / −964。`packages/` / `apps/` の実装コードとテストは触っていない（`.dev.vars.example` はコメントの帰属表 2 行のみ）。`pnpm lint:fix` / `pnpm format` 差分なし、`wranglerConfig.test.ts` + `lint/` の 3 ファイル 104 件緑
- 方針: §1 の各行を「現状を断定で述べる」形で反映。まだ真である gap（#73 `pnpm start`、#18 origin rate limit、#22 observability、#23、#79 Pulumi の D1 残骸、#81 PITR の production entry、#83 手動再駆動）だけを残し、実装済みの「Reality: None」「#NN で追加」はすべて削除。テスト不能・環境依存・判断値は「limit」として書いた（gap と区別）

### 変更内容（ファイルごと）

- `docs/test.md`（全面）: unit / DOM / integration の 3 project、`setupFiles = components/__tests__/domSetup.ts`（`asyncUtilTimeout` 5 s）、DOM 29 ファイルをディレクトリ別に各 1 行、integration は DO pool のみ（`vitest.config.do.ts`、32 suite / 188 件をディレクトリ別に件数つき）、fakes roster（`fakes.ts` 5 + gateway tripwire 5 + `unitContainer` / `memoContainer` / `memoFixtures`）、helper の所在（`testContainer.ts` / `helpers.ts` / `memoFixtures.ts`）。削除: D1 project / `_occ_guard` / fast-check・property-test / 存在しない例示ファイル / `markSearchEntry`。新設「Limits — what no suite can pin」（tx 原子性の失敗注入、`rotate-encryption` と s4 の割り込み、チャンク途中の DO リセット、24 MiB のブラウザ再現、sagaRecovery 26、`aiConnectionsList.dom` の 1 回の flake）
- `docs/runtime_cloudflare.md`（178 行変更）: §1 D1（binding / adapter / Drizzle 無し、残るのは Pulumi の `cloudflare.D1Database` と `render-wrangler.ts` の placeholder = #79）、§3 secrets roster 15 + `[vars]` 3 + dev 2、`secret put` 13 行、新設「Rotating the two keys」（写像鍵 / 暗号鍵の手順、デプロイ順序、CLI、121〜146 ms、未初期化 bucket の初期化と復号不能チャンクの `poison` を limit として）、§4.4 DLQ、§4.5 dev / preview の `.wrangler/state` 共有と `[dev-mail]` の `APP_URL`、§6 fail-closed の検知は `read-schema-version`（ゲート外）+ workerd の `id.name` 欠落と `_meta.self_locator` fallback、§7.3〜7.5（backlog / `list-bucket-user-ids` × bucket → `read-schema-version` / `sweep-reset-tokens`）、§8.2〜8.6（`POST /__operator/<entry>`、`OPERATOR_TOKEN` 未設定 404 → 未知 entry 404 → 405 → 401 → 400、keyring 両世代の locator 検査、Access は前段、監査 `{ entry, locator, outcome, id? }` と `id` の 5 出所、CLI、3 entry ずつの列と応答、DLQ 自動 1 回）、§10 表（15 entry × target × gate 内外 × rearm × body）、§11.1 export（24 MiB、同一 origin、決定的 zip）、§11.2 withdrawal（`abandon-account` 経路のみ、到達性の削除、tombstone、`withdrawal` 行、no-op 再発行、`purge-user-mappings` の運用注意）、§11.3 PITR（表は実在、手順は sqlite で local、復元 bucket は `remap-chunk` 再駆動 → checkpoint 再読）、§12（11 handler 登録済み、`listPoisonedJobsLimit` / `deleteNoopReissueDelayMs` の行、§12.4 は WAF のみ）、§13 38 行の再判定、§14 の整理
- `docs/backend_implementation_example.md`（492 → 538 行）: ファイル一覧（`ports/` 同期 4 / 非同期 3、`di/aiToolContainer`、`stores/` 群、`jobs/` 11 + `cleanup/`、`rotation/`、gateway 6、`mail/` `sso/` `fflate/` `webcrypto/`、presentation 22、worker 4、`scripts/`）、UoW ctx の 2 roster、`beginSignup` を例示と明記 + 実物（`reserveCredentialProcedure` が `sweep-reservations` + `resume-signup` を投入）、新設「Gateway and procedure」（usecase → gateway → facade `envelope(() => runUnitOfWork(ctx => *Procedure))` → 同期 procedure、`postMemo` の実経路、ジョブの procedure 再利用）、`RequestContainer` / `QueueContainer`、`TransactionalRepository` 4 メソッド同期、driver error は `adapters/cloudflare/unitOfWork.ts` の `translate()`（`mapDbError` / D1 / `pendingBatch` 削除）、facade の operator entry roster、job handler 結果 `finished / rearm / yield / poison` + `commit` + `TerminalStageSelector`、quarantine / poison 各 3 entry、DLQ 自動 1 回
- `docs/frontend_implementation_example.md`: 採用プリミティブ（`useActionState` / `useTransition` / `useOptimistic`、plain form、`router.invalidate()`）を明記し TanStack Query / Conform / sonner の節を削除、streaming は `routes/_app/index.tsx` + `streamingRouteOptions`（`ssr: !DEV`、`pendingComponent: () => null`）、`Deferred = use(useDeferredValue(promise))`、skeleton 8 + `ui/Skeleton`、route-level pending の相互作用、`presentation/currentUser.ts`（`getCurrentUserId` / `requireUserId`、epoch 照合、`redirectPathSchema`）、`readServerFnResult` の節、`postMemoFn` / `TimelineBoard`（`inFlight` + `guardSubmit`、optimistic add、`runDelete`）を実例に、`DocumentFeed` + `guardStreamedRender` + `KnowledgeNotFound`、`_app.tsx` + `AppShell`、`readAuthStateFn` / `toSafeRedirect` / `routeHead`
- `README.md`（全面）: fog の説明（Worker / package 名は `tanstack-start-template` のまま）、`AuthForm` / `LogoutButton` / optimistic list の実物、3 project、`scripts/` 3 本、Node 22.12+、Quick Start（必須 secret 6 + `MAIL_DEV_SINK` / `SSO_DEV_STUB`、`OPERATOR_TOKEN` 任意、dev / preview 同時起動不可）、Development（operator CLI / ai-client / export の節）、Out of scope（実 IdP・Apple adapter 無し・実 provider・実 LLM は契約検証、退会 usecase・本番デプロイ未実装）。**`pnpm start`（#73）は実行して確定**: `timeout 45 pnpm start:cf` → wrangler は両 config を読むが esbuild が `#tanstack-router-entry` / `#tanstack-start-entry` / `#tanstack-start-plugin-adapters` / `tanstack-start-manifest:v` / `tanstack-start-injected-head-scripts:v` の 5 モジュールを解決できず失敗、port は開かない（`scratchpad/start73.log`）。README にその 5 名を書いた
- `CLAUDE.md`（4 文。Manager が受け入れ時に確認）: (1) Retry strategy — 「queue の先で retry を委譲する唯一の場所」に DLQ handler の自動 1 回再配送（`queueHandlers.ts`）と、削除 saga の 2 世代 no-op 再発行（`deleteNoopReissueDelayMs`、`spec/rotation`「削除の no-op 確定」）は retry ではない旨を追記。(2) 非同期実行契約 5 — 「queue の retry、次に DLQ handler の自動 1 回再配送」。(3) Error handling の broad catch 列挙 — 実際の境界に合わせて追記: bare request handler（`/__operator/*`、SSO callback、AI の OAuth / MCP / REST、`/export`、session 検証）、spec が部分失敗でも続行と定める per-item（`sweep-orphan-mapping` / `emptyTrash` / `pruneExpiredTrashItems` / `revokeAllAiClientConnections` / `remap-chunk` / `import-remapped-mappings`）、saga の補償 catch（signup / link）、宣言済みの `loginWithPassword`。すべて grep で実在を確認（`signupSaga.ts` / `linkSsoCredential.ts` / `emptyTrash.ts:66` / `pruneExpiredTrashItems.ts:111` / `revokeAllAiClientConnections.ts:56` / presentation 5 ファイル）。(4) Reference runtime — 「`PasswordHasher` / `MailSender` が唯一の非同期ポート」は UoW が到達しうる domain / application ポートの話として据え置き、presentation 専用ポート（`SessionCodec` / `SsoIdentityProvider`、同期の `TokenGenerator`）を UoW ctx に載らない別群として 1 文で区別
- `apps/web/.dev.vars.example`: 帰属表に `APP_URL` / `DIAGNOSTICS_ENABLED`（request Worker `[vars]`。後者はローカルのみ）の 2 行。`=` 行は足していない。`wranglerConfig.test.ts` に `[vars]` 2 変数の帰属固定を足すのは PH-09B 受け入れ後（テスト変更禁止中）

### 確認した事項 / 限界の扱い

- `docs/runtime_cloudflare.md` §12.4 の「`createIdentityTuning` が窓 < TTL を検査する」は `application/identity/tuning.ts:96`（`resetRequestWindowMs >= delivery.resetTokenTtlMs` で throw）で確認
- Manager 判断 (b) の反映: DDL 差 G-1〜G-3 は無記述（spec と矛盾なし）。design O-5 は docs の対象外。スラッグ末尾 / macOS `unzip` / `EXPORT_TOO_LARGE` / 復号不能チャンクの `poison` は limit として記述（unzip は README「Export」と runtime §13、`EXPORT_TOO_LARGE` は runtime §11.1 の「`SystemError(ExportTooLarge)`」、スラッグ末尾は README / docs に該当節が無いため §2 の issue 候補のまま — spec 変更を伴うので最終報告で提案）
- 残る "Reality: None" は #73（§4.5、README 参照）と #18（origin rate limit）の 2 箇所で、どちらも現に無い

### 次のステップ（PH-09B 受け入れ後、Manager の指示待ち）

- §2「直す」の小修正（テスト付き）と `wranglerConfig.test.ts` の `[vars]` 2 行 → 監査（§3.4）→ `.wrangler/state` 作り直し → マニュアルテスト実走（§3.2）→ 最終実行 2 回 → 最終報告

## PH-10 実装報告（Implementer 引き継ぎ、2026-09-11T12:00+09:00〜。開始 HEAD `9bec53e`）

> 完了候補（2026-09-11T14:00+09:00、HEAD `7446b5e`）。Manager 判断後の文言修正で `c1f3366`（§7）、差し戻し対応で `f47777c`（§8）、issue 番号の反映で最終 HEAD は `fef0e3b`（§9、docs のみ）。以降のコード変更は止めた。進捗ログは末尾の「0. 進捗ログ」

### R-INT-01 の結果（完了条件ごと）

| # | 完了条件 | 結果 | 根拠 |
|---|---|---|---|
| 1 | `spec/manual-tests/*.md` 7 カテゴリ 208 ケースをブラウザで実行して合格。実行不能は理由と代替を記録 | **合格 207 / 部分実施 1 / 不合格 0**。部分実施は account TC-47（bucket 単位の cross-DO RPC 障害を dev で起こせないので、sqlite シードで終端モード・poison・requeue を観測。後始末の巻き戻しは integration が固定）。実走で出た不合格・不具合 13 件（document 6・trash TC-21・search の観察 1・trash / settings の観察 4・確認ダイアログ全般 1）は、document TC-11 以外を直して再走で合格。document TC-06 / TC-11 と account TC-46 は Manager の spec 判断（`d31f2eb` / `acc356f`）の後に再判定して合格 | §1、`reviews/PH-10-manual/{account,timeline,document,search,trash,ai,settings}.md` |
| 2 | `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` が最終 HEAD で 2 回連続成功 | **成功**（`7446b5e`、13:56:47〜13:57:10 と 13:57:10〜13:57:36。どちらも unit+dom 129 files / 1,598 passed + 2 skipped、integration 32 files / 191 passed、lint warning 3 は既存） | §4、`scratchpad/ph10c/final-{1,2}.log` |
| 3 | アーキテクチャ監査（§3.4 の 11 観点）に重大な違反なし | **重大な違反なし**。前任の軽微 4 件は全部直した（`425f36b` / `ebbac54` / `f14fcd8`）。新たな軽微 1 件（application procedure 2 本が `SqlStorage` で読み取り SQL を持つ）は issue 候補 | §2 |
| 4 | docs / README / CLAUDE.md / `.dev.vars.example` が現状を述べる | **最終化した**。前任の現状化（`02676d3` / `90281ae`）に、監査と実走で分かった事実を反映: 存在しない issue 番号・`.thread/` の参照を全削除（このリポジトリの issue は 0 件）、DLQ は自動 1 回が定義（`spec/async`）、AI トークン codec の port 化、DOM / integration の件数と射程。`CLAUDE.md` は 1 語（presentation 専用 port の列挙に `AiTokenCodec`） | §3 |
| 5 | `pnpm start`（#73 は存在しない issue 番号だった）の現状が README に記録 | **記録済み**。`f14fcd8` で `timeout 60 pnpm start:cf` を再実行し、同じ 5 つの仮想モジュールの未解決で bundle 失敗・port 未オープンを確認、README に HEAD と日付を書いた | README「`pnpm start` does not boot…」、`scratchpad/ph10c/start73.log` |

### Manager の判断・対応が必要な事項

1. **issue の起票**（§5 の一覧）。特に、docs が「tracked in #NN」と書いていた 6 件（デプロイ不能 / Pulumi の D1 残骸 / DLQ ログの保持 / PITR の maintenance entry / 滞留の通知 / origin 単位の rate limit）は、このリポジトリに issue が無いので docs から番号を外した。起票後に docs へ番号を戻すかは Manager の判断
2. **spec 内の用語の揺れ（軽微）**: 削除済みメモの表示文言が spec/pages P-07 / P-08 は「削除済みのメモ」、design（topic-detail.html）と実装は「削除されたメモ」。ドキュメント側は spec どおり「削除済みのドキュメント」。どちらに揃えるか
3. **`CLAUDE.md` の変更 1 語**（`425f36b`）: presentation 専用 port の列挙に `AiTokenCodec` を足した（`SessionCodec` / `SsoIdentityProvider` と同じ扱い）。受け入れ時に確認してほしい
4. **受け入れ済み項目に触れた変更**（すべて完了条件は不変、dom / integration テスト付き、事前・事後に SendMessage 済み）: R-DT-01 / R-DT-02（空の名前・タイトルのエラー文、P-07 の完了ボタン）、R-SE-01（検索チップの current）、R-TR-01〜03（別タブで消えた行のアラート、復元先ダイアログ）、R-ST-01（保持期限フォーム）、全画面の確認ダイアログの位置と危険ボタンの色、R-AC-05（AI トークン codec の port 化・PKCE の移動、振る舞い不変）、R-ST-02（zip writer を entry point から渡す）、R-REC-01（`finalize-withdrawal` の最終 tx を UoW の口経由に）

### 1. マニュアルテストの結果（7 カテゴリ × 208 ケース）

TC ごとの観測値・時刻・session・ユーザーは `reviews/PH-10-manual/<category>.md`（表の下の「Implementer の再走」「sqlite 窓」節が最終判定）。

| カテゴリ | 件数 | 合格 | 部分 / 不合格 | 実行者・環境 | 修正して再走したケース / 代替検証 |
|---|---|---|---|---|---|
| account | 47 | 46 | TC-47 部分実施 | 前任の補助（01〜38, 40〜44）+ Implementer（TC-38 手順 4 の補強、TC-39 再走、TC-45〜47）、dev | SSO 11 件は `SSO_DEV_STUB`（契約検証）。TC-30 は使用済みトークンで期限切れ画面を代替。TC-45 は producer binding を外した**本物の publish 失敗**。TC-46 は `MAIL_DEV_SINK` 不正値で consumer 失敗 → DLQ → 自動 1 回 → ack、手順 6 は `acc356f` の期待で再判定。TC-47 は bucket 単位の RPC 障害が起こせず sqlite シード + integration（`recovery` / `jobTerminal`） |
| timeline | 37 | 37 | — | 前任の補助 + Implementer（TC-03 / 07 / 08 / 09 / 21 / 24 / 29）、dev | TC-21 は `9bec53e`。TC-03 / 07 / 24 / 08（過去日付）は sqlite で `posted_at` を多日化。TC-09 / 29 の AI 部分は ai-client の複製で認可 |
| document | 41 | 41 | — | 前任の補助 + Implementer（06 / 11 / 14 / 20〜23） | TC-20〜23: `87a1f46`（空の名前・タイトルのエラー文）。TC-06: `b7deeb4`（完了ボタンをヘッダーへ、`d31f2eb`）。TC-11: 手順書改訂 `d31f2eb` で再判定。TC-14 の AI 部分は `PH10 Document AI` で直接 |
| search | 23 | 23 | — | 補助（`ph10-search`）、dev | 観察の「選択チップが区別できない」は `c844897` で修正。TC-16 は `maxLength=500` の側で成立 |
| trash | 25 | 25 | — | 補助（`ph10-trash`）+ Implementer（13 / 21 / 22 / 23 / 24） | TC-21: `ce6501a`（別タブで消えた行はアラート + 一覧を読み直す）。TC-13 / 23 / 24 は sqlite で `trashed_at` / `purge_after` を期限切れ・期限直前・40 日前に |
| ai | 23 | 23 | — | Implementer、dev | 実 LLM アプリは無い（spec が許す代替: MCP `tools/call` / REST を直接）。依頼文からツールを選ぶ振る舞いは対象外 |
| settings | 12 | 12 | — | Implementer、dev | エクスポートの多日分割は sqlite で 2 件を別日へ。展開は macOS `unzip` が非 ASCII 名で失敗（zip は UTF-8 フラグ 0x800 付きで正しい）→ `ditto -x -k` / Python `zipfile` |
| **計** | **208** | **207** | **部分 1** | | |

- **preview（SSR ストリーミング経路）**: 13:13〜13:15、`pnpm build` → `pnpm preview`。保護 9 画面の SSR 済み本文（cookie 付き `curl`）、未ログイン 307、投稿（楽観 UI）、検索チップ、P-07 の完了ボタン、P-09 のタイトル空エラー、ゴミ箱の復元、エクスポート、AI REST を確認（進捗ログ 13:13）
- **環境依存で FAIL にしないもの**: 実 IdP / 実 mail provider / 実 LLM アプリ（契約検証、plan.md「実接続の検証手順」）、macOS `unzip`、24 MiB 超のエクスポート（integration）、10 GB 逼迫、bucket 単位の RPC 障害（TC-47）
- **実走で見つけて直したもの**（手順書の判定外を含む）: 検索チップの current（`c844897`）、確認ダイアログが左上に出る・危険ボタンが赤地に濃赤（`1210ec5`、全画面）、保持期限フォームの拒否後に入力が戻る・英語の拒否文（`c734ddb`）、復元先ダイアログの枠・空白名・「候補を読み直す」の出し分け（`ce6501a`）

### 2. アーキテクチャ監査（§3.4 の 11 観点。HEAD `7446b5e`）

| # | 観点 | 判定 | 根拠 / 処置 |
|---|---|---|---|
| 1 | 依存方向 | 合格（軽微 2 → 修正、軽微 1 → issue 候補） | `domain/` → application / adapters の import 0。`application/` → adapters は `di/serverCloudflare.ts` / `di/secrets.ts`（合成根）だけ。**修正**: presentation の adapter 直参照 2 件（`presentation/ai/{oauth,auth}.ts` が `AiTokenCodec` と PKCE を adapter から、`presentation/export/handler.ts` が zip writer を自前で）→ `application/ports/aiTokenCodec.ts`（presentation 専用 port）、`presentation/ai/pkce.ts`、`createArchiveWriter` を entry point から（`425f36b`）。`apps/web/app/presentation/__tests__/adapterImports.test.ts` が entry point（`server.cloudflare.ts` / `worker/cloudflare/`）以外の adapter import を赤くする（修正前のコードで赤を確認）。**残る軽微**: `application/identity/abandonAccount.ts` と `rotation/recordRemappedLocator.ts` が `SqlStorage` を受け取り `SELECT caller_token … FROM account` / `operations` を直接読む（読み取りのみ。caller binding を domain port に出さないための設計と読めるが、application が SQLite の列名を知る）→ issue 候補 |
| 2 | Unit of Work | 合格 | `run` / `runUnitOfWork` に `async` callback を渡す箇所 0（型でも拒否）。`transactionSync` を呼ぶのは adapters のみ（17 ファイル。domain / application の出現は JSDoc の文中）。UoW ctx 型に `Promise` を返すメンバなし |
| 3 | 書き込み口の全数 | 合格（軽微 2 → 修正） | `application/**` / `domain/**` / `apps/web/app` に書き込み SQL 0。**修正**: `finalize-withdrawal` の最終 tx が `credential_locators` / `ai_client_connections` を生 SQL で書いていた → `finishWithdrawalProcedure`（store / repository / entity 経由、`ebbac54`、integration で active 1 件の失効と既失効の不変を固定）。書き口の固定テストが `rotation_checkpoints` だけだった → `storageWriters.test.ts`（`INSERT` / `UPDATE` / `DELETE` / `REPLACE` は `packages/core/src/adapters/` の外に無い、`f14fcd8`）。adapter 内の job / rotation / sweep が非集約表を直接書くのは CLAUDE.md の射程（usecase の書き口）の外 |
| 4 | Retry | 合格 | `isConflictError` の分岐は job runner の終端判定、`terminalReason`、per-connection 続行（spec）、saga の業務コード読み替え、SSO の表示分岐だけで、再試行するものは無い。`attempt` のループは job runner と relay のみ（`identity/tuning.ts:48` は backoff の合計計算）。queue は `message.retry()` と DLQ の自動 1 回（dev で実物を観測: TC-46） |
| 5 | 入力検証 2 点・RPC は primitives | 合格 | `createServerFn` 49 のうち `inputValidator` 39、残り 10 は入力を取らない。path param の route は zod、`validateSearch` 7 route、`serverData` の呼び手は検証済みの値だけ。DO facade の引数は primitives / plain object、DO 内で VO を再構築 |
| 6 | `jobs.kind` / `event.type` 全数表 | 合格 | 11 kind（User Data 6 / Directory 5）+ event 1 種が `spec/async` と一致、`jobKinds` / `rosterGrep` テストが固定（最終 run 緑） |
| 7 | エラー契約 | 合格 | `lint/banList.test.ts` / `pluginWiring.test.ts` 緑。今回の変更で新しい error class なし。broad catch の追加なし（trash の notFound 分岐は既存の catch 内の表示分岐） |
| 8 | 非同期 port の列挙 | 合格 | `Promise` を返す port は `PasswordHasher`（domain）と application の `MailSender` / `SessionCodec` / `SsoIdentityProvider` / `AiTokenCodec`。UoW が届く port は前 2 つ、残り 3 つは presentation 専用で CLAUDE.md の列挙と一致（`425f36b` で 1 語追加） |
| 9 | AI スコープ | 合格 | `tools/list` は 11 個のみ、`list_trash` / `hard_delete` / `restore` / `empty_trash` / `list_revisions` / `rollback` は MCP `-32601` / REST 404、`edit_document` の `mode: rollback` は `-32602`（ai TC-14 で実物）。`AiToolContainer` は `trashGateway` / `exportGateway` を型で持たない |
| 10 | ログ衛生 | 合格 | 実走の dev ログ 6 本（1,682 行）で `[dev-mail]`（dev の sink）以外にメール・トークン・`ownerToken` / `callerToken`・64 hex の出現 0。relay は `{ eventId, type, cause }`、DLQ は `{ eventId, type, outcome }`、operator 監査は `{ entry, locator, outcome, id? }` だけ（TC-45 / 46 で実物） |
| 11 | docs の断定 | 合格 | `docs/` / README / CLAUDE.md / deploy テンプレートに存在しない issue 番号・`.thread/` 参照 0（`58705f2` / `6dd0fd4`）。「Reality: None」は現状の記述として残し、追跡の約束は外した（§3） |

**PH-01 T-2**（alarm の 2 pass 独立上限・`leaseUntil` 再武装）は `alarm.integration` に 2 件足して固定（`f14fcd8`。test worker に ack だけの `queue()` を足した — relay が一度に 25 件を publish するとバッチが即時配信されるため）。最終 run で 1 回だけ、既存の `recovery.integration`「abandon-account: the six checks」が `finalize-withdrawal` の `running` を観測して落ちた（pool が即時に Alarm を起こすため、RPC 待ちの途中の行を読む競合）。期待を「pending / running / done」「deleting / deleted」に広げた（`3d23270`、単独 3 回・全体 2 回で緑）

### 3. docs の最終差分

- 前任のコミット（`02676d3` / `90281ae`）+ 今回（`425f36b` / `58705f2` / `6dd0fd4` / `7446b5e`）。`9bec53e..7446b5e` の docs 系は 7 ファイル +55 / −54
- `docs/runtime_cloudflare.md`: 冒頭の marker 定義から「None (#N) = issue #N が受ける」を外し、「spec との食い違いは issue tracker に置き、ここには書かない」に。#73 / #79 / #83 / #22 / #81 / #23 / #18 / #37 / #58 の番号を本文から外して事実だけを残した（デプロイ不能の原因、D1 の撤去は runtime に影響しない、DLQ は自動 1 回が定義で手動再駆動は無い、Workers Logs が無い、PITR の手順に entry が無い、滞留の通知が無い、origin rate limit が無い、3.4 ms / 1,000 行の測定値）。§14 を「Known limits」（行ごとに記述先の節）に
- `README.md`: `pnpm start` の再確認を HEAD `f14fcd8`・日付つきに、#73 のリンクを削除
- `docs/test.md`: DOM 6 行（documentEditor / searchPanel / retentionForm / topicHeader / topicList / trashBoard の射程）、scan テスト 2 本（`storageWriters` / `adapterImports`）、integration 32 files / 191（`alarm` 3、`aiClientConnections` 5）
- `docs/backend_implementation_example.md`: `ports/` に `aiTokenCodec`、presentation の `ai/`（codec は entry point から、`pkce.ts`）と `export/`（archive writer は entry point から）
- `CLAUDE.md`: Reference runtime の presentation 専用 port の列挙に `AiTokenCodec`
- deploy テンプレート 2 本のコメントから「Tracking: #73」を削除。`apps/web/.dev.vars.example` は変更なし（実走の 6 秘密 + `MAIL_DEV_SINK` / `SSO_DEV_STUB` / `OPERATOR_TOKEN` の形と一致）

### 4. 最終検証（`pnpm typecheck && pnpm lint && pnpm format:check && pnpm test`、HEAD `7446b5e`、dev 停止中）

| 回 | 開始〜終了（+09:00） | typecheck / lint / format | unit + dom | integration |
|---|---|---|---|---|
| 1 | 2026-09-11T13:56:47 〜 13:57:10 | exit 0（lint warning 3 は既存: `currentUserPanel.dom` の non-null ×2、`wranglerConfig.test` の template 文字列） | 129 files / 1,598 passed + 2 skipped | 32 files / 191 passed |
| 2 | 2026-09-11T13:57:10 〜 13:57:36 | exit 0（同上） | 129 files / 1,598 passed + 2 skipped | 32 files / 191 passed |

参考: `3d23270` の前の run（13:54:26）で上記 `recovery.integration` の競合が 1 回出た（§2 末尾）。`3d23270` / `7446b5e` で計 4 回連続緑

### 5. issue 候補一覧（このリポジトリの issue は 0 件。起票は Manager）

| 候補タイトル | 出典 | 優先度の目安 |
|---|---|---|
| request Worker をデプロイできない / `pnpm start` が起動しない（TanStack Start の仮想モジュールを wrangler が解決できない） | README、docs §4.2（旧 #73） | 高（本番の前提） |
| Pulumi `resources` stack に D1 と render の placeholder が残る | docs 1 章 / 2 章（旧 #79） | 低 |
| DLQ の 1 行ログを本番で保持する（`[observability]` / Workers Logs） | docs §8.7（旧 #22） | 中 |
| PITR の必須 4 手順の maintenance entry | docs §11.3（旧 #81） | 中 |
| 配送の滞留を能動的に通知する | docs §7.3 / §8.3（旧 #23） | 中 |
| origin 単位の rate limit（WAF ルール） | docs §9.4（旧 #18） | 中 |
| application procedure（`abandonAccount` / `recordRemappedLocator`）の `SqlStorage` 直読みを store の読み取りに移す | 監査 1 | 低 |
| TimelineBoard: 失敗後の再送で二重投稿になりうる | PH-01 N-1 | 中 |
| `reserveCredential` を単一 CAS 文に | PH-01 C-3 | 低 |
| User Data facade で RPC 入力の `actor.userId` と self locator の一致を確かめる | PH-01 O-2 | 低 |
| 絞り込み中に編集して不一致になった行が再取得後も残る | PH-02 A-2 | 低 |
| search cursor に HMAC 署名（AI 経路で外部入力） | PH-04 O-2 | 低 |
| Apple の実 IdP adapter | PH-06 判断 2 | 中（Apple ボタンは dev stub のみ） |
| `/export` と MCP の origin 基準を揃える | PH-08 O-2 | 低 |
| スラッグ 50 文字の切り詰めが `-` / `.` で終わりうる（spec 変更を伴う） | PH-08 O-3 | 低 |
| `sagaRecovery.md` の未固定 19 行のテスト | PH-09A §1 | 低 |
| 既存ドキュメントに出典を後から足す経路が UI に無い（P-09 編集モードは読み取り専用。spec の意図の確認から） | timeline / document の観察 | 要 spec 確認 |
| 40 万字の本文を持つドキュメントの編集画面が `open` + `networkidle` で 2 回 `chrome-error` になった（SSR の応答サイズ） | document TC-39 の観察 | 低（再現性未確認） |
| 検索キーワード 501 文字（入力欄の上限を迂回）で `q` が黙って落ちる | search TC-16 の観察 | 低 |
| SSO 連携の `already_used` 文言が自分のアカウントの主体でも「別のアカウントに」 | account TC-43 の観察 | 低 |
| 投稿失敗の文言が汎用（通信断と分からない） | timeline TC-17 の観察 | 低 |

### 6. Verifier への引き継ぎ

- **起動**: dev（:3000）を 13:58 に起動済み（`pnpm dev`、ログ `scratchpad/ph10c/dev-final.log`）。preview は停止（使うときは dev を止めてから `pnpm build && pnpm preview`、計画 §3.1）。`.dev.vars` はバックアップ（`scratchpad/ph10c/dev.vars.bak`）と同一、`wrangler.state.toml` もバックアップ（`scratchpad/ph10c/wrangler.state.toml.bak`）と同一で `git status` clean
- **DO 状態**: `.wrangler/state` は 07:47 に作り直した状態 + PH-10 の全データ。12:54 に sqlite を編集した 6 ファイルのバックアップは `scratchpad/ph10c/sqlbak/`
- **テストアカウント**（パスワード）: `ph10-ai-main@example.com`（`password123`、ai / 修正確認の主ユーザー、AI クライアント `Claude Test` の状態は `apps/web/.ai-client.json`）/ `ph10-settings-main@example.com`・`ph10-settings-empty@example.com`（`password123`）/ `ph10-timeline-main@example.com`（`Ph10Timeline#Pass1`、60 件のうち 7 件が 9/1・9/6・9/8）/ `ph10-document-main@example.com`（`Ph10docMain!23`）/ `ph10-search-main@example.com`（`Ph10Search#Pass1`）/ `ph10-trash-{main,expire,edge,extend}@example.com`（`Ph10Trash#Pass1`）/ `ph10-account-main@example.com`（`newpassword456`）ほか account の `ph10-account-*`（`reviews/PH-10-manual/account.md` 冒頭）/ `ph10-main-r39@example.com`（`reset39pass4`）/ `ph10-main-q45@example.com`（`q45newpass1`、`dir:g1:b0`）/ `ph10-main-q46@example.com`（`password123`、`dir:g1:b1`、13:29 のリセットメール未使用）
- **agent-browser**: 全 session を閉じた。前任と今回の session 名は `ph10-*`。**実走中に Chrome が 3 回作り直され Cookie が消えた**（原因不明、再ログインで続行）。ref は直前の snapshot でのみ有効、`eval` は `batch` に入れない、React の controlled input は native setter + `input` イベント
- **ai-client**: `apps/web/.ai-client.json` = `ph10-ai-main` の `Claude Test`（有効）。複製 `scratchpad/aic39`（loopback 8766、`ph10-main-r39` の失効済み接続）/ `scratchpad/aicB`（8767、`ph10-settings-main` の `PH10 Settings Seed`）
- **operator**: `node scripts/operator.ts <entry> --locator <dir:g1:bN | userId>`。`list-poisoned-jobs` / `list-quarantined-events` は現在すべて空
- **確認の要点**: (1) §1 の 13 件の修正（dom テスト + dev / preview の再走）、(2) 監査の処置（`adapterImports` / `storageWriters` / `alarm.integration` の赤→緑の意味）、(3) docs から外した issue 番号の扱い（§5 と Manager 判断 1）


### 7. Manager 判断後の追補（2026-09-11T14:05〜14:08+09:00、最終 HEAD `c1f3366`）

- Manager 判断（14:03 受領）: (1) issue の起票はユーザー確認のうえ Manager が行う。docs から番号を外した対応は据え置き。(2) 削除済み出典メモの文言は requirements 4.1・scenario・spec/pages・手順書に合わせて「削除済みのメモ」に揃える（design は Manager が `c2d36f0` で修正）。(3) `CLAUDE.md` の `AiTokenCodec` 追加は承認
- **`c1f3366`** `fix(web): a trashed source memo reads 削除済みのメモ`: `apps/web/app/components/knowledge/OriginRow/index.tsx`（表示と JSDoc）と `knowledge/__tests__/originRow.dom.test.tsx`。grep（`apps/web/app` / `packages/core/src` / `docs` / `README.md`）で「削除されたメモ」の残りは 0。spec 側の残りは `spec/mock.html:920` の 1 箇所（spec の変更は Manager）と、意味の違う「完全に削除されたメモ」（`spec/domains/knowledge.md:289`、`spec/adr/003`。ハードデリート後に表示を残さない、という文脈）
- 検証: `pnpm typecheck && pnpm lint:fix && pnpm format && pnpm test` を 14:05:58〜14:06:20 に実行し、exit 0（lint:fix / format は差分なし、warning 3 は既存、unit+dom 129 files / 1,598 passed + 2 skipped、integration 32 files / 191 passed）。コミット後の `c1f3366` で `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` をもう 1 回、14:07:22〜14:07:43 に実行し、同じ件数で exit 0。**`7446b5e` の 2 回と合わせ、最終 HEAD `c1f3366` では 2 回連続で成功**（14:06 は作業ツリー = `c1f3366` の内容、14:07 はコミット後）
- dev の確認（14:06:52、session `ph10-word`、`ph10-ai-main`）: 出典メモ `蒸留器の温度管理は 78 度前後が目安` を AI の `delete` でゴミ箱へ入れる → P-08 `蒸留器の温度設定まとめ` の「元になったメモ」が `2026/09/11 12:06 | 削除済みのメモ`（`div.fog-origin-row.deleted[aria-disabled=true]`、リンク 0、クリックしても URL 不変）。P-07 `自作蒸留器` の関連メモも「削除済みのメモ」。ゴミ箱から復元して本文とリンクに戻ることも確認（リンク 1）
- 引き継ぎ状態は §6 のまま（dev :3000 は起動中、`git status` clean、agent-browser の session は全部閉じた）。**コード変更はここで止める**

### 8. 差し戻し対応（reviews/PH-10.md §6 D-1〜D-6、2026-09-11T14:50〜15:04+09:00、最終 HEAD `f47777c`）

| ID | 処置 | コミット |
|---|---|---|
| D-1（重大） | **テストを足した**。`memoRepository` の文の組み立てを `timelinePageStatement` / `pivotStatement` として export し、`stores/__tests__/memoTimelinePlan.integration.test.ts`（4 件）がリポジトリの実行する文そのものの `EXPLAIN QUERY PLAN` を固定する: カーソル無しの 1 ページ目は `memos_timeline_idx` の順序どおりの走査（LIMIT で止まる、TEMP B-TREE なし、表の SCAN なし。`planAssertions` に `expectOrderedIndexWalk` を追加）、カーソル後は older / newer × inclusive × keyword の全 8 形が `memos_timeline_idx` の SEARCH、日付アンカーの 2 文は `posted_at` の範囲 SEARCH、メモアンカーはキー SEARCH。keyword（`body LIKE`）は spec/database のとおり同じインデックスの範囲内で評価される（同じ plan で固定）。**測って分かった欠陥を直した**: カーソル条件が `posted_at < ? OR (posted_at = ? AND id < ?)` で、SQLite はこれを最新行からの走査にしていた（深いページほど読む行が増える）。spec/database が定める `(posted_at, id)` の行値比較に変えて SEARCH になった。OR 形は負の対照としてテストに残した。意味は同じで、memo の integration 37 件と dev のページング・日付ジャンプ・絞り込み（60 件・重複なし、45 件）で確認。docs の文はテストの実名に合わせた | `eb1a084` / `f47777c` |
| D-2（重大） | `docs/test.md` の該当文を実際の経路に書き直した: route の `paramsSchema`（1〜200 文字、`validateInput` 経由で render の server function へ）→ 何も指さない id は streamed leaf の `notFound` 分岐。leaf の分岐は新しい `knowledge/__tests__/knowledgeFeeds.dom.test.tsx`（3 件: `DocumentFeed` / `TopicDetailFeed` の notFound → 「〜が見つかりません」+ トピック一覧へ、他の失敗は route の error boundary へ通す）で固定。`validator.test.ts` は `validateInput` 自体を固定し、各 route の schema は固定しない。URL から leaf までは manual（document TC-30 / TC-31）と限界を書いた | `f47777c` |
| D-3 | DLQ に入るまでの失敗は 4 回（最初の配信 + `max_retries = 3`） | `f47777c` |
| D-4 | backend example の木から `scripts/` を外し、`apps/web/scripts/`（`app/` の隣）として本文に書いた | `f47777c` |
| D-5 | 命名例を実在する `rosterGrep.test.ts` に。Aim の「event-type roster」も実在の `jobKinds.test.ts` / `rosterGrep.test.ts`（delivery の roster）と `EventId` に直した | `f47777c` |
| D-6 | `save` の抜粋を実コード（`user.updatedAt.getTime()`、`occConflict()`）に合わせ、`occConflict()` の 1 文を足した | `f47777c` |

**同種の誤りの機械的な突き合わせ**（`scratchpad/ph10c/docrefs.py` / `docrefs_strict.py`）:
- 対象: `docs/*.md`・`README.md`・`CLAUDE.md` のコードブロック外のバッククォート内の語
- パス: `/` を含む語と拡張子つきの語を `git ls-files`（新規 2 ファイルは `git add -N` で含めた）のファイル・ディレクトリに完全一致または末尾一致させた（strict 版は basename だけの一致を認めない）。残ったのは URL のルート（`__diagnostics/…`、`__dev/sso/…`、`_serverFn/`）、`Asia/Tokyo`、`lint/plugin`（biome のルール名）、「無い」と述べている `adapters/d1/`、git-ignored の生成物（`wrangler.*.toml` / `.wrangler/deploy/config.json`）、DOM の短縮名 30 件。**DOM の短縮名は `components/<dir>/__tests__/<name>.dom.test.tsx` に、integration の短縮名は `find` で全件実在を確認した**
- シンボル: camelCase / PascalCase の語と `name()` の形を、追跡しているソース（ts / tsx / toml / json / css …、docs と spec を除く）の識別子の集合と突き合わせた。残ったのは例示（`FooId` / `BarId` / `fooProcedure` / `findPage` / 「足さない」と書いた `generate()` / 「提供しない」と書いた `useServerAction`）、外部ライブラリの API（`createCompositeComponent` / `CompositeComponent` は `@tanstack/react-start/rsc` の再 export 先に実在、`cloneElement`、RSC の低レベル API、vitest の `testTimeout`）、テストファイルの短縮名、`CLAUDE.md` の `leaseUntil`（列 `lease_until` の camelCase 表記。コードには同名の識別子が無いが概念名として使っている。`CLAUDE.md` は Manager の範囲なので変えていない）
- 結果: D-1〜D-6 以外に、存在しないファイル・テスト・シンボルを名指す文は見つからなかった

**検証**: `pnpm typecheck && pnpm lint:fix && pnpm format && pnpm test`（15:01:06〜15:01:28、exit 0、差分なし）→ コミット後の `f47777c` で `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test` を 2 回連続（15:02:26〜15:02:47、15:02:47〜15:03:08）。どちらも unit+dom **130 files / 1,601 passed + 2 skipped**、integration **33 files / 195 passed**、lint warning 3 は既存。`docs/test.md` の件数（DOM 30 ファイル、integration 33 / 195）と一致

**受け入れ済み項目への影響**: `eb1a084` は R-TL-02 / R-TL-03（タイムラインのページングと日付ジャンプ）の読み取り文を変えた。結果の行と順序は同じ（行値比較は OR 形と同値）で、memo の integration と dev で確認。完了条件は不変

**コード変更はここで止める**。dev は :3000 で起動したまま（`eb1a084` の DO コードも HMR で反映済み、上の dev 確認はその後）。`git status` clean

### 9. issue 番号の反映（2026-09-11T15:10+09:00、HEAD `fef0e3b`、docs のみ）

- Manager がユーザーの承認を得て起票した #3〜#21（GitHub で 19 件の実在を確認）を docs に戻した。1 コミット `fef0e3b`（`docs/runtime_cloudflare.md` / `README.md`）
- `docs/runtime_cloudflare.md`: marker 定義を「**None (#N)** — issue #N が限界を追跡。番号の無い None は設計どおりで理由を書く」に、冒頭の方針を「限界は issue で追跡し、この文書は事実と issue 番号だけを持つ」に戻した。本文の該当節に「tracked in [#N](https://github.com/tuanemuy/fog/issues/N)」: #3（冒頭の marker 規則、2 章の source entry、3 章、4.2 の Reality / コマンド例 / 本文、4.5）、#4（1 章、2 章 Pulumi の 2 箇所）、#5（8.6、8.7）、#6（11.3）、#7（7.3）、#8（8.8、9.4、12.4 の表）、#21（13 章 row 21）。§14 Known limits を「Limit / Described in / Tracked in」の 3 列にし、#21 の行（bucket 単位の RPC 障害をローカルで起こせない、manual-tests account TC-47）を足した
- `README.md`: 「request Worker cannot be deployed today」と「`pnpm start` does not boot」の 2 箇所に #3
- `docs/test.md`: #17（`sagaRecovery.md` の未固定 19 行）/ #21 に当たる限界の記述は無いので付けていない（Limits 節にある `sagaRecovery.md` row 26 は運用値でテスト不能という別の事項）。#9〜#16・#18〜#20 は docs に対応する記述が無い
- `pnpm lint:fix && pnpm format` は差分なし（warning 3 は既存）。docs 内のパス突き合わせ（`docrefs_strict.py`）も新たな不一致なし。リンクの番号は 3 / 4 / 5 / 6 / 7 / 8 / 21 だけで、すべて実在
- コード変更は止めたまま。`git status` clean

### 0. 進捗ログ

- 12:01 引き継ぎ。残っていた vite dev（07:47 起動、workerd 無し）を停止し、`pnpm dev` を起動（ログ `scratchpad/ph10c/dev.log`）。`.wrangler/state` は 07:47 に作り直した状態のまま（`.dev.vars` は単一変数の鍵 6 + `MAIL_DEV_SINK` / `SSO_DEV_STUB` / `OPERATOR_TOKEN` / `MAIL_PROVIDER_API_KEY`）
- 前任の補助エージェントの結果ファイルが `.spec-implement/reviews/PH-10-manual/{account,timeline,document}.md` に残っていた（TC ごとの判定あり）。これを根拠として引き継ぎ、残り（保留・未実施・要判断）を再実行する
- 12:05 補助 2 本を起動: search.md（session `ph10-search`）、trash.md（`ph10-trash`）。結果は `reviews/PH-10-manual/{search,trash}.md`。Implementer は ai.md → settings.md → 保留ケースの順
- 12:19 **ai.md 完了: 合格 23 / 23**（`reviews/PH-10-manual/ai.md`。ユーザー `ph10-ai-main@example.com`、クライアント `Claude Test`。MCP `tools/call` を主に REST を併用。依頼文からツールを選ぶ LLM の振る舞いは実 LLM アプリが無いため対象外）
- 12:05〜 account TC-39 を新しいユーザー `ph10-main-r39@example.com` で再走中（前任の r5 は 2 回目のリセットの結果が未記録のため）。前提のリセット 12:05、接続 `R39 Client 1` 12:06、パスワード変更後も active（12:06）。`scratchpad/aic39/`（ai-client の複製、状態ファイル分離、loopback 8766）
- 12:21 **search.md 完了（補助）: 合格 23 / 23**（`reviews/PH-10-manual/search.md`）。観察の「選択中のトピックチップが区別できない / `aria-current="page"` が 2 つ」は不具合として `c844897` で修正（`activeOptions exact` + CSS を `page` に。dom テストはルーター位置を `/search?…` に）。dev で `すべて` / 選択チップだけが `page` + 塗りになることを確認
- 12:22 account TC-39: 2 回目のリセット完走で `R39 Client 1` が失効（`call` 401、`refresh` 400 `invalid_grant`）。`R39 Client 2` を 12:22 に作成。3 回目のリセットは 12:36 以降
- 12:25 document の要判断 6 件を判定。TC-20〜23 はデザインのエラー状態との差 → `87a1f46` で修正して再走合格。TC-06（完了と削除が同じメニュー）と TC-11（手順書の Markdown 期待）は Manager に判断を依頼（SendMessage）
- 12:29 Manager 判断受領: (1)(2) 承認、(A) 完了ボタンをヘッダーへ（`b7deeb4`、dom テストで「メニューに完了が無い」「ヘッダーのボタンで完了 / 解除」を固定。dev で確認: ヘッダーに「完了にする / 完了を解除」、メニューは 編集 / 削除）、(B) TC-11 は手順書を Manager が改訂（`d31f2eb`）→ 再判定で合格（`ph10-document-main` の `読書メモまとめ` が h2 / p / h1、元になったメモ・所属トピック導線あり）。document TC-06 は `b7deeb4` の後に再走して合格
- 12:31〜12:33 timeline TC-09（AI 編集の実行者名）/ TC-29 の AI 部分 / TC-21（再確認）合格。document TC-14 の AI 部分も `ph10-document-main` で直接確認（P-10 r8 `PH10 Document AI · AI による追記`）
- 12:40 **trash.md（補助）: 合格 21 / 不合格 1（TC-21）/ 保留 3（TC-13 / 23 / 24、sqlite 要）**。TC-21 と観察 4 件、自分で見つけた dialog の位置・危険ボタンの色を修正（`ce6501a` / `c734ddb` / `1210ec5`、dom テスト付き）。TC-21 / TC-22 は修正後に再走して合格
- 12:34〜 settings.md 実走中（`ph10-settings-main` / `ph10-settings-empty`）: TC-01 / 02 / 07 / 08 / 09 / 10 / 11 合格。TC-03〜06 は多日のメモを sqlite で作ってから
- 12:54〜12:56 **sqlite 窓 R1**（dev 停止 → `scratchpad/ph10c/sqlbak/` にバックアップ → timeline の多日化 / settings のメモ 2 件を別日へ / trash TC-13・23・24 の期限 / TC-47 の終端モード行と poison 行のシード → `wrangler.state.toml` の producer binding を一時的にコメントアウトして起動）。TC-45: `ph10-main-q45@example.com`（`dir:g1:b0`）でリセット依頼 → relay の publish が `TypeError` で 5 回失敗 → 約 30 秒で `quarantined`（本物の publish 失敗）
- 12:56 **R2**（binding を元に戻して再起動、`git status` clean）: TC-45 requeue → 30 秒以内に `[dev-mail]` 到着・リンクで再設定完了。TC-47 のシード行の list / requeue、trash TC-13 / 23 / 24、timeline TC-03 / 07 / 24 / 08（過去日付）、settings TC-03〜06 / 12 を実走して合格。**timeline 37/37・settings 12/12**
- 観察: agent-browser のセッションが 3 回、Cookie ごと初期化された（Chrome が作り直された。12:43 / 12:47 / 13:00 頃。原因不明で、再ログインすれば続行できる）
- 13:06〜13:11 **R3**（`MAIL_DEV_SINK` を不正値）で TC-46: consumer 失敗 ×4 → DLQ → `dlq { outcome: 'failed' }` で ack、発行元の行は `published` のまま。手動の DLQ 再駆動は無いので手順 6 は実行不能（#83）。**R4**（`.dev.vars` を戻す、`git status` clean）。trash TC-24 手順 5 を実施。**trash 25/25、account 45 合格 + TC-47 部分 + TC-46 手順 6 実行不能**
- 13:13〜13:15 **preview**（`pnpm build` → `pnpm preview` :4173、dev 停止中）: SSR ストリーミング経路 — 保護 9 画面（`/`、`/topics`、`/topics/:id`、`/search?q=…`、`/trash`、`/settings`、`/documents/:id`、`/documents/:id/history`、`/memos/:id/history`）を cookie 付き `curl` で取り、`<main>` に本文が SSR 済み（スケルトンなし、TC-21 のメモも `&lt;script&gt;` のエスケープ済み）。未ログインは 307 → `/login?redirect=…`。ブラウザで投稿（楽観 UI）、検索チップの `aria-current`、P-07 ヘッダーの完了ボタン、P-09 のタイトル空エラー、ゴミ箱の復元、エクスポート（`fog-export-20260911.zip`、16 ファイル）、AI REST（`recent_memos` 200）を確認。OAuth メタデータは `APP_URL`（:3000）を指す（PH-07 で受け入れ済みの限界）
- 13:15〜13:28 監査の軽微 4 件を処置: presentation の adapter 直参照 2 件を解消（`425f36b`、`adapterImports.test.ts` で固定）、`finalize-withdrawal` の生 SQL を UoW の口へ（`ebbac54`）、alarm の 2 pass 独立上限と lease 再武装を integration で固定 + storage 書き込みが adapters の外に無いことを scan で固定（`f14fcd8`）。docs / コードコメント / テンプレートの存在しない issue 番号・`.thread/` 参照を削除（`58705f2` / `6dd0fd4`。このリポジトリの issue は 0 件、PR 2 件のみ）。`pnpm start` を `f14fcd8` で再実行し README を更新
- 13:29 Manager の手順書改訂（`acc356f`）で account TC-46 を再判定 → 合格（R3 の観測 + 窓明けの再依頼でメール到着）。**account 46 合格 + TC-47 部分実施**
