# Inventory — usecase

生成元: spec/usecases/（最終同期: 2026-08-01）

| ID | 要素 | 定義場所 | 実装されるべき振る舞いの要点 |
|----|------|---------|------------------------------|
| UC-export-001 | exportAllData | spec/usecases/export.md#exportAllData ★（人間 UI 専用） | 全データ（完了済みトピック含む・最新リビジョンのみ・ゴミ箱除外）を Markdown ツリーの zip として同期生成し `filename`/`contentType`/`data` を返す。**読み出しは Durable Object 内の1回の `transactionSync`、レンダリングと zip はリクエストを受ける側**で行う。**1回で返せる総バイト数に上限があり、超過は拒否する**。人間 UI 専用で AI 側に配線しない。timezone 不正は BusinessRuleError(InvalidTimezone)、OrphanDocument/InvalidArchivePath/DuplicateArchivePath は防衛的 BusinessRuleError、DB 障害/zip 失敗は SystemError。データ 0 件はマニフェストのみの zip を正常応答 |
| UC-identity-001 | registerWithPassword | spec/usecases/identity.md#registerWithPassword | メール+パスワードで登録し userId を返す。ハッシュは UoW 外。**認証情報側でメールの予約を取り、勝った場合だけユーザー単位設定側の UoW 内で User.registerWithPassword→UserSettingsRepository.insert し、最後に予約を確定して検証材料を記録する**。2つの物理境界をまたぐので単一トランザクションに収まらない（観測できるのは「中間状態のあいだは登録もログインもできない」「前進不能は一様な終端に落ちる」の2点のみ）。メール重複（SSO 含む・レース含む）は ConflictError("EMAIL_ALREADY_REGISTERED")、形式不正/弱パスワードは BusinessRuleError(InvalidEmail/PasswordTooWeak) |
| UC-identity-002 | registerOrLoginWithSso | spec/usecases/identity.md#registerOrLoginWithSso | 検証済み IdP 主体情報で findBySsoIdentity 既存なら isNewUser:false でログイン、不在なら **SSO 主体とメールの予約を2本取ってから** User.registerWithSso→insert（クレデンシャル要約は sso / email の2件）。既存アカウントとメール一致は ConflictError("EMAIL_ALREADY_REGISTERED")（自動リンクしない）、初回サインインレースは ConflictError("SSO_IDENTITY_ALREADY_REGISTERED")、未対応プロバイダは BusinessRuleError(UnsupportedSsoProvider)。中間状態と終端の保証は registerWithPassword と同じ |
| UC-identity-003 | loginWithPassword | spec/usecases/identity.md#loginWithPassword | 認証情報側の findByEmail でクレデンシャルを解決し、**不在でもダミーの検証材料で同じ計算量を通す**。照合は Durable Object の外で行う。照合後に**到達可能性を検査する**（解決した credentialId が対象ユーザーのクレデンシャル集合に active な要素として存在すること）。VO 生成失敗・未登録・検証材料なし・不一致・到達不能はすべて同一の ValidationError("INVALID_CREDENTIALS") に統一し原因を明かさない |
| UC-identity-004 | logout | spec/usecases/identity.md#logout | ドメイン操作・永続化なしの入口（void）。セッション破棄は presentation 層の責務。ユースケースを設けない実装判断も許容 |
| UC-identity-005 | requestPasswordReset | spec/usecases/identity.md#requestPasswordReset | **登録済み / 未登録 / SSO 専用 / スロットル中の4ケースで処理経路を完全に一致させる**（同じトランザクションで送信ジョブ行を1行書き、同じ起床を張り、同じ応答を返す。違うのは行の中身だけ）。送る側の判定は「クレデンシャル行の有無」ではなく「パスワードの検証材料の有無」。トークン発行は PasswordResetTokenPort.issue(credentialId, now) でそのクレデンシャル宛の未使用トークンを全置換する。**ジョブ行に載せるのはトークンの識別子だけ**。同一メールへの連打は1本のジョブ行に収束する。メール形式不正は BusinessRuleError(InvalidEmail)、基盤障害は SystemError |
| UC-identity-006 | executePasswordReset | spec/usecases/identity.md#executePasswordReset | 新パスワード検証→verifyAndConsume でトークン消費（無効/期限切れ/使用済みは区別せず ValidationError("RESET_TOKEN_INVALID")）→**認証情報側で検証材料を差し替え未使用トークンを全無効化**→**ユーザー単位設定側でリセット完了を契機とする AI クライアント接続の自動失効を適用**。検証材料を持たない場合は防衛的 BusinessRuleError(PasswordNotSupported)。2つの物理境界をまたぎ、中間状態のあいだは旧新どちらのパスワードでもログインできない |
| UC-identity-007 | changePassword | spec/usecases/identity.md#changePassword | 現在パスワード照合（不一致は ValidationError("CURRENT_PASSWORD_MISMATCH")。**照合の失敗はログイン失敗と同じ回数カウンタを進める**）後、認証情報側で検証材料を差し替えて未使用トークンを全無効化し、ユーザー単位設定側でセッションの世代を進める。検証材料なしは BusinessRuleError(PasswordNotSupported)、弱パスワードは BusinessRuleError(PasswordTooWeak)。**AI クライアント接続は失効しない** |
| UC-identity-008 | approveAiClientAuthorization | spec/usecases/identity.md#approveAiClientAuthorization | OAuth 認可承認の事実として AiClientConnection.create→UoW 内 insert し connectionId を返す。**作成時点のリセット世代を接続に写す**（リセット完了時の自動失効の射程を決める材料）。クライアント名不正（空/100文字超）は BusinessRuleError。プロトコル処理はアダプター責務 |
| UC-identity-009 | denyAiClientAuthorization | spec/usecases/identity.md#denyAiClientAuthorization | 認可拒否時に接続エンティティを一切作らない（void・永続化なし）。拒否応答はアダプター責務。ユースケースを設けない実装判断も許容 |
| UC-identity-010 | listAiClientConnections | spec/usecases/identity.md#listAiClientConnections | listByUserId（connectedAt 降順）で接続一覧を AiClientConnectionView（connectionId/clientName/status/connectedAt/lastUsedAt/revokedAt）に射影して返す。失効済みも返し、0 件は空配列 |
| UC-identity-011 | revokeAiClientConnection | spec/usecases/identity.md#revokeAiClientConnection | UoW 内で findById（自分の Durable Object の中だけを引く。不在は NotFoundError("CONNECTION_NOT_FOUND")。到達可能性による構造的保証で `connection.userId` 照合は不要）→既に revoked なら冪等に正常終了→AiClientConnection.revoke→save。**失効の権威は `status` であり、次のリクエストの DO 内ガードが直読みする**。OCC は ConflictError |
| UC-identity-012 | changeTrashRetentionDays | spec/usecases/identity.md#changeTrashRetentionDays | TrashRetentionDays.create（1 以上の整数。違反は BusinessRuleError(InvalidTrashRetentionDays)）→UoW 内で UserSettingsRepository.find→User.changeTrashRetentionDays→save→**同じトランザクションでゴミ箱内全項目の `purgeAfter` を再計算し、`purge-trash` の起床を張り直す**（遡及適用はこれで成立する）。ユーザー不在は NotFoundError、OCC は ConflictError |
| UC-identity-013 | getCurrentUser | spec/usecases/identity.md#getCurrentUser | UserSettingsRepository.find で userId/**credentials（`{ credentialId, kind, label }` の3つ組の集合）**/trashRetentionDays を返し、email は認証情報側の原本を本人の自己参照として1件だけ復号して取得する。**解除操作を出してよいのは `kind: "sso"` の要素だけ**。検証材料・SSO 主体 ID（`provider` / `providerSubject`）は含めない。不在は NotFoundError("USER_NOT_FOUND") |
| UC-knowledge-001 | createTopic | spec/usecases/knowledge.md#createTopic | Topic.create で ActiveTopic 生成→UoW 内 insert しビューを返す。トピックはインデックスエントリを持たないので projection の更新は無い。名前・説明の規則違反は BusinessRuleError（EmptyTopicName/TopicNameMultiline/TopicNameTooLong/EmptyTopicDescription/TopicDescriptionTooLong）。人間 UI/MCP 両公開 |
| UC-knowledge-002 | updateTopic | spec/usecases/knowledge.md#updateTopic | 入力に応じ rename/changeDescription/archive/unarchive を合成適用（現状態と同じ archived 指定は冪等 no-op）→save。トピック名は projection に複製せず join で解決するので、リネームは検索結果へ即座に反映される。全フィールド省略は presentation で ValidationError。不在・ゴミ箱内は NotFoundError、OCC は ConflictError("OPTIMISTIC_LOCK_FAILURE") |
| UC-knowledge-003 | listTopics | spec/usecases/knowledge.md#listTopics | listByUser + listActiveByTopics（1 クエリ・N+1 禁止）でトピックと配下 active ドキュメントを返す。AI presentation は includeArchived:true 固定で渡す。0 件は空配列 |
| UC-knowledge-004 | getTopic | spec/usecases/knowledge.md#getTopic ★ | 人間 UI 専用。findById（Live のみ。null は NotFoundError）+配下 active ドキュメント+出典リンク一括逆引き+listByIdsIncludingTrashed で関連メモ（deleted フラグ付き）を返す。ハードデリート済みメモは現れない（ADR-003）。AI に配線しない |
| UC-knowledge-005 | trashTopic | spec/usecases/knowledge.md#trashTopic | UoW 内で findById（Live のみ）→listActiveByTopic→`purgeAfter` を算出して TopicTrashService.trashTopicSet でセット削除（各ドキュメント trashedWith=topic.id、トピックと配下で同一の purgeAfter）→save 全件→同じ `transactionSync` で配下ドキュメントのエントリを projection から除去。個別削除済みはセットに含めない。不在・ゴミ箱内は NotFoundError、OCC は ConflictError。AI `delete` の type:"topic" ディスパッチ先 |
| UC-knowledge-006 | createDocument | spec/usecases/knowledge.md#createDocument | トピック実在検証（OCC トークン付き）+出典メモ全件を listActiveByIds で検証（1 件でも欠ければ NotFoundError で全体失敗）→changeReason 省略時「作成」補完→Document.create（リビジョン#1+SourceLink 群）→作成先トピックを touch save して trashTopic/archive とレース排除→insert 群→同じ `transactionSync` で当該ドキュメントと出典メモのエントリを作り直す。touch 競合は ConflictError |
| UC-knowledge-007 | editDocument | spec/usecases/knowledge.md#editDocument ★ | 人間 UI 専用（actor は UserActor 限定で型排除）。expectedVersion 不一致は書き込まず正常応答 result:"conflict"+ConflictView（現在値・currentVersion・最新リビジョンの誰が/なぜ）。同一内容は "unchanged"。変更あれば save+insertRevision+同一トランザクションでの projection 更新で "saved"。changeReason 省略時「手動編集」補完。不在は NotFoundError、レース/一意制約は ConflictError |
| UC-knowledge-008 | editDocumentByAi | spec/usecases/knowledge.md#editDocumentByAi | MCP `edit_document`。mode:"patch"（各 hunk の oldText がちょうど 1 箇所一致。0 箇所は BusinessRuleError(PatchTargetNotFound)、2 箇所以上は PatchTargetAmbiguous、部分適用しない）/"replaceAll" を Document.edit に集約。changeReason 必須（省略は ValidationError・補完しない）。タイトルは変更しない。ゴミ箱内は NotFoundError（存在事実を漏らさない）、同一内容は changed:false |
| UC-knowledge-009 | rollbackDocument | spec/usecases/knowledge.md#rollbackDocument ★ | 人間 UI 専用（UserActor 限定）。過去リビジョンの内容で新リビジョンを積む（履歴は削除しない）。同一トランザクションで当該ドキュメントのエントリを作り直す。changeReason 省略時「リビジョン{n}の内容に戻す」補完。同一内容は changed:false。ドキュメント/リビジョン不在は NotFoundError、RevisionDocumentMismatch/InvalidRevisionNumber は BusinessRuleError、レースは ConflictError |
| UC-knowledge-010 | trashDocument | spec/usecases/knowledge.md#trashDocument | UoW 内で findById（active のみ）→`UserSettingsRepository.find()` の trashRetentionDays から `purgeAfter` を算出→Document.softDelete(trashedWith:null, purgeAfter)→save→同じ `transactionSync` で当該ドキュメントのエントリを除去し出典メモのエントリを作り直す。不在・ゴミ箱内は NotFoundError、OCC は ConflictError。AI `delete` の type:"document" ディスパッチ先 |
| UC-knowledge-011 | getDocument | spec/usecases/knowledge.md#getDocument | findById（active のみ）で全文+latestRevision+version（編集開始時 expectedVersion 用）を返す。ゴミ箱内は NotFoundError（AI からゴミ箱不可視）。出典メモ一覧は含めない。AI `get` の type:"document" ディスパッチ先 |
| UC-knowledge-012 | listDocumentRevisions | spec/usecases/knowledge.md#listDocumentRevisions ★ | 人間 UI 専用。findByIdIncludingTrashed で存在確認（ゴミ箱内も閲覧可。null は NotFoundError）→listRevisions を revisionNumber 昇順でメタデータのみ（actor 射影/changeReason/createdAt。全文は含めない）返す |
| UC-knowledge-013 | diffDocumentRevisions | spec/usecases/knowledge.md#diffDocumentRevisions ★ | 人間 UI 専用。二点の findRevision で全文スナップショット+メタデータを返すだけ（差分計算は presentation 責務）。同一番号指定は ValidationError、いずれか不在は NotFoundError、1 未満・非整数は BusinessRuleError(InvalidRevisionNumber) |
| UC-knowledge-014 | listDocumentSourceMemos | spec/usecases/knowledge.md#listDocumentSourceMemos ★ | 人間 UI 専用。findByIdIncludingTrashed で存在確認→出典リンク→listByIdsIncludingTrashed（1 クエリ）で snippet/postedAt/deleted/linkedAt を返す。ソフトデリート済みは deleted:true で残し、ハードデリート済みは現れない（ADR-003）。不在は NotFoundError |
| UC-knowledge-015 | listDocumentsReferencingMemo | spec/usecases/knowledge.md#listDocumentsReferencingMemo ★ | 人間 UI 専用。メモの存在確認（IncludingTrashed）→listSourceLinksByMemo→listByIdsIncludingTrashed で参照元ドキュメント（title/topicId/deleted/linkedAt）を返す。ソフトデリート済みは deleted:true、ハードデリート済みは現れない。メモ不在は NotFoundError |
| UC-memo-001 | postMemo | spec/usecases/memo.md#postMemo ★ | 人間 UI 専用（UserActor 限定）。Memo.create→UoW 内 insert+insertRevision（初版必須）+同じ `transactionSync` での projection 更新（当該メモのエントリを作る）で MemoView を返す。postedAt は自動付与。空/10,000 文字超は BusinessRuleError(EmptyBody/BodyTooLong) |
| UC-memo-002 | getTimeline | spec/usecases/memo.md#getTimeline ★ | カーソルページング（direction 両方向・limit 1〜100・keyword 絞り込み込み）で active メモを取得し、listSourceLinksByMemos+listByIdsIncludingTrashed の一括逆引き（N+1 禁止）で sourceDocuments（isTrashed 付き）を付与。0 件は items:[]。cursor デコード不能/limit 範囲外/newer で cursor null は ValidationError |
| UC-memo-003 | jumpToDate | spec/usecases/memo.md#jumpToDate ★ | findTimelineAround({kind:"date"}) で指定日前後の初期ページ+olderCursor/newerCursor を返す。指定日にメモがなければ最も近い位置が返る。0 件は空+両カーソル null。date 不正は ValidationError |
| UC-memo-004 | showMemoInTimeline | spec/usecases/memo.md#showMemoInTimeline ★ | findByIdIncludingTrashed で対象状態を判定し targetState "found"/"trashed"/"notFound" を返す（trashed/notFound はエラーにせず空結果+案内用状態）。found は findTimelineAround({kind:"memo"}) で対象を含む前後ページ+出典導線を返す |
| UC-memo-005 | editMemo | spec/usecases/memo.md#editMemo ★ | 人間 UI 専用（UserActor 限定）。expectedVersion 不一致は書き込まず result:"conflict"+ConflictView（currentBody/currentVersion/latestRevision）で正常応答。同一本文は "unchanged"（リビジョンを積まない）。変更あれば save+insertRevision+同一トランザクションで当該メモのエントリを作り直して "saved"。不在・trashed は NotFoundError、判定後レースは ConflictError |
| UC-memo-006 | listMemoRevisions | spec/usecases/memo.md#listMemoRevisions ★ | findByIdIncludingTrashed で存在確認（ゴミ箱内メモも可）→listRevisions を昇順で「誰が・いつ」のみ（本文なし・ActorView 射影）返す。不在（ハードデリート含む）は NotFoundError |
| UC-memo-007 | diffMemoRevisions | spec/usecases/memo.md#diffMemoRevisions ★ | 二点の findRevision で全文スナップショット+メタデータ（RevisionView）を返すだけ（差分計算は presentation 責務）。同一番号は ValidationError、不在は NotFoundError、1 未満・非整数は BusinessRuleError(InvalidRevisionNumber) |
| UC-memo-008 | rollbackMemo | spec/usecases/memo.md#rollbackMemo ★ | 人間 UI 専用（UserActor 限定）。expectedVersion を受けず UoW 内読み直しに OCC 保存。Memo.rollback で対象リビジョン内容の新リビジョンを積む（同一本文は "unchanged"）→save+insertRevision+同一トランザクションでの projection 更新。メモ/リビジョン不在は NotFoundError、別メモのリビジョンは BusinessRuleError(RevisionMismatch)、OCC は ConflictError |
| UC-memo-009 | softDeleteMemo | spec/usecases/memo.md#softDeleteMemo ★ | UoW 内で findById（active のみ。既に trashed も NotFoundError）→`UserSettingsRepository.find()` の trashRetentionDays から `purgeAfter` を算出→Memo.softDelete(purgeAfter)→save→同じ `transactionSync` で当該メモのエントリを除去し出典先ドキュメントのエントリを作り直す。void。OCC は ConflictError |
| UC-memo-010 | post_memo | spec/usecases/memo.md#post_memo（AI API） | AI がトークン解決の actor（AiClient）でメモ投稿。処理は postMemo と同一（Memo.create→insert+insertRevision+同一トランザクションでの projection 更新）で、リビジョンに AI クライアントが記録される。空/超過は BusinessRuleError でメモは作成されない |
| UC-memo-011 | update_memo | spec/usecases/memo.md#update_memo（AI API） | AI による全文置換のみの編集（パッチ非対応・ADR-006）。expectedVersion を受けず UoW 内で読み直した最新に適用。同一本文は "unchanged"、変更あれば save+insertRevision+同一トランザクションでの projection 更新で "saved"。trashed・不在は区別なく NotFoundError（ゴミ箱の存在事実を漏らさない） |
| UC-memo-012 | recent_memos | spec/usecases/memo.md#recent_memos（AI API） | findTimelinePage を cursor:null・direction:"older"・keyword:null 固定（既定 limit 20）で直近 active メモを返す。ページング・絞り込み・出典導線は公開しない。trashed は返らない。limit 範囲外は ValidationError |
| UC-memo-013 | get | spec/usecases/memo.md#get（AI API） | findById（active のみ）でメモ全文（id/body/postedAt/updatedAt/latestRevisionNumber）を返す。ゴミ箱内・不在は NotFoundError。presentation が {type:"memo"\|"document"} で本ユースケース/getDocument へディスパッチ |
| UC-memo-014 | delete | spec/usecases/memo.md#delete（AI API・メモ対象） | AI によるメモのソフトデリート。処理は softDeleteMemo と同一（findById→purgeAfter 算出→Memo.softDelete→save→同一トランザクションでの projection 更新）。既に trashed は NotFoundError。presentation が {type:"memo"\|"document"\|"topic"} で本/trashDocument/trashTopic へディスパッチ |
| UC-search-001 | search | spec/usecases/search.md#search（検索する） | 人間 UI/AI 共通の単一ユースケース。`userId` は対象 Durable Object の選択に使い `SearchQuery` には渡さない。SearchQuery.create（trim 後非空・500 文字以内・limit 1〜100・cursor の妥当性。違反は BusinessRuleError(EmptyKeyword/KeywordTooLong/InvalidCursor)）→SearchIndexPort.query（到達可能性による境界・ゴミ箱除外・アーカイブ込み・topic 絞り込み・安定順位・カーソルはポート契約）→document 項目の topicName を TopicRepository.listByIds で一括解決（AI 向けには露出しない）→`items` / `count` / `nextCursor` に射影。**ページ番号を受け取る入口を持たない**。未知・ゴミ箱内トピックは NotFoundError(TOPIC_NOT_FOUND)、0 件は空で正常、インデックス障害は SystemError(SearchIndexUnavailable) |
| UC-trash-001 | listTrash | spec/usecases/trash.md#listTrash ★ | TrashQueryPort.listTrashItems（削除日時降順・ページング。**retentionDays 引数を渡さない**）で TrashItemView（kind 直和・excerpt/title/deletedWithTopic/setDocumentIds・trashedAt・**保存済みの `purgeAfter` がそのまま載る expiresAt**）+totalCount を返す。0 件は空配列。page/limit 範囲外は ValidationError |
| UC-trash-002 | restoreMemo | spec/usecases/trash.md#restoreMemo ★ | UoW 内で findByIdIncludingTrashed（trashed でなければ NotFoundError）→Memo.restore（`purgeAfter` は落ちる）→save→同じ `transactionSync` で当該メモのエントリを作り直し、出典先ドキュメントのエントリも作り直す。postedAt 不変で元の位置に戻り、出典側「削除済み」表示が解消される。OCC は ConflictError |
| UC-trash-003 | restoreDocument | spec/usecases/trash.md#restoreDocument ★ | RestorePolicy.decideDocumentRestore の plan で 3 分岐: restoreAlone（復元先トピックを touch してレース排除→Document.restore）/restoreWithTopic（confirmSetRestore なしなら書き込まず確認要求応答。確認済みなら restoreTopicSet+要求対象が skipped なら追加 restore）/selectDestination（destination なしなら選択要求応答。existing は touch、new は Topic.create→moveToTopic→restore）。復元は同一トランザクションで projection を作り直す。再呼び出し時も現況から plan 再判定。不在は NotFoundError、TrashedWithMismatch は BusinessRuleError、touch/OCC 競合は ConflictError |
| UC-trash-004 | restoreTopic | spec/usecases/trash.md#restoreTopic ★ | UoW 内で findByIdIncludingTrashed（trashed でなければ NotFoundError）→listTrashedByTopic→TopicTrashService.restoreTopicSet（trashedWith 一致分のみセット復元・wasArchived に従い active/archived 復帰・個別削除分はゴミ箱に残す）→save 全件→同じ `transactionSync` で復元した全ドキュメントのエントリを作り直す。TrashedWithMismatch は BusinessRuleError、OCC は ConflictError |
| UC-trash-005 | hardDeleteTrashItem | spec/usecases/trash.md#hardDeleteTrashItem ★ | TrashQueryPort.findTrashItem で対象確認（ゴミ箱外は NotFoundError）→HardDeletePolicy.expandTargets で展開（トピックは setDocumentIds を含み個別削除分は含めない）→UoW 内で影響先確定→本体+全リビジョン+出典リンクを消去し、同じ `transactionSync` で当該エントリを projection から除去して影響先のエントリを作り直す。並行で既に消えた対象は no-op 続行。OCC は ConflictError |
| UC-trash-006 | emptyTrash | spec/usecases/trash.md#emptyTrash ★ | 全ゴミ箱項目をページ送りで取得→expandTargets 結果を種別ごとに和集合（重複除去）→各 ID を項目ごとの UoW で hardDeleteTrashItem と同一手順（影響先確定 → OCC トークン付き再取得 → ハードデリート → リンク消去 → projection 更新）で消去。個別失敗（OCC 等）は記録して続行し全体は中断しない。既に消えた対象は no-op。空なら deletedCount:0 を正常応答 |
| UC-trash-007 | pruneExpiredTrashItems | spec/usecases/trash.md#pruneExpiredTrashItems | **自分のユーザー単位 Durable Object の `purge-trash` ジョブから実行される**（cron による全ユーザー横断のバッチは存在しない。外部非公開・`userId` も受け取らない）。起床時にまず保持日数変更に伴う `purgeAfter` の再計算の残件を空にし、次に自 DO の索引から `purgeAfter < now` を `chunkLimit` 件取得→expandTargets→項目ごとの UoW でハードデリート+リンク消去+同一トランザクションでの projection 更新。期限内には一切触れない。個別失敗は記録して続行、OCC/行不在は no-op で冪等、残件は `hasMore` で次の起床へ委ねる |
