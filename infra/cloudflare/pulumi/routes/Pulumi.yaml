name: fog-cf-routes
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
description: |
  Cloudflare custom-domain binding for the fog request Worker. Deploy this
  stack after the state Worker and request Worker.
main: index.ts
