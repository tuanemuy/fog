name: fog-cf-resources
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
description: |
  Cloudflare DNS resources and stage metadata for fog. Durable Object
  namespaces are managed declaratively by the state Worker configuration.
main: index.ts
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
