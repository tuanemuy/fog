# Testing

Tests are classified along two axes: **layer × purpose**. A fast unit layer and a jsdom layer run in Node; an integration layer runs inside a Workers isolate against real SQLite-backed Durable Objects. Keeping the day-to-day loop on the first two keeps it light while the third covers the adapter pitfalls — OCC, transactions, the Alarm, the relay.

## Test layer classification

### Unit (`pnpm test:unit`)

- **Targets**: domain-layer + application-layer logic (the pure parts), plus the pure logic behind `apps/web`'s screens — formatters, schemas, list state, and presentation-layer helpers.
- **Dependencies**: the fakes under `packages/core/src/application/__tests__/` (see "Fake policy"). `Clock` is passed to the usecase as a freestanding `now: Date`; repository-style fakes are intentionally absent (imitating transaction / OCC with an in-memory fake is no substitute for integration). Application-layer logic is not exhaustively covered with fakes; behavior verification is pushed onto integration tests.
- **Aim**: invariants of the domain layer (value objects, entities, `EventId`), the delivery roster (`jobKinds.test.ts` / `rosterGrep.test.ts`: the `jobs.kind` and `event.type` values in the sources are exactly the ones `spec/async/index.md` lists), error-code branching, and the behavior of pure application-layer helpers like the constraint checks in `createDeliveryTuning`, the rotation's pure functions (`judgeCanonicalRow`, `isRetired`, `confirmDeletion`) and the job runner's `failureOutcome`.
- **Speed**: a few to a dozen-or-so milliseconds. `vitest.config.unit.ts` excludes `**/*.integration.test.ts` and `**/*.dom.test.{ts,tsx}`, so nothing here touches a DB or a DOM.
- **Naming**: `**/__tests__/<target>.test.ts` (e.g. `entity.test.ts`, `rosterGrep.test.ts`, `tuning.test.ts`). Repository-wide scans are unit tests too: `lint/banList.test.ts` (the error-class roster), `packages/core/src/adapters/cloudflare/__tests__/rotationCheckpointWriters.test.ts` (the one writer of `rotation_checkpoints`), `packages/core/src/adapters/cloudflare/__tests__/storageWriters.test.ts` (no `INSERT` / `UPDATE` / `DELETE` / `REPLACE` statement outside `packages/core/src/adapters/`; reads, and a statement assembled from pieces, are outside it), `lint/designTokens.test.ts` (`styles/tokens.css` carries exactly the names and values of `spec/design/tokens.md`'s code blocks; nothing under `styles/` has a `var()` fallback or reads an undefined custom property; no raw color, and no raw length, typeface, weight, line-height, radius, shadow or transition outside tokens.md's allowlist, appears in the hand-written CSS under `apps/web/app/styles/` or in the utilities Tailwind generates from `apps/web`'s sources; a projected token is read by its utility name, not as `(--token)`; every token of a class list written in a source generates CSS, so a name reading a scale the tokens do not carry — `p-0`, `min-w-0` — is red rather than silently missing; the two ways a primitive's look could be redefined from outside it are shut, a variant or utility that styles another element outside `components/ui` and a class selector in the hand-written CSS. The mock HTML, a TSX `style` attribute, a class list of a single token, a class name assembled at runtime, and a property a primitive inherits from its wrapper because it does not set it itself are outside it) and `apps/web/app/presentation/__tests__/adapterImports.test.ts` (no module under `apps/web/app` but the entry points — `server.cloudflare.ts` and `worker/cloudflare/` — imports a concrete adapter; the scan reads `from "@repo/core/adapters/…"` text, so a dynamic `import()` or a re-export through another package is outside it).

### DOM (`pnpm test:unit --project dom`)

- **Targets**: the rendering branches of `apps/web`'s components — which of two shapes a branch draws, and where a link leads. Three kinds are drawn: `"use client"` islands, server components that are pure functions of their props, and `async` data-loading leaves, the last by `await`ing the component and rendering the element it returned. Routes, loaders, `renderServerComponent` (Flight), hydration and CSS are **not** drawn: reaching them takes the Flight boundary and yields no branch these three do not already reach. The one exception is `layout/routeBoundaries`, which draws a stub route tree carrying the production frames and boundary components, because which frame a failure or a not-found lands in is decided by the router and exists only as a drawn route tree.
- **Dependencies**: `jsdom`, `@testing-library/react` and its `@testing-library/dom` peer. One `setupFiles` entry, `apps/web/app/components/__tests__/domSetup.ts`, which raises `@testing-library/react`'s `asyncUtilTimeout` from 1 s to 5 s — a ceiling, not a wait, for components that chain two or three server-function round trips through transitions under the full parallel run. `globals: true` is set only so the library registers its `afterEach` cleanup; test bodies import from `vitest` explicitly. A component carrying a `Link` or reading `useRouter` is drawn through the shared harness `apps/web/app/components/__tests__/renderWithRouter.tsx`, which builds a memory-history router with stub routes for the paths under test — each may carry the `staticData` a shell under test reads its header from — and, after the render, feeds every internal `href` on the page back through `router.matchRoutes` so that a `to` with no stub behind it goes red instead of silently building a URL nothing serves.
- **Aim**: "what does this branch draw". Assert **both sides** of a branch — that the dead step is not a link *and* that the live one leads to the expected URL; asserting only the first stays green when every step is drawn dead. An assertion that something is **absent** takes the same string from the present side first (`getByText` finds it there, `queryByText` answers `null` inside the absent row), because a misspelled query answers `null` whether or not the branch works. Queries are accessibility-first (`getByRole` / `getByText`) plus `closest("a")` / `getAttribute("href")`. No snapshots — a diff whose meaning is buried in class names stops claiming that a branch does anything.
- **Norm**: **when you add or change a rendering branch, add the DOM test with it.**
- **Naming**: `apps/web/app/**/__tests__/<target>.dom.test.{ts,tsx}`. The unit project drops that suffix and this project's `include` claims it, rooted at `apps/web/app/`. **A file outside that root runs nowhere** — a correctly named test under `packages/core/` or `apps/web/scripts/` is dropped from the unit project by its suffix and claimed by no project here, so it reports nothing rather than failing. When you start putting DOM tests in another directory, add it to `vitest.config.dom.ts`'s `include` in the same change.
- **What is covered**: the 55 files under `apps/web/app/components/*/__tests__/`, and no more. Nothing enforces the Norm mechanically; it is a review discipline.
  - `aiClients/authorizeSheet` — the P-14 sheet: client and account named, allowed / disallowed operations listed under their labels, approve posts and disables both buttons before following its redirect, deny does the same without claiming to approve, a request that expired between the load and the click is the error at the head of the decision with the page staying, an invalid request draws the error in place of the lists and the buttons.
  - `auth/authForm` — signup failures attributed to their field, a duplicate address turned into a form error offering login with the redirect carried, every login failure collapsed into one message, the SSO provider links carrying origin and redirect with their glyphs — only the configured ones, a name alone for one with no drawing, 登録 wording and no reset entry on signup — an SSO callback failure at the head of the form replaced by the failure of the attempt made on the page, the fields and button disabled in flight, login navigating to the carried target, a resolved value of the wrong shape as a system error that stays, and the signup / login cross links with the requirement on signup only.
  - `auth/passwordReset` — the request form says under the title that the mail goes only to an address with an account, posts and answers with the same toast on the same form whatever the address, and draws a failed request at the head of the form with no toast; the reset form asks for the new password with its requirement, posts token + password and reaches the done screen, offers a new request at the head of the form for a spent / expired link, draws any other failure there without that link, and marks a weak password on its field.
  - `documents/documentActions` — editor / history / delete as header icons outside the sheet with the two links resolving; delete asks first and does nothing on cancel, trashes on confirm and leaves for the topic with a toast; a failure at the head of the sheet keeps the page and retries the confirmed delete; a resolved value of the wrong shape is a system error.
  - `documents/documentEditor` — the save in the header, outside the form yet submitting it; a blank title refused with its message (`aria-invalid`, body kept, nothing posted), the source picker opened in place of its button with the recent memos, a keyword searched on Enter, a candidate added and removed, the picker closed back onto its button, the empty result and the failed search's retry, the picked sources sent before leaving for the new document with a toast, a failure keeping the input and retried from the alert; on edit the sources read-only, the opened version posted, a conflict warned at the head of the sheet and re-submitted on top of the current version, a rejection keeping the draft.
  - `documents/documentFeed` — the document's title as the page's one `h1` under the topic's link with the header naming it `document`, the body typeset with its sources under 「出典」, no sources region without one; `DocumentSkeleton` busy under its loading label, that label the `h1` in read mode with the stand-in update time, neither in edit.
  - `documents/documentRevisionHistory` — the document's title as the `h1` then 履歴, a single revision without selection or rollback, the ascending who · why list diffing against the latest on the first click and base → target on the second with a title change, no fetch when the latest is chosen first, rollback behind a confirmation leaving for the document with a toast, the no-op toast and the rejection under the button, a diff of the wrong shape as a system error fetched again on 再試行.
  - `knowledge/knowledgeFeeds` — the streamed leaves `DocumentFeed` / `TopicDetailFeed` draw their not-found sentence as the page's `h1` with the way back to the topic list for a `notFound` from their loader, let any other failure through to the route's error boundary, and `TopicDetailFeed` draws the head, the documents and the related memos when the topic is there.
  - `knowledge/knowledgeNotFound` — one sentence naming the missing topic or document as a paragraph with no heading, and the link back to the topic list as the only other content.
  - `knowledge/originRow` — a live memo linked to its timeline position ending in the jump mark, a deleted one a non-navigable grayed row naming no snippet; `OriginList` draws nothing for no memos and one item per memo under its label at the heading level asked.
  - `layout/appShell` — the nav lists only destinations the production route tree serves, the memo history route is served without being listed; every screen under `_app` declares its header — the five destinations the lockup titled as the nav names them, the six back pages their `topic` / `memo` / `document` and a served place to return to — and the shell draws it (a hidden `h1` on a destination, the back header under a topic whose English word is a label and no heading of the frame's, the product name for an undeclared route); the current destination marked from the shell's own match on a page no nav item links to; the lockup on the sidebar's link home; the icon menu; the sheet as the scroll container the router's `scrollToTopSelectors` names, on the header's horizontal frame, handed to the screen as its scroll container (the viewport outside the shell); the text column inside it holding the top and side padding and `--content-max` and no bottom padding, which is the screen's; `HeaderActions` in the header and `BottomDock` under the toast region, both outside the sheet and refusing to run without the shell.
  - `layout/authSheet` — the five auth-sheet screens served at their unchanged URLs under `_sheet` and never under `_app`, declaring no header; the session check (`requireSessionBeforeLoad`, whose read is what marks the document no-store) above exactly the two that need a session and above every app-shell screen, held by `_app` and `_sheet/_authenticated` and no other layout; `AuthSheet` as one narrow card centred both ways and padded the same top and bottom, the lockup at its head, the screen's `AuthSheetTitle` after it, no navigation or header, and the toasts outside the card.
  - `layout/navSheet` — shut until the menu opens it, a white card with a handle on the sheet's frame over the overlay, the five destinations with the current one marked, closed by choosing an item (and going there), by a press on the overlay but not inside the card, and by the browser's own close (Escape).
  - `layout/pageHeader` — `BrandLockup` at logo.md's size with its one orange point inside the icon and its own ink; a destination's lockup hidden from `lg` with the title as a hidden `h1`; a back page's back button and English title in the brand face with the point hidden from `lg`, as the `h1` or as a plain label when the sheet holds it; the back button leading to the declared place filled with the route's params, going there without in-app history and back through it when there is one.
  - `layout/routeBoundaries` — the router's defaults (`RouteError`, `NotFound`, `RoutePendingFallback`, `reportRouteError` on catch, not-found answered at the root), an error component declared by `__root`, `_app` and `_sheet` only and all three `AuthSheetRouteError`, a not-found component declared by no route; drawn with those over stub screens: a screen's failure in the app shell's sheet under its header and nav with none of the error's words, 再試行 rerunning the loader into the screen or staying ready when it fails again, a streamed fragment's rejected promise drawn where its skeleton was and streamed afresh on 再試行, a screen's `notFound()` in the sheet with the way back to the timeline; a failure of the root, `_app` or `_sheet` on an auth sheet of its own and the shell back once `_app`'s check passes, a failure of a screen on the auth sheet inside the card, both with the sentence as the page's `h1`; a URL no route serves — also one a layout's prefix matches — as the 404 on the auth sheet, never in the shell.
  - `memoHistory/memoHistoryFeed` — the history list for a memo that exists, 「メモが見つかりません」 as a paragraph with the way back to the timeline for a `notFound`, any other failure through to the route's error boundary.
  - `memoHistory/revisionHistory` — the memo-side counterpart: one revision without selection or rollback, the ascending 履歴 list of when and who, the first click the base and the second the target with the diff drawn, `Sk` lines while the two versions load, a diff of the wrong shape retried on 再試行, rollback behind a confirmation returning to the memo's position with a toast, the same-content toast that stays, a rejection under the button rather than in a toast, and a cancelled confirmation calling nothing.
  - `memoHistory/revisionHistorySkeleton` — one busy status with the row text laid over and nothing to press, the memo's three rows under 履歴 with no `h1` of its own, the document's title line before the list with the loading label carrying the `h1`.
  - `search/searchPanel` — no navigation on a blank keyword, scope kept across keywords, chips with the archived badge and only the chosen chip marked current, result rows with kind / highlighted snippet / time / topic / destination, a full-width original marked from a half-width keyword, zero results offering to widen only a scoped search, a missing scope topic offering to clear it, もっと読む appending the next page with a spinner in its place and the scope posted, a failed page offering 再試行 there and an expired snapshot 「もう一度検索」; `SearchSkeleton` inert with the URL's keyword under 検索中, 読み込み中 without one.
  - `settings/aiConnectionsList` — active connections with their dates in Asia/Tokyo and never a revoked one, the empty state in one line carrying the MCP URL, revoke asks first, the optimistic removal and its reconciliation, a failure putting the row back with its reason under that row.
  - `settings/aiConnectionsPanel` — revoke-all asks first, reports the count in a toast and reconciles; a partial failure splits — the revoked count as a toast, the rest under the row with a retry that runs it again; no toast when nothing was revoked; an answer without the failure count, or of the wrong shape, is a system error on the row.
  - `settings/credentialList` — the email row named by the address or else by its kind, unlink on SSO rows only, the last login method marked and not unlinkable with no uniqueness hold listed, the link entry as a plain anchor to the provider's start with the link intent, only for a configured provider and never on P-03, the optimistic removal reconciled through the router, the server's refusal putting the row back under it.
  - `settings/currentUserPanel` — settings.html's five sections in order, each a region named by its label; the address on the email row, the retention, the AI empty state without 「すべて失効」, the export and logout; the reset beside the password change; neither for a uniqueness-hold-only email; a link entry only for a configured provider; never a verifier-looking value or the user id.
  - `settings/exportPanel` — the form posts the browser's zone to `/export`, 「生成中…」 without the button, the zip saved once through `<a download>` and kept on offer as 「ダウンロード」 without a second request, the Blob released when the screen goes, a failure named in the row with 「リトライ」 that runs it again, a lost session linking to the login that returns here, a network failure as the generic wording.
  - `settings/logoutButton` — navigates to `/login` once the server confirms; a rejection or a wrong-shaped value stays.
  - `settings/passwordChangeForm` — the requirement under the new password's label and the reset link inside the form, both fields read-only under 「変更中…」, both passwords posted and the fields cleared with the success in a toast rather than on the form, a wrong current password and a weak new one each marked on their own field, the attempt limit first in the form with no field marked.
  - `settings/passwordResetDoneFeed` — the login methods and the AI connections under their labels with the way on to the timeline after them, no revoke-all row when there is no connection, no way to link another account and no notice of its own; `PasswordResetDoneSkeleton` as one busy region with its loading label, the onward button hidden and disabled, and the same section labels so the swap moves nothing.
  - `settings/retentionForm` — the current value described by its note, a save held read-only under 「保存中…」 then said in a toast, 0 / a negative / a fraction / a blank refused under the row before the server, a rejected draft kept in the focused input, the transport ceiling refused with its own wording, the domain's rejection named as the rule and other failures shown as they come.
  - `settings/settingsFeed` — `SettingsUnavailable` as one sentence with logout as its one control; the account load failure degrades to it without the failure's words, the panel is drawn when the account loads, and the guard's redirect passes through.
  - `settings/settingsSkeleton` — one busy status whose only exposed words are its label, the five section labels and the disabled controls hidden from assistive technology, no input, link or form.
  - `settings/ssoNotice` — nothing when the callback sent neither outcome, a link that went through as a toast with nothing on the screen, a link that did not as an alert under the link entry with the list unchanged, the three failure codes worded the same, and no failure on a list drawn outside it.
  - `timeline/composer` — a one-line input led by the 「メモを追加」 glyph docked outside the sheet, the glyph spinning and the input held while a post is in flight, a failure right above the form with a retry that submits the draft again and none without one, the input sized to its content only where the browser cannot size it itself.
  - `timeline/memoEntry` — ISO instant + Asia/Tokyo wall clock, a position-specified visit marked current, Markdown lists, no script element from the body, the pending entry busy with its saving status and no menu, あなた vs an AI client's name, the edit / history / delete menu closing on Escape with focus back on the trigger; inline edit opening focused on the body and refusing a blank draft, saving with the version it started from and closing, closing without reporting when nothing changed, a conflict warned and re-submitted on top of the current version, a rejection keeping the editor and the draft, cancel discarding it; the source trail with a live link and a disabled trashed entry, and none for a memo nothing cites.
  - `timeline/sourceDocumentLinks` — nothing for an empty trail, a live document linked and a trashed one disabled.
  - `timeline/timelineBoard` — dedupe by id preferring the loader page and the higher version on either side, newest-first with id tiebreak, day grouping in Asia/Tokyo even when the UTC date is the same, the empty state and posting enabled once a body is typed, the composer docked outside the sheet, the optimistic entry then the cleared draft and its toast, a failure keeping the draft right above the composer with 再試行, a wrong-shaped value as a system error, one post for two same-frame submits and no re-send when the first fails; the older / newer sentinels watched against the shell's sheet appending and prepending their pages with 読み込めませんでした and 再試行 where the sentinel was; the keyword bar opened from the header, the no-match state with its two clears, a post under a filter returning to the plain timeline; the date card and its jump, the note on the heading when the asked-for day is empty, the pivot's day group or row scrolled into view and neither on the plain list; a found target highlighted and centred, a notFound / trashed one toasted once; delete owned by the board — cancel keeps it, the optimistic removal confirmed and toasted, a failure bringing it back with リトライ.
  - `timeline/timelineSkeleton` — one busy status whose only words are its label, the stand-in text laid over a day heading and three entries, and no heading of its own.
  - `topics/skeletons` — `TopicsSkeleton` as three rows in the list's own shape with the text laid over; `TopicDetailSkeleton` as the head, the status line and two document rows, the loading label carrying P-07's `h1` and 「ドキュメント」 the only real heading.
  - `topics/topicDetailFeed` — each document as a row link with its title and update day ending in the jump mark, 新しいドキュメント last, and that entry alone when there is no document.
  - `topics/topicHeader` — name / description / 完了済み, 完了にする as a header button and never a menu item with 完了を解除 in the same place, no description line without one, inline edit prefilled and focused with a blank name refused with its message, an optimistic save that invalidates, the rejection at the head of the kept form, キャンセル restoring the fields, archive and unarchive optimistically, a rejected archive under the head, delete behind a confirmation leaving for the list and a rejected delete staying.
  - `topics/topicList` — the one-sentence empty state with the list ending in 新しいトピック, the form opening in its place and キャンセル turning it back with the draft gone, a blank name refused with its message (description kept, nothing posted), the trimmed description or null, the optimistic row first before 新しいトピック comes back, failure keeping the form and its inputs, a dismissed rejection dropped and the next one shown, a wrong-shaped value, one post for two same-frame submits, archived topics folded under a collapsed toggle without their descriptions; delete owned by the list — cancel keeps the row, the optimistic removal invalidates once the server confirms, a failure puts the row back with リトライ.
  - `topics/topicRow` — the detail link with name / count / description, an archived row neutral and without its description, the 編集 (a link to the detail) / 削除 menu handed to the owner (also on right click, closed on Escape), the owner's failure under the row and outside its link and none without one, the pending row busy with 保存中… and no link or menu.
  - `trash/trashBoard` — days rounded up and 「まもなく削除」 past the deadline, set nesting under the topic, the empty state with 空にする disabled, every kind with badge and days left only and icon buttons named by the row, only the acted-on row busy with a set's documents disabled under their topic, optimistic restore with its toast and the failure's retry under the row, the set-restore confirmation with and without a count and its cancel, the destination picker (an existing topic or a new one, an unavailable choice explained with 「候補を読み直す」, a blank new name refused on its field), the hard-delete confirmation naming the set for a topic only, 空にする splitting a partial failure into a toast and an alert with 再試行, a row that vanished elsewhere answered with an alert and 「一覧を読み直す」, もっと読む loading the next page and retrying a failed one.
  - `trash/trashSkeleton` — one busy status whose only words are its label, the header sentence and three rows from the board's own parts with every text node laid over and both row buttons disabled, the bare 空にする disabled outside the list, and nothing animated.
  - `ui/button` — each step's box and color, the red focus ring on the destructive step only, no submit unless `type="submit"`, native props through; `ButtonLink` as a router link with the step's look; a forced `className` / `style` dropped on both.
  - `ui/confirmDialog` — nothing while closed, title then sentence then confirm above cancel, the primary fill vs the destructive red outline and ring, cancel on a backdrop click only, both buttons disabled and relabelled while pending.
  - `ui/emptyState` — the sentence alone, or the sentence with its one control under it, and the sentence as the page's `h1` with the paragraph's look; `SectionLabel` as a heading at the asked level that names the list after it.
  - `ui/feedback` — `RowError` as an alert with the error glyph and a retry only when given one, under the row in `Row`'s error slot; `InlineAlert`'s three tones (info as a status without a glyph, warning and error as alerts with theirs) and a retry on the error tone only (the other two are held by the type); `ComposerError` floating with its retry, and with no control without one.
  - `ui/formField` — `TextField` named by its label (also when the label is hidden), invalid and described by its error only while it has one — error first, then the helper — with the helper under the label and the error under the control, native props through, a forced `className` / `style` / `id` dropped; `TextAreaField` wired the same and growing with its content; `FormGroup` handing its ids to a control it does not draw; `FieldError` under the id the control points at; `FormError` as one alert carrying its link.
  - `ui/iconButton` — every glyph distinct and hidden, each of the four sizes and no other, only the spinner spinning; `IconButton` named by `label` and never by a forced `aria-label`, header vs row box, tone and the red focus ring; `IconButtonLink` as a router link named by its label.
  - `ui/markdown` — dash markers on unordered items only (not on numbered or task items), memo vs document leading and headings, a code block without the inline-code background, raw HTML left as text, links, a table in its own scroll box.
  - `ui/popoverMenu` — the three-dot trigger naming the menu, focus on the first item when it opens, the destructive item in red, an action run once focus is back on the trigger, the arrow keys / Home / End walking and wrapping, Escape returning focus, a press outside closing it, the controlled open state; `PopoverMenuLink` leading to its route and closing the menu.
  - `ui/routeError` — `RouteError` as one alert of the fixed sentence and a filled 再試行 that calls `router.invalidate()` once and stays disabled until it settles; `NotFound` as the sentence and a filled link to `/`, no alert; both making the sentence the page's `h1` under a frame whose screen owns the heading; `RoutePendingFallback` as one busy status whose only words are its label, every line an `Sk` over a real text element, nothing animated.
  - `ui/row` — `RowList` as `<ol>` only when ordered; `RowLink` as one link ending in the jump mark; `Row`'s controls beside the text, the error under the row and outside its text column, the nested level, `aria-busy` only while busy.
  - `ui/sk` — the stand-in text kept in the line, hidden from sight and from assistive technology, never animated.
  - `ui/toast` — drawn with the `ToastProvider` and `ToastRegion` a frame hosts: the empty live region mounted before the first toast, a toast with nothing to press gone after `TOAST_DURATION_MS` and not before, a toast outliving the screen that raised it, different sentences stacked and a repeated one replaced with its time restarted, `useToast` / `ToastRegion` refusing to run without a provider. Where a frame places the region is the frame's, not this file's.
- **Where the cover stops**: what `renderWithRouter`'s href check verifies is that the href built from `to` / `params` hits a path in the harness's stub tree — nothing more. The stubs reproduce neither a route's `validateSearch` nor any search serialization production configures, and the check reads only the pathname, so an asserted href's search string is still the URL the harness built. An id in a knowledge route is refused in two places: the route's `paramsSchema` (a 1–200 character string, through `validateInput` into the render server function) and, for an id that names nothing, the streamed leaf's `notFound` branch (`knowledgeFeeds`). `validator.test.ts` pins `validateInput` itself, not each route's schema, and no suite draws a route — the step from the URL to the leaf is manual testing (document TC-30 / TC-31). `layout/authSheet` reads the production route tree's options — the parent chain and the identity of each layout's `beforeLoad` — without drawing a route; what the guard does with the session, and that its read goes through `noStoreMiddleware`, is the unit test `apps/web/app/presentation/__tests__/authGuard.test.ts`, and the response header itself is manual testing. `layout/routeBoundaries` draws the production frames and boundary components over stub screens whose guards and loaders fail on cue; that a production route's own server function failure reaches that boundary, and 再試行 against a real server, are browser checks — and so is the retry's pending flag in a browser, where a transition around `router.invalidate()` was seen to stay pending after a retry that failed again (`RouteError` holds plain state for that reason) while jsdom settled it. No suite checks that a screen under `_app` gives its root the bottom padding (`pb-sheet-end` / `pb-sheet-end-composer`). A DOM test's `vi.mock` of a module replaces the request-scoped entry point or usecase module the component imports, never a port implementation, and is not a fake in the sense of "Fake policy".

### Integration (`pnpm test:integration`)

- **Targets**: the Durable Object adapters and the usecases that run through them — the two DO classes, their stores, OCC, the schema gate and lazy migration, the job runner and every `jobs.kind` handler, the Outbox relay, the operator entries, the mapping-key and encryption-key rotations, and the request-side gateways' stub selection.
- **Dependencies**: a real Workers isolate (Miniflare) via `@cloudflare/vitest-pool-workers`, one project: `vitest.config.do.ts` (SQLite-backed `USER_DATA` / `IDENTITY_DIRECTORY` bindings, an `EVENTS_QUEUE` producer with its consumer settings, three state-Worker secrets as literal bindings, and `packages/core/src/adapters/cloudflare/__tests__/testWorker.ts` as `main` so `runInDurableObject` hands back the same instances the bindings address). `vitest.config.integration.ts` only lists that project; it has no `test` options of its own.
- **Aim**: realistically verify the writes only the adapter can produce — `ConflictError("OPTIMISTIC_LOCK_FAILURE")` from the matched-row count `updateMatchedRow` reads back, same-transaction placement of the outbox row and the FTS5 projection, the claim / publish / finalize passes of `alarm()`, terminal mode and the cleanup stages, fail-closed on a schema ahead of the code, the transfer's CAS predicates and checkpoints.
- **Speed**: roughly 10× unit. Day to day you run `pnpm test:unit`, and run `pnpm test:integration` when you touch an adapter or before a PR.
- **Naming**: `**/__tests__/<target>.integration.test.ts`. The suffix alone is not enough to get a file run: `vitest.config.do.ts` selects tests through an explicit `include` of `packages/core/src/adapters/cloudflare/`, `packages/core/src/application/` and `apps/web/app/worker/cloudflare/`. A new `*.integration.test.ts` outside those directories is excluded from the unit suite by its suffix **and** claimed by no project — it silently runs nowhere. When you add integration tests under a new directory, add it to that `include` in the same change.
- **The suites** (33 files, 195 tests):
  - `packages/core/src/adapters/cloudflare/__tests__/` — `alarm` (3: one relay pass and one jobs pass per wake-up, each pass spending only its own count limit, a DO holding only claimed rows re-arming on the earliest `lease_until`), `alarmSchedule` (3), `migrationGate` (2: the gate seeds `reindex` / `migrate-bulk` and both walk their `migration_progress` cursor to `done`, through an RPC and through `alarm()`), `occ` (2), `operatorEntries` (5: poisoned / quarantined listings and re-drives, `purge-user-mappings`, fail-closed keeping the Alarm), `rotation` (14: the transfer's guards, pass-overs, the five verdicts, predicate 2, the cursor), `rotationLifecycle` (5: reservations during a rotation, roll-back and the retirement-proof invalidation, an unlink meeting a transfer), `rowRunner` (2), `schema` (2), `selfLocator` (2).
  - `packages/core/src/adapters/cloudflare/stores/__tests__/` — `memoTimelinePlan` (4: the timeline statements' `EXPLAIN QUERY PLAN` — an ordered walk of `memos_timeline_idx` for a first page, a seek from the cursor both ways, a seek on `posted_at` for a date anchor and a key seek for a memo anchor, each with and without a keyword, plus the negative control for the `OR` spelling of the cursor).
  - `packages/core/src/adapters/cloudflare/jobs/__tests__/` — `jobTerminal` (4), `recovery` (12: terminal mode, S1〜S4 / L1〜L3 / C1, `abandon-account`, `finalize-withdrawal`), `rotateEncryption` (3).
  - `packages/core/src/application/export/__tests__/` — `exportAllData` (4).
  - `packages/core/src/application/identity/__tests__/` — `aiClientConnections` (5), `lookupGeneration` (4: active → previous → active re-probe), `passwordReset` (9), `signupSaga` (9), `sso` (7).
  - `packages/core/src/application/knowledge/__tests__/` — `documents` (11), `editDocumentByAi` (3), `sourceLinks` (3), `topics` (5), `trashKnowledge` (3).
  - `packages/core/src/application/memo/__tests__/` — `aiMemo` (4), `editMemo` (7), `revisions` (5), `softDeleteMemo` (3), `timeline` (4), `timelineWindow` (10).
  - `packages/core/src/application/search/__tests__/` — `search` (18). `packages/core/src/application/trash/__tests__/` — `trash` (18).

## Fake policy

The fakes live in `packages/core/src/application/__tests__/fakes.ts` (which also re-exports the five gateway tripwires under `fakes/`), and that is the roster:

- **`FakeIdGenerator`** — returns deterministic ids by embedding a counter into a UUIDv7 template. The output is shaped to pass the adapter-side rehydration validation (`IdGenerator.validate`), so it won't fail format checks even in round-trip tests through storage. The starting number can be fixed via the constructor, and the prefix is `ffffffff-...` so that generated ids sort after the test's fixed rows.
- **`FakeTokenGenerator`** — a deterministic stream for the opaque tokens (`callerToken`, reset tokens) a usecase mints, so a unit test can assert what was handed to the gateway.
- **`FakeLogger`** — records each `info` / `warn` / `error` call into an `entries` array. Use `byLevel("error")` to extract them and assert on the observability behavior of a usecase or of the presentation-layer error middleware.
- **`FakePasswordHasher`** — a cheap deterministic digest, so unit suites don't pay a real key derivation (the real algorithm is covered by the WebCrypto adapter's own unit tests, and the integration suites inject `createPbkdf2PasswordHasher({ iterations: 1000 })` through `createTestContainer()` instead). Its output deliberately does **not** embed the plaintext.
- **`trippingIdentityGateway`** and **`tripping{Memo,Knowledge,Search,Trash,Export}Gateway`** — gateways whose every entry throws through a caller-supplied `trip`, with `overrides` supplying the entries a given suite does expect to be called. Each is written as a total implementation rather than a partial cast, so a method added to the port turns these suites red instead of leaving one entry silently unguarded. The unit suites that assert "this usecase / this presentation helper reaches no Durable Object" are built on them: the identity usecase suites through `packages/core/src/application/identity/__tests__/unitContainer.ts` (`recordingGateway` / `trip` / `makeContainer`), the memo suites through `packages/core/src/application/memo/__tests__/memoContainer.ts`, the knowledge / search / export suites directly, and on the web side `apps/web/app/presentation/__tests__/{currentUser,session,errorResponseMiddleware}.test.ts`, `presentation/ai/__tests__/handlers.test.ts` and `presentation/export/__tests__/handler.test.ts`.

The `FakePasswordHasher` case is the criterion for adding a fake at all: the port is a pure CPU-bound transform whose real implementation is separately unit-tested, and the fake preserves the one property the tests read off it. The tripwires do not meet that criterion and are not an exception to it — a tripwire fails the moment every method of the port is walked, is not a stand-in for the gateway's behavior, and no test reads a value off it. Fakes for repositories, the UoW, and the Clock are intentionally not kept.

- Even if you fake repositories / the UoW in-memory, you can't reproduce the essential adapter-derived behaviors like a `transactionSync` rollback or `ConflictError("OPTIMISTIC_LOCK_FAILURE")`. Logic tests for application services are better done at the integration layer (real SQLite), where they cover actual harm.
- `Clock` is just a `() => Date`, so it's enough to construct a constant like `new Date(0)` within a test and pass it to the usecase / domain. There's no need to fake it as a port object.

## Real DB test (integration) policy

- `pnpm test:integration:cf` runs every integration suite inside a **Workers isolate** via `vitest-pool-workers`. The pool configuration lives in `vitest.config.do.ts`, because `@cloudflare/vitest-pool-workers` configures `main` and `durableObjects` per project.
- There is no `setupFiles` and no per-test truncation: a Durable Object is identified by its name, so the suites get isolation by addressing a fresh locator — `nextUserId()` / `uniqueEmail()` in `packages/core/src/adapters/cloudflare/__tests__/helpers.ts`. Identity Directory buckets are shared across a file (the same 16 bucket names for every email), so a bucket-wide assertion compares against the live count rather than a constant, and a test that deploys a commitment or a keyring to a bucket (`overrideDirectoryEnv`) restores it afterwards.
- `createTestContainer()` / `registerTestUser()` / `bucketOfEmail()` (`packages/core/src/adapters/cloudflare/__tests__/testContainer.ts`) return a production-equivalent request container wired to the two Durable Object bindings with a cheap PBKDF2, and address the bucket a canonical email lives in. `helpers.ts` holds the seams the suites reach storage through — `inUserDataStorage` / `inDirectoryStorage` (a Durable Object's SQLite and `DurableObjectState`, via `runInDurableObject`), `overrideDirectoryEnv` / `overrideUserDataPlan` (a bucket's `env`, a User Data object's migration plan), `twoGenerationKeyring` / `commitmentJsonFor` (the rotation suites' second generation). Seeding a state no production caller can create yet — `credentialVersion` disagreement, a change in flight, a lockout, a copy in a second generation, a `poison` row — is done through these seams. Adding an RPC that writes them instead would break "a table with no write path is adapter-owned". Memo suites use `packages/core/src/application/memo/__tests__/memoFixtures.ts` (`post` / `postAt` / `setPostedAt` / `searchHits` / `readMemoRow`).
- OCC in a Durable Object is a conditional `UPDATE` guarded on `version` whose matched-row count is read back through `updateMatchedRow`: zero matched rows raise `ConflictError("OPTIMISTIC_LOCK_FAILURE")` right there, and nothing above it retries. A test reaches that branch by handing `save` a `version` that is already stale — reading and writing inside one `run` callback cannot produce it, because the callback is fully synchronous and a Durable Object is single-threaded, so two such callers serialize and both succeed. No test races two OCC-guarded writes; in one that did, which caller loses is not fixed, so keep the assertions loose enough to accept either.
- Asserting that a projection **did not run** takes a sentinel, not a before/after comparison of the row. `upsertSearchEntry` keeps the rowid and writes back the same title, body and timestamp, so re-running it on unchanged data leaves the row byte-identical — a comparison passes whether or not the projection ran. Write a value only the projection owns (`search_entries.body`) from the test, run the operation, and assert the value survives: re-running would overwrite it with the entity's own text.
- The pool fires a due Alarm at once: a row whose `next_run_at` is in the past may already have run by the time the test reads it. Assert "pending or already done" for such rows, or pull `next_run_at` back and call `runDurableObjectAlarm` yourself.

## Timeout / flakiness

- The configs use Vitest's default timeouts. Unit tests finish in a few hundred milliseconds; if an integration test needs a longer ceiling, set `testTimeout` in `vitest.config.do.ts` rather than slowing the unit suite. A `test` option on `vitest.config.integration.ts` does not reach the project: in projects mode the root config only lists them. The same holds for `vitest.config.ts`, which lists `vitest.config.unit.ts` and `vitest.config.dom.ts` and carries no `test` options of its own.
- No suite asserts on elapsed time. A Miniflare timing is a local number, not a production one, so a threshold on it turns ordinary CI variance into a red build whose failure reads as broken behavior. When a decision needs the order of magnitude, take the measurement through an existing suite as a one-off, and record the number **with the conditions it was taken under** wherever that decision lives — the suite keeps asserting behavior only. Such a number is a snapshot and nothing re-takes it when the code moves.
- The Durable Object suites race nothing: contention over a row is expressed by seeding its lease and `owner_token` state and then running one pass, not by firing two callers. `signupSaga.integration.test.ts` fires two registrations of one address but asserts only that exactly one wins, because which one is not fixed. Interleavings inside one RPC (a row moved between two probes, a credential change landing between s3 and s4) are reproduced by seeding the intermediate state or by a delegating stub wrapper (`lookupGeneration.integration.test.ts`), never by timing.

## Limits — what no suite can pin

- The atomicity of a `transactionSync` under a failure in the middle (a rollback's failure injection). The suites pin what each transaction writes together; the rollback itself is the runtime's.
- `rotate-encryption`'s read / write interleaving (a row deleted and re-reserved between the chunk's read and its `UPDATE`) and the s4 re-read interleaving of the mapping-key transfer. The guard predicates are pinned as statements (`rotateEncryption.integration`, `transfer.test.ts`), not as races.
- A Durable Object reset in the middle of a `remap-chunk`: the absence of the s6 checkpoint is pinned for an entry-level refusal only.
- The export size ceiling (`EXPORT_MAX_SOURCE_BYTES`, 24 MiB) is pinned by integration; a browser cannot produce the input.
- The reservation TTL inequality of `spec/testcases/recovery/sagaRecovery.md` row 26 is an operational value, not a testable behavior.
- `apps/web/app/components/settings/__tests__/aiConnectionsList.dom.test.tsx` went red once under the full parallel run (PH-09A) and passed on every rerun; `domSetup.ts`'s 5 s ceiling is the response. A second occurrence is a bug report, not a retry.

## Commands

| Purpose | Command |
| --- | --- |
| All | `pnpm test` |
| Unit + DOM | `pnpm test:unit` |
| Integration only (Workers pool: Durable Objects) | `pnpm test:integration` (alias of `pnpm test:integration:cf`) |
| DOM only (the jsdom project inside the unit run) | `pnpm test:unit --project dom` |
| A single file or path pattern | `pnpm test:unit packages/core/src/domain/identity`, `pnpm vitest run --config vitest.config.integration.ts <path>` |

## Coverage

Coverage numbers are not enforced. Rules of thumb:

- **Domain**: aim for ~100%. Logic is local and easy to fully cover, and a missing test translates directly into a broken invariant.
- **Application + Adapter (integration)**: per "representative path". Provide at least one for each route, such as OCC success / OCC failure, same-tx placement of the outbox row, per-row isolation in the Outbox relay (one row that cannot be published backs off without touching the others), and a saga's terminal mode. For usecase orchestration coverage, prioritize confirming it "ran on a real DB" via integration over exhaustive coverage with fakes.
- **Frontend**: rendering branches are held by the DOM tests (the DOM section above is the authority on which ones, and where the cover stops); the pure logic behind the screens — formatters, schemas, list state, presentation-layer helpers — is held by unit tests (the Unit section above); what is left, such as form validation as the reader meets it and how the optimistic UI looks, is manual testing (`spec/manual-tests/`).
